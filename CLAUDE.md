# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## ⚠️ PRODUCTION DATABASE: READ-ONLY, ALWAYS, NO EXCEPTIONS

**Claude executes READ-ONLY queries against the production database. Ever. Only the OWNER executes
writes.** Owner rule, stated 2026-09-04 during `v0.112.0` while connecting the Render MCP server.

This is not a per-task permission that a later session can re-negotiate, and it is **not** softened by
any of the usual justifications: not by a request being urgent, not by an incident being in progress,
not by a fix being "obviously correct", not by the write being small, reversible, or already agreed in
principle, and **not by the owner approving a plan that happens to contain one** — approving a plan is
not approving Claude to execute its writes.

**Read-only means:** `SELECT`, `SHOW`, `EXPLAIN` (without `ANALYZE`, which executes the statement),
and reads of catalog/stats views such as `pg_stat_activity`. **Everything else is the owner's to run** —
`INSERT`, `UPDATE`, `DELETE`, `TRUNCATE`, every DDL statement, every Flyway migration, `VACUUM`,
`REINDEX`, session/database `SET`, `SELECT … FOR UPDATE`, and anything calling a function that writes.

**⚠️ THIS BINDS EVERY ROUTE TO PRODUCTION DATA, NOT JUST A SQL PROMPT** — the Render MCP server (or any
future database MCP/tool), `psql`, a JDBC or script connection, a one-off endpoint, and an admin
surface that performs the write on Claude's behalf. **Routing a write through a tool does not make it
Claude's to run.** The Render MCP server can also modify environment variables and trigger deploys;
those are the same class of action and are the owner's, not Claude's.

**When a write is genuinely needed:** write the exact statement into a `docs/claude-plans/*.sql` file
with the expected row count and how to verify it, hand it to the owner, and stop. That is the whole
protocol — **do not run it and then report it.**

**⚠️ IF A PRODUCTION WRITE HAS ALREADY HAPPENED, SAY SO IMMEDIATELY AND PLAINLY.** An unreported write
to production is far worse than a reported one, and concealment is the failure this rule exists to make
impossible.

## What this project is

**NoteLib** (rebranded from StudySnap — db/package names still use `studysnap`) is a notes-first study workspace. Users capture notes, generate AI-powered Study Packs, and practice with quizzes. Database schema uses the old name; do not rename unless explicitly asked.

**⚠️ NOW SCOPED AS THE OPEN RELEASE — `v0.113.0 — Session Anchoring`, kicked off 2026-09-04 (owner). THIS BLOCK IS THAT RELEASE'S STANDING ANTI-DRIFT.** **⚠️⚠️ IT DELIBERATELY OVERRIDES THREE RECORDED RULINGS THAT SAY *DO NOT DO THIS*, AND THE OVERRIDE IS WRITTEN DOWN RATHER THAN ROUTED AROUND — READ THIS PARAGRAPH BEFORE CONCLUDING THE RELEASE IS DRIFT.** `v0.105.0` ruled *"the session stays anchored on the CALLER-SUPPLIED primary … No migration, no index change, no lookup change"*; `v0.107.0` ruled **"REJECTED WITH REASONS, DO NOT RE-PROPOSE: widening the partial unique index with a scope discriminator"**; `v0.110.0` found the migration *"has no forcing slice any more, which is this release's own finding"* and left the fix as *"a separate decision"*. **⚠️ NONE OF THE THREE IS A TECHNICAL OBJECTION TO THE MIGRATION — every one objects to taking it WITHOUT A FORCING REASON, and `v0.110.0` explicitly parked it as an owner decision.** **THE OWNER TOOK THAT DECISION 2026-09-04, and the reason is budget rather than product: the release fires the FULL THREE-AGENT COLD PRESSURE TEST, and that verification capacity is available now and will not be later.** **⚠️ THIS IS THE FAILURE MODE THE REPO HAS ALREADY PAID FOR ONCE — the Architecture no-op verdict was re-rejected from a superseded paragraph — so a later session reading `v0.107.0`'s "DO NOT RE-PROPOSE" in isolation must come here first.** **⚠️ AND THE SHAPE `v0.107.0` REJECTED IS NOT THE SHAPE THIS RELEASE BUILDS, WHICH IS WHY THE REJECTION DOES NOT APPLY VERBATIM:** it rejected **adding a nullable scope discriminator TO THE EXISTING INDEX KEY** — `(user_id, study_pack_id, session_mode, source_collection_id)` — which genuinely breaks one-active-per-pack, because two rows sharing a pack but differing in a NULL collection id no longer collide. **THIS RELEASE LEAVES THE EXISTING KEY ALONE AND ADDS A SEPARATE INDEX FOR THE NEW SCOPE.** **⚠️ EVERY PREMISE BELOW WAS RE-VERIFIED BY READING CODE AND MIGRATIONS AT KICKOFF, NOT TRUSTED FROM A HANDOFF:** `study_pack_id UUID NOT NULL REFERENCES study_packs(id) ON DELETE CASCADE` (`V4:4`); `note_id` was added by `V19:152-153`, set `NOT NULL` at `V19:161-162` after a backfill FROM `study_packs.note_id`, with FK `fk_quick_review_sessions_note_id` → `notes` ON DELETE CASCADE (`V19:172-174`); **`V41` DROPS AND RECREATES TWO PARTIAL UNIQUE INDEXES** — `idx_quick_review_sessions_one_active_generation` on `(user_id, study_pack_id, session_mode)` and `idx_quick_review_sessions_one_active_generation_note` on `(user_id, note_id, session_mode)`, **both `WHERE status IN ('GENERATING','IN_PROGRESS')`**, so a still-`GENERATING` session already blocks; `QuickReviewSessionEntity:32-36` declares **both anchors `nullable = false`**; and **`sourceCollectionId` EXISTS ONLY AS A REQUEST FIELD** (`LongExamStartRequest`, `ChallengeQuizStartRequest`, `PlanSourcedExamVerifier`) with **no column anywhere** — exactly as `v0.107.0` ruled, and exactly what this release reverses. **⚠️⚠️ THE PRIMARY HAZARD IS NOT THE INDEX — IT IS THE ANCHOR CALL-SITE SWEEP, AND MAKING THE COLUMNS NULLABLE TURNS THOSE SITES INTO POTENTIAL NPEs ON A PATH THAT TODAY CANNOT FAIL.** **⚠️ THE KICKOFF'S FIGURE WAS A HEURISTIC GREP AND IS CORRECTED HERE BY AN EXACT ENUMERATION (2026-09-04, reading code) — IT WAS BOTH LOW AND POINTED AT THE WRONG SERVICES, AND ONE PRESSURE-TEST AGENT IS AIMED AT THESE SITES, SO THE CORRECTION IS LOAD-BEARING RATHER THAN COSMETIC.** It said *"52 session-scoped call sites across NINE services"*; the true figure is **54 SESSION-SCOPED `getStudyPackId()` CALL SITES ACROSS SIX SERVICES** — `ChallengeQuizService` (20), `QuickReviewSessionService` (13), `LongExamService` (8), `QuickReviewAdaptivePracticeService` (5), `InterviewPracticeService` (5), `DashboardService` (3). **⚠️ OF THE 63 TOTAL `getStudyPackId()` OCCURRENCES, NINE ARE NOT SESSIONS AT ALL AND ARE UNAFFECTED BY THIS MIGRATION:** `ConceptHealthEntity` (`ConceptHealthService:179,:225`, `ProgressReportService:200,:229`), `ExamQuestionPoolEntity` (`ExamQuestionPoolService:158`), `UserActivityEventEntity` (`DashboardService:533,:723,:900`, all already null-checked) and one interface declaration (`SharedNoteListProjection:15`). **⚠️ SO THE KICKOFF'S NAMED "SHARP ONES" ARE A MISCLASSIFICATION AND MUST NOT BE BRIEFED TO AN AGENT — `ConceptHealthService`, `ExamQuestionPoolService` AND `ProgressReportService` HAVE ZERO SESSION-SCOPED CALL SITES**; they read their OWN entities' `study_pack_id`, which this release does not touch. **⚠️ THE HAZARD IS REAL BUT LIVES AT THE CALLER SEAM — the sites that PASS a session anchor INTO a `ConceptHealth` write — AND THE SHARPEST IS `QuickReviewAdaptivePracticeService:797`, `parseSourceStudyPackId(entry.getKey(), session.getStudyPackId())`, WHERE THE FALLBACK DEFAULT ITSELF BECOMES NULL** so an unstamped item attributes to NOTHING rather than throwing; `:793` and `QuickReviewSessionService:304` are the same shape. **⚠️ AND `note_id` GOES NULLABLE TOO, WHICH THE KICKOFF'S `getStudyPackId()`-ONLY FRAMING MISSES ENTIRELY: session-scoped `getNoteId()` is FOUR SITES ACROSS TWO SERVICES — `InterviewPracticeService:436,:483` (NOT null-guarded, both flow into DTOs) and `QuizSessionHistoryService:129,:130` (already guarded). `QuizSessionHistoryService` IS A SEVENTH SERVICE THE `getStudyPackId()` SWEEP CANNOT REACH.** **⚠️ THE SWEEP IS MADE TRACTABLE BY A REACHABILITY PARTITION, NOT BY 54 UNIFORM NULL CHECKS — ONLY PLAN-SCOPED ADAPTIVE PRACTICE EVER GOES NULL-ANCHORED.** Long Exam and Board Exam keep caller-supplied primaries (`v0.105.0`/`v0.106.0`); Quick Review and Interview Practice are note-scoped. **So every creation path EXCEPT plan-scoped Adaptive continues to set both anchors non-null, and that is an INVARIANT to pin with a test over the five construction sites** — `ChallengeQuizService:2570`, `InterviewPracticeService:374`, `LongExamService:750`, `QuickReviewAdaptivePracticeService:871`, `QuickReviewSessionService:107` — **rather than a licence to sprinkle defensive nulls; a negative assertion needs a REACHABLE subject, which is this repo's own carried lesson.** **⚠️ SO THE MIGRATION IS THE SMALL HALF AND THE CALL-SITE SWEEP IS THE LARGE ONE — STATED AT KICKOFF RATHER THAN DISCOVERED MID-IMPLEMENTATION, which is the `v0.112.0` quota-site lesson applied one release later.** **SCOPE: (1)** `study_pack_id` and `note_id` become NULLABLE **together**, and `source_collection_id UUID REFERENCES note_collections(id) ON DELETE SET NULL` is added — **⚠️ CORRECTED 2026-09-04 BY THE PRESSURE TEST — THE KICKOFF SAID *THE FK CASCADE IS NOT OPTIONAL … or account deletion leaves orphans*, AND THAT JUSTIFICATION IS FALSE: `quick_review_sessions.user_id` HAS CASCADED SINCE `V4:3`, so orphan cleanup on account deletion was ALREADY covered and a cascade here added only a SECOND, USER-FACING deletion trigger.** Proven on real PostgreSQL: deleting a Study Plan hard-deleted its `COMPLETED` sessions while the `ConceptHealth` rows they wrote survived — evidence without the history explaining it, on an ORGANISATIONAL act where *"Remove never means canonical note deletion"* is standing doctrine. **⚠️ SO IT SHIPS `ON DELETE SET NULL`, WITH THE ANCHOR `CHECK` CARRYING A TERMINAL BRANCH** (`OR status IN ('COMPLETED','FORFEITED')`) so an orphaned terminal row is legal, **AND `NoteCollectionService.delete` CLEARING THE NON-TERMINAL PLAN-SCOPED SESSIONS FIRST — that sweep is LOAD-BEARING, not tidying: without it the `SET NULL` violates the CHECK and deleting a plan fails outright.** **⚠️ Do NOT restore the cascade;** **(2)** a **`CHECK` constraint that a session always has exactly one anchor** — `(study_pack_id IS NOT NULL AND note_id IS NOT NULL) OR source_collection_id IS NOT NULL` — because an anchorless row is unreachable by every lookup and invisible to every sweep — **⚠️ and note the DEPLOY-TIME COST, which is a lock question rather than a design one: a plain `ADD CONSTRAINT … CHECK` validates every existing row under `ACCESS EXCLUSIVE` on THE ONE TABLE EVERY QUIZ MODE SHARES, so either confirm the row count makes that a non-event or use `ADD CONSTRAINT … NOT VALID` followed by `VALIDATE CONSTRAINT`, which takes only `SHARE UPDATE EXCLUSIVE`**; **(3) THE INDEX SHAPE IS DECIDED AT KICKOFF, NOT LEFT TO IMPLEMENTATION (`v0.107.0` records what happens when it is left open): the two existing partial indexes gain `AND study_pack_id IS NOT NULL` / `AND note_id IS NOT NULL` to their predicates, and a THIRD is added on `(user_id, source_collection_id, session_mode) WHERE status IN ('GENERATING','IN_PROGRESS') AND source_collection_id IS NOT NULL`.** **⚠️ NO SENTINEL, NO `COALESCE`, NO GENERATED COLUMN** — PostgreSQL already treats NULLs as DISTINCT, so the predicates are belt-and-braces that make the intent readable and survive a future `NULLS NOT DISTINCT`; **⚠️ do NOT "simplify" them away.** **(4)** the three-way `ADAPTIVE` collision dissolves — note-scoped Adaptive Practice, Interview Practice and plan-scoped Adaptive Practice stop contending for one index — and **`v0.107.0`'s two named Known limitations close**; **(5)** the mutable-`position` anchor hazard closes with them, since a plan-scoped session no longer borrows a pack anchor that reordering can move. **(6) ⚠️ ADDED 2026-09-04 BY OWNER DECISION AFTER A PRE-PROMPT VERIFICATION PASS, AND IT IS NOT OPTIONAL POLISH — WITHOUT IT THE RELEASE CHARGES A LEARNER FOR A SESSION THEY CANNOT ENTER: the plan-scoped ENTER/RESUME PATH, because it is ENTIRELY NOTE-KEYED TODAY and a NULL-anchored session is unreachable through every one of its steps.** Verified by reading, not inferred: `collection-detail-page-client.tsx:3110` routes on `if (session.noteId)` and otherwise falls through to `showActionToast(session.message)`; the practice page (`app/study-packs/[id]/adaptive-practice/page.tsx:135,:245,:402`) is note-addressed and resolves its session via `getInProgressAdaptivePracticeSession(noteId)`; and **the collection-addressed in-progress endpoint IS GONE — `v0.108.0` item 4 removed it, leaving only `/notes/{id}/adaptive-practice/in-progress` (`NoteController:536`).** **⚠️ SO A NULL-ANCHORED PLAN-SCOPED SESSION CANNOT BE NAVIGATED TO AND CANNOT BE FOUND, WHILE QUOTA IS CHARGED BEFORE THE RESPONSE RETURNS — a paid dead end, which is why this is scope rather than a Known limitation.** **⚠️ THE FIX IS SMALL AND MUST REUSE WHAT EXISTS RATHER THAN INVENT A SURFACE — ALL THREE PIECES ARE ALREADY IN THE REPO:** `QuickReviewAdaptiveQuizResponse` **already carries `sessionId` as its first field**, `findByIdAndUserIdAndSessionMode` **already resolves an ADAPTIVE session by id** (`QuickReviewAdaptivePracticeService:718`, `:819`), and `AdaptivePracticeController` **already uses session-addressed routes** (`/adaptive-practice/sessions/{sessionId}/complete` and `/forfeit`). What is owed is a `GET /adaptive-practice/sessions/{sessionId}`, a frontend route that renders from a `sessionId`, and `toAdaptiveResponse` tolerating a NULL anchor (title from the collection). **⚠️ Do NOT restore the collection-addressed in-progress endpoint `v0.108.0` deliberately removed, and do NOT make the client compute an anchor — `api.ts` records that a plan's item order is mutable so a client-computed anchor drifts.** **⚠️ AND THE ROUTING TEST WAS RE-RUN ON THIS DISCOVERY RATHER THAN INHERITED: the release stays CODEX, now spanning backend, frontend and a migration.** **⚠️⚠️ A SECOND BLOCKING FINDING, AND IT IS THE ONE MOST LIKELY TO SHIP A SILENT DEFECT: PLAN SCOPE ALREADY HAS A SOURCE OF TRUTH IN JSONB, SO THIS RELEASE CREATES A DUAL ONE.** `QuickReviewAdaptivePracticeService` already stores `SESSION_STATE_SOURCE_COLLECTION_ID = "sourceCollectionId"` in `session_state` (`:86`, written `:886`, read `:606-610`), **and the resume filter at `:296` reads it.** Because anti-drift forbids a backfill, an IN-FLIGHT plan-scoped session has the JSONB key and `source_collection_id IS NULL` — **so repointing `:296` at the new column makes that lookup MISS, a second session inserts, and NULLs being DISTINCT means the new third index does NOT catch it: TWO active plan-scoped sessions with the first PERMANENTLY UNREACHABLE**, verbatim the failure this release's own `position` reasoning already names. **⚠️ REQUIRED SHAPE: ONE RESOLVER — column first, JSONB fallback second — with EVERY reader routed through it, and new rows writing BOTH for this release; the JSONB key becomes legacy-fallback and its removal is a LATER decision, once no pre-migration row can still be active.** **⚠️ THIS JAVA-SIDE FALLBACK IS NOT A VIOLATION OF "NO SENTINEL, NO `COALESCE`" — that rule governs the INDEX PREDICATES, and a careful implementer will otherwise read it as forbidding exactly this.** **⚠️⚠️ A THIRD BLOCKING FINDING, AND IT IS THE MOST LIKELY WAY THIS RELEASE SHIPS GREEN AND DELIVERS NOTHING — FOUND 2026-09-04 BY READING THE START PATH, NOT THE MIGRATION: THE PLAN-SCOPED START PATH IS BUILT END TO END AROUND FINDING AN UNOCCUPIED PACK TO BORROW AS AN ANCHOR, AND THAT MACHINERY MUST BE RETIRED WITH THE MIGRATION OR THE CONTENTION NEVER ENDS.** `generateAdaptiveQuizForCollection` (`QuickReviewAdaptivePracticeService`, ~`:395-470`) does five linked things: builds `occupantByPackId` from every pack carrying an active `ADAPTIVE` session (`:414-419`), filters those packs OUT of `focusEligiblePackIds` (`:420-422`), **returns an ERROR RESPONSE when nothing survives** (`:423-433`, `ADAPTIVE_SESSION_ELSEWHERE_MESSAGE` / `INTERVIEW_SESSION_ACTIVE_MESSAGE`), picks the borrowed primary (`:459`), then re-checks it via `anchorCollision` (`:465-470`). **⚠️ A COLLECTION-ANCHORED SESSION NEEDS NO FREE PACK, SO ALL FIVE ARE OBSOLETE — AND IF THEY SURVIVE, PLAN-SCOPED PRACTICE STILL REFUSES TO START WHENEVER THE LEARNER HAS NOTE-SCOPED SESSIONS ON THEIR WEAK PACKS, WHICH IS EXACTLY THE CONTENTION THIS RELEASE EXISTS TO END.** The migration, indexes, `CHECK` and sweep would all land and change nothing observable — the *"fix that looked correct and changed nothing"* shape this repo has paid for three releases running. **⚠️ COLLISION FOR PLAN SCOPE IS NOW THE THIRD INDEX, and the `:405-414` comment must be rewritten — its premise is precisely what this release falsifies, and left standing it will invite a later session to restore the filter.** **⚠️ BUT KEEP WHAT IS NOT PART OF THE BORROWED ANCHOR, OR THIS BECOMES SCOPE CREEP: SOURCE SAMPLING STAYS** (`longExamPlanSourceSampler.sample(..., MAX_PLAN_SOURCE_PACKS, ...)` — `v0.107.0` made that bound a PRECONDITION of keeping generation inside the transaction), **per-source focus stamping stays**, and **the `!isInterviewSession` filtering stays everywhere.** **⚠️ A PACK OCCUPIED BY A NOTE-SCOPED SESSION BECOMES A LEGITIMATE SOURCE — that is the intended behaviour change, not a regression.** **⚠️ AND THE GUARD FOR THIS IS PRE-DECLARED BECAUSE GUARDS 1-4 ALL PASS WHILE THE DEFECT IS LIVE: a learner holding an active note-scoped Adaptive session on EVERY focus-eligible pack in the plan must STILL start plan-scoped practice and receive a real session. A fixture whose packs happen to be unoccupied proves nothing.** **⚠️ THE SEMANTIC CONSEQUENCE IS A PRODUCT CHANGE AND MUST BE STATED, NOT ABSORBED: after this release a learner may hold an active plan-scoped session AND an active note-scoped session on a pack inside that plan AT THE SAME TIME.** Today they collide and one silently resumes the other — the `v0.107.0` limitation whose worse leg (b) makes plan scope *look broken in production*. **That contention ending is the release; it is not a regression, and the copy must not describe it as one.** **⚠️ ANTI-DRIFT: EXISTING ROWS ARE LEFT ALONE — NO BACKFILL, NO REWRITE, NO `source_collection_id` INFERRED FOR ANY HISTORICAL SESSION.** Every existing row has both anchors populated and stays exactly as it is; the migration widens what is PERMITTED and changes nothing that exists. **⚠️ `v0.99.0` records getting a backfill shape wrong TWICE by writing a timestamp onto rows that should have had none — do not repeat it in the other direction by inventing a collection id.** **⚠️ `QuickReviewSessionMode` STAYS AT FOUR VALUES — no new mode, no new sub-mode, and `SUB_MODE_INTERVIEW` stays the Interview Practice discriminator** (`InterviewPracticeService:57`, `isInterviewSession:121`); this release changes the ANCHOR, never the mode taxonomy, and `EXAM_MODES.md` is a locked five-mode contract. **⚠️ NO quota, entitlement, limit or meter change** — nothing about what a learner may start or what a unit buys moves. **⚠️ Do NOT change `BOARD_EXAM_STARTED`, `ADAPTIVE_PRACTICE_STARTED`'s `entry`/`sourceScope` values or firing conditions, or `QUIZ_SHARE_LINK_CREATED`** — several dated checkpoints read them and an anchor change is exactly the edit that moves a firing condition by accident. **⚠️ `frontend/app/onboarding` STAYS FROZEN** — dated reads fall between `2026-09-10` and `2026-09-17`. **⚠️ Do NOT fold `v0.112.0` Phase 3 into this release.** They both touch transaction and session plumbing and are ORTHOGONAL: Phase 3 is about how long a CONNECTION is held, this is about what a SESSION is keyed on, and Phase 3 is independently gated on `[CHECKPOINT — due 2026-10-04]` plus an OSIV decision `v0.112.0` Phase 2 proved it needs. **⚠️ CANONICAL CONCEPT IDENTITY IS NOT THIS RELEASE AND ITS SIZING READ CAME BACK AGAINST IT — RECORDED SO IT IS NOT RE-DERIVED:** `v0.112.0`'s kickoff earmarked `v0.113.0` for it, the owner-executed read ran 2026-09-04 (`docs/claude-plans/v0.113.0-concept-vocabulary-sizing.sql`), and **Q5 — the justifying number — returned 6 user-concept pairs across 5 users, 0.4%.** Q2 returned **10,361** distinct authored concepts, so **the `ADR-001` curated-catalog pattern does NOT transfer** (41 rows versus five figures), and Q4's 34.8% authored sharing is genuine per Q6 rather than generic-string noise. **⚠️ SO IT IS PREMATURE, NOT WRONG — it is RESCOPED AND RE-READ LATER, not cancelled**, and its Backlog row carries the re-read trigger. **VERIFICATION: FULL THREE-AGENT COLD PRESSURE TEST IN ISOLATED `git worktree`s, DECLARED NOT INHERITED** — a migration plus production-data semantics on **the one table every quiz mode shares**, which is the gate's named trigger twice over. **⚠️ THE ISOLATION IS NOT OPTIONAL** (`v0.105.0` lost an agent's entire result set to shared-tree corruption) and **ONE AGENT MUST BE POINTED AT THE 52 CALL SITES, NOT AT THE MIGRATION** — in the last four releases the worst defect was outside the stated scope every time, and here the stated scope is the half that is easy to review. **⚠️ PRE-DECLARED DISCRIMINATING GUARD, AIMED AT WHAT THE CHANGE COULD SILENTLY DESTROY RATHER THAN AT WHAT IT ADDS: two note-scoped sessions on the SAME pack must STILL COLLIDE after the columns become nullable.** A fixture that exercises only the new plan-scoped path **passes under both the defect and the fix and proves nothing** — it is the same shape as every pre-declared guard this repo has paid for. **⚠️ AND IT MUST BE A REAL-ROW TEST IN `NativeQueryPostgresIntegrationTest`, NOT A MOCKED ONE — A MOCKED REPOSITORY CANNOT TEST A UNIQUE INDEX**; `v0.93.0` records a cold agent deleting a predicate and passing **1,760 tests** for exactly this reason, and `PREPARE` validates syntax and arbiter resolution but never predicate correctness. **⚠️ THE HARNESS WAS CHECKED AT PROMPT TIME RATHER THAN ASSUMED, AND IT SUPPORTS THIS — BUT NOT FOR FREE:** `NativeQueryPostgresIntegrationTest` **is** a real-row harness (Testcontainers PostgreSQL 16, the full Flyway migration set, `jdbcTemplate.update` fixtures) and not merely a `PREPARE` sweep, **so the guard is writable there; HOWEVER `quick_review_sessions` APPEARS IN IT ZERO TIMES, so a NEW fixture chain is owed — `users` → `notes` → `study_packs` → `quick_review_sessions`, supplying the entity's NOT NULL set. Follow the existing pattern (`seedUser:1497`, `seedPublicNote:1795`, `seedArcGeneratedQuiz:1734`); do NOT bolt the assertion onto the `PREPARE` sweep, which would pass under both the defect and the fix.** **⚠️ A SECOND GUARD IS OWED AT THE CALL-SITE END, AND ITS SUBJECT IS CORRECTED BY THE ENUMERATION ABOVE: a plan-scoped session must reach the `ConceptHealth` WRITE SEAM without a pack anchor and either attribute correctly or fail loudly — never write against a null key. ⚠️ AIM IT AT `QuickReviewAdaptivePracticeService:793` AND `:797`, THE CALLER SITES THAT PASS THE ANCHOR IN — NOT at `ConceptHealthService` or `ExamQuestionPoolService`, which have no session-scoped call sites and cannot exhibit the defect.** **⚠️ A THIRD GUARD IS OWED AT THE NEW ENTER/RESUME PATH (scope item 6): a plan-scoped session started from a collection must be ENTERABLE end to end with BOTH anchors NULL — a fixture that starts one and merely asserts a 200 on the START call passes under the dead end and proves nothing, because the failure is at the NAVIGATE-and-RESOLVE step.** **⚠️ A FOURTH GUARD IS OWED AT THE JSONB SEAM: a session carrying ONLY the legacy `session_state.sourceCollectionId` and a NULL column must still resolve on resume — that fixture is the ONLY thing standing between this release and two active plan-scoped sessions with the first unreachable.** **⚠️ CARRIED MEASURED LESSONS: a negative assertion must have a REACHABLE subject; write the guard at the LAYER THE DEFECT LIVES AT; MUTATE and confirm a NAMED test fails; read `./mvnw`'s EXIT STATUS directly, never through a pipe; COUNT executed tests from `target/surefire-reports/*.xml` AND CLEAN THE DIRECTORY FIRST, since a `-Dtest=` run leaves stray reports that inflate the count (measured in `v0.112.0`); run `npm test`; SWEEP BY SURFACE, not by diff; VERIFY "X already does Y" AGAINST CODE BEFORE IT REACHES A PROMPT; and call `advisor()` BEFORE writing the Codex prompt.** **Routing: CODEX** — a migration, an entity contract change and a 52-site sweep across nine services. **⚠️ NOW SCOPED AS THE OPEN RELEASE — `v0.113.1 — Anchoring Hardening`, kicked off 2026-09-04 (owner). A PATCH, and it closes the residuals `v0.113.0`'s three-agent pressure test found and DELIBERATELY LEFT.** **⚠️ IT EXISTS BECAUSE THE MAJOR ROADMAP ITEMS ARE TIME-GATED, NOT BECAUSE THESE RESIDUALS ARE URGENT — stated so a later session does not read it as the roadmap's priority order:** Canonical Concept Identity's own Phase 0 sizing read came back AGAINST building it now (Q5 returned **0.4%** — 6 user-concept pairs across 5 users — and **10,361** distinct authored concepts kill the `ADR-001` curated-catalog analogy), `v0.112.0` Phase 3 is gated on `[CHECKPOINT — due 2026-10-04]` plus an OSIV decision, and **TWELVE DATED READS LAND BETWEEN 2026-09-10 AND 2026-09-17**, so this release is scoped to work touching NONE of the surfaces they measure. **SCOPE: (1)** collapse the anchor contract to ONE authority — the `CHECK`, the entity `@PrePersist` validator and `assertValidAnchor` encode it at three strengths and the service copy is a redundant WEAKER one that misses the partial pack/note case; **⚠️ the ENTITY validator is the authority (it fires on every JPA write and is mutation-verified), and the database `CHECK` STAYS — it is the only rule a future native write would meet;** **(2)** a legacy plan-scoped session whose collection was deleted stops 404ing on the session-addressed route, which has no fall-through to its still-valid pack anchor while the note-addressed route resumes it fine; **(3)** completion cannot brick a plan — `requireSourceStudyPackId` throws inside the completion transaction, rolling back a session that already reached `COMPLETED`, and the recovery sweeper covers only `LONG_EXAM`/`CHALLENGE` while the row keeps occupying the collection index; **⚠️ TWO INDEPENDENT REVIEWERS FAILED TO FIND A REACHABLE UNSTAMPED ITEM, so this hardens a LATENT path and MUST NOT weaken the fail-loud guarantee** that keeps a null key out of `ConceptHealth`; **(4)** the nine H2 fixtures mirror `V133`'s COLUMNS but none of its CONSTRAINTS, so a test can persist a shape the database rejects — add the `CHECK` and the three partial unique indexes where H2's PostgreSQL mode supports them, and **⚠️ if it cannot express a partial unique index, STATE THAT rather than silently omitting it;** **(5)** the harness pins `postgres:16` while production runs **PostgreSQL 18** (verified against the live instance), so the guards proving the index predicates run on a different major version than serves them. **⚠️ ANTI-DRIFT: NO MIGRATION — `V133` is deployed and every item is code or test; if an item appears to need one, THE SCOPE IS WRONG.** **⚠️ NO BEHAVIOUR CHANGE to what `v0.113.0` shipped, WITH TWO STATED EXCEPTIONS — the lock was AMENDED AT SIGNOFF rather than left contradicting the release's own Shipped list, which is the same correction this repo made mid-`v0.113.0` when the cascade justification turned out false.** Anchor semantics, the `SET NULL` cascade, note credit and retention suppression are settled, and items 1, 4 and 5 change only WHERE a rule is enforced and WHAT the tests cover. **⚠️ ITEMS 2 AND 3 DO CHANGE WHAT THE PRODUCT DOES, and both RESTORE A PATH `v0.113.0` CLOSED BY ACCIDENT rather than adding capability:** item 3 turns the hard throw on an unresolvable source into a **skip-and-log**, because the throw rolled back a session already marked `COMPLETED` and Adaptive Practice is **not covered by the recovery sweeper**, so one unresolvable item bricked the plan permanently while the active-session index refused every retry; item 2 makes the session-addressed read of a legacy plan-scoped session whose collection was deleted **fall back to its pack anchor instead of 404ing**. **⚠️ Item 1 NARROWS what the entity accepts and is still observably nothing** — the deleted service-local copy accepted a partial pack/note pair beside a collection anchor, but the entity validator already rejected that shape on `@PrePersist`, so **no row that previously persisted can now fail.** **⚠️ `frontend/app/onboarding` STAYS FROZEN** (lifts 2026-09-11; twelve dated reads 2026-09-10 → 2026-09-17). **⚠️ Do NOT change `ADAPTIVE_PRACTICE_STARTED`'s `entry`/`sourceScope`, `BOARD_EXAM_STARTED` or `QUIZ_SHARE_LINK_CREATED` — `v0.113.0` already shipped one regression of exactly this kind, caught only by a cold agent because the fixture omitted the field production always sends.** **⚠️ NO quota, entitlement, limit or meter change; no new mode or sub-mode.** **⚠️ `v0.112.0` Phase 3 and the `PESSIMISTIC_WRITE` duration stay OUT — do NOT take them opportunistically because item 3 is nearby.** **VERIFICATION: a single `advisor()` call on the diff** — no permission substrate, no cross-user read, no money semantics, no migration, no behaviour change. **⚠️ ESCALATE TO ONE SCOPED COLD AGENT IF** item 1 changes WHICH shapes are rejected rather than merely where, or item 5's bump surfaces a behavioural difference. **⚠️ PRE-DECLARED GUARD, aimed at what item 1 could silently destroy: a partial pack/note shape AND a both-anchors shape must both STILL be rejected, and an anchorless TERMINAL row must still be permitted** — a happy-shape fixture passes under both the defect and the fix. **Routing: CLAUDE CODE inline.** **⚠️ NOW SCOPED AS THE OPEN RELEASE — `v0.114.0 — Connection Evidence`, kicked off 2026-09-04 (owner). THIS BLOCK IS THAT RELEASE'S STANDING ANTI-DRIFT.** **⚠️ IT EXISTS BECAUSE THE KICKOFF SCAN FOUND A CHECKPOINT THAT WOULD HAVE COME DUE UNANSWERABLE, NOT BECAUSE THE WORK WAS NEXT IN A QUEUE.** `[CHECKPOINT — due 2026-10-04]` is named in `v0.112.0`'s own anti-drift as **"THE DECIDING INPUT FOR PHASE 3, AND THE REASON PHASE 3 IS DEFERRED RATHER THAN BUILT"**, and it is **30 days out**. **⚠️ VERIFIED BY READING CONFIG AT KICKOFF, NOT TRUSTED FROM A HANDOFF: PHASE 1 SHIPPED IN FULL** — `leak-detection-threshold: 60000` (`application.yaml:22`), `maximum-pool-size: 20` (`:30`), `connection-timeout: 5000` (`:37`), each carrying its reasoning in a comment — **BUT PHASE 2 DID NOT.** Its PRIMARY observation, logging the effective `hibernate.connection.handling_mode`, returns **ZERO matches across `backend/src/main/java` AND `backend/src/main/resources`**, and `spring.jpa.open-in-view` is **still unset** (Spring Boot default `true`). **⚠️ THE COMMENT AT `application.yaml:21` LITERALLY CALLS LEAK DETECTION "the instrument Phase 2 reads to settle the open-in-view question" — AN INSTRUCTION RECORDED AND NEVER EXECUTED**, so the checkpoint has a date, a kill criterion and a row, and **NO WORKING METRIC**: the *decorative checkpoint* failure the signoff gate names outright. **SCOPE: (1)** log the EFFECTIVE `hibernate.connection.handling_mode` from the `EntityManagerFactory` properties at startup — **⚠️ PRIMARY, and specified this way because `v0.112.0` already recorded that "read the leak logs" COMES BACK AMBIGUOUS:** leak detection reports that a connection was held past 60 s and BY WHOM, but **never whether it stayed bound AFTER THE TRANSACTION CLOSED**, which is the actual OSIV question; **(2)** sample Hikari's `active` count against a request whose transaction commits early and whose response is slow to serialize — **⚠️ CONFIRMS ONLY, and do NOT substitute an argument from Spring Boot defaults for either, which `v0.112.0` forbids explicitly**; **(3)** run the seven read-only falsification queries (§5's five plus the two the cold re-read found missing) and record whether the cause is CONFIRMED or still a hypothesis; **(4) ⚠️ READ RENDER REQUEST-RATE DATA FOR 05:55–05:57 UTC ON 2026-09-04, AND THIS IS THE ITEM MOST LIKELY TO CHANGE PHASE 3's TARGET: ALL FIVE §5 QUERIES READ TABLES THAT RECORD *WRITES*, SO NONE CAN DETECT AN ANONYMOUS *READ* BURST** — *"zero rows refutes it"* is FALSE there, they would merely fail to see it. The only path the outage log names as starved is `NoteCollectionService.listPublic:206` — `permitAll`, unpaginated, read-only — and this repo has a DOCUMENTED precedent of public-endpoint fetches saturating the backend. **⚠️ Run it BEFORE Phase 3 is shaped.** **⚠️ ANTI-DRIFT: DO NOT SET `open-in-view: false` HERE** — real blast radius (`LazyInitializationException` on any lazy association touched during serialization), and `v0.112.0` ruled it **wants a STAGING run, never a direct production edit**; this release MEASURES and the decision is the owner's. **⚠️ DO NOT START PHASE 3** — gated on `[CHECKPOINT — due 2026-10-04]` AND on this release's evidence, and it restructures six services and twelve quota sites; **do NOT take it opportunistically because the instrumentation makes the problem legible.** **⚠️ NO quota, entitlement, limit or meter change; NO MIGRATION** (if one appears needed the scope is wrong); **do NOT change what `BOARD_EXAM_STARTED`, `ADAPTIVE_PRACTICE_STARTED` or `QUIZ_SHARE_LINK_CREATED` record**; **`frontend/app/onboarding` STAYS FROZEN** (lifts 2026-09-11) with **twelve dated reads between 2026-09-10 and 2026-09-17**. **⚠️ PgBouncer IS NOT THE FIX and must not be proposed as one.** **⚠️ PRODUCTION DATABASE IS READ-ONLY — items 3 and 4 are `SELECT`s and metric reads; any write, env-var change or deploy is the OWNER's.** **VERIFICATION: a single `advisor()` call on the diff** — no permission substrate, no cross-user read, no money semantics, no migration. **⚠️ ESCALATE TO ONE SCOPED COLD AGENT IF item 1's reading CONTRADICTS `v0.112.0`'s premise** (the connection is NOT held past commit), because Phase 3's entire shape then changes. **⚠️ PRE-DECLARED GUARD: the logged value must be the EFFECTIVE one read back from the `EntityManagerFactory`, NOT the configured or defaulted one — a test asserting the constant we passed in passes under both the defect and the fix and proves nothing**, the same shape as `v0.105.0`'s schema test with hardcoded booleans instead of the derivation. **Routing: CLAUDE CODE inline.** Current version: **v0.119.1 — Public Catalog Bounds** (Released, kicked off and signed off 2026-09-06, base branch `releases/v0.119.1`, cut from `main` after `v0.119.0` merged and tagged). **A PATCH, and the THIRD OCCURRENCE OF ONE SHAPE** — a latent unbounded fetch over the public catalog crossing a threshold as data grows (2026-09-01 build failure, 2026-09-04 outage, 2026-09-05 outage). Governed by `docs/claude-plans/2026-09-05-public-catalog-unbounded-read-fix-plan.md`; **EXECUTE ITS DECISIONS, DO NOT RE-DERIVE ITS §1 SETTLED FACTS.** `GET /notes/public` is anonymous and does work proportional to the **whole catalog** (now **1,442** notes), holding a pooled connection for all of it; the health check then starves on the same pool and the platform restarts a process whose only problem is that it is busy. **⚠️ THE HOLD IS INSIDE THE TRANSACTION — this is NOT open-in-view and that question must not be re-opened.** **ALL THREE LEGS (owner, 2026-09-06): (B)** the two callers that never needed a catalog move onto the paginated `count(*)`+`LIMIT` path — **⚠️ `size` IS NOT A PAGINATION PARAMETER**; **(A)** bound the server-side work — the legacy branch loads every `NoteEntity` **including `content`**, and six of eight sorts **including the DEFAULT `RECOMMENDED`** load every candidate, so a request with **no `sort`** takes the unbounded path even when paginated; **(C)** the health group **EXCLUDES `db`** (owner decision, trade stated: it also keeps an instance alive serving errors when the database is genuinely gone). **⚠️ OWNER DECISION, STATED NOT ABSORBED: `sort=recent` is accepted for the related-notes call — a VISIBLE product change on ~250 public SEO pages.** **⚠️ A1 IS BOUNDED, NOT RETIRED** — `/notes/public` is a public HTTP contract and an unpaginated request must degrade to a bounded response, never a 400. **⚠️ ANTI-DRIFT, ALL REJECTED WITH REASONS: do NOT raise `maximum-pool-size` (already raised 10→20 and the same failure recurred at 20 — the holds are unbounded in DURATION), instance memory or the database plan; do NOT set `open-in-view: false`; do NOT propose PgBouncer; do NOT start `v0.112.0` Phase 3 (gated on `[CHECKPOINT — due 2026-10-04]`); do NOT trim `contentPreview`/`summaryPreview` (`v0.100.0` rejected it — it moves the threshold); do NOT make identifying the traffic source a prerequisite; do NOT silently replace `RECOMMENDED` with a differently-scoring SQL `ORDER BY`; do NOT "fix" Leg A by moving work off the transaction — the WORK is the problem.** **NO migration, no quota/entitlement/meter change, no new mode; onboarding frozen.** **VERIFICATION: ONE SCOPED COLD AGENT framed as falsification for Leg A; a single `advisor()` call for Legs B and C — NOT the three-agent tier.** **⚠️ PRE-DECLARED GUARDS: assert the request URL carries `pageSize` (a count-only assertion passes under both the defect and the fix); show the tightened `server-public-notes.test.ts:252` assertion FAILING against unmodified source first; Leg A fixtures must hold MORE notes than the page size and assert REPOSITORY rows; exercise the NO-`sort` request explicitly; and pin that items and ordering are UNCHANGED for all eight sorts.** **Routing: SPLIT — Legs B and C inline, Leg A cold agent (Codex unavailable).** Previous: **v0.119.0 — Curator Bulk Regeneration** (Released, kicked off 2026-09-05, base branch `releases/v0.119.0`, cut from `main` after `v0.118.0` merged and tagged). **PHASE 2 OF THE REGENERATION ARC**, governed by `docs/claude-plans/curator-bulk-regeneration-stage1.md` — **EXECUTE ITS DECISIONS, DO NOT RE-DERIVE THEM.** **⚠️ IT DELIBERATELY OVERRIDES ITS OWN PLAN'S §14 SEQUENCING, WHICH REQUIRES PHASE 1 TO HAVE DEPLOYED AND BEEN OBSERVED ON REAL CANONICAL NOTES FIRST — `v0.118.0` was tagged minutes before this kickoff and has been observed on none.** **THE OWNER OVERRODE IT 2026-09-05 BECAUSE UPDATING THE ALE REVIEW SET IS BLOCKED ON BULK REGENERATION**, so the sequencing cost is paid by stalled curator work rather than by impatience. **⚠️ A later session reading §14 in isolation must come here first — recorded override, not drift. The accepted residual: B1's job record is shaped by Phase 1's CODE rather than its observed behaviour, so a production surprise about combined regeneration is a RE-OPEN, not a shock.** **⚠️ OWNER DECISION 1 TAKEN AT KICKOFF AGAINST THE PLAN'S RECOMMENDATION AND IT MOVES THE VERIFICATION TIER: bulk is available to ADMIN **AND TEACHER** curators, not ADMIN only. A single action therefore spends metered units on a PAID account — up to 50 note-generation and 50 Study Pack units — which is a MONEY-SEMANTICS change and fires the full three-agent trigger.** **⚠️ THE SUB-DECISION IS ANSWERED, NOT LEFT OPEN: TEACHERs are METERED NORMALLY under the already-settled block-and-reduce policy, and the ADMIN-only quota BYPASS IS NOT WIDENED — a TEACHER's batch is capped by their remaining allowance and the preflight must say so BEFORE they commit. No entitlement, plan-tier or limit change. Do NOT 'fix' a short allowance by extending the bypass.** **Batch cap 50 under its OWN config key; NO cancellation in v1 — do NOT offer a control implying in-flight LLM work stops.** **⚠️ TWO BLOCKERS MUST BE SOLVED, BOTH ARITHMETIC: the existing bulk receipt CANNOT report a batch this long and must be REPLACED not inherited (the poller abandons after ~5 minutes; regeneration is TWO LLM calls per item), and the batch loop occupies one of only TWO threads on the same executor each item is dispatched onto — do NOT raise that 2/2 bound, it is gated on `[CHECKPOINT — due 2026-10-04]`.** **⚠️ Do NOT reuse `applyBulkGeneratedMetadataToNote` — it overwrites `title` and `tags` unconditionally and would DESTROY curator-authored canonical titles.** **⚠️ ONLY B1 MAY ADD A MIGRATION; bulk changes CONTENT only, never membership, Section, ordering, Subject, Domain Context, Depth or Applicable Programs; no propagation to learner copies; selection stays explicitly HUMAN; no whole-Review-Set one-click; onboarding stays frozen through 2026-09-19.** **⚠️ THE `v0.118.0` PRODUCTION OUTAGE FIX IS NEXT AND IS NOT IN THIS RELEASE.** **VERIFICATION: FULL THREE-AGENT COLD PRESSURE TEST in isolated `git worktree`s** — two independent triggers (production-data semantics at scale plus a migration, AND money semantics on a paid account). **Routing: CODEX for B1-B2, Claude Code inline for B3-B5.** Previous: **v0.118.0 — Note and Study Pack Regeneration** (Released, kicked off and signed off 2026-09-05, base branch `releases/v0.118.0`, cut from `main` after `v0.117.0` merged and tagged). **PHASE 1 ONLY — THE SINGLE-NOTE PRIMITIVE**, governed by `docs/claude-plans/note-and-study-pack-regeneration-stage1.md`, **owner-ratified 2026-09-05 (§20). EXECUTE ITS DECISIONS; DO NOT RE-DERIVE THEM.** **⚠️ THE AUDIT'S HEADLINE IS THAT THE FEATURE IS SMALLER THAN THE BRIEF ASSUMED, ON ONE FACT: `NoteGenerationService.generateFromTopic` DOES NOT CREATE A NOTE** — it returns a content string and the frontend saves it through the ordinary create path, so the brief's headline prohibition (*do not delete and recreate the Note*) guards a risk that does not exist. **SCOPE: (1)** a Note's content and its Study Pack regenerate as ONE operation, preserving `notes.id`, `notes.created_at`, `study_packs.id` and the note's `note_collection_items` row with `position` and `label`; **(2)** a FAILED Study Pack generation leaves the ORIGINAL content and ORIGINAL pack in place — a half-regenerated note is the failure mode this exists to prevent; **(3)** both meters unchanged on failure, asserted on PERSISTED counters. **⚠️ ANTI-DRIFT: CURATOR BULK REGENERATION IS NOT IN THIS RELEASE** — `docs/claude-plans/curator-bulk-regeneration-stage1.md` §29 rules it *NOT safe to ship in the same release as the single-Note primitive*, on two arithmetic grounds: the existing bulk receipt cannot report a batch this long (the poller abandons after ~5 minutes while regeneration is strictly slower per item — TWO LLM calls, not one), and the batch loop occupies one of only TWO threads on the same executor each item's generation is dispatched onto. **⚠️ Do NOT build a concept-migration engine** — stale `ConceptHealth` rows are already inert; no rename matching, no canonical concept identity. **⚠️ NO MIGRATION** (the audit states none is required; if one appears needed the scope is wrong); no pricing, entitlement, plan-gate, mode or sub-mode change; onboarding stays frozen pending live dated reads through 2026-09-19; do NOT change what `BOARD_EXAM_STARTED`, `ADAPTIVE_PRACTICE_STARTED` or `QUIZ_SHARE_LINK_CREATED` record; Slices 4-5 and `v0.112.0` Phase 3 stay OUT. **VERIFICATION: ONE SCOPED COLD AGENT framed as falsification** — trigger is MONEY/QUOTA SEMANTICS, a single operation charging TWO meters — **with pre-declared guards that a FAILED generation leaves both the original content and the original pack intact (a succeeding fixture proves nothing), that both meters are unchanged on failure (assert PERSISTED counters, not the call), and that identity survives a success.** **Routing: CODEX.** Previous: **v0.117.0 — Authoring and Quiz Legibility** (Released, kicked off and signed off 2026-09-05, base branch `releases/v0.117.0`, cut from `main` after `v0.116.0` merged and tagged). **⚠️ THE FIRST RELEASE IN SOME TIME WHOSE EVIDENCE IS OWNER OBSERVATION RATHER THAN INFERENCE** — five issues reported from real use with screenshots 2026-09-05, audited against code, tightened by the owner, then a GPT tightening pass. Plan: `docs/claude-plans/authoring-and-quiz-legibility-fix-plan.md`; **⚠️ READ ITS §10 FIRST — two owner premises are contradicted by the code.** **⚠️ THIS IS "RELEASE A" OF THAT PLAN'S OWN SPLIT; ITEM 5 (deferred save-order) IS DELIBERATELY HELD BACK** because items 4 and 5 both touch persistence. **SCOPE: (1)** a Challenge Quiz assertion question stops reading as one run-on statement — the `Statement N:` splitter swallows the trailing interrogative — fixed at the presentation seam AND the upstream prompt, **presentation only, never mutating stored text** (`v0.110.1`); **(2)** a plan-scoped Adaptive Practice session stops presenting itself as a single note — the note-addressed read calls a 2-arg overload hardcoding `sourceCollection = null`; **(3)** the section drag preview stops swallowing the page — **the overlay must NOT regrow**; **(4)** picking an existing section commits immediately — **the `editing`-gated debounced writer and its comment STAY** (`v0.88.0`, reproduced defect recorded in it); **(5)** **adopting a Goal stops erasing a learner's own exam date** (`NoteCollectionService:918` NULLs `targetCompletionDate` on a PRE-EXISTING adoption) — learner data loss, folded in deliberately. **⚠️ ANTI-DRIFT: plan item 5 is OUT; no numeric pending-change count; do NOT change `ADAPTIVE_PRACTICE_STARTED`'s fields or firing conditions (item 2 changes a RESPONSE TITLE, not an event), nor `BOARD_EXAM_STARTED`/`QUIZ_SHARE_LINK_CREATED`; onboarding STAYS FROZEN (verified — none of the five reach it) with twelve dated reads 2026-09-10 → 2026-09-17; no migration, no new mode or sub-mode, no quota/entitlement/meter change, no `ProfileType` gate; Slices 4-5 and `v0.112.0` Phase 3 stay OUT.** **VERIFICATION: ONE SCOPED COLD AGENT framed as falsification**, the trigger being item 2 crossing two systems on a surface dated checkpoints measure, **with pre-declared guards each naming the fixture that would pass under the defect: a trailing interrogative AFTER the last `Statement N:` label, a plan-scoped session read through the NOTE-ADDRESSED route, and a pre-existing adoption that ALREADY carries a learner-set date.** **⚠️ CARRIED LESSON FROM `v0.116.0`: a guard whose fixture cannot occur in production proves nothing — that release's item 4 was mutation-verified against a hand-set state adoption could never produce and shipped as an observable no-op.** **Routing: SPLIT — items 1, 3, 4, 5 inline; item 2 CODEX.** Previous: **v0.116.0 — Additive Review Set Updates** (Released, kicked off and signed off 2026-09-05, base branch `releases/v0.116.0`, cut from `main` after `v0.115.0` merged and deployed). **SLICE 2 of the Comprehensive Review Set Overhaul.** **⚠️ IT RUNS NOW BECAUSE SLICE 1 HAS DEPLOYED — the order was a correctness constraint, not a preference.** **⚠️ LIVE PRODUCT DEBT, MEASURED: 92 learners · 523 adopted collections · 4,374 placements, with 364 source placements and 68 whole Subject Plans ALREADY STRANDED (100% from upstream growth, 0% from learner removal) and 10 no-op re-adopt attempts by 4 distinct users** — `adopt()` bails before `copySourceItems`, so re-adoption is a hard no-op. **SCOPE: (1) upstream ADDITIONS apply; rename, reorder, retire and move are SURFACE-ONLY**, because until source-at-sync provenance exists the system cannot distinguish learner customization from upstream change; **(2) five provenance facts per placement** — source section and position at sync being the genuinely unreconstructable ones — **schema defined BEFORE implementation if they share a JSON snapshot; (3) `Detached from source` as a real resolver state** (two exist today); **(4) `companionMayBeOutdated:1193` stops returning `false` for non-`ADMIN`.** **⚠️ ANTI-DRIFT: ADDITIVE ONLY — never replace a learner note body, Study Pack, edit, `ConceptHealth`, or quiz/session history; the invariant is "learner study history is preserved", NOT the withdrawn "the numerator never shrinks"; no synchronization engine, no event sourcing, no revision numbers; the tombstone ships with an EMPTY backfill and that is not grounds to omit it; Slice 1 is NOT re-opened; Slices 0A/0B are owner-executed and are not a gate; no quota/entitlement/meter change; onboarding frozen until 2026-09-11.** **⚠️ IDEMPOTENCE IS SETTLED AT THE STORAGE LAYER AND UNPROVEN AT THE APPLICATION LAYER: `note_collection_items` carries `UNIQUE (collection_id, note_id)` so placement identity IS `(source Subject Plan, source Note)` — no new column, no title matching — but `persistAdoptedPlan:1460` returns `alreadyAdoptedResponse` and never re-reaches the insert, so the update path does not exist yet and its idempotence must be DESIGNED IN.** **VERIFICATION: FULL THREE-AGENT COLD PRESSURE TEST in isolated `git worktree`s**, with pre-declared guards that a SECOND application adds nothing and disturbs no learner reordering, that an EDITED learner note and its history are byte-identical after a pass, and that a detached adoption resolves and offers no update. **⚠️ CARRIED LESSON: `v0.115.0` found a bare `role == ADMIN` substitution for `isCurator` SURVIVING ALL 99 TESTS; item 4 touches that predicate class, so PIN BOTH LEGS.** **Routing: CODEX.** Previous: **v0.115.0 — Learner Publication Authority** (Released, kicked off and signed off 2026-09-05, base branch `releases/v0.115.0`, cut from `main` after `v0.114.0` merged and tagged). **SLICE 1 of the Comprehensive Review Set Overhaul.** **⚠️ ITS ORDER IS A CORRECTNESS CONSTRAINT: 100% of adopted placements' source notes carry program join rows against 11.8% of existing learner copies, so Slice 2 (additive Review Set updates) — which mints learner notes THROUGH `copyNote` — would grow the affected population ~8x if it ran first. Do NOT reverse.** At the `PRIVATE → PUBLIC` transition for learner-owned notes, copied `note_course_program` rows are cleared, which **un-shadows the learner's own field — authority TRANSFERRED, not merely revoked.** **⚠️ The hazard is real and UNREALISED: 536 learner copies carry copied rows, ZERO are public** — this closes a live mechanism before it fires. **⚠️ The defect is that the learner CANNOT edit, not that they do not**: shadowing is unconditional at one row and the editor requires `isOwner && isCurator`. **⚠️ ANTI-DRIFT: do NOT broaden into a public-discovery redesign** (no new tables, enums, workflow or visibility state — `NoteVisibility` stays `PRIVATE | PUBLIC`); **restrict clearing to NON-CURATORS or it destroys the catalog**; **no provenance column, no backfill**; **options D and C stay REJECTED** per `ADR-001` as of 2026-09-05, and their two rejections are different in kind and must not be collapsed; **no migration**; **do NOT start Slice 2**; **Slices 0A/0B are owner-executed curator work, not in this release and not a gate on it**; **onboarding stays frozen** (lifts 2026-09-11, twelve dated reads 2026-09-10 → 2026-09-17). **VERIFICATION: a single `advisor()` call** — one method, zero-row blast radius. **⚠️ PRE-DECLARED GUARD: a CURATOR publishing must KEEP its rows while a learner publishing a copy LOSES them** — a learner-only fixture passes under a version that clears for everyone and proves nothing. Full architecture: `docs/claude-plans/v0.115.0-stage1-master-architecture-reconciliation.md`. **Routing: CLAUDE CODE inline.** Previous: **v0.114.0 — Connection Evidence** (Released, kicked off and signed off 2026-09-04, base branch `releases/v0.114.0`, cut from `main` after `v0.113.1` merged and tagged). Full scope in `RELEASES.md`. Previous: **v0.113.1 — Anchoring Hardening** (Released, kicked off and signed off 2026-09-04, base branch `releases/v0.113.1`, cut from `main` after `v0.113.0` merged, tagged and **DEPLOYED TO PRODUCTION**). Full scope in `RELEASES.md`. Previous: **v0.113.0 — Session Anchoring** (Released, kicked off and signed off 2026-09-04, base branch `releases/v0.113.0`, cut from `main` after `v0.112.0` merged, tagged and **DEPLOYED TO PRODUCTION**). Full scope in `RELEASES.md`. Previous: **⚠️ SUPERSEDED AS THE OPEN RELEASE BY `v0.113.0`, AND RETAINED BECAUSE IT IS THE CONNECTION-POOL WORK'S STANDING ANTI-DRIFT — ITS PHASE 3 IS DEFERRED, NOT DONE, AND IS GATED ON `[CHECKPOINT — due 2026-10-04]`.**  **`v0.112.0 — Connection Pool Integrity`, kicked off and signed off 2026-09-04 (owner). THIS BLOCK IS THAT RELEASE'S STANDING ANTI-DRIFT.** **⚠️ THE RELEASE WAS REPOINTED AT ITS OWN KICKOFF AND THE SWAP WAS NOT ROUTINE:** it opened as *Canonical Concept Identity*, the ADR-sized item six releases had deferred by name; **kickoff step 8's Backlog Index scan surfaced two UNTRACKED files dated the same day — a PRODUCTION OUTAGE and its diagnosis — and the owner ruled to swap.** **⚠️ Concept identity is NOT cancelled — it becomes `v0.113.0`, loses nothing by moving, and its Phase 0 sizing read is owner-executed and runs in parallel** (`docs/claude-plans/v0.113.0-concept-vocabulary-sizing.sql`). **⚠️ THE SCAN IS WHAT CAUGHT THIS, WHICH IS THE PROCESS WORKING** — the diagnosis file's own §10 says it needs a Backlog Index row, and notes the PREVIOUS incident file in that directory never got one: **the same failure mode, twice.** Both rows are added at kickoff. **WHAT HAPPENED (source: `docs/claude-findings/2026-09-04-prod-outage-hikari-pool-exhaustion.md` + the 763-line `docs/20260904_prod-issue-down.log`): the pool was exhausted and THE HEALTH CHECK THEN STARVED ON THE SAME POOL.** At 05:55:14 UTC Hikari reported `total=10, active=10, idle=0, waiting=15`; `DataSourceHealthIndicator` needs a pool connection to answer `/actuator/health`, queued behind the same 30 s acquisition timeout, failed at 30,002 ms, and Render **RESTARTED** the instance ~65 s later. **⚠️ CORRECTED 2026-09-04 BY A COLD RE-READ — THREE CLAIMS HERE WERE OVERSTATED AND ARE NOW WEAKER: (a) the log says `restarted`, NOT replaced, with NO deploy line and the SAME version (`v0.110.2`) on restart, and a 4.4-SECOND WINDOW WITH NO PROCESS — so it was not a rolling replacement; the last merge to `main` was 2 h 48 m earlier. (b) *"killed for failing a health check"* IS AN INFERENCE, NOT A MECHANISM — the log attributes the restart to nothing, and a manual restart by the owner fit every observation and had never been considered — **⚠️ ASKED AND ANSWERED 2026-09-04: THE OWNER DID NOT RESTART IT MANUALLY, so that branch is CLOSED and a Render-side action (health-check auto-restart most likely) is what remains. ⚠️ ELIMINATING THE ONE NAMED ALTERNATIVE IS NOT EVIDENCE FOR THE SURVIVOR — the log still attributes the restart to nothing, so it stays an INFERENCE, better supported but unproven, until Render's event history for 05:55-05:57 UTC is read.**; ASK BEFORE ASSERTING A CAUSE. (c) *"nothing recovered on its own"* IS NOT ESTABLISHED and the evidence leans mildly against it — 65 s of silence, `waiting` fell 15→11 with zero further logged timeouts, and graceful shutdown completed in 20 ms, which Spring reports only at ZERO in-flight requests; the log cannot separate recovery from de-routing.** The shutdown was graceful, with no OOM. **⚠️ EVERY LOAD-BEARING CLAIM WAS RE-VERIFIED BY READING CODE AT KICKOFF, NOT TRUSTED FROM THE FINDING:** **there is NO HikariCP configuration anywhere** (zero matches across `application.yaml` and `application-prod.yaml`), so `total=10` is Hikari's **DEFAULT** `maximumPoolSize` and `30001 ms` its default `connectionTimeout` — **nobody chose 10**; `server.tomcat.threads.max: 25` (`application.yaml:35`), so **the ratio is 25 request threads against 10 connections** and `active=10 + waiting=15 = 25` is exactly the thread cap; `ChallengeQuizService` is class-level `@Transactional` (`:84`) with the LLM call **INSIDE** it (`:449`), and the LLM read timeout is **180 s** (`OpenAiLlmConfig.java:26`) against a **30 s** acquisition timeout; `spring.jpa.open-in-view` is **UNSET**, taking Spring Boot's default of `true`. **⚠️ FOURTEEN CODE PATHS HOLD A JDBC CONNECTION ACROSS AN OPENAI CALL, so SEVEN concurrent synchronous generations exhaust a pool of ten.** **⚠️ RENDER WAS NEVER THE CONSTRAINT** — Render Postgres allows ≥100 connections on every plan; the app was configured for 10. **⚠️ THE CONSEQUENCE THE DIAGNOSIS DOES NOT SEE, FOUND AT THIS KICKOFF AND VERIFIED IN CODE — IT IS THE SINGLE MOST LIKELY WAY THIS RELEASE DOES HARM.** The finding was written from the POOL's perspective; read from the QUOTA's, the structural fix is far more dangerous than it looks. **TWELVE quota-increment sites sit INSIDE the six class-level `@Transactional` boundaries Phase 3 would move** — `ChallengeQuizService:342,343,384,385,484,486,492`, `QuickReviewAdaptivePracticeService:257,499`, `InterviewPracticeService:154`, `GeneratedQuizService:172`, `StudyPackService:612`. **⚠️ AND ONLY THREE OF THE TWELVE HAVE ANY REVERSAL TO EXTEND — VERIFIED BY OPENING `UserUsageService`, NOT INFERRED FROM THE PRECEDENT: `UserUsageRepository` exposes exactly TWO decrements (`decrementLongExamUsageNotBelowZero:120`, `decrementBoardExamUsageNotBelowZero:144`), so the three `board_exam` sites are covered and NINE ARE NOT, spanning FIVE meters that need NEW decrements — `challenge_quiz` (`ChallengeQuizService:342`,`:384`,`:484`, `GeneratedQuizService:172`), `adaptive_quiz` (`QuickReviewAdaptivePracticeService:257`,`:499`), `multi_note` (`ChallengeQuizService:486`), `interview_practice` (`InterviewPracticeService:154`) and `study_pack` (`StudyPackService:612`). "EXTEND THE EXISTING MACHINERY" IS THEREFORE FIVE REPOSITORY METHODS AND FIVE SERVICE METHODS, NOT A CALL-SITE CHANGE — STATED AT KICKOFF RATHER THAN DISCOVERED MID-IMPLEMENTATION.** **⚠️ EVERY ONE OF THE TWELVE ROLLS BACK AUTOMATICALLY ON A GENERATION FAILURE TODAY, AND THAT IS NOT A DESIGN — IT IS AN ACCIDENT OF ORDERING PLUS THE TRANSACTION**, recorded twice already in the opposite direction: `v0.106.0` found *"Board Exam's quota safety is an accident of its transaction — moving generation off the transaction DESTROYS that implicit rollback"*, and `v0.107.0` ruled *"do NOT copy the refund machinery into Adaptive Practice — the refund question opens ONLY if the transaction boundary moves, so do NOT move it."* **⚠️ PHASE 3 IS EXACTLY THAT MOVE, ON SIX SERVICES AT ONCE, SO THE REFUND QUESTION IS RE-OPENED DELIBERATELY AND EVERYWHERE:** relocating the LLM call without extending quota reversal to each relocated path **silently converts twelve auto-reversing charges into PERMANENT-ON-FAILURE charges** — charging learners for generations they never received, on paid paths, with no error. **THIS IS A MONEY-SEMANTICS CHANGE HIDING INSIDE A PERFORMANCE FIX.** **⚠️⚠️ PHASE 3 MAY BE AIMED AT THE WRONG TARGET, AND THIS IS THE MOST CONSEQUENTIAL OPEN QUESTION IN THE RELEASE (found 2026-09-04 by a cold re-read). §5's FIVE FALSIFICATION QUERIES ALL READ TABLES THAT RECORD *WRITES*, SO NONE OF THEM CAN DETECT AN ANONYMOUS *READ* BURST — "zero rows refutes it" is FALSE; it would merely fail to see it.** The ONLY path the log names as starved is `NoteCollectionService.listPublic:206` — **`permitAll`, unpaginated, read-only** — this repo has a DOCUMENTED precedent of public-endpoint fetches saturating the backend (`2026-09-01`, fixed in `v0.100.0` but cited as precedent for the class), and `revalidate = 300` means those endpoints are re-fetched every ~5 minutes **independent of any deploy**. **⚠️ THE DECISIVE EVIDENCE IS RENDER REQUEST-RATE DATA, NOT POSTGRES — run it BEFORE building Phase 3, which restructures six services and twelve quota sites.** **⚠️ PHASE 1 → 2 → 3, AND PHASE 3 MUST NOT START BEFORE PHASE 2'S EVIDENCE — this is a DEPENDENCY, not caution.** **PHASE 1 (config only, ships immediately, CLAUDE CODE inline):** `leak-detection-threshold: 60000` **FIRST**, a `maximum-pool-size` raise, `connection-timeout: 5000`, plus `studyPackGenerationTaskExecutor` to core 2 / max 2 (`AppConfig.java:55-56`) and the LLM read timeout from 180 s toward ~90 s. **⚠️ THE POOL BUMP HAS A CEILING THAT MUST BE CHECKED, NOT ASSUMED — Render runs the NEW instance alongside the OLD during a deploy, each with its own pool, so the safe bound is roughly `N ≤ (max_connections − reserved) / 2`**; read it from the dashboard and confirm with `SHOW max_connections;`. **⚠️ `connection-timeout: 5000` IS A DELIBERATE TRADE, NOT AN OPTIMISATION** — waiters fail fast with a 500 instead of queueing 30 s, and that queueing is what let the health check blow past Render's probe threshold; **it trades user-visible errors for staying up, and must be stated as such.** **⚠️ PHASE 1'S THREE ITEMS HAVE DIFFERENT RISK PROFILES AND MUST NOT SHIP AS ONE BLOCK MERELY BECAUSE THE FINDING LISTED THEM TOGETHER: items 1 and 2 are pure upside with no behaviour change, while ITEM 3'S TIMING IS WRONG IN THE OBVIOUS ORDERING — it converts today's 30 s-queue-then-succeed requests into immediate 500s, and the window where it is live is PRECISELY the window where holds are still 180 s because Phase 3 has not yet removed them. ITEM 3 IS A CANDIDATE FOR DEFERRAL UNTIL AFTER PHASE 3, and that is an OWNER DECISION OWED AT PROMPT TIME, not a default either way.** **PHASE 2 (evidence, and the reason the phases are ordered): `open-in-view` DEFAULTS TO `true`, and if the connection follows the EntityManager then PHASE 3 CAN LAND, LOOK CORRECT, AND NOT FIX THE EXHAUSTION** — moving the LLM outside `@Transactional` releases the TRANSACTION while the connection stays bound until the response is written. **⚠️ THE OBSERVATION THAT SETTLES IT IS SPECIFIED, BECAUSE "READ THE LEAK LOGS" COMES BACK AMBIGUOUS AND LEAVES SOMEONE REASONING TO AN ANSWER — WHICH IS WHAT §7 WARNS AGAINST. Leak detection reports that a connection was held past 60 s and BY WHOM; it does NOT report whether the connection stayed bound AFTER THE TRANSACTION CLOSED, which is the actual OSIV question. (a) LOG THE EFFECTIVE `hibernate.connection.handling_mode` FROM THE `EntityManagerFactory` PROPERTIES AT STARTUP** — a direct read of the setting that decides this — **and (b) sample Hikari's `active` count against a request whose transaction commits early and whose response is slow to serialize. (a) IS PRIMARY, (b) CONFIRMS; do NOT substitute an argument from Spring Boot defaults for either** — and run §5's five read-only falsification queries, which decide whether the cause is CONFIRMED or still a hypothesis. **⚠️ `open-in-view: false` has a REAL BLAST RADIUS** (`LazyInitializationException` wherever a lazy association is touched during serialization), **so it wants a STAGING run, never a direct production edit.** **PHASE 3 (structural, CODEX): ⚠️ THE CORRECT PATTERN ALREADY EXISTS IN THIS REPO THREE TIMES AND MUST NOT BE RE-INVENTED — TWO SHORT TRANSACTIONS WITH THE LLM CALL BETWEEN THEM** (`StudyPackService` LLM `:688` then `execute` `:692`; `ExamQuestionPoolService` `:148`/`:175`/`:185`; `OfficialChallengeQuizTemplateService` `:214`/`:236`/`:245`), applied in exposure order: `ChallengeQuizService:449`,`:1157` → `QuickReviewAdaptivePracticeService` `:234`,`:539` → `InterviewPracticeService` `:604`,`:615` (**which must additionally STOP HOLDING `findByIdAndOwnerUserIdForUpdate` ACROSS THE LLM CALL**) and `:193` → `GeneratedQuizService:126`, `StudyPackService:130`,`:274`,`:326`, `AskCompanionService:147` → the executor-side holders at `ChallengeQuizService:611` and `LongExamService:317`. **⚠️ "DISPATCHES AFTER COMMIT" ≠ "DOES NOT HOLD A CONNECTION", AND THIS TRAP HAS ALREADY MISLED RELEASE-SEQUENCING ADVICE ONCE:** `LongExamService.startSession` is `@Transactional(NOT_SUPPORTED)` and is safe **for request latency only**; `generateLongExamAsync` wraps `execute(...)` (`:317`) **around** the LLM call at `:1151`, holding the connection for the whole generation on a `study-pack-generation-` thread — **Hikari does not care which thread.** **⚠️ TWO RECORDED LANDMINES, BOTH OF WHICH BROKE PRODUCTION WHILE EVERY TEST PASSED — NEITHER SHAPE IS SAFE, USE ONLY THE TWO-SHORT-TRANSACTIONS SHAPE:** `ChallengeQuizService:370-384` documents that a previous **afterCommit restructuring broke every Board Exam start** because `MockitoExtension` has no transaction manager and the tests took the inline fallback; and `v0.81.0` records that **`REQUIRES_NEW` for bank inserts broke every Challenge start** on FK visibility across connections — **do NOT propose `REQUIRES_NEW`.** **⚠️ A THIRD, SMALLER AMPLIFIER: `ActivityTrackingEventListener.java:43-44` is `@Transactional(REQUIRES_NEW)` on an `AFTER_COMMIT` listener running synchronously while the outer connection is still bound, so quiz start/completion briefly holds TWO connections.** **⚠️ ANTI-DRIFT: EVERY RELOCATED PATH MUST CARRY ITS QUOTA REVERSAL** — reuse `v0.105.0`/`v0.106.0`'s existing machinery, **do NOT invent a second reversal shape**, and per that precedent it fires at EVERY transition into `FAILED` **including the recovery sweeper**, is **idempotent**, and **never drives usage below zero**; **NO entitlement, plan-tier, limit or meter change** (this changes WHEN a charge is reversed, never what anyone is entitled to or what a unit buys); **PgBouncer IS NOT THE FIX and must not be proposed as one** — it solves *too many clients* while this was *ten connections held too long*, and it is transaction-mode only, breaking session variables, temp tables, `LISTEN`/`NOTIFY` and **session-level advisory locks**; **do NOT touch `ExamQuestionPoolService`'s pool refresh — it is ALREADY CORRECT and is a reference implementation here, not a target**; **⚠️ THE OCR FINDING IS RETRACTED AND MUST NOT BE RE-DERIVED AS A DEFECT** — `NoteTextExtractionService` reads as the worst connection-hold ceiling in the codebase but **Vision is DISABLED in production** (`OCR_ENABLED=false`) and `extractFromPdfViaOcr:144-146` throws immediately, so **the code-level observation stands and the production conclusion does not**, recorded so re-enabling Vision is a decision made with that in view; **NO MIGRATION** (if one appears needed the scope is wrong); **no new mode or sub-mode; no `ProfileType` gate; `frontend/app/onboarding` STAYS FROZEN** with dated reads between `2026-09-10` and `2026-09-17`; **the Domain Context taxonomy and concept identity are BOTH OUT**; **do NOT change what `BOARD_EXAM_STARTED`, `ADAPTIVE_PRACTICE_STARTED` or `QUIZ_SHARE_LINK_CREATED` record** — several dated checkpoints read them and a transaction-boundary change is exactly the edit that can move an event's firing condition by accident. **VERIFICATION: FULL THREE-AGENT COLD PRESSURE TEST IN ISOLATED `git worktree`s**, declared not inherited, and this fires the gate's triggers more clearly than most — **it changes money/quota semantics on TWELVE charge sites**, restructures transaction boundaries on production paths, and **two recorded landmines in this exact area each broke production while the whole suite stayed green.** **⚠️ THE ISOLATION IS NOT OPTIONAL** (`v0.105.0` lost an agent's entire result set to shared-tree corruption) and **ONE AGENT MUST BE POINTED AT THE BLIND SPOT, NOT THE FEATURE.** **⚠️ PRE-DECLARED DISCRIMINATING GUARD, AIMED AT THE LANDMINE RATHER THAN THE FEATURE: a test asserting the new transaction shape MUST FAIL under `MockitoExtension`'s inline fallback** — that fallback is exactly what let a previous afterCommit restructuring pass every test while breaking every Board Exam start, so **a test that passes with no transaction manager present proves nothing about this release.** **⚠️ A SECOND GUARD IS OWED AT THE MONEY END: a generation that FAILS after the boundary moves must leave usage UNCHANGED, pinned per relocated path — a fixture whose generation SUCCEEDS passes under both the defect and the fix and proves nothing.** **⚠️ CARRIED MEASURED LESSONS: a negative assertion must have a REACHABLE subject; write the guard at the LAYER THE DEFECT LIVES AT; MUTATE and confirm a NAMED test fails; read `./mvnw`'s EXIT STATUS directly, never through a pipe; COUNT executed tests from `target/surefire-reports/*.xml`; run `npm test`; SWEEP BY SURFACE, not by diff; VERIFY "X already does Y" AGAINST CODE BEFORE IT REACHES A PROMPT; and call `advisor()` BEFORE writing the Codex prompt.** **Routing: SPLIT, stated explicitly rather than resolved once — PHASE 1 IS CLAUDE CODE INLINE (config plus two one-line changes); PHASE 3 IS CODEX (six services, relocated transaction boundaries, twelve quota sites). ⚠️ Re-run the routing test if Phase 2's evidence changes Phase 3's shape.** Current version: **v0.112.0 — Connection Pool Integrity** (Released, kicked off and signed off 2026-09-04, base branch `releases/v0.112.0`, cut from `main` after `v0.111.0` merged and tagged). Full scope in `RELEASES.md`. Previous: **⚠️ SUPERSEDED AS THE OPEN RELEASE BY `v0.112.0`, AND RETAINED BECAUSE IT IS THE DOMAIN CONTEXT TAXONOMY'S STANDING ANTI-DRIFT — `v0.111.0 — Multidisciplinary Domain Context`, kicked off and signed off 2026-09-04 (owner). THIS BLOCK IS THAT RELEASE'S STANDING ANTI-DRIFT, NOT A CARRIED NOTE, AND ITS OWN ORIGINAL HEADLINE IS SUPERSEDED: the curator copy NO LONGER blocks the ALE build — all three fixes are verified shipped — while the MULTI-PROGRAM case does, and is what the release exists to fix.** A Stage 1 calibration audit ran 2026-08-31 (two cold agents; full findings and unrun production SQL: `docs/claude-plans/domain-context-taxonomy-calibration-audit.md`). **⚠️ VERDICT CORRECTED 2026-09-04 — IT WAS SCOPED TO SINGLE-PROGRAM NOTES AND DOES NOT COVER THE CASE THAT IS ACTUALLY BLOCKING THE OWNER. Read the correction below BEFORE acting on the paragraph that follows.** The original verdict — **NO TAXONOMY CHANGE; `ARCHITECTURE` is NOT added, not because it failed the bar but because it is a PROVABLE NO-OP** — rests on: a Domain Context value's entire generation payload is its **label string plus one `quantitative` boolean**, and the resolver already falls back to the single joined catalog program name — so a single-program Architecture note **already sends `Domain: Architecture`**. A new value would emit a byte-identical line. **⚠️ THE NO-OP ARGUMENT HOLDS ONLY WHERE THE FALLBACK CAN FIRE, AND IT CANNOT FIRE ON A MULTI-PROGRAM NOTE — VERIFIED 2026-09-04, NOT INFERRED.** `StudyPackGenerationContextResolver.resolveCourseProgram:143` returns the joined catalog program name **only when `joinedPrograms.size() == 1`**, and `assertGenerationReady:33-37` **throws `MultiProgramDomainContextRequiredException`** when a note has 2+ programs and a null Domain Context. **So a note carrying BOTH `Architecture` and `Architectural Engineering` has no fallback and CANNOT BE GENERATED AT ALL**, and no existing value honestly describes e.g. *Architectural Design* — `ENGINEERING_SCIENCES` is shared engineering knowledge, `CIVIL_ENGINEERING` is civil-specific, `PROFESSIONAL_PRACTICE_AND_REGULATION` is codes and ethics. **⚠️ THIS IS A REPORTED, REPRODUCED OWNER BLOCKER ON THE ALE REVIEW SET, NOT A HYPOTHETICAL.** `docs/claude-plans/ale-let-review-set-unblock-and-domain-context-evidence-plan.md` already records *"`ARCHITECTURE` is settled as rejected → **Superseded.** Re-opened on the multi-program case specifically"* — that plan supersedes this paragraph and is the one to follow. **⚠️ So the takeaway is neither "Architecture didn't qualify" NOR "Architecture is a no-op": the no-op finding is TRUE for single-program notes and IRRELEVANT to the multi-program ones, which is precisely the criterion the expansion bar names — *Automatic cannot express it in the relevant multi-program cases*.** **⚠️ Do NOT re-reject `ARCHITECTURE` on the strength of the no-op paragraph above.** **What DOES block the owner is copy, not vocabulary:** ~41 Building Utilities notes generate with **computation guidance silently OFF** (no `architecture` in `QUANTITATIVE_KEYWORDS`), the fix is classifying them `ENGINEERING_SCIENCES` (`quantitative=true`), and the curator cannot discover that because `frontend/lib/domain-context.ts` describes that value as a **Civil-flavoured list omitting plumbing, HVAC, lighting, acoustics and fire protection**. **⚠️ ALL THREE OF THOSE COPY FIXES HAVE SHIPPED — VERIFIED AGAINST CODE 2026-09-04, and this line previously read as still-owed work.** `ENGINEERING_SCIENCES`' description now names plumbing, HVAC, electrical distribution, lighting, acoustics, fire protection and vertical transportation; `CIVIL_ENGINEERING` no longer claims Surveying or Construction Management and routes shared knowledge to Engineering Sciences; and `REVIEW_SET_SHAPING_CONTEXT.md` no longer contains *"Architecture, notably, deliberately has no Domain Context"* (**zero matches**). **⚠️ CONSEQUENCE: the ALE curator copy is NOT the blocker any more.** Phase 1 of the plan — the targeted Domain Context pass over the 215 `(unset)` rows — is curator work with **no code and no release** and can run now. **What still blocks is the MULTI-PROGRAM case above, which needs a taxonomy value.** **⚠️ EXPLICITLY NOT GATED on that checkpoint, and it does not pre-empt it** — classifying under existing values is not a taxonomy action, and `ADR-001` records the multi-program rule as itself the forcing function generating the calibration evidence. **⚠️ AMENDED 2026-09-04 BECAUSE THE OPEN RELEASE IS SCOPED TO THIS: adding a value is no longer forbidden outright, it is GATED — no enum value may be added before Phase 3 owner approval, and every candidate must clear the ratified expansion bar plus `ADR-001` clause (a)'s ~10-note floor.** **⚠️ Do NOT add `GENERAL_ENGINEERING`** (name-rejected: a plausible grouping nobody teaches under — **name rejection ≠ family rejection**), **do NOT extend `QUANTITATIVE_KEYWORDS`, do NOT remove the zero-usage values, do NOT resolve the zero-usage question by reasoning, and do NOT write a second rubric — R4's exists.** **⚠️ Every new value ships `quantitative = false` unless computation guidance is affirmatively wanted — `true` is PERMANENT PER NOTE.** Current version: **v0.111.0 — Multidisciplinary Domain Context** (Released, kicked off and signed off 2026-09-04, base branch `releases/v0.111.0`). **⚠️ CUT FROM `docs/correct-architecture-domain-context-verdict`, NOT FROM `main` — an owner-ruled exception (2026-09-04)** so the corrected Architecture verdict and the `v0.111.0` handoff ride in this release; **PR #1260 is closed unmerged** and that commit reaches `main` through this release's PR. **THE BLOCKER IS THE BLOCK DIRECTLY ABOVE, AND IT IS REPORTED AND REPRODUCED, NOT HYPOTHETICAL:** a note with Subject *Architectural Design* and both `Architecture` and `Architectural Engineering` **cannot be generated at all** — the resolver fallback is gated on `joinedPrograms.size() == 1` and `assertGenerationReady:33-39` throws on 2+ programs with a null Domain Context, and no existing value honestly fits. **GOVERNED BY `docs/claude-plans/ale-let-review-set-unblock-and-domain-context-evidence-plan.md` — EXECUTE ITS PHASES, DO NOT RE-DERIVE ITS CONCLUSIONS, and read its *Superseded assumptions* table first.** **⚠️ PHASE 0 → 1 → 2 → 3, and NO IMPLEMENTATION BEFORE PHASE 3 APPROVAL.** Phases 0 and 1 are owner/curator work with no code; **Phase 1 is UNBLOCKED NOW and runs in parallel**; **Phase 2 is blocked on two owner-executed inputs — Phase 0.3's LIVE catalog capture and Phase 1's corrected ALE distribution plus residue.** **⚠️ THE `ADR-001` RATIO DENOMINATOR OF 21 IS STALE** — production holds at least 22 and the catalog is admin-manageable, so computing the ratio from 21 repeats the exact error the ADR was corrected for on 2026-08-31; **Phase 0.3 is load-bearing, not bookkeeping.** **SCOPE IS SHAPE, NOT VALUES — which enum values ship is Phase 2's output and Phase 3's decision, and naming candidates now would pre-commit the answer exactly as the shaping module did to produce the 215 `(unset)` ALE rows:** enum values · labels · `quantitative` flags · `frontend/lib/domain-context.ts` descriptions · `REVIEW_SET_SHAPING_CONTEXT.md` rules · the `ADR-001` amendment · tests. **⚠️ NO MIGRATION** — `notes.domain_context` is `VARCHAR(64)` with no CHECK (`V102:2`, verified at kickoff). **⚠️ ONE RELEASE (owner, because it blocks the ALE build)** — splitting it ships a taxonomy whose curator-facing descriptions and strategist rules lag the enum, the precise defect that produced the 215 unset rows. **⚠️ TWO `ADR-001` OBLIGATIONS, NOT ONE: the failure-condition amendment, argued against the LIVE catalog count, AND clause (b)'s explicit owner decision in the revision log.** **⚠️ ANTI-DRIFT: every new value defaults `quantitative = false`** — the one irreversible thing in the release, since `true` is permanent per note and two values were delivered `true` and corrected; **remove NO existing value** (removal can lock existing multi-program rows); **borrow real curriculum vocabulary and never invent**; **program-stripping is an EMERGENCY WORKAROUND ONLY and every use is logged as Phase 2 input**; **the three zero-usage values must have their explanation STATED, not resolved by reasoning**; **the taxonomy stays single-valued, closed and non-admin-editable**; **no quota/meter change; no `ProfileType` gate; no new mode or sub-mode**; **`frontend/app/onboarding` STAYS FROZEN** — verified, not assumed: its only Domain Context reference is `domainContext: null` as a payload field, and **TWELVE dated reads fall between `2026-09-10` and `2026-09-17`.** **VERIFICATION IS DECLARED CONDITIONALLY ON A PHASE 3 DECISION: a single `advisor()` call if every new value ships `quantitative = false`; ONE SCOPED COLD AGENT framed as falsification if any ships `true`** — with a pre-declared guard pinning the multi-program case end to end, because a single-program fixture passes under both the defect and the fix and proves nothing. **⚠️ `[CHECKPOINT — due 2026-09-28]` is RETIRED by this release, not confounded — recorded as a choice now, with Phase 5's query re-specified before it runs.** **Routing: CLAUDE CODE inline.** Full scope in `RELEASES.md`. Previous: Current version: **v0.110.2 — Shared Link Integrity** (Released, kicked off and signed off 2026-09-04, base branch `releases/v0.110.2`, cut from `main` after `v0.110.1` merged and tagged). **A PATCH, and a LIVE DEFECT found during `v0.110.0`'s item 4 audit and carried unfixed through TWO releases.** **⚠️ RE-VERIFIED AGAINST `main` AT KICKOFF, not trusted from the note:** `GeneratedQuizService.generate:139` reuses the EXISTING entity and `:152` overwrites its questions, so **the row id never changes**, while `quiz_share_links.generated_quiz_id` points at that id and `GeneratedQuizService` references share links **ZERO times**. **So regenerating an already-shared quiz mutates the quiz a recipient may be part-way through, and it fails two different ways:** if the question count CHANGED (a `TEACHER` picking 10/20/30) the exact `answers.size() != questions.size()` check **400s them on submit**; if the count STAYED THE SAME (every non-`TEACHER`, who always gets 10) there is **no error at all** and they are graded against **questions they never saw**. **⚠️ THE SILENT ONE IS THE WORSE ONE** — a wrong score returned as a correct one, to the person the quiz was made for. **⚠️ THIS IS THE EXACT HAZARD `v0.110.0` REJECTED REFS TO AVOID; the snapshot design removed it for COMBINED quizzes and never for single-note ones, which is the path that actually ships.** **SCOPE: (1)** regenerating a shared quiz turns its live link OFF rather than swapping questions underneath it, so the recipient meets the already-built *"no longer active"* screen instead of a wrong score; **(2)** the owner is told before and after. **⚠️ ANTI-DRIFT: DEACTIVATE the row, do NOT DELETE it** — deleting forces a new link and spends share-link quota, punishing the owner for a fix. **⚠️ Do NOT snapshot the single-note shared quiz here** — copying at share time is the cleaner fix and is a migration plus a new table, recorded as the larger alternative and deliberately not taken. **⚠️ Do NOT drop or weaken `uq_generated_quizzes_note_id`** — versioning is blocked by it, and `NoteService.mapToResponse` throws on a second row. **⚠️ Do NOT change what `getSharedQuizResults` does on a size mismatch** — the check is correct; the defect is that a live link can point at a changed quiz at all. **⚠️ No quota/meter/counter change; no `ProfileType` gate; no Learning Connection requirement; do not touch the analytics fields four dated checkpoints read.** **⚠️ `frontend/app/onboarding` STAYS FROZEN and TWELVE dated reads fall between `2026-09-10` and `2026-09-17` — this release must not touch any surface they measure.** **VERIFICATION: a single `advisor()` call on the diff** — no migration, no permission substrate, no cross-user read, no money semantics. **⚠️ ESCALATE TO ONE SCOPED COLD AGENT IF** the fix must alter `QuizShareLinkService`'s active-link semantics or a second write path to `generated_quizzes.questions` appears. **⚠️ PRE-DECLARED GUARD: the discriminating fixture is a regeneration returning the SAME NUMBER of questions** — the silent path — because a count-changing fixture fails the size check with or without the fix. **Routing: CLAUDE CODE inline** — one service, one guard, one confirmation string. Full scope in `RELEASES.md`. Previous: Current version: **v0.110.1 — Quiz Text Integrity** (Released, kicked off and signed off 2026-09-04, base branch `releases/v0.110.1`, cut from `main` after `v0.110.0` merged and tagged). **A PATCH, and corrective on both items.** **⚠️ THE NUMBER WAS CORRECTED AT KICKOFF — `v0.111.1` was proposed, but all eight patches in this repo are a `.1`/`.2` of a `.0` that EXISTS and `v0.111.0` does not.** **⚠️ ITEM 1 IS THE ONLY CRITICAL FROM `v0.110.0`'s TWO-AGENT PRESSURE TEST, IS PRE-EXISTING AND PRODUCT-WIDE, AND WAS REPRODUCED RATHER THAN INFERRED:** `QuizItem`'s `@JsonCreator` routes into the sanitizing constructor and `QuizValidationUtils.sanitizeChoiceTexts` is **NOT idempotent**, so the leading-label strip runs at generation AND on EVERY deserialization — `"A. B. Smith"` stores as `"B. Smith"` and reads back `"Smith"`; `"B. D.C. generator"` reads back `"C. generator"` then `"generator"`. **⚠️ IT COMPOUNDS ON EVERY READ**, across `generated_quizzes`, `study_packs`, `quick_review_sessions.session_state`, `exam_question_pool`, `challenge_quiz_question_bank` and `combined_quizzes`, in every mode — and Electrical (`A.C.`/`D.C.`) plus biology binomials sit squarely in `[A-Da-d]` on a board-exam catalog. **SCOPE: (1)** stop re-running the sanitizer on deserialization — **⚠️ the GENERATION-time sanitizer is LOAD-BEARING and STAYS; do NOT weaken `sanitizeChoiceTexts` or `LEADING_CHOICE_LABEL_PATTERN`. The defect is WHERE it runs, not WHAT it does.** **(2)** a supporter can find and REVOKE a combined quiz they already shared — today `/library/combined-quiz/{id}` is reachable ONLY by the post-assembly redirect, so closing the tab leaves a live public link the owner cannot turn off; **⚠️ `V132` already builds `idx_combined_quizzes_owner_created_at` and nothing queries it, which is why this reads as an omission rather than a decision.** **⚠️ ANTI-DRIFT: ALREADY-CORRUPTED ROWS ARE UNRECOVERABLE — the stripped text is gone and nothing records it, so do NOT write a guessing backfill and do NOT re-prefix labels; state the residual as a Known limitation.** **⚠️ No migration, no new field, no re-serialization pass — a read must simply stop mutating what it read.** **⚠️ Item 2 adds FIND and REVOKE, never EDIT — combined quiz rows stay IMMUTABLE.** **⚠️ No quota/meter/counter change; no `ProfileType` gate; no Learning Connection requirement; no new mode or sub-mode.** **⚠️ Do NOT change `BOARD_EXAM_STARTED`, `ADAPTIVE_PRACTICE_STARTED`, `QUIZ_SHARE_LINK_CREATED`'s `scope` metadata, or the `generate-quiz-combined-multi-note` tip id — four dated checkpoints read them.** **⚠️ `frontend/app/onboarding` STAYS FROZEN — `[CHECKPOINT — due 2026-09-11]` is 7 days out.** **VERIFICATION: ONE SCOPED COLD AGENT framed as falsification** — item 1 changes what a read of six existing tables returns. **⚠️ PRE-DECLARED GUARD: a stored choice must round-trip BYTE-IDENTICALLY, and the fixture must use an ALREADY-LABEL-LOOKING value (`"B. Smith"`, `"D.C. generator"`) — an unlabelled value round-trips cleanly under BOTH the defect and the fix and proves nothing. A second guard is owed at the other end: generation must still strip exactly one label.** **Routing: CLAUDE CODE inline for item 1** (one class, narrow); **re-run the routing test for item 2** if it grows a backend endpoint. Full scope in `RELEASES.md`. Previous: Current version: **v0.110.0 — Supporter Combined Quiz** (Released, kicked off 2026-09-03, signed off 2026-09-04, base branch `releases/v0.110.0`, cut from `main` after `v0.109.0` merged and tagged). **SLICE 6 OF THE APPROVED ASSESSMENT SEQUENCE — THE LAST ONE.** Slices 1–5 shipped across `v0.104.0`–`v0.107.0`; `v0.109.0` announced them. **⚠️ IT OPENS NOW BECAUSE A ROADMAP PREMISE WAS CHECKED AND FOUND FALSE.** `assessment-architecture-audit.md` §15 said the session-anchoring migration was deferred *"until slice 6 forces it"*, so slice 6 read as a `quick_review_sessions` migration touching `V41`'s two partial unique indexes — i.e. a full-pressure-test release. **VERIFIED AGAINST CODE 2026-09-03 AND FALSE: `GeneratedQuizService` and `QuizShareLinkService` reference `quick_review_sessions` ZERO TIMES.** The supporter quiz path persists to `generated_quizzes` and is taken by a recipient who needs **no account**, so it creates no session and session anchoring cannot bind on it. **§15 is corrected.** **⚠️ THE REAL CONSTRAINT IS A DIFFERENT TABLE AND IT IS SHARP: `generated_quizzes.note_id` is `NOT NULL` WITH A UNIQUE INDEX (`uq_generated_quizzes_note_id`, `V43`), so EXACTLY ONE generated quiz exists per note.** A **combined** quiz spanning several notes has no `note_id` it can legally take, and anchoring it on one would collide with any quiz already generated for that note. **⚠️ So this release DOES owe a migration — on `generated_quizzes`, NOT on `quick_review_sessions`.** **⚠️ HALF OF THIS IS ALREADY BUILT — AUDIT BEFORE SCOPING ANY ITEM, exactly as `v0.89.0` audited a supposedly-unbuilt phase and found half of it shipped:** generating a quiz for someone else is **already ungated**, `/quiz/share/**` is **already `permitAll`** so the recipient needs no account, and `QuizShareLimitService` **already meters** share links per plan. **WHAT IS MISSING IS THE COMBINED PART, NOT THE SHARING.** **⚠️ SCOPE AMENDED 2026-09-03 AFTER A COLD AUDIT — THE RELEASE ROUGHLY DOUBLED AND THREE OF THE KICKOFF'S FOUR CLAIMS WERE WRONG. READ `RELEASES.md` FOR THE CURRENT SCOPE; the summary below is the SUPERSEDED kickoff version, kept only because its anti-drift still binds.** **The audit found TWO LIVE DEFECTS on the existing single-note path: shared quizzes silently MIS-GRADE MULTI_SELECT questions** (`PublicQuizItem` drops `questionFormat` and `correctIndices`; grading compares against `correctIndices.getFirst()`, so a genuinely correct answer scores zero — proven by probe, invisible to all 10 existing tests), **and a PLUS/PRO supporter picks 30 questions and silently receives 10** (`resolveQuestionCount` ignores the request for any non-`TEACHER` profile while the modal offers 10/20/30). **A guidance tip also advertises the multi-note flow to non-teachers, who cannot reach it.** **⚠️ STORAGE DECIDED (owner): SNAPSHOT INTO A NEW TABLE** — `generated_quizzes` and its unique index stay untouched, because dropping that index breaks `NoteService.mapToResponse:1529` on every note-detail load. **⚠️ Refs REJECTED** (regenerating a source note would mutate a live shared quiz and 400 the recipient mid-take). **⚠️ Do NOT generate fresh across N notes — quota semantics. ⚠️ Do NOT raise the question cap — pricing decision.** **SCOPE: (1)** a generated quiz can span several notes — **⚠️ THE ANCHOR PROBLEM HAS A KNOWN ANSWER IN THIS REPO, DO NOT INVENT A SECOND ONE:** `QuizItem` has carried `sourceStudyPackId` since `v0.104.0`, so items hold their own provenance while the row keeps a primary, exactly as Long Exam, Board Exam and plan-scoped Adaptive Practice all do; **⚠️ decide at prompt time whether the migration relaxes `note_id`'s uniqueness, makes it nullable, or adds a source list — and RECORD which, with the reason**; **(2)** the share path carries a combined quiz unchanged — **⚠️ VERIFY BEFORE BUILDING**, and note `PublicQuizItem`/DOCX export are **MCQ-only** (audit §15, a recorded sharing gap); **(3)** note selection **reuses the existing picker** (`collection-exam.ts`, the Challenge multi-note picker) rather than adding a surface. **⚠️ ANTI-DRIFT: NO SESSION-ANCHORING MIGRATION — IT HAS NO FORCING SLICE ANY MORE, WHICH IS THIS RELEASE'S OWN FINDING**, so the plan/note session collision stays a **named Known limitation** and its fix is a separate decision. **⚠️ Do NOT take it opportunistically because a migration is already in the diff. ⚠️ Do NOT touch `quick_review_sessions` AT ALL — if an item appears to need a session, that is the signal the scope is wrong.** **⚠️ NO Learning Connection requirement** (the slice's whole point) and **⚠️ do NOT gate on `ProfileType`** — `v0.89.0` records that as the axis error this capability exists to correct. **⚠️ Invitations stay ONE-AT-A-TIME** (`v0.90.0`); the share link stays the many-recipient mechanism and the two are never merged. **⚠️ No new mode or sub-mode** — a combined supporter quiz is a CAPABILITY and fails `v0.103.0`'s sub-mode test. **⚠️ NO quota, limit or meter change — `QuizShareLimitService` already meters share links; do NOT add a second counter** (`v0.92.0` rejected it as an untaken pricing decision). **⚠️ DOCX and multi-version exports stay `TEACHER`-gated. ⚠️ `frontend/app/onboarding` STAYS FROZEN (`[CHECKPOINT — due 2026-09-11]`, 8 days out). ⚠️ Do NOT change `BOARD_EXAM_STARTED` or `ADAPTIVE_PRACTICE_STARTED` fields — the two `2026-10-13` checkpoints re-armed in `v0.109.0` own those metrics.** **VERIFICATION: ONE SCOPED COLD AGENT framed as FALSIFICATION** — the trigger is named by the gate outright, this changes **production-data semantics via a migration**; it is **NOT** the three-agent test, since there is no permission substrate, no new cross-user read, and no money or quota change. **⚠️ ESCALATE TO THE FULL TEST IF** the migration turns out to need `quick_review_sessions` after all, or item 2 finds the share path incomplete. **⚠️ CARRIED LESSONS, EVERY ONE PAID FOR IN THE LAST THREE RELEASES: a negative assertion must have a REACHABLE subject; write the guard at the LAYER THE DEFECT LIVES AT; a test can SURVIVE ITS OWN MUTATION if the fixture cannot discriminate; MUTATE and confirm a NAMED test fails; read `./mvnw`'s EXIT STATUS directly, never through a pipe; COUNT executed tests from `target/surefire-reports/*.xml`; run `npm test`; SWEEP BY SURFACE, not by diff; VERIFY "X already does Y" AGAINST CODE BEFORE IT REACHES A PROMPT; and call `advisor()` BEFORE writing the Codex prompt.** **Routing: CODEX.** Full scope in `RELEASES.md`. Previous: Current version: **v0.109.0 — Assessment Discoverability** (Released, kicked off and signed off 2026-09-03, base branch `releases/v0.109.0`, cut from `main` after `v0.108.0` merged and tagged). **⚠️ TRIGGERED BY AN OWNER OBSERVATION, NOT A DEFECT REPORT (2026-09-03):** *"we don't yet announce that the existing plans were updated to be much comprehensive."* **Both `2026-10-06` assessment checkpoints were LIFTED to `[EFFORT]` gates rather than re-dated** — a read on an unannounced capability measures **DISCOVERABILITY, NOT DEMAND**, and `v0.107.0`'s kill criterion (*"if plan-scoped starts are ZERO … do NOT build slice 6"*) would have fired on evidence it was never designed to weigh, producing a kill signal that measured our own silence. **THIS RELEASE IS WHAT UN-LIFTS THEM.** **⚠️ THE GAP IS COMMUNICATION, NOT WIRING, AND THE OBVIOUS READING IS WRONG:** `v0.103.0`'s defect was a capability **UNREACHABLE** through the UI. **This is NOT that** — the CTAs exist and work (*Practice Weak Areas Across This Plan*, *Practice Across This Plan*, Board Exam from a Review Set). **⚠️ Do NOT "fix" reachability; there is nothing to wire.** What is missing is that nothing TELLS a learner it changed. **⚠️ THE CORE IS VERIFIED, NOT ASSERTED: `board-exam-guide.tsx` contains the string "Review Set" ZERO TIMES** — the Help Center guide for Board Exam does not mention the thing a Board Exam is now built from — and it still advises organising *"one note per topic"*, **which is now the SMALLEST POSSIBLE Board Exam rather than the intended one.** `quiz-modes-guide.tsx` mentions Review Set zero times and Adaptive Practice once. **These guides describe the product as it was FOUR RELEASES AGO.** **⚠️ THIS IS THE SWEEP-BY-SURFACE RULE FAILING IN THE DIRECTION IT ALWAYS FAILS** — `AGENTS.md` records it: *a file that explains a feature is exactly the file that never changes when the feature does.* **SCOPE: (1)** the Help Center guides describe the product that exists — **⚠️ anchor every claim to code before writing it**, copy only; **(2)** announce it where learners already are, not only in release notes — **⚠️ the SURFACE is a decision owed AT PROMPT TIME, and a one-time tip must go through `pickActiveGuidance()`**, never a bespoke localStorage flag; **(3)** re-arm both lifted checkpoints dated from THIS deploy, **⚠️ re-using their preserved reads, kill criteria and denominator clauses VERBATIM rather than re-deriving them.** **⚠️ ANTI-DRIFT: NO BEHAVIOUR CHANGE — this release changes what the product SAYS, not what it does.** **⚠️ Do NOT add a second entry point because the capability feels hidden — that solves the wrong problem AND confounds the very reads item 3 re-arms.** **No new mode or sub-mode; do NOT change `BOARD_EXAM_STARTED`'s or `ADAPTIVE_PRACTICE_STARTED`'s field names, values or firing conditions (both lifted checkpoints still own those metrics); slice 6 and the session-anchoring migration are NOT started; the plan/note session collision stays a NAMED Known limitation; `frontend/app/onboarding` STAYS FROZEN (`[CHECKPOINT — due 2026-09-11]`, 8 days out, 62.4% baseline); marketing and pricing copy are OUT per `v0.76.0`'s money-surface doctrine.** **VERIFICATION: a single `advisor()` call on the diff** — no permission substrate, no cross-user read, no money semantics, no migration, and by construction no behaviour change. **⚠️ ESCALATE TO ONE SCOPED COLD AGENT IF** item 2's surface decision becomes a behaviour change. **⚠️ THE VERIFICATION RISK IS UNUSUAL AND IS NAMED DELIBERATELY: A COPY RELEASE HAS NO FAILING TEST TO CATCH A FALSE CLAIM.** The guides went stale silently for four releases precisely because nothing executes them. Anchor each claim to the code that implements it, and prefer a pinned string in a test over prose nothing checks — `quiz-session-history.test.ts` is the in-repo precedent. **Routing: CLAUDE CODE inline.** Full scope in `RELEASES.md`. Previous: Current version: **v0.108.0 — Session Identity** (Released, kicked off and signed off 2026-09-03, base branch `releases/v0.108.0`, cut from `main` after `v0.107.0` merged and tagged). **⚠️ DELIBERATELY SMALL, AND THE REASON IS MEASURED RATHER THAN STYLISTIC:** `v0.107.0` opened at three items, closed at **five plus a four-batch remediation**, and its three-agent pressure test found the feature's **central mechanism had never fired in production**. This release is scoped to what that release RECORDED AND DID NOT FIX. **⚠️ NO MIGRATION, NO NEW CAPABILITY.** **⚠️ THE PATTERN, STATED ONCE: three session types share the `ADAPTIVE` discriminator** — note-scoped Adaptive Practice, Interview Practice, and plan-scoped Adaptive Practice — and `V41` permits one active session per `(user, pack, mode)`. `v0.107.0` taught **five** read sites to tell them apart and left **two** that cannot. **⚠️ THE HEADLINE ITEM IS NOT PURELY INHERITED — `v0.107.0` MADE IT WORSE, which is why it leads.** `DashboardService.findInProgressSessionsByRecency:990-1002` queries `(userId, ADAPTIVE, IN_PROGRESS)` with **NO sub-mode filter** (verified by reading — zero `subMode` references in that method) and `continue-spotlight.tsx:55-58` routes the result to `/notes/{id}/adaptive-practice`. **Before `v0.107.0` that card rendered the interview session — wrong, but it went somewhere. Item 4's guard now makes `getAdaptiveQuizSession` REFUSE interview sessions, so the card routes to a page that explains the conflict and offers nothing: a DEAD END THIS PROJECT CREATED.** **SCOPE: (1)** the Dashboard *Continue Studying* card stops offering another mode's session — **⚠️ decide at prompt time whether to FILTER or to ROUTE CORRECTLY and record which**, since routing serves the learner better but touches `ContinueStudyingResumeType`; **(2)** `PostSessionNextStepService:160` stops giving an interview session Adaptive Practice's copy; **(3)** eligibility resolution stops materializing every DONE pack — **⚠️ VERIFIED VIABLE WITHOUT TOUCHING THE SHARED SAMPLER, which is what kept it out of `v0.107.0`**: reuse the EXISTING `findProgressViewsByNoteIdIn` projection (it already carries `id`, `noteId`, `ownerUserId`, `keyConcepts`, `status`), filter owner and status in **Java**, and load full entities only for the focus-eligible set. **⚠️ Do NOT add a new projection query — `StudyPackRepository:151-158` records a REPRODUCED `ConverterNotFoundException` trap.** **⚠️ RESIDUAL STATED NOT HIDDEN: this is a REDUCTION, not a bound** — a plan where every pack is weak still loads every pack, and a true bound needs `LongExamPlanSourceSampler.EligiblePlanSource` to stop taking a `StudyPackEntity`; **(4)** decide the unconsumed `GET /collections/{id}/adaptive-practice/in-progress`'s fate — give it a consumer or remove it, **do not carry it a second release.** **⚠️ ANTI-DRIFT: NO MIGRATION — the session-anchoring migration (nullable `study_pack_id` + `source_collection_id`) stays deferred to slice 6 per audit §15, and it is TEMPTING HERE PRECISELY BECAUSE IT WOULD DISSOLVE THE SHARED-DISCRIMINATOR PROBLEM OUTRIGHT. Do NOT take it** — it touches `V41`'s two partial unique indexes and is a full-pressure-test release on its own. **⚠️ No new mode or sub-mode; slice 6 (supporter combined quiz) NOT started; the plan/note session collision stays a NAMED Known limitation; viewing never writes `ConceptHealth`; entitlements UNCHANGED; no `ConceptHealth` backfill; `frontend/app/onboarding` STAYS FROZEN (`[CHECKPOINT — due 2026-09-11]` is 8 DAYS OUT and the ~12-read cluster is deliberately left its own slot); do NOT change `ADAPTIVE_PRACTICE_STARTED`'s `sourceScope`/`entry` values or firing condition — `[CHECKPOINT — due 2026-10-06]` and the re-specified `[CHECKPOINT — due 2026-09-12]` both read them.** **VERIFICATION: a single `advisor()` call on the diff**, chosen rather than inherited — no permission substrate, no cross-user read, no money/quota semantics, no migration. **⚠️ ESCALATE TO ONE SCOPED COLD AGENT IF** item 1 needs a `ContinueStudyingResumeType` change or item 3 cannot avoid the shared sampler. **⚠️ CARRIED LESSONS, ALL PROVED AT COST BY `v0.107.0`: a negative assertion must have a REACHABLE subject** (four vacuous assertions found, one written while hunting that exact class); **write the guard at the LAYER THE DEFECT LIVES AT** (a card-level test could not catch a page-level bug); **a test can SURVIVE ITS OWN MUTATION** if the fixture cannot discriminate; **MUTATE and confirm a NAMED test fails**; **read `./mvnw`'s EXIT STATUS directly, never through a pipe**; **COUNT executed tests from `target/surefire-reports/*.xml`**; **run `npm test`**; **SWEEP BY SURFACE, not by diff**; and **call `advisor()` BEFORE writing any Codex prompt.** **Routing: CLAUDE CODE inline.** Full scope in `RELEASES.md`. Previous: Current version: **v0.107.0 — Curriculum-Scale Remediation** (Released, kicked off and signed off 2026-09-03, base branch `releases/v0.107.0`, cut from `main` after `v0.106.0` merged, tagged and **DEPLOYED TO PRODUCTION**). **Slice 5 of the approved assessment sequence** (`docs/claude-plans/assessment-architecture-audit.md` §13) — **READ THAT AUDIT BEFORE TOUCHING ANY EXAM CODE** — **folded with the provenance debt slice 5 depends on.** **⚠️ THE FOLD IS AN EXPLICIT OWNER DECISION (2026-09-03), TAKEN AGAINST A RECOMMENDATION TO SHIP THE FIX ALONE, and the cost was STATED BEFORE IT WAS ACCEPTED rather than absorbed afterwards:** the recommendation was two small items; the owner chose to add slice 5, which **forces the FULL THREE-AGENT COLD PRESSURE TEST IN ISOLATED `git worktree`s (~490k tokens)** — the same fold shape that made `v0.105.0` the most expensive release this project has run. **The reasoning that makes it coherent is recorded so it is not re-derived: slice 5 exists ONLY to consume per-source evidence, and Interview Practice is the LAST MODE STILL CORRUPTING that evidence**, so the two are one dependency chain rather than two adjacent features. **⚠️ THE FIX IS PROSPECTIVE AND SLICE 5 READS THE STORE IT DOES NOT REPAIR — ACCEPTED, NOT OVERLOOKED.** Verified against precedent rather than assumed: `v0.104.0` shipped **NO backfill** (in-flight unstamped items kept the historical fallback; already-written rows were left alone), so `ConceptHealth` rows from past multi-source Interview Practice sessions **stay over-attributed**. That is the direct argument for landing the fix **now** rather than deferring it a fourth time — every release it slips adds more bad evidence. **⚠️ It ships as a NAMED Known limitation, not as a silent condition.** **⚠️ THE DEFECT WAS RE-VERIFIED BY READING CODE AT KICKOFF AND IS SMALLER THAN THREE RELEASES OF HANDOFFS IMPLIED:** `InterviewPracticeService.recordConceptsForSourcePacks:270-298` iterates `resolveSourceStudyPackIds(session)` and writes the **same concept list to EVERY source pack**, filtered only by that pack's own `getKeyConcepts()` — byte-for-byte the shape `v0.104.0` replaced in `LongExamService` — and `generateQuizForSources:573-604` stamps **NOTHING** (zero `withSourceStudyPackId` in the file). **⚠️ BUT THE HELPER ALREADY EXISTS: `QuizSessionReviewUtils.computeKeyConceptBreakdownBySourceStudyPack` (`:173`) is the `keyConcept`-variant per-source breakdown, ALREADY USED by `LongExamService:508` with identical semantics.** So this is ONE stamp at the merge seam plus a near-verbatim port of `LongExamService:550-600` — **no new util, no new API, no DTO change, no migration.** **⚠️ Do NOT build a second breakdown helper.** **⚠️ `resolveSourceStudyPackIds` MUST BE DEMOTED TO FALLBACK-ONLY** (reached only by unstamped, in-flight items) — **left ambiguous, per-source aggregation AND the old broadcast BOTH run, which is WORSE THAN TODAY, and the obvious fixture does not catch it** because *'pack A got Shear'* still passes when the broadcast also wrote it. **⚠️ THE FIXTURE THEREFORE NEEDS THE NEGATIVE ASSERTION `v0.104.0` LEARNED: a third pack C whose `getKeyConcepts()` CONTAINS the concept but which contributed NO item must receive NOTHING.** **⚠️ A CORRECTNESS-SEMANTICS TRAP THE MULTI-PACK FIXTURE CANNOT CATCH:** Interview Practice calls the **3-arg** `computeFullyCorrectKeyConcepts(quiz, selectedChoices, Map.of())`, where that `Map.of()` is **`selectedMultiChoices`** and identification/enumeration default empty; the breakdown helper takes **FIVE**. Both funnel through the same `effectiveKeyConcept` resolver, so semantics are preserved **ONLY IF** called as `(quiz, selectedChoices, Map.of(), Map.of(), Map.of())`. Get it wrong and the release silently changes **WHAT COUNTS AS CORRECT** while appearing to change only attribution — invisible to a multi-pack fixture, because both packs move together. **⚠️ PRE-DECLARED PINNING GUARD: a SINGLE-SOURCE Interview Practice session must write BYTE-IDENTICAL `ConceptHealth` before and after.** **SCOPE: (1)** per-source attribution in Interview Practice, per the above; **(2)** **Subject-Plan- and Review-Set-scoped Adaptive Practice remediation** — slice 5 proper — **⚠️ EXISTING NOTE-SCOPED ADAPTIVE PRACTICE IS UNCHANGED**, this ADDS a scope and does not replace or re-point the one that ships today; **(3)** a **docs-only** re-specification of `[CHECKPOINT — due 2026-09-12]`'s **QUERY**, **⚠️ A DEPENDENCY OF ITEM 2 RATHER THAN A THIRD FEATURE.** **⚠️ ITEM 3 WAS MIS-SPECIFIED AT THIS RELEASE'S OWN KICKOFF AND IS CORRECTED HERE — THE CORRECTION IS RECORDED RATHER THAN QUIETLY SWAPPED, BECAUSE THE STALE CLAIM WAS COMMITTED TO THREE FILES.** The kickoff asserted that `[CHECKPOINT — due 2026-09-12]`'s contradictory kill criterion was un-actioned and would fire in 9 days. **THAT IS FALSE: the remedy was ALREADY RE-SPECIFIED ON 2026-08-14, before the read**, and the checkpoint row carries it struck through — *"~~Old remedy: restore a demoted entry point.~~ NEW REMEDY — the result now changes SEQUENCING, not placement"*, the dashboard recommendation becoming a **prerequisite** for any further entry-point removal. **⚠️ THE ERROR CAME FROM A GENUINELY STALE ROW, WHICH IS WHY IT IS WORTH FIXING RATHER THAN JUST APOLOGISING FOR: the *Adaptive Practice becomes the recommendation engine* DIRECTION row still says the checkpoint "must be re-specified now, or it will fire in September"** — a warning written **before** the obligation was discharged and never updated afterwards. **Two rows describe one obligation; one carries the completed action and the other still carries the warning**, and reading the warning row first produced a wrong scoping decision this session. The direction row is marked **DISCHARGED** so it cannot cause a fourth re-derivation. **⚠️ THE RELEASE STILL OWES SOMETHING ON THAT CHECKPOINT, AND IT IS SHARPER THAN WHAT THE KICKOFF CLAIMED — A RE-SPECIFIED QUERY, NOT A RE-SPECIFIED REMEDY.** Its metric is a **BARE COUNT** of `ADAPTIVE_PRACTICE_STARTED` per active learner, post-deploy against the equivalent pre-deploy window — **it does NOT read entry metadata.** Item 2 adds a **new way to start an `ADAPTIVE` session inside that measurement window**, so plan-scoped starts would **silently inflate the count and the checkpoint would report *starts held* while the NEW surface did the work** — answering a question about the removed Quick Review route with evidence produced by a feature that did not exist when the question was asked. **⚠️ SO THE QUERY MUST FILTER BY `entry`, counting only the pre-existing note-scoped entries against the pre-deploy window** — which is possible only because `v0.76.1` put `entry` on that event for exactly this purpose. **Docs only; the metric's DEFINITION is narrowed, its remedy and kill criterion are UNTOUCHED.** **⚠️ THE ENTRY POINT IS DECIDED AT KICKOFF (owner, 2026-09-03): BOTH the collection detail page AND the dashboard, sharing ONE backend path.** **⚠️ THE COLLECTION PAGE ALREADY DEMONSTRATES THE CORRECT AGGREGATION AND THAT IS WHY IT IS THE NATURAL HOME:** `collection-detail-page-client.tsx:224` already computes `due: previous.due + counts.dueConceptCount` — **SUMMATION ACROSS PACKS, NOT CONCEPT MERGING**, which is exactly the semantics this release's anti-drift requires — renders it as a `N% · M due` readiness pill (`:291`), and already offers a per-note **_Review due concepts_** CTA (`:252`, `:917`). Plan scope is the **plan-level sibling of a CTA that already exists**, not a new concept. **⚠️ THE DASHBOARD IS IN SCOPE BECAUSE THE RATIFIED REMEDY NAMES IT, NOT FOR SYMMETRY.** `[CHECKPOINT — due 2026-09-12]`'s remedy — re-specified **2026-08-14, before the read** — states that *"the dashboard recommendation must carry that discovery load, and must be built and proven BEFORE any further entry point is removed."* Shipping slice 5 with no dashboard surface would leave that remedy pointing at something the release deliberately did not build. **⚠️ `DashboardFocusAreasResponse` carries a single `practiceNoteId` today, so this is a real contract change plus a *which plan do we surface* ranking rule — priced in, not discovered later.** **⚠️ ROUTE SHAPE IS COLLECTION-ADDRESSED, AND THIS IS FORCED BY THE ANCHOR DECISION RATHER THAN CHOSEN FOR TASTE.** `buildAdaptivePracticeHref(noteId, …)` builds `/notes/{noteId}/adaptive-practice`, so a `?collectionId=` param would require the **FRONTEND** to supply the anchor — **reintroducing the exact anchor-drift hazard the anchor decision mitigates for resume but NOT for start.** A collection-addressed route lets the **BACKEND** derive the anchor, which is what that decision already assumes. **⚠️ A `?collectionId=` param on the note-addressed route CONTRADICTS the anchor decision; do not use it.** **⚠️ `entry` AND `sourceScope` ARE SEPARATE FIELDS AND MUST NOT BE CONFLATED — `entry` answers WHERE the learner started, `sourceScope` answers WHAT was practised.** Add entry values for the two new surfaces, and record `sourceScope` (`note` | `plan` | `review-set`) as analytics metadata following the Board Exam precedent exactly — **the VERIFIED outcome, never the caller's claim** (`ChallengeQuizService:511,646`). **⚠️ Do NOT mint one entry value per scope**; collapsing the two axes into one string is how `v0.103.0`'s leakage returned. The server already allowlists `entry` and normalizes unknown input to `direct` (`QuickReviewAdaptivePracticeService:87-95`) — extend that set, do not bypass it. **⚠️ SECTIONS ARE NOT A THIRD SCOPE.** The audit says Subject-Plan- **and** Review-Set-scoped; a section-scoped variant is creep. **⚠️ And Subject Plan vs Student Study Plan is NOT a distinction — they are the same `NoteCollectionEntity`** (`v0.105.0` anti-drift); do not invent one. **⚠️ THE KICKOFF'S ACCOUNT OF ITEM 3 WAS WRONG AND IS SUPERSEDED — see the item 3 entry in Planned Scope.** It claimed the `2026-09-12` kill criterion was un-actioned; **the remedy was in fact re-specified 2026-08-14, before the read.** What the release actually owes is a re-specified **QUERY**, because the metric is a bare count that item 2 would silently inflate. The stale *direction* row that produced the error is marked DISCHARGED. **⚠️ ANTI-DRIFT: CROSS-PACK CANONICAL CONCEPT IDENTITY STAYS ADR-SIZED AND OUT** — `ConceptHealthEntity` stays keyed `(user_id, study_pack_id, concept)` with free-text concept scoped per pack, so **plan- and Review-Set-scoped remediation must AGGREGATE OVER packs, NEVER MERGE concepts across them**; a Subject Plan's packs sharing a concept string is exactly the case that is not yet decidable, and slice 5 must not quietly decide it. **⚠️ CONCEPT AGGREGATION IS DECIDED AT KICKOFF (owner, 2026-09-03): TWO ENTRIES, EACH STAMPED WITH ITS SOURCE PACK. NEVER MERGE BY CONCEPT STRING.** **⚠️ THE REPO ALREADY ANSWERS THIS AND THE ANSWER IS UNAMBIGUOUS — THE PRODUCT HAS NEVER DISPLAYED A BARE CONCEPT NAME AT MULTI-PACK SCOPE.** `ConceptHealthService.getDueConceptsByStudyPackIds` returns `Map<UUID, List<String>>` — **per pack, unmerged** — and already has four callers (`DashboardService:309,:400`, `RetentionService:351`, `NoteCollectionService:2197`); `DashboardService:400` builds `TodayFocusConceptResponse(concept, noteId, studyPack.getTitle())`, the DTO is `record(concept, noteId, noteTitle)`, and `today-focus-card.tsx:62,65` **renders `concept.noteTitle` beside every concept.** So *Shear Force · Structural Analysis* and *Shear Force · Reinforced Concrete Design* are visibly distinct rows and **do NOT read as a duplicate bug** — the source pack is what disambiguates them. **⚠️ MERGING WOULD BE A CROSS-PACK CANONICAL IDENTITY CLAIM — exactly what is locked ADR-sized and OUT, and what `v0.104.0`'s entire provenance arc exists to prevent.** It is also **unfixable later without a migration**, because once merged in the UI the learner can no longer tell which pack the weakness came from. **⚠️ AND THE DECISION IS NECESSARY BUT NOT SUFFICIENT — THE COLLAPSE POINT IS A DATA STRUCTURE, WHICH IS THE `v0.104.0` LESSON REPEATING ON A NEW SURFACE.** `resolveAdaptiveFocus` returns `new ArrayList<>(focusConcepts)` where `focusConcepts` is a **`LinkedHashSet<String>` of BARE CONCEPT NAMES**. That is harmless for one pack and **silently merges across packs the moment plan scope reuses it** — the identical shape as `computeConceptBreakdown` keying by `normalizeConcept(item.concept())` alone, which made `v0.104.0`'s correct stamp change nothing downstream. **⚠️ SO THE PLAN-SCOPED FOCUS STRUCTURE MUST BE KEYED BY `(studyPackId, concept)`, NOT BY A `Set<String>`. A test that only asserts "two entries appear" against a fixture with DIFFERENT concept names passes under the merge; the fixture must use the SAME concept string in two packs.** **⚠️ `ConceptHealthService.normalizeConcept` is `trim()` ONLY (`:387-393`) and is applied INSIDE the per-pack map (`:176`), so it does not collapse across packs — do not "harden" it into a lowercase-and-key step, which would.** **⚠️ COUNT AGGREGATION IS UNAFFECTED AND STAYS AS IT IS:** `collection-detail-page-client.tsx:224` sums `dueConceptCount` across packs for the readiness pill. **Summing COUNTS makes no identity claim and is correct. ⚠️ Do NOT "fix" it to dedupe by concept name** — that would introduce this very merge on the one surface that displays no names at all. **⚠️ GENERATION FALLS OUT FOR FREE AND MUST NOT BE "OPTIMISED":** generation already fans out per pack, so *Shear Force* from pack A is generated against pack A's title, summary and key concepts and pack B's against pack B's, with **no special handling. ⚠️ Do NOT pass a merged concept list into a single prompt** — it would ask one pack's context to generate another pack's weakness. **⚠️ DEDUPLICATION MUST ACCUMULATE ACROSS PACKS, AND THIS IS THE HIGHEST-COLLISION CASE THE DEDUPE WILL EVER SEE** — two packs weak on the SAME concept generating in ONE session. The note-scoped path passes only `extractQuestionTexts(studyPack.getQuiz())`, a single pack's saved quiz; **plan scope must follow `InterviewPracticeService.generateQuizForSources`, accumulating `disallowedQuestions` across sources AND excluding each pack's own saved quiz**, which is also what `v0.106.0` shipped for both exam modes. **⚠️ Keep that exclusion at the generation seam; do NOT move it to a caller.** **⚠️ "WHICH PACK DO WE SURFACE" ALREADY HAS AN ANSWER — DO NOT BUILD A RANKING MODEL.** `DashboardService:400` returns inside its loop, surfacing the **first pack with due concepts** over `findByOwnerUserIdOrderByCreatedAtDescIdDesc` — a **deterministic total order** (`id` breaks `createdAt` ties), verified rather than assumed. **⚠️ A weakness-ranking engine is the recommendation engine's own UNRATIFIED scope and is explicitly OUT of this release.** **⚠️ NO NEW MODE AND NO NEW SUB-MODE** (`EXAM_MODES.md` is a locked five-mode contract; broader scope is a CAPABILITY of Adaptive Practice) and **modes are never differentiated primarily by question count**. **⚠️ VIEWING MUST NEVER WRITE `ConceptHealth`** — locked since `v0.37.0` to move only from genuine assessment; a remediation surface that READS evidence must WRITE none. **⚠️ QUOTA IS DECIDED AT KICKOFF (owner, 2026-09-03): ONE `adaptive_quiz_generations` UNIT PER SESSION, regardless of scope or pack count, CHARGED AFTER SUCCESSFUL GENERATION, WITH GENERATION STAYING SYNCHRONOUS INSIDE THE TRANSACTION.** Both precedents say one and the arithmetic is not close: `BOARD_EXAM_QUOTA_UNITS_PER_SESSION = 1` (`ChallengeQuizService:187`) charges a single unit for a Review-Set-scoped Board Exam of up to **30** questions, and `LongExamService:272` charges `QUOTA_UNITS_PER_SESSION` once for a multi-source plan-sourced exam. **⚠️ REJECTED — per-pack charging:** it changes what a unit BUYS, which is the quota change this release explicitly locks, and it would charge more for **identical output**, since a plan-scoped session produces the same **≤10** questions as a note-scoped one. **⚠️ THE NO-REFUND PROPERTY IS AN ACCIDENT OF ORDERING PLUS THE TRANSACTION — THE SAME SHAPE `v0.106.0` FOUND IN BOARD EXAM, AND IT MUST NOT BE BROKEN THE SAME WAY.** `incrementAdaptiveQuizGeneration` runs at `:220`, **AFTER** `markSessionReady`, inside a class-level `@Transactional` (`:50`), so a generation failure simply never charges. **⚠️ THEREFORE: DO NOT COPY `v0.105.0`/`v0.106.0`'s REFUND MACHINERY INTO ADAPTIVE PRACTICE — it is not needed and adding it is scope creep. The refund question opens ONLY if the transaction boundary moves, so DO NOT MOVE IT.** **⚠️ AND THE TRANSACTION DECISION IS VALID ONLY UNDER A BOUND — this is a PRECONDITION, not a residual.** `InterviewPracticeService` already fans out one LLM call per source inside its own class-level `@Transactional` (`:52`), but its fan-out is bounded by a handful of learner-picked `additionalNoteIds`; **a Subject Plan is ~77 notes and a Review Set ~550, so "Interview Practice already does this" licenses N≈3, NEVER N≈77.** An unbounded fan-out would hold a database transaction open across dozens of LLM calls — **precisely the pathology `v0.105.0` and `v0.106.0` moved generation off the transaction to escape, arriving here by the back door.** **⚠️ SO THE SAMPLED PACK COUNT MUST BE BOUNDED — REUSE `LongExamPlanSourceSampler` (already injected into `ChallengeQuizService`), do NOT build a second sampler, and never fan out over every note in a plan. IF IT CANNOT BE BOUNDED, THE TRANSACTION BOUNDARY MUST MOVE AND THE REFUND QUESTION REOPENS — the two decisions are coupled and must be taken together.** **⚠️ A SECOND BOUND IS OWED AND IS EASY TO MISS BECAUSE THE QUESTION-COUNT CAP LOOKS LIKE IT COVERS IT — IT DOES NOT. `focusConcepts` IS UNBOUNDED:** `resolveAdaptiveFocus` returns a `LinkedHashSet` union of `getDueConcepts(...)` and the previous session's weak concepts with **no cap**, bounded today only because a single pack has few key concepts. Aggregated across a plan it **grows without limit and is passed straight into the prompt**, while `resolveAdaptiveQuestionCount` (5 / 7 / 10, a step function on WEAK CONCEPT COUNT with **no source-count input**) caps only the **OUTPUT**. **⚠️ Bound the focus-concept list explicitly for plan scope; do NOT rely on the question cap, and do NOT change `resolveAdaptiveQuestionCount`** — its lack of a source-count input is what already satisfies `v0.102.0`'s standing *"do NOT scale questionCount with source count"* rule, for free. **⚠️ THE UNLOCKED QUOTA READ IS A PRE-EXISTING DOUBLE-SPEND HOLE — NAMED, NOT INTRODUCED, AND DELIBERATELY NOT FIXED HERE.** `assertAdaptivePracticeQuotaAvailable` (`:185`) is an unlocked read and the `GENERATING` insert lands at `:188`. Concurrent starts on the **same** anchor are caught by `V41`'s unique index; concurrent starts on **different** anchors (a note-scoped session on pack A beside a plan-scoped one on pack B) both pass the check and both charge. **⚠️ This is reachable TODAY with two packs, so plan scope makes it easier to hit without creating a new defect class.** **⚠️ DO NOT "FIX" IT WITH `userRepository.findByIdForUpdate(userId)`.** That is the right pattern in `ChallengeQuizService`, where it is taken **conditionally** on the multi-note path, and it is safe there **only because Board Exam generation no longer holds the transaction across generation.** Adaptive Practice generation **does**, by the decision above — so taking the user-row lock here would hold a `PESSIMISTIC_WRITE` across the LLM call and serialize every other quiz start on that account, which is the exact cost `ChallengeQuizService`'s own comment documents. **The lock and the in-transaction generation are mutually exclusive; this release keeps the generation and names the hole.** **⚠️ PLAN-TIER ENTITLEMENTS UNCHANGED** — a separate monetization contract; *"nothing is gated"* is never licence to add, widen or remove a subscription gate. **No quota or meter change.** **⚠️ NO BACKFILL of historical `ConceptHealth`.** **⚠️ Do NOT remove the Challenge Quiz Adaptive Practice entry point** — item 3 re-specifies the checkpoint, it does NOT license the removal that checkpoint was guarding. **⚠️ THE SESSION ANCHOR IS DECIDED AT KICKOFF, NOT LEFT TO IMPLEMENTATION (owner, 2026-09-03) — and the handoff's "§15 is not needed for slice 5" was RIGHT IN CONCLUSION BUT UNDERIVED, so the reasoning is recorded rather than the verdict alone.** Verified by reading migrations: `quick_review_sessions.study_pack_id` **and** `note_id` are both **`NOT NULL`** (`V4:4`, `V19:162`, the latter backfilled FROM `study_packs.note_id`), and **`V41` DROPS AND RECREATES** the active-session unique indexes as `(user_id, study_pack_id, session_mode)` and `(user_id, note_id, session_mode)`, both **`WHERE status IN ('GENERATING','IN_PROGRESS')`** — so a still-**GENERATING** session already blocks, and the collision window is wider than *"in progress"* suggests. `QuickReviewSessionMode` has four values and **`ADAPTIVE` is shared** (no new mode is permitted), so a session is anchored to exactly one `(pack, note)` pair. **⚠️ DECISION: the session stays anchored on a PRIMARY PACK, and `sourceCollectionId` is a REQUEST FIELD that widens the SOURCE set only — never the pack the session is KEYED on. NO MIGRATION, NO INDEX CHANGE, NO NEW COLUMN.** This is the third consecutive release to answer it the same way and the precedent is what settles it: `v0.105.0` ruled it for plan-sourced Long Exam, and `v0.106.0` shipped Review-Set-scoped Board Exam **still taking a caller-supplied primary from the URL path**, recording scope as **analytics metadata** (`sourceScope`, the VERIFIED outcome, never the caller's claim) rather than as a column. **⚠️ BUT SLICE 5 HAS NO CALLER TO SUPPLY IT — its entry is DASHBOARD-DRIVEN, so nothing hands it a pack, and that is the part the precedent does NOT answer. THE ANCHOR IS THE PLAN'S LOWEST-POSITION ELIGIBLE PACK** (`findByCollectionIdOrderByPositionAsc` already exists), which matches the convention already in the repo — `resolveSourceNoteRefs` adds the primary first and `v0.105.0` force-includes it at index 0 — and is stable across visits with no learner action. **⚠️ RESIDUAL HAZARD, NAMED BECAUSE IT IS NOT OBVIOUS: `position` IS MUTABLE** (`NoteCollectionService.setPosition` fires on reorder-save and on add/remove), so reordering a plan — or removing its first note — **CHANGES THE ANCHOR MID-SESSION.** A resume lookup that **RECOMPUTES** the anchor would then miss the active session and the insert would succeed on a different `study_pack_id`, leaving **TWO active plan-scoped sessions with the first permanently unreachable.** **⚠️ SO THE RESUME MUST BE RESOLVED FROM A `(user, mode, status)` LIST FILTERED IN JAVA ON THE RECORDED COLLECTION ID, NEVER BY RECOMPUTING THE ANCHOR** — `findTopByUserIdAndSessionModeAndStatusOrderByCreatedAtDesc` already exists (`:372`) and a `StatusIn` List variant is a derived query. **⚠️ Do NOT add a JSONB predicate to do this — `v0.103.0` REJECTED that** (first in the repo, PostgreSQL-only, invisible to the mocked tests that are most of the suite). **⚠️ THE ACCEPTED COST IS A TWO-WAY COLLISION, RATIFIED BY THE OWNER (2026-09-03) AND SHIPPING AS A NAMED KNOWN LIMITATION — BOTH DIRECTIONS MUST BE PINNED BY TESTS:** (a) a plan-scoped session active on pack X makes a **note-scoped** request on X resume the **plan** session; (b) a note-scoped session active on X makes a **plan-scoped** request resume the **note** session. **⚠️ (b) IS THE WORSE SYMPTOM AND THE ONE THAT MAKES ITEM 2 LOOK BROKEN IN PRODUCTION** — the learner asks for whole-plan remediation and silently receives a single-note session. **Board Exam ↔ Challenge already ships this exact collision** on the same index (`resolveExistingChallengeSession` is mode-agnostic within `CHALLENGE`), which is why it is acceptable rather than novel. **⚠️ THE ANTI-DRIFT GUARANTEE IS THEREFORE NARROWED TO WHAT IT ACTUALLY PROTECTS: note-scoped Adaptive Practice is NOT re-pointed, replaced, or changed in behaviour — but it MAY CONTEND with a plan-scoped session for its anchor pack.** Stated at kickoff rather than discovered as a bug report. **⚠️ WIDENED 2026-09-03 DURING THE ITEM 1 DIFF AUDIT — THE COLLISION IS THREE-WAY, NOT TWO-WAY, AND ONE LEG ALREADY SHIPS.** `InterviewPracticeService` also creates its sessions with **`QuickReviewSessionMode.ADAPTIVE`** (`:370`) and distinguishes its own by a **sub-mode string in session state** (`isInterviewSession:511-513`, `SUB_MODE_INTERVIEW`), the same discriminator pattern Board Exam uses on `CHALLENGE`. **So three session types share the `ADAPTIVE` enum and therefore the `(user_id, study_pack_id, session_mode)` unique index: note-scoped Adaptive Practice, Interview Practice, and the plan-scoped Adaptive Practice this release adds.** **⚠️ THE INTERVIEW ↔ ADAPTIVE LEG WAS A LIVE PRE-EXISTING DEFECT — ✅ FIXED IN THIS RELEASE AS ITEM 4, and the finding is kept because it is why the guard exists — verified by reading, not inferred:** `QuickReviewAdaptivePracticeService:131-145` looks up by `(userId, studyPackId, ADAPTIVE, ACTIVE)` and **does NOT check the sub-mode**, while `toAdaptiveResponse:503-527` does not throw on an interview-shaped session — it extracts the quiz from session state and returns it. **So an active Interview Practice session on pack X, when the learner requests Adaptive Practice on pack X, is RETURNED AS AN ADAPTIVE PRACTICE SESSION with the interview questions.** **⚠️ THE GUARD IS ASYMMETRIC, WHICH IS THE EVIDENCE IT IS AN OVERSIGHT RATHER THAN A DESIGN CHOICE:** Interview Practice resumes only its own (`existing != null && isInterviewSession(existing)`); Adaptive Practice never added the reciprocal check. Quota is not spent (the early return precedes `assertAdaptivePracticeQuotaAvailable`). **⚠️ DECISION TAKEN 2026-09-03 (owner): FOLDED INTO `v0.107.0` AS A NAMED ANTI-DRIFT EXCEPTION AND SEQUENCED AFTER ITEM 2 — it is ITEM 4.** Raised as pre-existing and out of the stated scope because fixing it **changes note-scoped Adaptive Practice behaviour, which this release's anti-drift otherwise locks**; the owner ruled to fix it here rather than defer it, since it is a one-line reciprocal guard in code item 2 already edits and the release is already at the full three-agent tier. **⚠️ THE ANTI-DRIFT LINE IS NARROWED A SECOND TIME, DELIBERATELY: note-scoped Adaptive Practice is not re-pointed or replaced, and its behaviour changes ONLY by ceasing to return another mode's session. That is the WHOLE of the permitted change.** **⚠️ SEQUENCING IS BINDING: item 4 lands AFTER item 2 merges, in its own branch and PR, NOT inside item 2's diff** — item 2's prompt already instructs Codex to exclude `SUB_MODE_INTERVIEW` in its NEW lookup and leave the existing one alone. **⚠️ THE FIX IS THE RECIPROCAL GUARD AND NOTHING MORE** — the existing lookup gains the sub-mode check `InterviewPracticeService:520` already applies in the other direction. **Do NOT restructure the lookup, do NOT add a sub-mode value, do NOT touch `toAdaptiveResponse`.** **⚠️ It needs a pinning test that FAILS ON REMOVAL, with a REACHABLE fixture subject** per item 1's vacuous-assertion finding. **⚠️ REGARDLESS OF THAT DECISION, item 2's NEW plan-scoped lookup MUST exclude `SUB_MODE_INTERVIEW` sessions** — omitting it would newly build the very bug this audit found. **⚠️ AND PLAN SCOPE MUST NOT TAKE A SUB-MODE VALUE:** by `v0.103.0`'s own sub-mode test it is a CAPABILITY — same questions, same scoring, same result screen, more notes — so discrimination is by the RECORDED COLLECTION ID, filtered in Java. **⚠️ REJECTED WITH REASONS, DO NOT RE-PROPOSE: widening the partial unique index with a scope discriminator.** A nullable column makes NULLs **DISTINCT** in PostgreSQL, which would BREAK the existing one-active-per-pack guarantee for note-scoped sessions unless `COALESCE`d to a sentinel — and it is a scope change plus a routing re-run that touches the very table slice 6 was meant to migrate. **⚠️ AND "NO MIGRATION" IS NOT "NO CONTRACT CHANGE": `generateAdaptiveQuiz(String studyPackIdRaw, UUID userId, String entry)` takes RAW PARAMS with no request DTO**, so plan scope needs a DTO or a new endpoint — a Codex-shaped surface. **⚠️ Do NOT touch Long Exam's or Board Exam's sampler, thresholds or refund path** beyond reading them. **⚠️ `frontend/app/onboarding` STAYS FROZEN — `[CHECKPOINT — due 2026-09-11]` is 8 DAYS OUT**; do not add, remove or reorder an onboarding FLOW step. **⚠️ Do NOT change `BOARD_EXAM_STARTED`'s `sourceScope` / `sourceCount` / `questionCount` / `expectedQuestionCount` field names or firing condition** — `[CHECKPOINT — due 2026-10-06]` reads them. **VERIFICATION: FULL THREE-AGENT COLD PRESSURE TEST IN ISOLATED `git worktree`s** — two independent triggers (production-data semantics on a shared evidence store, and a new learner-facing assessment scope consuming it). **⚠️ THE ISOLATION IS NOT OPTIONAL** — `v0.105.0` ran three mutating agents in one tree and they corrupted each other's builds, costing one agent's entire result set. **⚠️ ONE AGENT MUST BE POINTED AT THE BLIND SPOT, NOT THE FEATURE** — in the last three releases the worst defect was outside the stated scope every time. **⚠️ CARRIED MEASURED LESSONS: WRITE THE GUARD AT THE LAYER THE DEFECT LIVES AT** (four releases running — `v0.106.0`'s own first pool fix sat in the wrong service and would have burned clean pool rows while leaving Long Exam exposed); **ASK WHAT IN THE FIXTURE IS NEUTRALISING A SURVIVING MUTANT** before weakening the assertion (a sampler's primary re-add and an absent transaction manager each absorbed a real defect in `v0.106.0`, one of which would have 500'd every Board Exam start with 1979 tests green); **ASSERTIONS CAN PASS VACUOUSLY** — a bystander row must hold NON-ZERO values it would lose; **MUTATE and confirm a NAMED test fails**; **read `./mvnw test-compile`'s EXIT STATUS directly, never through a pipe**; **COUNT executed tests from `target/surefire-reports/*.xml`**; **run `npm test`**; **SWEEP BY SURFACE, NOT BY DIFF**; and **call `advisor()` BEFORE writing the Codex prompt.** **Routing: CODEX** — item 2 is multi-system. **⚠️ Item 1 alone would be inline; re-run the routing test if implementation discovers a surface not in the agreed plan**, which is how `v0.103.0` was mis-routed. Full scope in `RELEASES.md`. Previous: Current version: **v0.106.0 — Board Exam Review Set Identity** (Released, kicked off 2026-09-02, signed off 2026-09-03, base branch `releases/v0.106.0`, cut from `main` after `v0.105.0` merged, tagged and **DEPLOYED TO PRODUCTION**). **Slice 4 of the approved assessment sequence** (`docs/claude-plans/assessment-architecture-audit.md` §8, §13) — **READ THAT AUDIT BEFORE TOUCHING ANY EXAM CODE.** **⚠️ SEQUENCED AFTER A PRODUCTION DEPLOY, NOT MERELY AFTER A MERGE (owner, 2026-09-02):** `v0.105.0` reworked exam generation, added a pessimistic lock inside the Long Exam generation transaction and changed quota semantics on a paid path; slice 4 operates on the **same machinery**, so it waited for prod evidence rather than stacking on an unvalidated layer. **⚠️ BOARD EXAM IS TODAY WHAT THE ANTI-DRIFT RULES FORBID BUILDING, re-verified at kickoff:** `MAX_ADDITIONAL_BOARD_EXAM_SOURCE_COUNT = 2` (`ChallengeQuizService:173`) so the largest possible Board Exam is **3 notes**, and `min(12 × sourceCount, 30)` caps it at **30 questions** — against a ~550-note Review Set that is a rounding error, and the mode does not do the job it is named for. **⚠️ THE FINDING THAT SHAPES THIS RELEASE IS NOT IN THE AUDIT.** The audit lists *"move generation off the transaction"* as a prerequisite but does not say what that REMOVES. Verified by reading both services: `ChallengeQuizService.startSession` is a plain **`@Transactional`** (`:201`) and the Board Exam charge `incrementBoardExamGenerationBy` runs **INSIDE it** (`:403`) alongside synchronous generation, whereas `LongExamService.startSession` is **`@Transactional(NOT_SUPPORTED)`** (`:161`), dispatches after commit and charges outside. **⚠️ SO BOARD EXAM'S QUOTA SAFETY IS AN ACCIDENT OF ITS TRANSACTION — a generation failure rolls the charge back automatically, which is the ONLY reason it has never needed a refund path. Moving generation off the transaction DESTROYS that implicit rollback.** **⚠️ THE PREREQUISITE IS THEREFORE THREE CHANGES THAT SHIP TOGETHER OR NOT AT ALL: (1) generation off the transaction; (2) the quota REVERSAL `v0.105.0` built for Long Exam extended to Board Exam; (3) the RESURRECTION RACE GUARD extended with it — lock, then re-read status through the scalar projection before installing the quiz. Shipping (1) alone reintroduces, in Board Exam, BOTH defects `v0.105.0` just fixed.** **SCOPE: (1)** the whole Review Set is the eligible syllabus — Review Sets **are** hierarchical (`NoteCollectionEntity.parentCollectionId:69`), so the Subject Plan stratum is real; **(2)** stratify across Subject Plans then sample within, **reusing `v0.105.0`'s sampler — do NOT build a second one**, and **⚠️ Subject Plans are coverage STRATA, never curriculum WEIGHTS** (note count ≠ curriculum weight, section size ≠ exam weight); **(3)** generation off the transaction **with** the refund and race guard, mirroring `LongExamService` (dispatch after commit, charge outside, reversal on every transition into `FAILED` **including the recovery sweeper**, idempotent, clamped at zero); **(4)** a **CONFIGURABLE** target item count — **⚠️ NOT a permanent universal constant; do not block the architecture on choosing 50/75/100.** The architecture must separate **whole Review Set = eligible syllabus → Subject Plans = coverage strata → sampled sources → configured target count.** **⚠️ ANTI-DRIFT: CLAIM REPRESENTATIVE COVERAGE, NEVER OFFICIAL FIDELITY** — the repo holds **no board blueprint metadata of any kind** and the "domain" groupings in scoring are **free-text concept strings, not curriculum domains**; copy stays *"representative board-review exam"* and **do NOT invent domain weighting, per-board format mix or time-per-item norms**. **⚠️ BOARD EXAM STAYS MCQ-ONLY** (already its prompt's status quo, and the one place format fidelity is a product CLAIM) — no True/False, no Identification, no Enumeration. **⚠️ Board Exam is NOT a session mode and does not become one** — it stays a `CHALLENGE` session with its `board_exam` string; `EXAM_MODES.md` is a locked five-mode contract; **no new mode, no new sub-mode**; **modes are never differentiated primarily by question count.** **⚠️ Never send 550 full notes into one prompt.** **⚠️ ENTITLEMENTS UNCHANGED** — Board Exam stays PRO-gated; **the refund changes when a charge is REVERSED, never what anyone is entitled to.** **⚠️ Do NOT touch Long Exam's sampler, thresholds or refund path beyond extending them** — `v0.105.0` is freshly deployed and its production behaviour is the evidence slice 5 reads. **⚠️ OUT OF SCOPE:** cross-pack concept identity (ADR-sized), format-weighted `ConceptHealth`, **`InterviewPracticeService`'s over-attribution (STILL OWED before slice 5, live Backlog row)**, the seven carried `v0.103.0` limitations, and the session-anchoring migration (§15, deferred to slice 6). **⚠️ No code under `frontend/app/onboarding` — `[CHECKPOINT — due 2026-09-11]` is 9 DAYS OUT.** **VERIFICATION: FULL THREE-AGENT COLD PRESSURE TEST IN ISOLATED `git worktree`s.** **⚠️ THE ISOLATION IS NOT OPTIONAL** — `v0.105.0`'s first round ran three mutating agents in ONE tree and they corrupted each other's builds; one agent's results were discarded. **⚠️ ONE AGENT MUST BE POINTED AT THE BLIND SPOT, NOT THE FEATURE** — splitting by feature reproduces the blind spot on every side, and in the last TWO releases the worst defect was outside the stated scope both times (a non-idempotent sanitizer re-run, in two different copy helpers). **⚠️ THE LESSON THAT COST `v0.105.0` MOST: WRITE THE GUARD AT THE LAYER THE DEFECT LIVES AT** — three of its guards sat one layer below what they claimed to protect and passed while the defect was live (a schema test with hardcoded booleans instead of the derivation; a race test pinning the read but not the lock; a `never()` satisfied by a missing stub). **Ask of every new test: if I revert the actual fix, does THIS test fail?** Also carried: **MUTATE and confirm a NAMED test fails**; **read `./mvnw test-compile`'s EXIT STATUS directly, never through a pipe**; **COUNT executed tests from `target/surefire-reports/*.xml`**; **run `npm test`** (a red frontend test shipped unnoticed because it was not run); and **call `advisor()` BEFORE writing the Codex prompt.** **Routing: CODEX.** Full scope in `RELEASES.md`. Previous: Current version: **v0.105.0 — Curriculum-Scale Exams** (Released, kicked off and signed off 2026-09-02, base branch `releases/v0.105.0`, cut from `main` after `v0.104.0` merged and tagged). **⚠️ THIS RELEASE FOLDS SLICES 2 AND 3 BY EXPLICIT OWNER DECISION (2026-09-02), OVERRIDING A RECOMMENDATION TO SHIP SLICE 2 ALONE — and the reasoning is recorded because it is sound: slice 3's *representative curriculum coverage* IS slice 2's sampler applied, and slice 3 is the one slice with a HARD dependency on slice 2, so building the sampler and re-opening it one release later is real waste.** **⚠️ THE COST IS STATED, NOT ABSORBED: this fires the FULL THREE-AGENT COLD PRESSURE TEST (~490k tokens)** — slice 2 alone carries **two** independent triggers (production-data semantics AND money/quota semantics) and slice 3 adds a question format to a scored assessment; the audit's §13 says *"at least four releases"*. **⚠️ THE SCOPE/COUNT CHAIN RUNS BACKWARDS AND THE ARITHMETIC WAS RE-VERIFIED AT KICKOFF:** `ExamSourceLimitResolver.resolveMaxSourceNotes` is literally `questionCount / MIN_QUESTIONS_PER_SOURCE` (`:16-18`, the constant is **3**) and `questionCount` derives from LEVEL (20/25/30), never scope — so the product reads **A ← B ← C**, inverted, and **an eligible pool A does not exist as a concept anywhere**; a 77-note Subject Plan has ~69 notes that cannot appear in its own exam. **⚠️ NOTHING BREAKS AT SCALE AND THAT IS THE PROBLEM — the cap discards the curriculum before generation sees it; the architecture degrades by ignoring the syllabus, not by failing loudly.** **SLICE 2: (1)** pool **A** becomes explicit and **UNCAPPED**; **(2)** sample **B** = `min(|A|, C/3)` by **coverage buckets → spread → sample within → deterministic per-session randomization** — **⚠️ NOT `unpracticed-first`, REJECTED (owner) because Long Exam is an ASSESSMENT, not a recommendation engine**, and **⚠️ Sections help SPREAD coverage but are NEVER weights**; **(3) C stays level-derived and UNCHANGED**; **(4) `ExamSourceLimitResolver` changes MEANING, not formula** — *"how many we sample from your curriculum"* — and **⚠️ stays the ONE place the formula lives**, since a second constant beside it is how `v0.103.0`'s leakage returned; **(5)** keep the even split **WITHIN the sampled set**; **(6)** anchor the session on the **SAMPLED** primary, not `position == 0`; **(7) GENERATION RESILIENCE IS COUPLED IN, NOT A FOLLOW-UP.** **⚠️ AND THE QUOTA MECHANISM IS SHARPER THAN THE AUDIT STATES, RE-VERIFIED AT KICKOFF: `dispatchLongExamGenerationAfterCommit:266` runs generation AFTER COMMIT, ASYNCHRONOUSLY, while the quota increment runs SYNCHRONOUSLY at `:254`** — so quota is charged in a **different execution context**, and a failure at `:310` leaves the learner **charged, with a `FAILED` session and no refund path**. **⚠️ "Move the increment later" is therefore NOT the fix**; it must move into the async success path or be reversed on failure, and that is an explicit design decision this release owes. Owner decision 3 binds: a shorter valid exam **ONLY** above a defined minimum assembly/coverage threshold, otherwise **fail WITHOUT consuming quota** — **never silently equate a severely incomplete exam with a complete one.** **SLICE 3: (8)** source = Subject Plan **AND** Student Study Plan — **⚠️ "both" costs nothing, they are the same `NoteCollectionEntity`; do not invent a distinction**; **(9)** representative coverage = slice 2's sampler applied — **⚠️ do NOT build a second sampler**; **(10) IDENTIFICATION is PLUMBING, every premise re-verified at kickoff** — `LongExamProgressRequest` carries exactly `questionIndex, selectedChoiceIndex, selectedMultiChoiceIndices`, `long-exam-developer.txt` has **ZERO** identification references while `challenge-quiz-developer.txt` has **nine**, the 6-arg `isAnswerCorrect` overload already exists and Challenge already uses it, and the session keys are already defined — so it is one DTO field, two existing keys, one overload swap, and prompt rules that exist verbatim. **⚠️ COPY THE NOTATION RULES, DO NOT WRITE NEW ONES:** `QuizValidationUtils:48`'s `NOTATION_ANSWER_STEM_PATTERN` was added after a **reproduced production failure**, and a new prompt would reproduce a bug this repo has already paid for. **⚠️ ANTI-DRIFT: NO True/False (deferred, owner — 50% chance floor); NO format-weighted `ConceptHealth` and Identification is NOT coupled to one; NO LLM semantic grading of free responses; BOARD EXAM IS SLICE 4 AND IS OUT** (stays MCQ-only, stays capped, generation stays in its transaction — moving it is slice 4's prerequisite, not an optimisation to sneak in); **do NOT lower `MIN_QUESTIONS_PER_SOURCE` to 2** (it weakens the per-note evidence slice 5 reads) **or scale `questionCount` with source count** (both rejected with reasons in `v0.102.0`); **no new mode or sub-mode**; **modes are never differentiated primarily by question count**; **never send 77 or 550 full notes into one prompt**; **note count ≠ curriculum weight**; **no invented board weighting** and copy stays *"representative"*; **cross-pack concept identity stays ADR-sized and OUT**; **plan-tier entitlements UNCHANGED**. **⚠️ NOT IN SCOPE AND STILL OWED: the `InterviewPracticeService` third instance of the provenance defect — a LIVE defect that must land before slice 5, with a Backlog Index row — and the seven carried `v0.103.0` limitations.** **⚠️ Do NOT add, remove or reorder an onboarding FLOW step and no code under `frontend/app/onboarding` — `[CHECKPOINT — due 2026-09-11]` is 9 DAYS OUT.** **⚠️ CARRIED MEASURED LESSONS, and `v0.104.0` proved every one: MUTATE and confirm a test FAILS** (its cold agent found **three surviving mutants** after the session had run five of its own); **⚠️ THE WORST DEFECT WAS OUTSIDE THE STATED SCOPE, which is the lesson for a FOLDED release** — a copy helper re-ran a **non-idempotent sanitizer** and corrupted quiz choice text on **every session load in every mode**, invisible to 1920 passing tests, in code the session had audited and called safe, so **check any copy helper for a re-applied normalizer**; **a check can ship behind a `@Mock` that makes it untestable** (third release running — verify guards by deleting them); **run `./mvnw test-compile` and read its EXIT STATUS**; **COUNT executed tests from `target/surefire-reports/*.xml`**; and **call `advisor()` BEFORE writing the Codex prompt.** **⚠️ AMENDED AT KICKOFF AFTER `advisor()` — §7.5 IS DROPPED AND THE QUOTA MECHANISM IS DECIDED. (1) §7.5 (*anchor the session on the SAMPLED primary*) CONTRADICTS §15 AND YIELDS.** Verified: `V41` creates **TWO** partial unique indexes on active sessions — `(user_id, study_pack_id, session_mode)` AND `(user_id, note_id, session_mode)` — and the existing-session lookup (`LongExamService:172`) keys on the **caller-supplied** `studyPackId`, resolved **before** sampling. So anchoring on the sampled primary either makes the lookup **miss an active session — creating a second Long Exam and spending a second quota unit** — or **trips a unique index at insert**. Fixing it properly is the **session-anchoring migration §15 defers until slice 6**. **⚠️ DECISION: the session stays anchored on the CALLER-SUPPLIED primary; sampling changes which sources are DRAWN, never which pack the session is KEYED on. No migration, no index change, no lookup change.** **⚠️ The primary is FORCE-INCLUDED in the sampled set at index 0** — matching two existing invariants, since `resolveSourceNoteRefs:909` adds it first, the allocator gives the remainder to `index == 0`, and `planSourced` already requires the primary be a plan member; a sampler that can drop it would anchor a session on a pack contributing zero questions. **⚠️ Two Subject Plans sharing a first note still cannot both have an active Long Exam — carried as a NAMED Known limitation; it predates this release and sampling does not worsen it. Do NOT "restore" §7.5 later without the §15 migration.** **(2) THE QUOTA DECISION IS TAKEN, NOT LEFT TO IMPLEMENTATION: keep the increment at `:254` and REVERSE it on failure.** Moving it into the async success path was **REJECTED** — `assertLongExamQuotaAvailable` runs at start, so with no increment until assembly a learner could start Long Exams on many different packs concurrently and all would pass the check, and the per-`(user, study_pack, mode)` indexes do not close that window; it trades a refund problem for a **quota bypass**. **⚠️ Reversal must fire at EVERY transition into `FAILED`, including `v0.86.0`'s recovery sweeper** — a JVM-killed generation reaches `FAILED` only there, and a reversal wired solely into the `catch` would permanently charge the learner. **⚠️ Reversal must be IDEMPOTENT and never drive usage below zero.** **Routing: CODEX.** Full scope in `RELEASES.md`. Previous: Current version: **v0.104.0 — Assessment Source Provenance** (Released, kicked off and signed off 2026-09-02, base branch `releases/v0.104.0`, cut from `main` after `v0.103.0` merged and tagged). **Slice 1 of the approved assessment sequence** (`docs/claude-plans/assessment-architecture-audit.md` §13) — **READ THAT AUDIT BEFORE TOUCHING ANY EXAM CODE.** **⚠️ THE OWNER MOVED PROVENANCE AHEAD OF CURRICULUM-SCALE SAMPLING ON 2026-09-02 AND IT MUST NOT BE REORDERED:** representative sampling pushes far more cross-source evidence through a mis-attributing writer, so sampling first makes a known defect worse exactly when more data flows through it; the audit's §32 goal — *"Structural Engineering needs work, Transportation is stronger"* — is not supportable until this lands. **⚠️ TWO INSTANCES, FAILING IN OPPOSITE DIRECTIONS, both re-verified at kickoff by reading code:** `LongExamService.recordConceptsForSourcePacks:494-521` **OVER-attributes** — the same concept list is written to EVERY source pack, filtered only by that pack's own `getKeyConcepts()`, and sources from one Subject Plan share vocabulary **by construction**, so this is the normal case; `ChallengeQuizService.completeSession:674,680` **UNDER-attributes** — it writes to `saved.getStudyPackId()`, the PRIMARY only, so a multi-note Challenge lands every concept from all six sources on the note the learner started from. `QuizItem` carries **no source-pack field** (verified: zero matches), which is why neither can do better. **⚠️ THE CHALLENGE INSTANCE IS NEW AS OF `v0.103.0` — that release created a second instance of the very defect this slice exists to fix.** **⚠️ THE CENTRAL FINDING IS NOT IN THE AUDIT AND IT CHANGES THE SCOPE: THE STAMP IS NECESSARY AND NOT SUFFICIENT.** `QuizSessionReviewUtils.computeConceptBreakdown:85` keys its counters by `normalizeConcept(item.concept())` **ALONE**, collapsing every source into one map **downstream of the stamp** — so if Note A answered *Shear Force* correctly and Note B missed it, the merged counter reports *has misses* and **both** packs are recorded incorrect. **A per-item stamp therefore changes NOTHING downstream on its own**, and implementing the audit's §5 *"minimum fix"* literally ships a stamped DTO, tests asserting the stamp, a green build, and `ConceptHealth` still wrong in both directions — **precisely the "fix that looked correct and changed nothing" shape that has now cost three releases running.** **The release is TWO COUPLED CHANGES: provenance on the item AND per-source aggregation on the `ConceptHealth` write path**, bucketing by `(sourceStudyPackId, concept)`. **⚠️ THE INDEX TRAP IS BINDING, NOT ADVISORY:** `computeConceptBreakdown` passes a **positional** index into `isAnswerCorrect(item, index, selectedChoices, ...)` while the selection maps are keyed by **absolute index in the session's quiz array** — so the natural implementation, filtering the quiz to one pack's items and reusing the existing helper on the sublist, **silently reads the WRONG learner answers for every item after the first.** Correct shape: **one pass over the full list preserving absolute index.** **⚠️ THE FIELD GOES ON `QuizItem`, NEVER A PARALLEL PER-INDEX ARRAY, AND THE REPO SETTLES IT:** `ChallengeQuizService:391` calls `shuffleQuestionOrderPreservingMatchingGroups` **after** the multi-source merge and `:522` re-shuffles the redo round, so an index-keyed array would be **scrambled by a shuffle that already ships**; provenance must travel WITH the item. It also follows the existing idiom (`questionFormat`, `keyConcept`, `questionGroup` are all `QuizItem` fields with matching `QuizSessionStateUtils` keys), and the objection is dead — `QuizDeduplicationUtils` is pure normalized-question-string matching and **never calls `QuizItem.equals`**. **⚠️ STAMP AT THE GENERATION SEAM**, where the source is already in scope and dedup has ALREADY run, so there is no reordering hazard: `generateChallengeQuizForSources:1407`, `generateBoardExamQuizForSources:1375`, and the `LongExamService` per-source loop at `:966-992` (`mergedQuiz.addAll` at `:992`). **⚠️ `+5 More Questions` PROVENANCE IS THE PRIMARY PACK AND THAT IS CORRECT, NOT A COMPROMISE** — verified: `generateMoreQuestions:801` generates from `session.getStudyPackId()` alone even on a multi-note session, so those items genuinely originate from the primary. **⚠️ IN-FLIGHT SESSIONS: a null stamp FALLS BACK to today's behaviour per service** — primary for Challenge, `resolveSourceStudyPackIds(session)` for Long Exam — and must neither attribute to nothing nor throw. **⚠️ GATING TAXONOMY (§0), three things kept apart: NO product or checkpoint gate on this initiative; engineering pre-signoff verification PRESERVED IN FULL; plan-tier entitlements UNCHANGED as a separate monetization contract — "nothing is gated" is NEVER licence to add, widen or remove a subscription gate.** **⚠️ EXPLICITLY OUT OF SCOPE:** cross-pack **canonical concept identity** (ADR-sized — `ConceptHealthEntity` stays keyed `(user_id, study_pack_id, concept)` with free-text concept scoped per pack); **format-weighted `ConceptHealth`**; **curriculum-pool sampling, the coverage blueprint and generation resilience** (slice 2, with resilience **COUPLED INTO** it rather than following); **True/False in Long Exam** (deferred); **Identification in Long Exam** (slice 3); anything in §15 of the audit. **⚠️ Quick Review and Adaptive Practice are SINGLE-SOURCE and stay untouched** — `QuickReviewAdaptivePracticeService:384-390` records against `session.getStudyPackId()`, which is correct there; **do NOT refactor them into the new helper.** **⚠️ `examQuestionPoolService.markServed` and `challengeQuizQuestionBankService.updateOutcomesAndReleaseClaims` stay PRIMARY-KEYED** — covered by §12.1's recorded bank-bypass integrity item, tracked and explicitly not in slices 1–4. **⚠️ No new quiz mode and no new sub-mode** (`EXAM_MODES.md` is a locked five-mode contract); **no change to what a concept IS**, only to which pack it is recorded against; **viewing must never write `ConceptHealth`** (locked since `v0.37.0` to move only from genuine assessment). **⚠️ Do NOT add, remove or reorder an onboarding FLOW step and no code under `frontend/app/onboarding` — `[CHECKPOINT — due 2026-09-11]` is 9 DAYS OUT**; this slice is backend-and-evidence work and does not approach it. **Verification tier: ONE SCOPED COLD AGENT framed as FALSIFICATION — the trigger is named by the gate outright, this changes what production evidence means.** **⚠️ A DISCRIMINATING FIXTURE IS PRE-DECLARED: two packs, one shared concept string, NON-UNIFORM answers** (A: Shear correct / Moment incorrect; B: Shear incorrect / Moment correct) — it discriminates concept-merge, index-shift, and both services' current behaviour at once, and **a uniform-answers fixture passes under the index bug and proves nothing.** **⚠️ CARRIED MEASURED LESSONS: MUTATE the production code and confirm a test FAILS — reading is not verification** (three releases running, the worst defect was a fix that changed nothing or a guard nothing exercised, including a lock that survived deletion against **1910 tests**); **when you mutate, check whether the value is computed in more than one place** (a mutation hitting one of two call sites survives for the wrong reason); **run `./mvnw test-compile` and check its EXIT STATUS first** — a compile error in a test class can leave `mvn test` green from a stale compiled class, which happened last cycle; **confirm new tests EXECUTED via `target/surefire-reports/*.xml`, not the source**; and **call `advisor()` BEFORE writing the Codex prompt**, not only on the diff. **⚠️ ROUTING: CODEX** — two services, a DTO, a shared util and the evidence model; **re-run the routing test if implementation discovers a third service or a surface not in the agreed plan.** **⚠️ AMENDED AT KICKOFF AFTER `advisor()` — THREE ADDITIONS THAT CHANGE THE IMPLEMENTATION. (A) `resolveSourceStudyPackIds` MUST BE DEMOTED, NOT LEFT STANDING** — verified to have **exactly one caller**, `LongExamService:502`, the over-attributing loop itself; once aggregation buckets by the stamp it is either dead or **fallback-only**, and the prompt must say which. **Left ambiguous the likely outcome is that BOTH run — per-source aggregation PLUS the old broadcast, which is WORSE THAN TODAY — and the pre-declared fixture does NOT catch it**, because *"pack A got Shear"* still passes when the broadcast also wrote it. **⚠️ SO THE FIXTURE GAINS A FIFTH, NEGATIVE ASSERTION: a third pack C whose `getKeyConcepts()` CONTAINS *Shear* but which contributed NO item to the session must receive NOTHING.** **(B) BOARD EXAM ATTRIBUTION CHANGES WITH THE SAME FIX AND IS IN SCOPE** — `generateBoardExamQuizForSources:1375` is a stamp site and `completeSession` is shared, so Board Exam gains per-source attribution as a direct consequence; **stated explicitly because it is a behaviour change on a PRO-quota path**, with no quota, entitlement or question-count change accompanying it. **(C) A MATCHING GROUP MUST NEVER SPAN TWO SOURCE PACKS — ASSERT IT** — `shuffleQuestionOrderPreservingMatchingGroups:2180` builds blocks from contiguous runs sharing a `questionGroup` string, so two sources emitting the same label adjacent at a merge boundary would fuse into one block spanning two stamps; **fail loudly rather than silently picking one.** Full scope in `RELEASES.md`. Previous: Current version: **v0.103.0 — Mixed Retrieval for Free and Plus** (Released, kicked off and signed off 2026-09-02, **SCOPE RESHAPED THE SAME DAY**, base branch `releases/v0.103.0`, cut from `main` after `v0.102.0` merged and tagged). **⚠️ AN ASSESSMENT ARCHITECTURE AUDIT LANDED 2026-09-02 AND RESHAPED THIS RELEASE — READ `docs/claude-plans/assessment-architecture-audit.md` BEFORE TOUCHING ANY EXAM CODE.** It supersedes the "Slice 3 of the Review Sets audit" framing below: the approved sequence makes **this release slice 0**, and its job is **making an already-built capability reachable**, not opening a new rung. **⚠️ THE DELIVERED CAPABILITY IS UNREACHABLE ON EVERY PLAN** — `challenge-quiz/page.tsx` derives the multi-note cap from `challengeSession`, which is **null at prestart by construction**, so the picker is permanently disabled; and PRO is routed to Board Exam setup before reaching it. **The server side is correct and reachable via API — UI wiring, not architecture.** **⚠️ THE PLUS CAP OF 4 IS REJECTED AS ARITHMETIC LEAKAGE (owner, 2026-09-02)** — it was `MULTI_NOTE_CHALLENGE_QUESTION_COUNT (12) / 3`. **⚠️ AND ~10 IS UNREACHABLE, VERIFIED:** `MAX_CHALLENGE_QUIZ_QUESTIONS` is **20**, so three-per-source bounds sources at **6**; ~10 needs 30 questions, ten past a ceiling `+5` depends on. **OWNER RULED 18 questions → 6 sources for Plus, Free unchanged at 3.** **⚠️ Do NOT lift `MAX_CHALLENGE_QUIZ_QUESTIONS`** to chase ~10 — declined as a Challenge Quiz identity change. **⚠️ The cap must stay DERIVED through `ExamSourceLimitResolver`, never a standalone constant** — a second constant beside the formula is how the leakage returns. **⚠️ NEXT RELEASE IS SOURCE PROVENANCE (slice 1), DELIBERATELY AHEAD OF CURRICULUM-SCALE SAMPLING:** `ConceptHealth` writes the same concept list to EVERY source pack and `QuizItem` has no source-pack field, so sampling first would push more evidence through a mis-attributing writer. **⚠️ GATING TAXONOMY: NO product or checkpoint gate on this initiative; engineering pre-signoff verification PRESERVED IN FULL; plan-tier entitlements UNCHANGED as a separate monetization contract — "nothing is gated" is never licence to add, widen or remove a subscription gate.** **Slice 3 of the Review Sets audit (§S2-3), and the ONE slice with a HARD dependency on the previous release** (§S2-D) — its Plus tier *is* plan-sourced assessment, which only began working in `v0.102.0`. **⚠️ READ THE AUDIT'S `STATE AT v0.100.0` SECTION FIRST — Stage 2 `file:line` refs are anchored to `v0.99.0` and have MOVED. Re-locate by string.** **THE DEFECT IS A PRICING ONE AND SHARPER THAN "Free lacks a feature": PLUS HAS IDENTICAL MIXED-RETRIEVAL CAPABILITY TO FREE — NONE.** `isLongExamAvailable` is `longExamAvailableForPro && plan == PRO` (`StudySnapProperties:253-255`), verified at kickoff, so a learner paying for Plus buys **nothing** on this axis. **⚠️ AND THE PLAN'S CTA IS ALREADY DISHONEST: `resolvePlanPremiumExamMode` (`exam-mode-visibility.ts:71-84`) resolves on `profileType` ALONE and never takes plan**, while the paywall fires on plan — so Free/Plus learners see a concluding action they cannot use and `PARENT` sees none. **(1) ⚠️ OWNER RULED 2026-09-02: it is a CAPABILITY of Challenge Quiz, NOT a sub-mode — superseding this line's first wording, which repeated §S2-X2's "Board Exam pattern, a `mode` string on the `CHALLENGE` discriminator".** `EXAM_MODES.md`'s own sub-mode test settles it: a sub-mode needs its own label, entry point AND a stated differentiator (Interview Practice's is scenario questions + per-answer critique + its own result framing). This has none — same questions, same scoring, same result screen, more notes. **So NO `subMode` value, NO row in the locked table, nothing to name**; `EXAM_MODES.md` gains one capability line under Challenge Quiz. **⚠️ Do NOT add a quiz mode OR a sub-mode, and do NOT unlock Long Exam for non-Pro.** **⚠️ TODAY `additionalStudyPackIds` ON A NON-`board_exam` REQUEST ARE SILENTLY DROPPED, NOT REJECTED** (`ChallengeQuizService:196-198`) — state explicitly what an over-cap or ineligible caller gets, because silent-ignore becoming silent-accept is how a cap gets bypassed. **(2) ⚠️ OWNER RULED 2026-09-02, THEN CORRECTED THE SAME DAY: the sub-limit is enforced by a COUNTER COLUMN.** A multi-note session spends `challenge_quiz_generations` like any Challenge session **and additionally** carries a monthly ceiling of ~2 (Free) / ~10 (Plus), config-backed. **⚠️ THIS LINE FIRST SAID "NO MIGRATION, NO COLUMN" AND THAT WAS AN UNVERIFIED CLAIM PUT IN FRONT OF THE OWNER — retracted.** Every per-type monthly cap in this codebase IS a counter column (`board_exam_used_this_month`, `adaptive_quiz_generations`, `long_exam_used_this_month`, `interview_practice_used_this_month`), and enforcement mirrors Board Exam exactly. **⚠️ "NO NEW METER" STILL HOLDS IN THE SENSE THAT BINDS: Settings renders NO row for it.** **⚠️ REJECTED: a JSONB `sourceNoteRefs` row count** — first JSONB predicate in the repo, PostgreSQL-only, invisible to the mocked tests that are most of the suite. **⚠️ REJECTED WITH REASONS — do not re-propose:** no separate limit (a Free learner could then run 20 multi-note sessions a month, on top of what Pro pays for) and Plus-only (drops the ladder's *experience the principle* rung). **(3) ⚠️ OWNER RULED 2026-09-02: source cap is Free 3 FLAT, Plus LEVEL-DERIVED 6/8/10** — the same honest cap `v0.102.0` shipped for Pro, so **Plus versus Pro is modes and allowance, NEVER an artificial note count.** **⚠️ REJECTED: a flat 5 for Plus** — it reintroduces the arbitrary constant `v0.102.0` replaced and sits BELOW what the engine allows most levels. **(4) The terminal CTA takes PLAN as well as profile.** **⚠️ ONE call site, not the four first claimed here** — `collection-labels.ts:109` via `getCollectionTerminalAction`, whose own single caller is `collection-detail-page-client.tsx:2611`. **⚠️ Handle the `PARENT`-gets-nothing branch in the same change — the fix is resolving the right action per (profile, plan), not hiding the button.** **(5) `sourceScope`/`sourceCount` carry through the non-Pro path unchanged. ⚠️ `sourceScope` RECORDS THE VERIFIED OUTCOME, NEVER THE CLIENT'S CLAIM** — a `v0.102.0` cold-agent finding, and `[CHECKPOINT — due 2026-09-28]` reads exactly this field. **⚠️ ONE §S2-X4 OBLIGATION IS ALREADY DISCHARGED — DO NOT RE-DO IT.** It predicted Slice 3 would falsify Slice 1's meter description; the owner pre-empted that on 2026-09-01 with the **one durable wording** ruling, and `v0.101.0` shipped a mode-agnostic description that stays TRUE when multi-note ships. A `quiz-session-history.test.ts` guard pins that it names no mode. **Do not "fix" it.** **⚠️ OUT OF SCOPE: no new mode, no new meter, Pro is NOT weakened** (Long Exam, Board Exam Mode, difficulty control, 30 Adaptive Practice all untouched), **Challenge Quiz length is NOT shortened**, Slices 4–5 are out, and the manual path keeps its same-subject rule as a carried Known limitation (§S2-X3). **⚠️ Do NOT add, remove or reorder an onboarding FLOW step, and no code under `frontend/app/onboarding` — `[CHECKPOINT — due 2026-09-11]` is 9 DAYS OUT, the closest this constraint has ever been to its date.** **Verification tier: ONE SCOPED COLD AGENT framed as FALSIFICATION — the trigger is MONEY/QUOTA SEMANTICS, which the gate names outright.** **⚠️ AND `v0.102.0` PROVED THE CARRIED LESSON THE HARD WAY: its cold agent deleted a security check and passed 1894 TESTS, because a correct refactor had moved that check behind a `@Mock` AFTER its mutant had already been killed. A PRE-EXTRACTION MUTATION KILL DOES NOT CARRY — re-run mutants after any extraction.** **⚠️ ROUTING: THIS RELEASE GOES TO CODEX, as a correction rather than a preference.** `v0.102.0` was implemented inline at **19 files / ~838 insertions**, hitting three separate "→ Codex" rows at once; the routing call was made at plan time and never re-evaluated as scope grew. **Re-run the routing test whenever implementation discovers a second service, a new class, or a surface that was not in the agreed plan.** Previous: Current version: **v0.102.0 — Plan-Sourced Assessment** (Released, kicked off 2026-09-01, signed off 2026-09-02, base branch `releases/v0.102.0`, cut from `main` after `v0.101.0` merged and tagged). **Slice 2 of the Review Sets audit (`docs/claude-plans/review-sets-first-class-and-ai-language-audit.md` §S2-2), and the audit calls it the single highest-leverage change in the whole brief.** **⚠️ READ THAT AUDIT'S `STATE AT v0.100.0` SECTION FIRST — its Stage 2 `file:line` references are anchored to `v0.99.0` and have MOVED. Re-locate by string, never by line.** **THE GAP: the Study Plan is first-class everywhere EXCEPT the one place that would prove its value — you cannot assess against it.** Verified at kickoff: `LongExamStartRequest` carries `difficulty` and `additionalStudyPackIds` and **nothing else**, so no collection id exists anywhere in the contract. **⚠️ THE KICKOFF'S OWN FRAMING WAS WRONG AND WAS RETRACTED THE SAME DAY, BEFORE IMPLEMENTATION — do not re-derive it.** It said plan-sourcing was *"genuinely new rather than half-built"*, reasoning from `LongExamStartRequest` carrying no collection id; **the DTO fact is true and the conclusion is false.** `collection-exam.ts:44-53` already selects sources by collection MEMBERSHIP with **no subject filter**, `long-exam/page.tsx:319-336` already pre-selects that set when `?collectionId=` is present, and the plan's own CTA already routes with it. **THE BACKEND REJECTS IT** at `LongExamService:842`. **⚠️ SO THIS IS A LIVE DEFECT ON A PAID PATH, AND THE BEHAVIOUR IS SELF-CONTRADICTING:** a PRO learner presses the plan's *Take the Long Exam* CTA, the product **pre-selects that plan's notes for them**, and Start returns *"Long Exam source notes must be owned by you and share the same subject"* — verbatim, via `long-exam/page.tsx:531-533`. The product refuses a selection it made itself. **(1) The backend STOPS REJECTING the plan-sourced selection the frontend already makes, keyed on PLAN MEMBERSHIP. ⚠️ THE SERVER MUST NOT TRUST A CLIENT-SUPPLIED `sourceCollectionId`** — skipping the subject check on its mere presence turns a validation rule into an opt-out flag; the gate is **caller owns the collection AND every additional pack's note is a live member**. **⚠️ `LongExamService` injects NEITHER collection repository today**; `findByCollectionIdOrderByPositionAsc` and `findByCollectionIdAndNoteId` exist. **⚠️ `notes.subject` IS NOT THE PREDICATE — the owner marked this "not tunable at all"**, because matching is `trim().toLowerCase()` on free text with no catalog, so two notes a learner deliberately put in the same plan are already refused when their subjects were typed differently (*Engineering Mathematics* vs *Engineering Math*); **this replaces a WRONG PREDICATE, it does not relax a rule.** **(2) ⚠️ THE CAP IS NOT A FREE CONSTANT AND THE ARITHMETIC WAS RE-VERIFIED AT KICKOFF:** `MIN_QUESTIONS_PER_SOURCE = 3` (`LongExamService:111`) is checked against `questionCount / sourceCount` (`:849-850`), and `questionCount` derives from the learner's **LEVEL, not their selection** (`resolveQuestionCount:543-550` → 20/25/30), so the real ceiling is **6 / 8 / 10** and **a College learner — the default and most common level — fails at 9.** **OWNER RULED 2026-09-01: ship `floor(questionCount / 3)`, surfaced before starting, ENGINE UNCHANGED.** **⚠️ REJECTED WITH REASONS — do not re-propose:** scaling `questionCount` with source count (a 10-note exam silently exceeds the learner's level norm) and lowering `MIN_QUESTIONS_PER_SOURCE` to 2 (**it weakens per-note evidence, which is exactly what Slice 4's remediation reads**). **⚠️ `MAX_ADDITIONAL_SOURCE_COUNT = 3` today (`:110`) = 4 sources; the raise applies to the PLAN-SOURCED PATH ONLY.** **(3) The learner is told what is in scope BEFORE starting. ⚠️ SMALLER THAN SCOPED — `long-exam/page.tsx:934` already renders *"Add up to N more notes from this plan"*; it needs the HONEST NUMBER and an eligible-vs-total count, not a new surface.** ELIGIBLE notes only (`LongExamService:799-802`). **⚠️ Do NOT compute that set again** — `collection-exam.ts` derives it. **⚠️ AND DO NOT RE-DERIVE THE CAP CLIENT-SIDE — `LONG_EXAM_MAX_ADDITIONAL_NOTES = 3` is a frontend constant while the level → questionCount mapping is BACKEND CONFIG; the cap must arrive from the server**, or it is the exact drift `v0.100.0` item 2 existed to prevent. **⚠️ Interview Practice is NOT a parallel defect — checked: `InterviewPracticeService` enforces no subject rule at all, so its plan path already works. Do not 'fix' it.** **(4) `BOARD_EXAM_COMPLETED` plus source-count/scope metadata on exam starts. ⚠️ Verified: `BOARD_EXAM_STARTED` EXISTS and `BOARD_EXAM_COMPLETED` DOES NOT**, so that funnel has a start with no end. **(5) `study-plans-guide.tsx:23` BECOMES FALSE IN THIS RELEASE. ⚠️ It has never once been in a diff when the behaviour it describes changed, and `v0.101.0`'s sweep removed the word "AI" from it — so the sweep-by-surface grep NO LONGER MATCHES IT.** It is named by file and line for that reason. **⚠️ EXPLICITLY OUT OF SCOPE: whole-Review-Set sourcing is DEFERRED BY DECISION, not unsupported by accident** (owner, 2026-08-31) — the hierarchy stays *Note → topic; Subject Plan → mixed retrieval; Review Set → readiness*, and the Review Set level overlaps Board Exam Mode, so the two are decided together or not at all. **⚠️ Slices 3–5 are OUT, and Slice 3 must NOT be folded in** — §S2-D records it as a HARD dependency on this release, and folding puts a quota change in the same diff as a new source path. **⚠️ NO quota, limit or meter change; NO new quiz mode** (`EXAM_MODES.md` is a locked five-mode contract — multi-note is a CAPABILITY on Long Exam). **⚠️ The manual path KEEPS its same-subject rule, and this release makes that worse, not better** — afterwards a plan-sourced exam spans mixed subjects while manual selection of the same notes still rejects them; **it ships as a named Known limitation rather than being discovered as a bug report.** **⚠️ Do NOT add, remove or reorder an onboarding FLOW step. The freeze is NOT engaged and this was VERIFIED, not assumed:** every `BOARD_EXAM` match in `app/onboarding/page.tsx` is `ProfileType.BOARD_EXAM`, not Board Exam **mode**, and the directory holds **no Long Exam reference at all**; `[CHECKPOINT — due 2026-09-11]` is 10 days out. **⚠️ Viewing must never write `ConceptHealth`**; Long Exam's existing writes are unchanged. **Verification tier: ONE SCOPED COLD AGENT framed as FALSIFICATION** — it changes what a scored assessment is built from, but adds no permission substrate, no cross-user read and no quota semantics. **⚠️ `v0.100.0`'s MEASURED blind spot carries forward** — call `advisor()` **before** any prompt, verify *"X already does Y"* against code first, **MUTATE and confirm a test fails**, confirm tests EXECUTED via `target/surefire-reports/*.xml`, read `tsc --noEmit`'s own exit code, and **ask the cold agent to COUNT executed tests, not read them.** **⚠️ A `[CHECKPOINT]` IS LIKELY OWED AT SIGNOFF — `v0.101.0`'s no-checkpoint ruling was SCOPED TO THAT RELEASE and does NOT carry.** Previous: Current version: **v0.101.0 — Language and Observability** (Released, kicked off and signed off 2026-09-01, base branch `releases/v0.101.0`, cut from `main` after `v0.100.0` merged and tagged). **Slice 1 of the Review Sets audit (`docs/claude-plans/review-sets-first-class-and-ai-language-audit.md` §S2-1) — THREE items, and it ships first because instrumentation should predate behaviour change.** **⚠️ READ THAT AUDIT'S `STATE AT v0.100.0` SECTION FIRST — the rest of the document is anchored to `v0.99.0` and many `file:line` references have moved. Re-locate by string, never by line.** **(1) The quiz meter says what it counts:** `AI quizzes` → **`Quiz generations`**, with a description that is factually complete for the first time — the current one omits **Board Exam**, which spends a unit on every start. **⚠️ Verified at kickoff, the ONLY three spend sites are `ChallengeQuizService:235`, `ChallengeQuizService:343` and `GeneratedQuizService:158`**; `+5 Questions` spends nothing, so **the metered unit is a session or quiz created** and the wording stays session-shaped. **⚠️ OWNER RULED ONE DURABLE WORDING, not two edits across two releases** — §S2-X4 proves Slice 3 falsifies any description enumerating today's modes, because a Free/Plus multi-note session rides the Challenge engine and spends this same meter, so the shipped description is mode-agnostic: *"Quiz sessions we generate for you, plus quizzes you make for someone. Board Exam sessions also count against their own allowance."* **⚠️ THE SECOND SENTENCE DISCLOSES A DOUBLE-SPEND AND THAT IS THE ONLY REASON IT NAMES BOARD EXAM** — Board Exam increments both this meter and `board_exam_used_this_month`, and Settings renders a **Board Exam** row directly beneath this one; the candidate wording *"Long Exam, Adaptive Practice and Interview Practice have their own allowances"* was **REJECTED at kickoff because that row falsifies it while the learner is reading it**. **⚠️ Do NOT restore an enumeration of who has their own allowance.** **⚠️ THE CONSTANT IS SPLIT (owner, 2026-09-01) — a blocking kickoff find:** `AI_QUIZZES_USAGE_LABEL` is **interpolated into four `src/config/plans.ts` pricing strings** (`:49`, `:69`, `:86`, `:116`), so renaming it alone would have **silently rewritten public pricing copy**, editing a money surface as a side effect against `v0.76.0`'s doctrine; the meter reads `Quiz generations`, pricing reads **`generated quizzes`**. **⚠️ Test blast radius is FOUR files, not the one the audit names**, and **`quiz-session-history.test.ts:87` must KEEP PASSING** (it pins label ≠ mode name, a `v0.92.0` guard). **(2) The Category A copy sweep, copy only.** **⚠️ SMALLER THAN ITS OWN PLAN SAYS — the Domain Context explainer is THREE sites, not six** (`note-editor-form.tsx:514`, `private-note-detail-page-client.tsx:2475`, `bulk-generation-page-client.tsx:639`), because `v0.100.0`'s Decision 3 copy absorbed the rest; plus `ai-suggestion-modal.tsx`'s **eight** strings, `study-packs-guide.tsx`, `study-plans-guide.tsx:23`, two landing strings, `applicable-programs-combobox.tsx:323`, two `note-editor-form` title strings and `profile-learning-section.tsx:153`. **(3) `NOTE_ADDED_TO_COLLECTION` — one enum value, one fire site.** **⚠️ `AnalyticsEventType` is at 129 values, NOT the 108 the audit records.** **⚠️ THE SINGLE FIRE SITE IS A STATED BOUNDARY:** `NoteCollectionService.addItems:936` is the deliberate add; creation-with-notes is already `COLLECTION_CREATED` and adoption is `STUDY_PLAN_ADOPTED`, so firing there too would **double-count one learner intent**. **⚠️ `addItems` filters to `newNoteIds`, so a re-add must not fire a zero-count event**; the call is wrapped so **analytics can never fail the membership write**; and **`addGeneratedItems` routes through it**, so bulk curator authoring reaches the event and the path goes in metadata. **⚠️ NO GATES AND NO CHECKPOINTS (owner, 2026-09-01)** — the signoff gate is ANSWERED *"none owed, owner ruled"*, never skipped. **⚠️ `[CHECKPOINT — due 2026-09-11]` is NOT engaged and this is VERIFIED, not assumed** — `frontend/app/onboarding/` contains **zero `AI` strings**, so no copy item reaches the frozen path and the sweep is **not deferred over it**. **⚠️ EXPLICITLY OUT OF SCOPE:** *"AI critique"* (a feature name in a **locked** contract doc and two SEO-indexed guides — its own owner decision), **every Category B disclosure** (privacy, terms, *"verify calculations"*, review-before-you-share), **every Category C code identifier** (do NOT rename symbols for branding), and Slices 2–5 (no Review Set becomes an assessment source, no cap moves, no meter is added). **Verification tier: a single `advisor()` call**, correct on size — **⚠️ but `v0.100.0`'s MEASURED blind spot carries forward: every one of its five deliveries carried a defect past its own tests, and six tests claimed more than they proved.** So call `advisor()` **before** any prompt, verify *"X already does Y"* against code first, **MUTATE and confirm a test fails**, confirm tests EXECUTED via `target/surefire-reports/*.xml`, and read `tsc --noEmit`'s own exit code. Previous: **v0.100.0 — Domain Context Resolution** (Released, kicked off and signed off 2026-09-01, base branch `releases/v0.100.0`, cut from `main` after `v0.99.0` merged and tagged). **⚠️ `v1.0.0` STAYS RESERVED and the minor rolls to THREE DIGITS** — the reservation (owner, 2026-07-23, re-confirmed 2026-09-01) is **conjunctive**, *live core AND product success*, and both halves are unmet. **SIX items — five at kickoff, one added 2026-09-01 by owner ruling; the work is resolver + UX + mental model.** **⚠️ THE A–F IMPLEMENTATION AUDIT RAN 2026-09-01 (`docs/claude-plans/v0.100.0-domain-context-implementation-audit.md`) AND RE-SHAPED THE RELEASE — read it before implementing.** **⚠️ THE TAXONOMY QUESTIONS ARE SETTLED AND ARE NOT RE-OPENED HERE** — do NOT add `ARCHITECTURE` (it PASSED the bar and is a **provable no-op**), no catch-all, no removals, no `QUANTITATIVE_KEYWORDS` extension, no bulk rewrite. **⚠️ THE RELEASE IS FORCED BY A DEADLINE THAT CLOSES ON ITS OWN:** Domain Context authoring emits **zero analytics** and nothing records what `Automatic` *would have* resolved, so the override signal **cannot be reconstructed retroactively at all**, and `[CHECKPOINT — due 2026-09-28]` is 27 days out. **(1) DECISIONS 1 AND 4 ARE COUPLED AND SHIP TOGETHER** — `Automatic — use note context` **plus** removing the profile fallback for canonical curated generation; **⚠️ the copy alone makes the label wrong again in a new way**, and **⚠️ learner personalization is NOT removed**; **⚠️ the boundary predicate ALREADY EXISTS** (`isTeacherSelectableOwner` / `isCurator` = `TEACHER || ADMIN` behind an onboarding guard, already used for this exact field at `NoteService:182`). **(2)** Show the **EFFECTIVE** writing domain as derived state — **⚠️ no new persisted field, and never a frontend re-implementation of resolver precedence**; the UI shows the **outcome**, never the five precedence levels. **(3) ⚠️ OWNER RULED 2026-09-01 that the existing save-time block MOVES to generation time** rather than being excepted — *note validity ≠ generation readiness* — across **four write paths** and an existing error contract (`MultiProgramDomainContextRequiredException`). **⚠️ THIS RELAXES A SAVE-TIME INVARIANT AND CREATES A PRODUCTION DATA STATE THAT HAS NEVER EXISTED** — a note with 2+ Applicable Programs and a NULL Domain Context — so the audit's Q6 invariant check **changes meaning** and must be updated in the same release. **(4)** One authoring event capturing *(what `Automatic` resolved, what was persisted)* — **⚠️ the item with the deadline**, and **⚠️ NOT a large analytics system**. **(5)** Decisions 3, 12, 13 copy. **⚠️ AUDIT FINDINGS, BINDING: (a)** only **three** save-time sites move — `NoteGenerationService:75` and `NoteBulkGenerationService:466` are **already generation-time**; **(b) `StudyPackService.startAsyncGenerationFromNote`, the PRIMARY path, VALIDATES NOTHING**, so removing the save-time block without adding a gate there **DELETES the invariant rather than moving it**, and the failure is **silent** — a null authoring domain makes the prompt builder drop the `Domain:` line and its constraint with no error; **⚠️ ORDER: add the generation gate FIRST, then remove the three blocks**; **(c) Decisions 4 and 6 are COUPLED too** — a naive item 3 re-opens item 1's defect through a new door and makes it legal going forward; **(d)** item 2 **shrinks** — `effectiveAuthoringDomain` already exists as a public static shared with the prompt builder, so *never a frontend re-implementation* holds by construction; **(e)** the curator predicate is **private or inlined in all four copies**, so the resolver cannot call it — **extract it, do NOT add a fifth**; **(f) two tests must be rewritten, never deleted**, and `StudyPackGenerationContextResolverTest:146` **keeps passing under a correct fix** because its fixture reads as a learner — green there proves nothing. **(6) ⚠️ ADDED THEN REDUCED THE SAME DAY (2026-09-01) TO A REGRESSION GUARD — TEST AND COMMENT ONLY.** It was scoped in on the claim that a learner practising a curator's public pack inherits their own profile program as the authoring domain. **THAT CLAIM WAS FALSE and is retracted at full strength in the audit — do NOT re-derive it.** Every quiz/exam path fetches the pack via `findByIdAndOwnerUserId` so **caller == owner by construction**; `AdminStudyPackTransactionHelper:147`, `OfficialChallengeQuizTemplateService:221` and `ExamQuestionPoolService:151` **already pass the OWNER**; and a learner cannot reach a curator's pack because the public-note copy path mints a copy they own (`NoteService:405-411`). `study_packs.owner_user_id` is `NOT NULL`. **⚠️ `buildFromStudyPackFallback:159` MUST NOT BE "FIXED"** — it fires only for a pack the caller OWNS whose note is unreachable, where their own profile is correct and is the personalization Decision 4 preserves. **What ships is a guard pinning those three sites**, because nothing in the build does and a later "simplification" to `callerId` would CREATE the leak and ship green. **⚠️ NO cross-owner read, no column, no endpoint — the release contains no cross-user read at all.** **(7) ⚠️ FOLDED IN 2026-09-01: THE PRODUCTION FRONTEND BUILD HAS BEEN FAILING SINCE 2026-08-31 23:54, ON EVERY BRANCH.** `/notes/public` returns **2,579,045 bytes**, past Next.js's **hard 2 MB data-cache limit**, so the cache stops deduping and **~250 static pages each issue their own 2.5 MB request in one build** until the backend saturates. **⚠️ NOT a `v0.99.0` regression — reverting will not fix it**; `server-public-notes.ts` last changed 2026-08-06 and this is **data growth crossing a threshold a latent unbounded fetch always had**. **⚠️ `foundation-engineering` is not special** — it is whichever page was in flight. **⚠️ THE FIX NEEDS NO SLUG→LABEL INVERSION, and this is the fact that makes it safe:** `PublicLibraryRepositoryImpl:209` matches `normalizedSlugSql(n.subject) = :subjectSlug` and `NoteService.normalizePublicLibraryFilterSlug:1204` is **operation-for-operation identical to the frontend `slugify`**, so **passing the SLUG returns exactly today's set, label variants included — behaviour-preserving by construction.** Fix: subject-slug filter server-side; `generateStaticParams` uses `GET /subjects?scope=public` (it needs only slugs); paginate `getServerPublicNotes()` for `sitemap.ts`; use the existing `courseProgram` filter. **⚠️ NO backend change — every parameter already exists.** **⚠️ Do NOT trim `contentPreview`/`summaryPreview`** — that only moves the threshold, which is what caused this. **⚠️ The 2 MB limit is a hard Next.js constant, not tunable.** **⚠️ Do NOT add, remove or reorder an onboarding FLOW step and no code under `frontend/app/onboarding`** — `[CHECKPOINT — due 2026-09-11]` is 10 days out; verified at kickoff that the directory contains **zero** domain/AI strings, so no copy item can reach it. **Pre-declared: ONE SCOPED COLD AGENT framed as falsification — the tier was RE-EVALUATED, not inherited**, because the Decision 6 ruling widened the release exactly as the audit predicted; there is no permission substrate and no cross-user read. **⚠️ Item 1 gets its `advisor()` call BEFORE implementation.** **⚠️ A `[CHECKPOINT]` is owed as a DEPLOY-SPLIT CAVEAT JOINING `2026-09-28`, not as a new date.** Full scope in `RELEASES.md`. Previous: **v0.99.0 — Connection Completeness** (Released, kicked off and signed off 2026-08-31, base branch `releases/v0.99.0`, cut from `main` after `v0.98.0` merged and tagged). **FOUR items — three scoped, one folded — and every `v0.98.0` Known limitation is closed.** **⚠️ THE SCOPED COLD AGENT FOUND THREE OF ITS FOUR CONFIRMED DEFECTS INSIDE ITEM 3'S OWN GUARD, the test written to stop guards from looking present and doing nothing** — it pinned the annotation default while `application.yaml` declares 8 of 10 keys and **wins**; it scanned one package, so a bare-literal cron placed elsewhere passed **both** assertions at once; and its disablement check was an unanchored leaf substring that a **comment** satisfied, arming the data-deleting purge job for every test run. **A fourth: nothing pinned the onboarding gate above the WRITE-ONCE birth-year write** — moving it below `persistBirthYear` left all 57 tests green. All four fixed, each mutant re-killed with the killing test named. **⚠️ ONE FINDING WAS THE SIGNOFF SESSION'S OWN: the `BP 344` correction cited the ratified spec as saying 344; the spec says `BP 334`** (`08-taxonomy-validation…:41,:60`), and a `RELEASES.md` misquote had been cited as proof of it. **The change stands on the CURRICULUM sources; do NOT 'restore' 334 from the spec.** Original scope, three items: **(1) AN EXPIRY STAYS VISIBLE AFTER A PAUSED OR BACKLOGGED SWEEP.** Retention is keyed on the DEADLINE, but `v0.98.0` added a 500-row batch bound and a pause hook, and both create backlogs — a request swept more than `request-ttl-days` after its deadline becomes `EXPIRED` with a timestamp already outside the window, so it **vanishes instantly and is never shown as expired**. **⚠️ OWNER DECISION 2026-08-31: A SEPARATE `expired_at` COLUMN**, over re-keying retention. `expires_at` therefore keeps meaning **the deadline for every status**; the table already carries a distinct terminal timestamp per status (`accepted_at`, `revoked_at`). **⚠️ THIS NEEDS THE THIRD AMENDMENT to the `v0.95.0` column prohibition** — raised explicitly, not reasoned past: it **adds a new fact rather than reinterpreting an existing one**, and none of `[2026-09-19]`, `[2026-09-26]`, `[2026-10-13]` is affected. **⚠️ V130'S BACKFILL IS THE TRAP: `expired_at` FROM `expires_at`, FOR `EXPIRED` ROWS ONLY, and nothing for any other status.** `v0.97.0` got this shape wrong TWICE — once expiring inherited consent pauses immediately, once merely delaying it 30 days — and both times by writing a timestamp onto rows that should have had none. **⚠️ `expires_at` MUST NOT BE OVERWRITTEN, RE-PURPOSED OR BACKFILLED; a NULL there is the entire mechanism protecting a consent-paused relationship.** **(2) THE ONBOARDING BAR REACHES `invite()` AND `accept()`.** Both still gate on `assertProfileComplete` alone, **which passes for a brand-new account**, so a verified-but-not-onboarded user can invite as SUPPORTER and reach an `ACCEPTED` relationship with cross-user read capacity. **⚠️ The property is currently HALF TRUE, which reads as enforced and is not.** **⚠️ It rejects the same two live cohorts as `v0.97.0`/`v0.98.0` — a failed `completeOnboarding` POST and the copy-on-signup cohort — so the `/onboarding` self-heal ships WITH the gates.** **⚠️ `accept()` is called internally by `acceptInvitation`, so check for double-gating**, and it is the guardian-consent path a supporter uses. **⚠️ Do NOT touch `assertProfileComplete`.** **(3) A TEST PINS THE THREE CONFIGURABLE CRONS to their production defaults**, which nothing in the build currently does. **⚠️ Do NOT add, remove or reorder an onboarding FLOW step and no code under `frontend/app/onboarding`** — `[CHECKPOINT — due 2026-09-11]` is 11 days out. **⚠️ Every standing connection rule is unchanged.** **Pre-declared: ONE SCOPED COLD AGENT framed as falsification** (item 1 changes production-data semantics via a migration; item 2 moves an authorization boundary — both one-agent triggers). **⚠️ Item 2 gets its `advisor()` call BEFORE implementation**, third gate tightening in three releases. **⚠️ The cold agent must be asked to COUNT EXECUTED TESTS, not read them** — `v0.98.0`'s worst finding was a test that had silently stopped running while the build stayed green.  Full scope in `RELEASES.md`. Previous: **v0.98.0 — Connection Consistency** (Released, kicked off 2026-08-30, signed off 2026-08-31, base branch `releases/v0.98.0`, cut from `main` after `v0.97.0` merged and tagged). Five items, all `v0.97.0` Known limitations or Backlog rows; the ceiling is deliberate, since nine items cost `v0.97.0` the full three-agent test plus a re-audit. **(1) THE EMAIL INVITATION PATH REQUIRES FINISHED ONBOARDING TOO.** `acceptInvitation` forms the IDENTICAL relationship as a link redemption — same guardian-consent handling, same cross-user capacity — but gates on `requireEmailVerified` + `assertProfileComplete`, whose own comment records it *"passes for a brand-new account."* **⚠️ OWNER RULED 2026-08-30 to tighten rather than document the asymmetry.** **⚠️ IT WILL NEWLY REJECT LIVE ACCOUNTS AND THAT IS KNOWN IN ADVANCE THIS TIME:** the frontend gates on `needsOnboarding()`, NOT `onboardingCompletedAt != null`, and two cohorts sit in the gap — a failed `completeOnboarding` POST (marker never retries) and the copy-on-signup cohort (dismissible prompt). **⚠️ SO THE `/onboarding` SELF-HEAL SHIPS WITH THE ITEM**, routing a `COMPLETE_ONBOARDING` remedy as `v0.97.0` taught the invite page to; without it this is a dead end. **⚠️ Do NOT touch `assertProfileComplete`** — deliberate exemption, protects the activation funnel, ~20 call sites. **⚠️ The gate runs BEFORE any invitation lookup** or it becomes an oracle beside `v0.90.0`'s single not-found contract. **(2) TERMINAL CARDS BECOME DISMISSIBLE.** **⚠️ Whether dismissal is per-viewer state or a PERSISTED FIELD is a data-model decision OWED BEFORE ANY CODE** — a server field means a migration and a column three dated checkpoints would read. **⚠️ Dismissal never deletes the relationship row.** **(3) A MIGRATION HEALS GRANT ROWS on relationships terminated before `v0.97.0`** — **⚠️ DISPLAY-ONLY, no access was ever open**, and **⚠️ TERMINAL STATUSES ONLY, NEVER the `ACCEPTED → PENDING` consent pause**, where `v0.93.0` made the row survive by design. **(4) THE SWEEP'S OPERATIONAL GAPS** — no `LIMIT` on `findDuePendingIds`, no `StudySnapProperties` field or `application.yaml` entry for `request-expiry-cron`, a bare `NoSuchElementException` on a benign delete race. **⚠️ No change to what expires or when.** **(5) `DATA_MODEL.md`'s LINKED-LEARNER SECTION**, frozen at `v0.89.1`: three statuses where there are four, *"Only ACCEPTED authorizes the progress read"* which has been false since `v0.93.0`, and four tables missing. **⚠️ Do NOT add, remove or reorder an onboarding FLOW step and no code under `frontend/app/onboarding`** — `[CHECKPOINT — due 2026-09-11]` is 12 days out. **⚠️ Do NOT change what expires, when, or from which clock**, and **⚠️ do NOT backfill `expires_at` onto inherited rows** — `v0.97.0` established after two wrong attempts that a NULL deadline IS the protection for a consent pause. **⚠️ Every standing connection rule is unchanged.** **Pre-declared: ONE SCOPED COLD AGENT framed as falsification** (item 1 moves an authorization boundary; item 3 changes production-data semantics — both one-cold-agent triggers, neither a permission substrate). **⚠️ Item 1 gets its `advisor()` call BEFORE implementation**, being the same gate-tightening class as `v0.97.0` item 5 and `v0.71.0`'s ADMIN lockout.  Full scope in `RELEASES.md`. Previous: **v0.97.0 — Connection Lifecycle** (Released, kicked off 2026-08-29, signed off 2026-08-30, base branch `releases/v0.97.0`, cut from `main` after `v0.96.0` merged and tagged). **Seven items, and the theme is again the scope filter (owner, 2026-08-29):** asked to fold widely, the release takes **everything un-gated on the Learning Connections surface** — the three items `v0.96.0` held out plus four open findings carried in `v0.94.0`/`v0.95.0` Known limitations — and leaves the curated title policy's second half out because it is **blocked on the unrun §5 production read**, not on merit. **⚠️ TWO THINGS WERE FOUND AT KICKOFF THAT THE HANDOFF DID NOT CARRY, recorded so they are not re-derived.** **(a) `v0.96.0`'s signoff owed TWO checkpoints and wrote ONE.** The Domain Context row requires a taxonomy calibration checkpoint *"dated after `2026-09-17`"*; only the title-debt row (`2026-09-28`) was written, so the calibration read existed as prose with **no date**, matching no scan pattern — exactly the silent drift the gate exists to prevent. Written in this kickoff commit as `[CHECKPOINT — due 2026-09-28]`, sharing its twin's date because the trigger is shared and evaluated once. **(b) Item 1 collides with `[CHECKPOINT — due 2026-09-26]` more sharply than that row admits, and the ARITHMETIC is what clears it.** That read's kill criterion literally names *"expiry for unconfirmed requests"* as its own response, and its metric is a `COUNT(*)` on `linked_learner_provisional_birth_years` — **the rows this sweep deletes.** The table only exists from the `v0.95.0` deploy (2026-08-29), so the earliest row is 2026-08-29 and a **≥30-day TTL dated from relationship `created_at`** puts the earliest possible expiry at **2026-09-28, after the read**. That is the same reasoning `[CHECKPOINT — due 2026-10-13]` used. **⚠️ THE TTL BOUND AND THE CLOCK ARE THEREFORE ANTI-DRIFT RULES, NOT DEFAULTS** — a shorter TTL or a different clock destroys a dated read. **Scope: (1) Unconfirmed requests expire** — a new `EXPIRED` terminal status plus `expires_at` and a sweep. **⚠️ `ACCEPTED` relationships never expire; only unconfirmed `PENDING`.** **⚠️ `EXPIRED` is TERMINAL** — no un-expire; re-inviting mints a new relationship, as revoke does. **⚠️ Conditional updates, never read-modify-save.** **⚠️ The provisional row is DELETED on expiry**, per `v0.89.1`'s retention rule. **⚠️ This is the REQUEST's clock, not the CARRIER's** — `invitation-ttl-days` is untouched. **(2) The provisional birth year appears in the account data export** — owner ruled INCLUDE IT, on **completeness, not user value**. **⚠️ A DISTINCT PROVISIONAL FIELD, never merged into `birthYear`**, and **⚠️ only rows where the CALLER is the learner** — the table is keyed by relationship, so a naive join exports someone else's declaration. **(3) Supporter onboarding — the DEFINITION step, docs only, no code.** It is **UNDEFINED, not gate-blocked.** **⚠️ Do NOT add, remove or reorder an onboarding FLOW step** — `[CHECKPOINT — due 2026-09-11]` is still owed. **(4) Terminal transitions cut grant rows, one rule for revoke and expiry.** **⚠️ Today's gap is INERT and stays described as inert** — `requireGrant` demands `ACCEPTED`. **⚠️ A PAUSE IS NOT A TERMINATION:** `v0.93.0` made the grant row survive an `ACCEPTED → PENDING` consent pause **by design**, so cut grants on terminal statuses only and never on the pause. **(5) `requireVerifiedOnboarded` actually requires onboarding** — `assertProfileComplete` passes on a null `onboardingCompletedAt`, so the stated property is false. **⚠️ IT TIGHTENS A GATE — check who it locks out first** (`v0.71.0`'s ADMIN lockout), and **⚠️ the redemption page carries a token THROUGH `/onboarding`**, so a mid-onboarding caller must still finish the flow. **(6) A two-thread real-row concurrency harness** for the provisional-row invariant — `v0.95.0` called it *"the natural first item for any future release that touches this path"* and **item 1 touches it**; the sweep is a third writer on that lock, and the existing harness MOCKS the provisional repository. **(7) Invitation-link endpoint security coverage, 1 of 5 → 5 of 5.** **⚠️ `EXPIRED` must NOT become a distinguishable failure** — the single not-found contract for unknown/revoked/expired/redeemed tokens is preserved. **⚠️ Every standing connection rule is unchanged** (acceptance load-bearing; guardian consent re-asserted inside the grant check and fail-closed on unknown age; no account-existence oracle; absence of a live grant means NO ACCESS; the progress read stays UNIDIRECTIONAL; invitations stay ONE-AT-A-TIME; no relationship-type column, no new profile type, nothing gated on `ProfileType`; `NoteVisibility` stays `PRIVATE | PUBLIC`; no endpoint accepts a learner user id; no public people search). **⚠️ Any predicate deciding whether a relationship expires, or whether a provisional year is promoted, read, exported or discarded, needs a REAL-ROW test** in `NativeQueryPostgresIntegrationTest`, mutation-verified with the killing test named. **⚠️ AMENDED AFTER KICKOFF 2026-08-29 — OWNER LIFTED ALL GATES, SO THE RELEASE IS NINE ITEMS AND THE TIER MOVES TO THE FULL THREE-AGENT COLD-CONTEXT PRESSURE TEST (~490k tokens).** **(8)** the docs cleanup; **(9)** the `v0.96.0` title rule also lands in `note-generation-developer.txt` — the **SAME** rule, never a second formulation, since two wordings of one semantic idea is how it degrades into the banned *"'in X' is bad"* form. **⚠️ The §5 read is not deleted, it simply stops gating.** **Item 3 grows from a definition step into definition PLUS implementation. ⚠️ AND THE ONBOARDING READ IS STILL PROTECTED: `[CHECKPOINT — due 2026-09-11]` is a live measurement window with 13 days left, so editing the FLOW would DESTROY it rather than confound it** — unlike `v0.78.0`/`v0.82.0`, whose lifted gates were confounds on discovery filters. The code allows both: the redemption page **traverses** `/onboarding` via a cookie and never edits it, so **`app/onboarding/page.tsx` stays untouched**; if the definition concludes otherwise, that is a NEW decision to bring back explicitly. **Superseded pre-declaration, retained because a deleted gate cannot be checked: ~~ONE SCOPED COLD AGENT framed as FALSIFICATION~~** — stated with its reasoning because seven items looks like a full-pressure-test release and is not: item 4 is inert by construction and item 5 TIGHTENS an existing gate, so neither introduces a permission substrate or a cross-user read; the single trigger is item 1's change to production-data lifecycle semantics, which is the shape `v0.95.0` carried for a strictly larger change. **⚠️ ESCALATE TO THE FULL TEST IF** item 1's sweep grows a second write path, item 4 turns out not to be inert, or item 5's tightening is found to lock out a live account. **⚠️ Checkpoints owed at signoff: item 1's expiry read, JOINED to `[CHECKPOINT — due 2026-10-13]`** rather than minting a date — same window, same surface, and the consolidation rule applies. Full scope in `RELEASES.md`. Previous: **v0.96.0 — Authoring Integrity** (Released, kicked off and signed off 2026-08-29, base branch `releases/v0.96.0`, cut from `main` after `v0.95.1` merged and tagged). **Four items, and THE THEME IS THE SCOPE FILTER (owner, 2026-08-29):** asked to include everything un-gated, the release takes what the product **generates, names and shows**, and holds three **connection privacy and lifecycle** items for `v0.97.0`. **⚠️ TWO "GATES" LIFTED AT KICKOFF TURNED OUT NOT TO BE GATES — recorded so it is not re-derived.** **(a) The onboarding freeze barely binds on Summary maths:** `app/onboarding/page.tsx` **already renders maths** in its quiz preview (`:2068`, `:2076`) since `31a8a2b9`, so fixing the summary preview makes one screen internally consistent rather than introducing maths to the measured path — **but `[CHECKPOINT — due 2026-09-11]` is STILL OWED and this release must not add, remove or reorder an onboarding FLOW step.** **(b) The title policy's blocking read is a MISSING FACT, not a gate, and both branches share a half:** `developer.txt` is the confirmed title source either way (`StudyPackService:1110`), and the unrun query only decides whether `note-generation-developer.txt` ALSO needs the rule — so this release ships the confirmed half and pre-empts nothing. **⚠️ Supporter onboarding is NOT gate-blocked, it is UNDEFINED** — the phase plan never defines it, so lifting a gate does not scope it. **Scope: (1) Deferred *Save order* model** — dragging performs no network call, an explicit Save commits once, covering ALL drag reordering because sections and notes share `persistLeafItems` and therefore the identical race. **⚠️ Three verified traps:** `refreshBuilder` calls `setLeafItems` from 13 call sites, so non-drag mutations must **flush, never discard**; there is no "section order", only per-item positions, so the dirty check needs a **last-saved baseline**; and a **500ms debounced combobox writer** (`:443`) fires on a timer a leave guard never sees. **(2) Summary renders maths** — **⚠️ the naive fix is wrong**, `_` is markdown emphasis so `$x_1 + x_2$` is mangled before the math is found; add `remark-math` for **tokenization only** and render through the **existing KaTeX setup**, keeping ONE math configuration. Affects 9 consumers including SEO-indexed public pages. **(3) Curated titles name the knowledge, not the curriculum container** — one unconditional rule in `developer.txt`; **⚠️ it changes the AI's default on learner-facing paths too and that is ACCEPTED**; **⚠️ wording stays POSITIVE and SEMANTIC**, never *"'in X' is bad"*, because *"Structural Applications of Differential Equations"* is a CORRECT title; **no per-program title logic, ever.** **(4) Domain Context doctrine, docs only** — **⚠️ `ADR-001`'s coarsest rule is NOT amended**, correct the *"narrowest tradition"* wording, record only the four new items and LINK the rest, and state that NULL is the backlog marker for **single-program notes only** because `NoteApplicableProgramsService:74-75` rejects it on two or more. **⚠️ No title post-processing, no suffix stripping, no mass rename, no migration, no backend change beyond the prompt file, no new analytics event.** **⚠️ Do not rename learner-owned notes or validate against a learner naming one *"Fluid Mechanics for my CE Finals"*.** **Pre-declared at kickoff: a single `advisor()` call on the diff** — no permission substrate, no cross-user read, no money/quota semantics; **the title prompt has the widest blast radius**, so review attention goes there. **A `[CHECKPOINT]` is owed for item 3 and SHARES ITS TRIGGER with the Domain Context calibration checkpoint** — state the trigger canonically once, cross-reference, and note that its metric is **non-authoritative**, so the two-stage rule (count, hand-check a sample, close permanently if legitimate) is stated BEFORE the read. Full scope in `RELEASES.md`. Previous: **v0.95.1 — Rendering and Reorder Fixes** (Released, kicked off and signed off 2026-08-29, base branch `releases/v0.95.1`). **A PATCH, and the scope test is deliberate: only fixes needing no new interaction model and no frozen-path change.** Two owner reports from real use the same afternoon, each investigated against code before scoping. **Scope: (1) Render math on the PLAIN-TEXT note surfaces** — the Full Notes body (`private-note-detail-page-client.tsx:2884`) and the public library note page render in a bare `<p className="whitespace-pre-wrap">` with **no markdown and no math**, so a well-formed `$Q = \frac{2}{3} C_d L \sqrt{2g} H^{3/2}$` displays as source. Apply the existing `renderMathText` — the same call pattern `quiz-question-text.tsx` uses — since `katex` is already a dependency and `normalizeBareMath` additionally repairs notes stored without delimiters. **⚠️ THIS IS A RENDERING GAP, NOT THE CORRUPTED-ESCAPE ITEM** in `v0.86.0-note-item-limit-mismatch.md`; the stored content here is well-formed, so do not close that item with this one. **(2) Make dragging responsive and stop it racing its own save** — every drop in the Study Plan Builder awaits `setCollectionItemOrder` **then `refreshBuilder()`**, which refetches the collection plus **`listNotes()`, the user's entire note list**, plus the goal and every child; and nothing gates dragging meanwhile, so a second drag writes from a diverging base and is clobbered when the first refresh lands. Add an `activationConstraint` to **both** `PointerSensor` and `TouchSensor`, disable dragging **visibly** during a save, and drop the `listNotes()` refetch on the reorder path. **⚠️ MITIGATION, NOT THE AGREED FIX** — the owner chose a deferred *Save order* model covering all drag reordering (2026-08-29), which is `v0.96.0`; only work that survives that rework is in scope here. **⚠️ DO NOT TOUCH `components/ui/summary-markdown.tsx`** — Summary math needs `remark-math` for tokenization, because `_` is markdown emphasis and `$x_1 + x_2$` becomes `<em>` before any post-processing could see it, and **`app/onboarding/page.tsx` renders `SummaryMarkdown`**, so the change lands on the signup path `[CHECKPOINT — due 2026-09-11]` measures against a 62.4% completion baseline; that freeze lifts 2026-09-11 and the fix is `v0.96.0`. **⚠️ Do NOT build the deferred save model here**; **do NOT change what `setCollectionItemOrder` sends or means** and add no endpoint; **`UNGROUPED_SECTION_NAME` stays excluded from section drags** as both source and target; **do NOT remove the 500ms debounced combobox writer** at `study-plan-builder-page-client.tsx:443` or its comment (a `v0.88.0` mechanism with a recorded reason); **do NOT wholesale-refactor** that 2282-line file; **never surface the word "label" in UI copy** and the unsectioned bucket stays *"Not in a section"*. **⚠️ No backend change, no migration, no analytics event.** **Pre-declared at kickoff: a single `advisor()` call on the diff** — two frontend items, no authorization or privacy boundary, no money or quota semantics, no migration, so the re-tiered gate resolves without a cold agent. **No `[CHECKPOINT]` is owed**: both items fix behaviour against a reported defect rather than shipping ahead of evidence. Full scope in `RELEASES.md`. Previous: **v0.95.0 — Redemption Integrity** (Released, kicked off and signed off 2026-08-29, base branch `releases/v0.95.0`). **Four items, capped deliberately** — the re-tiered gate below resolves to **one scoped cold agent** at this size, and *ship as much as possible* is served by more releases, not a bigger one. **It exists because `v0.94.0` recorded, as a Known limitation rather than a defect, that a single UNCONFIRMED invitation-link redemption is irreversible in two ways:** it permanently writes the redeemer's account-global, **write-once** birth year, and it hands the link creator that person's display name **and whether they are a minor** while the creator need never accept. **Widened, not introduced** — the email path discloses the same on acceptance but required already knowing an address, whereas a link harvests from arbitrary clickers, bounded only by the creation rate limit. **⚠️ OWNER RULED 2026-08-29: DEFER THE BIRTH-YEAR WRITE**, over disclosing more loudly before redemption, accepting it as equivalent to the email path, or deferring the ruling — it removes half the irreversibility outright rather than describing it better, and links are new and not yet promoted, so this is the cheapest moment to change their shape. **Scope:** **(1) Defer the birth-year write until the relationship is confirmed** — redemption stops writing `users.birth_year`; the declared year is held provisionally and promoted only on the creator's confirmation. **⚠️ THE NAIVE FIX IS KNOWN TO BE WRONG, audited at kickoff, do not re-derive:** `recordGuardianConsent` (`LinkedLearnerService:427-430`) throws `LinkedLearnerBirthYearRequiredException` on a null year **and requires `PENDING`**, so merely dropping the `captureLearnerBirthYearIfMissing` call at `LinkedLearnerInvitationLinkService:122` leaves a link-redeemed minor `PENDING` with no year and **their supporter can NEVER record guardian consent — the path becomes unreachable for exactly the population it protects**; `accept()` (`:364-373`), `toResponse`'s `consentRequired` / `learnerAgeUnknown` (`:612-613`, `:637`, `:661`) and the separate `recordBirthYear` writer (`:397`) read the same field. **⚠️ THE STORAGE SHAPE IS DECIDED AT KICKOFF, NOT BY DELIVERY, and the obvious answer is UNBUILDABLE — recorded so it is not re-proposed:** `redeem()` sets `redeemed_at`/`redeemed_by_user_id`, so the link row is **terminal** the moment it is used, and **`linked_learner_relationships` carries NO link id**, so `recordGuardianConsent(relationshipId, …)` and `accept()` have **no path back to the link**; reconstructing one from `(redeemed_by_user_id, creator_user_id)` is not unique, since revoke + re-invite mints a new relationship for the same pair. **⚠️ SO: a NEW SIDE TABLE KEYED BY RELATIONSHIP ID** — provisional year plus declared-at, one row per pending relationship, **deleted on promotion and on revoke**, which also honours `v0.89.1`'s rule that declared-value history is minor's personal data and is not retained. **⚠️ Do NOT add a column to `linked_learner_relationships`, `_invitations` or `_guardian_consents`**, whose meaning `[CHECKPOINT — due 2026-09-19]`, `[CHECKPOINT — due 2026-09-26]` and `[CHECKPOINT — due 2026-10-13]` read. **⚠️ AMENDED AT THE `v0.97.0` KICKOFF (owner, 2026-08-29), narrowly and with the reason recorded — raised as a letter-versus-reason conflict rather than reasoned past, per the `v0.96.0` precedent: `v0.97.0` item 1 adds `expires_at` and an `EXPIRED` status to `linked_learner_relationships`. The prohibition exists to keep a row's **MEANING** stable for three dated reads; item 1 changes **LIFECYCLE, not meaning** — `PENDING`/`ACCEPTED`/`REVOKED` keep their definitions and `EXPIRED` names a timed-out `PENDING` rather than reinterpreting any existing value — and both affected reads carry **re-specified queries recorded before they run** (`2026-09-26`, `2026-09-19`), while `2026-10-13` reads the **carrier** clock, untouched. **⚠️ OTHERWISE THE PROHIBITION STANDS IN FULL:** nothing may be added to `_invitations` or `_guardian_consents`, and no further column to `linked_learner_relationships`, without the same explicit amendment.** **⚠️ AND THE DEFERRAL IS FROM `users.birth_year` ONLY — NOT from the consent machinery, which would DEADLOCK:** `accept()` (`:364-370`) throws `LinkedLearnerBirthYearRequiredException` when the learner's year is null and **the caller is not the learner**, so on a supporter-created link redeemed by a learner, deferring the year out of reach leaves **the supporter permanently unable to confirm the relationship only they can confirm.** The provisional year feeds `consentRequired`, `learnerAgeUnknown` and `accept()` exactly as a persisted year does today; what changes is that **nothing reaches the account-global write-once column until the creator confirms**, so an unconfirmed redemption leaves no trace on the redeemer's account. **⚠️ Guardian consent working end to end for a link-redeemed minor while `PENDING` is the ACCEPTANCE TEST for this item, not a side condition.** **⚠️ `users.birth_year` stays account-global and WRITE-ONCE** — this changes *when* it is written, never that it is written once; `v0.89.1`'s correction path is untouched. **⚠️ Birth year is still collected from the LEARNER, never the inviter** (`AGENTS.md` states it, a test pins it), and **the email-keyed path is NOT changed.** **(2) The link surfaces stop reporting state they cannot see** — the live-links list never refetches, so **Copy and Revoke can act on a dead link** and Copy hands out a token the server will reject; reloading after a successful redemption reads as a dead link; and the paused banner claims sharing will *"resume"* on never-accepted `PENDING` rows link redemption newly creates, where it never started. **⚠️ The frontend genuinely CANNOT distinguish "granted, now paused" from "never granted"** — the DTO zeroes `*SharedWithMe` on a non-`ACCEPTED` row — so copy describes the STATUS and must not guess at access, the same rule `v0.94.0` applied to `linked-learner-status.ts` and `SharingPanel`. **(3) The connection accessibility set** — `birth-year-input` steppers nested inside `<label>` in three of four call sites, a raw checkbox where the shared `Checkbox` component is used elsewhere on the same page, disabled toggles left in the tab order. **(4) The "Quiz for someone" modal discloses that quizzes are AI-generated** — **⚠️ copy only, no quota, limit, counter or metering change**, and the Challenge Quiz **mode name** stays distinct from the quota **label**, which a regression test pins. **⚠️ SECOND KICKOFF FINDING, correcting a `v0.94.0` premise:** that release blocked supporter onboarding on "onboarding is frozen", but `[CHECKPOINT — due 2026-09-11]` is the **signup funnel read ALONE** (375 signups against a 62.4% completion baseline, measuring `app/onboarding/page.tsx`), **"supporter onboarding" has NO definition anywhere in the plan** (grepping `learning-connections-phase-plan.md` for `onboard` returns two hits — a caller-gate description and the Phase 4 row itself), and the redemption page already treats `/onboarding` as a **waypoint it carries a token through** (`app/linked-learners/invite/[token]/page.tsx:45,56`), not a surface it edits. **The block rests on an assumption nobody has checked — it is NOT claimed unblocked, it is claimed unchecked** — so it needs a definition step and becomes `v0.96.0` rather than being folded beside a security migration. **⚠️ Do NOT touch `app/onboarding/page.tsx` or the signup → verify-email → onboarding path in this release**; that baseline cannot be re-run. **⚠️ Standing rules unchanged:** acceptance stays LOAD-BEARING and redemption must become *less* consequential, never more; guardian consent is not bypassable by link and stays re-asserted inside the grant check, with `v0.94.0`'s fail-closed unknown-age behaviour staying fail-closed; **no account-existence oracle** and the single not-found contract for unknown/revoked/expired/redeemed tokens is preserved; absence of a live grant means NO ACCESS; the progress read stays UNIDIRECTIONAL with its explicit caller-is-supporter assertion; `PROGRESS` stays learner-issued only while activity stays mutual; every read re-verifies `ACCEPTED`, no cache, no grace; **invitations stay ONE-AT-A-TIME** and the quiz share link stays the many-recipient mechanism; **no relationship-type column, no new profile type, nothing gated on `ProfileType`**; **`NoteVisibility` stays `PRIVATE | PUBLIC`**; **no endpoint accepts a learner user id**; **no public people search**. **⚠️ Deprioritized deliberately, not forgotten:** `revoke()` still not revoking grant rows (recorded inert — `requireGrant` demands `ACCEPTED` and a re-invite mints a new relationship id) and the 1-of-5 endpoint/cross-mode coverage items both stay in `v0.94.0`'s Known limitations. **⚠️ Explicitly OUT OF SCOPE, each needing its own decision (Phase 5): mastery comparison between people, scores, leaderboards, activity rings, social feed, reactions.** **Pre-declared at kickoff: ONE SCOPED COLD AGENT framed as FALSIFICATION**, not the full test — item 1 fires a single trigger (a privacy boundary and guardian consent) and this release adds no permission substrate, no new cross-user read and no money/quota semantics; escalate only if item 1 grows a second write path or a new grant interaction. **⚠️ Also pre-declared: any predicate deciding whether a provisional year is promoted, read or discarded needs a REAL-ROW test** in `NativeQueryPostgresIntegrationTest`, mutation-verified with the killing test named — a mocked repository cannot test a predicate, and that class has now recurred three times. Plan: `docs/claude-plans/learning-connections-phase-plan.md`. Full scope in `RELEASES.md`. Previous: **v0.94.0 — Connection Experience** (Released, kicked off 2026-08-28, signed off 2026-08-29, base branch `releases/v0.94.0`). **Phase 4 of the ratified Learning Connections direction.** **⚠️ Phase 4 is ONE LINE in the plan** — *"shareable invitation links, supporter onboarding, connection management"* — so it was audited against code before scoping, the way `v0.89.0` audited Phase 1 rather than trusting its own roadmap entry, and **the audit changed the release's shape.** **Supporter onboarding is OUT as SEQUENCING, not as a defect:** onboarding is frozen until `[CHECKPOINT — due 2026-09-11]`, so it becomes a clean `v0.95.0`. **"Resend" turned out HALF-BUILT, and the built half is the hard half:** there is no resend endpoint anywhere (verified across all of `backend/src/main/java` and `frontend/lib/api.ts`), **but re-inviting the same address already RE-ARMS the expired row by updating `expires_at`** (`LinkedLearnerInvitationReArmTest`) — what is missing is that nobody is told, because `listInvitations` filters on `findByInviterUserIdAndStatusAndExpiresAtAfter`, so **an expired invitation SILENTLY VANISHES from the inviter's list** and `LinkedLearnerInvitationResponse` carries no `expiresAt` for any surface to show. **Scope:** **(1) Split the progress payload** — remove `engagement` from `LinkedLearnerProgressResponse` so streaks, longest streak, study days and engagement mode need a live `ACTIVITY` grant. **⚠️ This fixes a carried `v0.93.0` defect found independently by two cold agents: a live `PROGRESS` grant makes the learner's activity toggle INERT**, because the progress response carries byte-for-byte the payload the ACTIVITY scope gates. **⚠️ OWNER CHOSE SPLIT (2026-08-28) over coupling the toggles or fixing only the copy**, because it is the only option in which turning activity off actually does something — *absence of a grant means no access*, structurally rather than by convention. **⚠️ Re-derive `hasActivity`** (it reads `engagement.studyDaysThisWeek`), **⚠️ a supporter holding only `PROGRESS` loses streaks and study days — intended, not a regression**, and `v0.93.0`'s *"study activity is visible through shared progress"* copy becomes FALSE and must go. **(2) Shareable invitation links.** **⚠️ RAISED AS A DEFERRAL AND THE OWNER RULED TO SCOPE IT NOW (2026-08-28), accepting three constraints as anti-drift rather than advice** — recorded so the reasoning is not re-derived: **a link BYPASSES email-keying**, which is exactly what `v0.90.0` shipped to close the account-existence oracle, so nothing about whether an address or account exists may be inferable from link creation, redemption or failure; **acceptance stays LOAD-BEARING** per `v0.89.0` (holding a link never by itself creates an `ACCEPTED` relationship, the redeemer takes an explicit act, and the inviter can revoke an unused link); and **guardian consent is NOT bypassable by link** — a minor redeeming one lands in the same `PENDING` + consent path, with birth year still collected at link time from the learner. **⚠️ Invitations stay ONE-AT-A-TIME by principle** (`v0.90.0`): a link delivers ONE relationship and is **not** a multi-recipient invite; the quiz share link stays the many-recipient mechanism and is neither extended nor merged into this. **(3) Invitations stop dying in silence** — surface `expiresAt` and show the clock; when one expires, say so and point at re-inviting, which already re-arms. **⚠️ Do NOT add a resend endpoint that writes a NEW invitation row**, and do not change what a `linked_learner_invitations` row means — `[CHECKPOINT — due 2026-10-13]` reads that table to ask whether the 30-day TTL is cutting real acceptances, and making expiry visible may itself change that rate, so item 3 owes a deploy-split caveat recorded BEFORE the read. **⚠️ Standing rules unchanged:** the progress read stays UNIDIRECTIONAL with the explicit caller-is-supporter assertion (and the test pinning it must keep failing on its removal); `PROGRESS` stays learner-issued only while activity stays grantable either way; granting requires `ACCEPTED` while **withdrawing deliberately does not**, and the panel keeps rendering on `PENDING`; every read re-verifies `ACCEPTED`, no cache, no grace; guardian consent stays re-asserted inside the grant check including its deny-on-null-birth-year branch; **no relationship-type column, no new profile type, nothing gated on `ProfileType`**; **`NoteVisibility` stays `PRIVATE | PUBLIC`**; **no endpoint accepts a learner user id**; **no public people search**, excluded from Phase 4 by the plan. **⚠️ Explicitly OUT OF SCOPE, each needing its own decision (Phase 5): mastery comparison between people, scores, leaderboards, activity rings, social feed, reactions.** **Pre-declared at kickoff: a FULL cold-agent pre-signoff pressure test** — under the gate rewritten at the `v0.93.0` signoff one trigger suffices, and **this release fires two**, introducing a link-based authentication path AND changing a permission payload, so it moves an authorization boundary in two independent places; the ~490k-token cost is accepted deliberately rather than by default. Plan: `docs/claude-plans/learning-connections-phase-plan.md`. Full scope in `RELEASES.md`. Previous: **v0.93.0 — Progress Refinement** (Released, kicked off 2026-08-27, signed off 2026-08-28, base branch `releases/v0.93.0`). **Phase 3 of the ratified Learning Connections direction.** `v0.92.0` shipped the permission substrate and used one of its two scopes; Phase 3 uses the other. **⚠️ It exists because of a finding `v0.92.0`'s own cold-agent pressure test recorded and was FORBIDDEN to fix:** `LinkedLearnerProgressService.getProgress` gates on `requireAcceptedLearnerId` alone — **no grant check** — and returns `getStudyEngagement(learnerUserId)` verbatim, the **identical four fields** Phase 2 put behind a grant, plus a superset (mastery, readiness, collection progress). **So the Phase 2 activity toggle is a real control that does not bound what a supporter sees.** Re-verified at kickoff: `LinkedLearnerReadAuthorizationService:20-25` is one `ACCEPTED` check plus one `caller == supporter` check, and that is the entire gate on the product's only cross-user read; it is unchanged `v0.89.0` code, not a `v0.92.0` regression. **Scope:** (1) `PUT /linked-learners/{relationshipId}/grants/progress` with `LinkedLearnerGrantService` scope-parameterized behind both paths — **`PUT /grants/activity` is a live contract and keeps working** — and `LinkedLearnerResponse` gaining `progressSharedByMe` / `progressSharedWithMe`; (2) `requireAcceptedLearnerId` reimplemented over `requireGrant(caller, relationshipId, PROGRESS)`; (3) the per-scope permission UI — **⚠️ DECIDED AT KICKOFF: the progress toggle renders on the LEARNER side only and the endpoint enforces it**, because item 2 keeps the read unidirectional, so a `PROGRESS` grant written `supporter → learner` is a row **nothing can ever consume** and would render a toggle that shares nothing while making the learner's DTO advertise a link that 404s on click; **granting `PROGRESS` therefore requires the caller to be the relationship's learner**, which makes `progressSharedWithMe` true only for a supporter, so ***View progress* gates on `progressSharedWithMe` alone** with no residual `callerRole` clause for a later agent to delete or misread — *activity is mutual; progress runs learner → supporter*; (4) three `CONNECTION_PROGRESS_*` funnel events, enum first; (5) two carried `v0.92.0` limitations; (6) the Help Center guide. **⚠️ THE READ STAYS UNIDIRECTIONAL, and this is the trap in the reimplementation:** `requireGrant` returns `resolveOtherParty(...)`, which is the **supporter** when the caller is a learner, so a thin passthrough would not raise the directionality question but silently answer it — a learner holding a `PROGRESS` grant would read their supporter's mastery, with `resolveDisplayName(learner)` mislabelling the payload. §4's per-direction table describes the grant **row** (`V125` already stores it per direction, correctly) and says nothing about the endpoint serving both directions. **Keep the caller-is-supporter assertion explicit**; the pinning test is that a learner calling `/progress` gets 404, grant or no grant. Bidirectional progress is a new capability needing its own decision. **⚠️ THE BREAKING SEMANTIC, STATED PLAINLY: after this release an `ACCEPTED` relationship no longer implies progress access** — that is the release, not a side effect, so every surface, doc and test assuming acceptance is sufficient must be found and corrected. **⚠️ NO BACKFILL MIGRATION granting `PROGRESS` to existing `ACCEPTED` relationships** — production was empty on 2026-08-26 so it would be free, and it is forbidden anyway because it converts an implicit rule into explicit consent nobody gave. **⚠️ NO MIGRATION AT ALL and no new scope value** — `V125` ships `scope IN ('ACTIVITY','PROGRESS')`, which is exactly why Phase 2 shipped both. **⚠️ PROGRESS CONTENT IS UNCHANGED** — mastery snapshot, readiness counts, quiz performance and collection progress stay exactly as `v0.89.0` shipped them; this **permissions an existing payload** and adds no field. **⚠️ Preserve the error contract** — `requireGrant` throws `LINKED_LEARNER_NOT_FOUND`, the progress path `LINKED_LEARNER_PROGRESS_NOT_FOUND`; both 404, different codes. **⚠️ Keep `getProgress`'s outer `requireEmailVerified` AND its comment** — it carries a documented defence-in-depth rationale, so deleting it as duplication deletes the reasoning. **⚠️ Guardian consent stays re-asserted inside the grant check** and is asymmetric by design (it gates the learner's data); a consent lapse must cut progress as well as activity. **⚠️ Every read re-verifies `ACCEPTED`** — no cache, no grace; a relationship revoke, a grant revoke and a `v0.89.1` birth-year correction each cut access immediately. **⚠️ Absence of a live grant means NO ACCESS.** **⚠️ Sharing is DIRECTIONAL and never reciprocal** — activity ON does not imply progress ON, and a grant in one direction never implies its mirror; **activity is mutual while progress is learner → supporter only.** **⚠️ One carried `v0.92.0` limitation is now BLOCKING rather than inert:** `toResponse` computes both grant fields with **no relationship-status filter**, and Phase 3 breaks both premises that made it inert — the DTO becomes the UI's access rule, and the reachable case is not terminal `REVOKED` but **`PENDING`**, because a `v0.89.1` birth-year correction pauses `ACCEPTED → PENDING` while the live grant row **survives the pause by design**; **⚠️ DECIDED AT KICKOFF, because the filter has a DIRECTION and adding it blindly just inverts the lie:** the row surviving a pause is load-bearing and documented (withdrawal must never require a status the learner does not control), so filtering both fields on `ACCEPTED` would make a learner's own toggle read OFF while the row is live and sharing would silently resume on re-acceptance. **`*SharedByMe` reflects the ROW** (the caller's own standing act of sharing, and what will resume); **`*SharedWithMe` reflects ACTUAL ACCESS and filters on `ACCEPTED`**, since it is what the view affordance gates on. **A paused relationship renders as paused, not as off.** **⚠️ Also close the grant-write race with this repo's CONDITIONAL-WRITE idiom, not a lock — checked rather than assumed:** `accept()` locks the *learner user row* (for the birth-year read, a different hazard) and guards its status transition with `markAcceptedIfPending`, as `markRevokedIfLive` and `pauseAcceptedForConsent` do; **there is no relationship-row lock anywhere in this codebase**, and `linked-learners.md` states the rule outright — *"status transitions are conditional updates, never read-modify-save."* Make the live-grant insert conditional on the relationship still being `ACCEPTED` at write time; withdrawal stays unconditional on status. **⚠️ Pre-declared at kickoff:** `learning-connections-guide.tsx:8` currently instructs that per-scope `PROGRESS` permissions *"are still Phase 3 and are NOT built"* — this release falsifies it, and naming it now is how the `v0.92.0` doc-comment defect (a `.tsx` instruction outliving its feature, which the `docs/features/` re-read pass structurally cannot catch) is prevented from happening twice. **⚠️ THE PRIVACY LINE IS UNCHANGED AND ABSOLUTE: a supporter sees readiness, progress and quiz performance, NEVER the learner's notes.** **⚠️ No relationship-type column** (`GUARDIAN | TUTOR | PARTNER`), **no new profile type, nothing gated on `ProfileType`**, **`NoteVisibility` stays `PRIVATE | PUBLIC`**, **no endpoint accepts a learner user id**, and **do NOT change what `linked_learner_relationships`, `_invitations` or `_guardian_consents` mean** — `[CHECKPOINT — due 2026-09-19]` and `[CHECKPOINT — due 2026-10-13]` read those tables. **⚠️ Explicitly OUT OF SCOPE, each needing its own decision (Phase 5): mastery comparison between people, scores, leaderboards, activity rings, social feed, reactions, public people search**; Phase 4 (shareable invitation links, supporter onboarding, connection management) is the next release. **⚠️ PHASES ARE SEQUENCED, NOT EVIDENCE-GATED (owner, 2026-08-27)** — adoption may still be zero, which is not a reason to gate Phase 3 but is a reason not to describe its value as proven. **Also pre-declared: a FULL cold-agent pre-signoff pressure test** (shared authorization code, and it flips the only cross-user read from a role check to a permission check), the permanence of `RELEASES.md`'s `### Planned Scope` heading, and a `[CHECKPOINT]` row carrying the deploy-split caveat because Phase 3 changes the progress surface inside `[CHECKPOINT — due 2026-09-26]`'s window. Plan: `docs/claude-plans/learning-connections-phase-plan.md`. Full scope in `RELEASES.md`. Previous: **v0.92.0 — Activity Sharing** (Released, kicked off and signed off 2026-08-27, base branch `releases/v0.92.0`). **Phase 2 of the ratified Learning Connections direction**, plus the quota-legibility item carried out of `v0.91.0`. **Item 1 — Phase 2, Activity Sharing:** a `linked_learner_grants` table (`relationship_id, from_user_id, to_user_id, scope, granted_at, revoked_at`), `scope IN ('ACTIVITY','PROGRESS')`, `CHECK (from_user_id <> to_user_id)`, live-row unique index on `(relationship_id, from_user_id, scope)`; a `LinkedLearnerGrantAuthorizationService.requireGrant(caller, relationshipId, scope)` check; directional opt-in UI; and a momentum view built from existing activity rows. **⚠️ Ship the table with BOTH scopes now and use only `ACTIVITY`** — Phase 3 uses `PROGRESS`, and this is the plan's **only cross-phase coupling**, so it is one migration, not two. **⚠️ Absence of a live grant means NO ACCESS** — accepting a connection grants nothing, it creates the *capacity* to grant; the default is closed structurally, not by convention. **⚠️ Sharing is DIRECTIONAL and never reciprocal by default** — an A ↔ B relationship with A→B activity ON and B→A OFF must be representable and must render correctly on both sides. **⚠️ NO NEW MEASUREMENT.** `ActivityType.MEANINGFUL_STUDY_ACTIVITIES` already defines what *studied* means and **deliberately excludes `OPENED_STUDY_PACK`**; `UserActivityEventEntity` rows, `users.current_streak` / `longest_streak` and `countStudyDaysThisWeek` already exist. Phase 2 is a **permissioned projection of data already written** — do not invent activity concepts and do not add an activity type. **⚠️ Do NOT touch `LinkedLearnerReadAuthorizationService.requireAcceptedLearnerId` in this release** — it hardcodes caller-is-supporter, and **Phase 3** reimplements it over `requireGrant(caller, relationshipId, PROGRESS)`; rewriting it here changes who can read progress during a release that is not about progress. **⚠️ Guardian consent must be re-asserted INSIDE the grant check** where the shared data belongs to a learner who requires it, or `v0.89.1`'s gate quietly reopens — a consent lapse must cut activity as well as progress. **⚠️ Every read re-verifies `ACCEPTED`** — no cache, no grace; a relationship revoke and a `v0.89.1` birth-year correction (`ACCEPTED` → `PENDING`) each cut activity access immediately, by the same predicate. **⚠️ No relationship-type column** (`GUARDIAN | TUTOR | PARTNER`) — permissions define the relationship, and a type column would immediately invite gating on it, the exact `ProfileType` mistake `v0.89.0` was built to correct. **⚠️ No new profile type and nothing gated on `ProfileType`.** **⚠️ Do NOT change what `linked_learner_relationships`, `_invitations` or `_guardian_consents` mean** — `[CHECKPOINT — due 2026-09-19]` and `[CHECKPOINT — due 2026-10-13]` read those tables. **⚠️ `NoteVisibility` stays `PRIVATE | PUBLIC`.** **⚠️ Explicitly OUT OF SCOPE, each needing its own decision (Phase 5): mastery, scores, leaderboards, comparison between people, activity rings, social feed, reactions, public people search.** **Item 2 — quota legibility, a CARRY not a new find:** folded into `v0.91.0` on 2026-08-27, never built, and **missed by that release's signoff gate**, so it is written into Planned Scope at kickoff rather than carried in conversation. Two verified disclosure defects, neither of which changes any limit: `GeneratedQuizService.assertQuizCreditAvailable` spends `user_usage.challenge_quiz_generations` when a user makes a quiz **for someone else** while every surface labels that meter *Challenge Quiz*, so a parent who never takes one cannot tell what they are spending; and `QuizShareLimitService.assertShareLinkQuotaNotExceeded` has **exactly one call site — link creation** — so a Free user pays the LLM cost for a 4th, 5th and 6th quiz before discovering none can be shared, meaning **the cheaper limit is enforced last**. **⚠️ DISCLOSURE ONLY — no limit, counter or metering change, and NO SECOND COUNTER** (a separate meter for shared quizzes is a pricing decision nobody has taken). **⚠️ Do NOT move the share-link check into the generation path** — generating without sharing is legitimate (teacher export, regenerate before sharing), so **surface the cap earlier; do not apply it earlier**. **⚠️ The quota LABEL and the Challenge Quiz MODE name are DIFFERENT STRINGS** — the mode keeps its name everywhere it names the mode, and a global find-and-replace would destroy that distinction; a regression test pins it. `MePlanResponse` does not carry the share-link limit today, which is why no frontend surface can disclose it — that is the backend half. **⚠️ PHASES ARE SEQUENCED, NOT EVIDENCE-GATED (owner, 2026-08-27).** `linked_learner_relationships` was **empty in production on 2026-08-26**; `v0.91.0` fixed the two reasons nobody could discover the capability (a landing page advertising it as *Coming Soon* for three releases after it shipped, and a Help Center with no supporter section), but adoption may still be zero. **That is not a reason to gate Phase 2 — the owner has ruled on this twice — but it is a reason not to describe Phase 2's value as proven.** `[CHECKPOINT — due 2026-09-26]` and `[CHECKPOINT — due 2026-09-19]` are observational and gate nothing here. **Pre-declared at kickoff: a FULL cold-agent pre-signoff pressure test**, because Phase 2 introduces a permission substrate touching shared authorization code and `v0.91.0` found its blocking defect in code that session had itself written *and* reviewed. **Also pre-declared: `RELEASES.md`'s `### Planned Scope` heading is permanent and delivery appends to `### Shipped` beneath it** — `v0.91.0`'s scope miss happened because delivery renamed that heading, leaving the signoff gate nothing in the file to read. Plan: `docs/claude-plans/learning-connections-phase-plan.md`. Full scope in `RELEASES.md`. Previous: **v0.91.0 — Shared Learning Material** (Released, kicked off and signed off 2026-08-27, base branch `releases/v0.91.0`). **Phase 1 of the ratified Learning Connections direction** (owner, 2026-08-27). **⚠️ The release exists because of one audited fact: sharing a note today means making it PUBLIC** — `NoteVisibility` has two values, and the Share action on a private note tells the owner to publish it first, so a parent giving their child one Study Pack must publish it into Explore for everyone. Scope: a `note_shares` grant table, share/unshare and `shared-with-me` APIs, authorized recipient reads for the note **and** the Study Pack, the three-option *"Who can access this note?"* control, a *"Shared with you"* Library section, Copy to my Library, five funnel events. **⚠️ `NoteVisibility` STAYS `PRIVATE | PUBLIC` — do NOT add a `SHARED` value.** Every read path is `findByIdAndOwnerUserId`, so the enum grants nobody anything and would only assert what a table decides; `AccountPurgeService.deletePrivateArtifacts` **retains `PUBLIC` and deletes `PRIVATE`**, so a `SHARED` note matches neither branch and would **survive the purge of a deleted account while staying readable by its recipients**; and the enum has **42 usages across 24 files**. A shared note stays `PRIVATE`, so it is excluded from Explore and included in purge **by default**. **⚠️ The three-way choice is DERIVED, never stored.** **⚠️ Selecting *Private* REVOKES every live share** (owner decision) with the count named in the confirmation; *Public* revokes none, because public is strictly broader. **⚠️ Connecting shares NOTHING** — a relationship creates the capacity to grant; no note, activity or progress is ever shared automatically and sharing is never assumed reciprocal. **⚠️ The owner's mastery, weak concepts, quiz attempts, scores and practice history NEVER travel with shared material** — assert the recipient DTO's shape, not just its values. **⚠️ The recipient Study Pack read must NOT call `recordActivity` with the owner's id** (`StudyPackService.getById` writes `OPENED_STUDY_PACK` today). **⚠️ Every read re-verifies the relationship is `ACCEPTED`** — no cache, no grace — so a revoke and a `v0.89.1` birth-year correction both cut material access immediately. **⚠️ No endpoint accepts a learner user id.** **⚠️ No new profile type and nothing gated on `ProfileType`** — no Guardian, no Parent implementation, no Teacher re-gating. **⚠️ Shared notes are NOT mixed into the owned Library** and cannot join a Study Plan without being copied first. **⚠️ "Quiz for someone" does NOT return beside Quick Review**, and neither lightweight sharing path is removed or merged. **⚠️ PHASES ARE SEQUENCED, NOT EVIDENCE-GATED (owner, 2026-08-27)** — zero connections in production means a gating read returns nothing; checkpoint rows are recorded but observational. Full audit and five-phase plan: `docs/claude-plans/learning-connections-phase-plan.md`. Previous: **v0.90.0 — Invitation Integrity** (Released, kicked off 2026-08-20, signed off 2026-08-26, base branch `releases/v0.90.0`). Executes the surfacing decision ratified 2026-08-20 plus the security half of the invitation model. **⚠️ Checking the ratified list against code before scoping found HALF of it already satisfied** — supporter surfaces are already gated on an accepted connection and the nav item is already unconditional — **so those are recorded as needing no work, not as scope.** Real work: **(1)** move *"Quiz for someone"* out of the Study Pack practice row into the existing note-actions menu (support/share action, not learner practice; beside *Start Quick Review* it risks becoming an avoidance path on the surface retention is decided) — **⚠️ it stays available to EVERYONE, gated on nothing, because a shared-quiz recipient needs no account and no relationship, and an earlier proposal that coupled them was overturned**; **(2)** **email-keyed invitations**, storing the invite against the typed address rather than a resolved user id, which closes the account-existence oracle (an unknown address writes no row while a real one writes a `PENDING` row visible in the inviter's list) and unlocks inviting someone who has not signed up; **(3)** a proper `BackLink` on the supporter progress sub-page. **⚠️ Learning Connections stays a CAPABILITY — no profile mode, no opt-in toggle, nothing gated on `ProfileType`.** **⚠️ Invitations stay ONE-AT-A-TIME by principle — the quiz LINK is the many-recipient mechanism — and email-keying must NOT become a reason to add multi-recipient invites.** **⚠️ `/progress` stays the current learner's.** **⚠️ Do not change what `linked_learner_relationships` means** — `[CHECKPOINT — due 2026-09-19]` reads it, and an unresolved email-keyed invitation is NOT an accepted connection. Full scope in `RELEASES.md`. Previous: **v0.89.1 — Birth Year Correction** (Released, kicked off 2026-08-19, signed off 2026-08-20, base branch `releases/v0.89.1`). **`users.birth_year` is account-global and WRITE-ONCE with no correction path** — `LinkedLearnerService:203` is the only writer and all three read sites guard on `== null`, so a learner who declares an adult year (by mistake, or coached by a supporter who wants the consent step gone) **permanently disables guardian consent for every future link they ever form**. **⚠️ The load-bearing half is re-evaluating EXISTING links on a downward correction**, not the correction itself: an `ACCEPTED` link without consent on a learner who turns out to be a minor is the exact state the gate exists to prevent. **⚠️ Revert those links to `PENDING`, never `REVOKED`** — the gate is about consent, not about ending a connection both people agreed to. **⚠️ Do NOT move birth year into signup, onboarding or profile editing** — it is collected at link time only, a `v0.89.0` test asserts signup and profile leave it null, and onboarding is frozen until `[CHECKPOINT — due 2026-09-11]`. **⚠️ Only the learner may correct their own year.** **⚠️ Do NOT retain a history of declared values** — minor's personal data, and `attested_at` already reconstructs what was believed at attestation time. The threshold number stays owner-owned pending counsel. **No change to the authorization model, the privacy line, the cross-user read, or what `linked_learner_relationships` means** — `[CHECKPOINT — due 2026-09-19]` reads that table. Full scope in `RELEASES.md`. Previous: **v0.89.0 — Support Another Learner** (Released, kicked off 2026-08-19 as *Regeneration Integrity*, **rescoped the same day**, widened to Phases 2 and 3, signed off 2026-08-19, base branch `releases/v0.89.0`, cut from `docs/ungate-reads` so the `v0.86.0` sweeper verdict and two production reads ship with it; PRs #1121 and #1122 closed unmerged). **⚠️ THE RESCOPE REASON MATTERS: the original item 1 was a live-probe gate — reproduce the regeneration defect before fixing it — and it was deferred for want of owner time. The hypothesis was NOT tested and NOT killed.** This is *not* the `v0.87.0` case where a probe falsified a premise; nothing was learned about regeneration. **Do not record it as cleared.** Its Backlog Index row stays open at full strength and the harness is preserved at `docs/claude-plans/v0.89.0-regeneration-reproduction.md`. **The replacement is owner-ratified direction (2026-08-14) whose gate was a decision — *"does Phase 1 already largely exist? Audit before scoping"* — and the audit ran 2026-08-19: it does.** Generating a quiz for someone else is **ungated**, `/quiz/share/**` is `permitAll` so the recipient needs **no account**, and `QuizShareLimitService` already meters share links per plan (FREE 3 / PLUS 10 / PRO unlimited). **The single blocker is `QuizShareLinkService.requireTeacherOrAdmin:160-166`**, which restricts share-link creation to `TEACHER`/`ADMIN`. **⚠️ That is an AXIS ERROR, which is why it is a defect rather than a missing feature: `ProfileType` answers "how do YOU learn?", not "may you help someone?"** A parent helping a child is a `STUDENT`/`BOARD_EXAM` profile, so the only workaround is to misrepresent their own learning profile — and `ProfileType` drives dashboard emphasis, quiz-mode availability and generation behaviour, so the workaround actively degrades the helper's own experience. **⚠️ Phase 1 records NO relationship** — no linking, invitation, acceptance, permissions model or cross-user read; those are Phases 2–3, and Phase 3 is the product's first cross-user authorization. **⚠️ Do NOT introduce a supporter `ProfileType` value or gate anything new on `ProfileType`** — `PARENT` exists unimplemented with **zero users**; do not wire it up as the mechanism. **⚠️ Consent, minors and Data Privacy Act obligations do NOT apply to Phase 1** — nothing about the recipient is stored, they take the quiz anonymously — and must not be built for it. **Quota needs no decision: the supporter generates on their own account, against their own quota and share limit.** **Keep DOCX export and multi-version exports `TEACHER`-gated** — printable exam papers are classroom administration; only the share link opens. **⚠️ Do NOT justify this on retention** — the supporter's return loop is the Phase 3 progress view, and Phase 1's value accrues to the *learner* receiving the material, so supporter return rate is the wrong metric. **No demand-signal capture in onboarding before 2026-09-11.** Also carries the `generation_status_at` sweep-stamp fix (`GenerationRecoveryRowWriter.recoverPool:58-59` sets `FAILED` without stamping), which makes the bounds half of `[CHECKPOINT — due 2026-09-01]` answerable. **⚠️ WIDENED 2026-08-19 TO PHASES 2 AND 3 at owner request** — Phase 2 alone would have ended the release with still no progress view, repeating the gap Phase 1 left. **⚠️ Phase 3 is the product's FIRST cross-user read; every read path today is owner-scoped, so this is an authorization model, not charts.** **⚠️ Linking is invite + accept in BOTH directions, revocable either side — acceptance is load-bearing**, since without it anyone could claim a relationship by knowing an email address. **⚠️ Age is collected AT LINK TIME, never at signup** (onboarding is under measurement until `[CHECKPOINT — due 2026-09-11]`), with guardian consent below a threshold that is **NOT decided** — the number is a legal question pending counsel, so build it as **configuration, not a literal**, and if a default must ship make it the most protective value. **⚠️ THE PRIVACY LINE IS ABSOLUTE: a supporter sees readiness, progress and quiz performance, NEVER the learner's notes** — this protects the core loop, because learners who suspect notes are visible write less honestly. **⚠️ Every cross-user read must verify an `ACCEPTED` relationship, not merely that a row exists; revocation cuts the read immediately.** **⚠️ Viewing must never write `ConceptHealth`** — it has moved only from genuine assessment since `v0.37.0`, and a view that touches it would corrupt the learner's signal with someone else's activity. **No sub-accounts, no shared quota or subscription, no supporter `ProfileType`, no signup/onboarding fields.** Full scope in `RELEASES.md`. Previous: **v0.88.0 — Section Authoring** (Released, kicked off 2026-08-18, signed off 2026-08-19, base branch `releases/v0.88.0`, cut from `docs/subject-plan-sections-assessment` so the plan ships with the release). **⚠️ Sections ALREADY SHIP end to end — the framing "build Sections" is wrong.** They are derived client-side from the existing per-item `note_collection_items.label`; learner rendering (collapsible cards, counts, a `N% · M due` readiness pill) and Builder rename/reorder/move-between/drag all exist today. **What is missing is a way to create the SECOND section, a guard on the first, and any path by which notes arrive pre-sectioned.** Approved scope is the seven-item list in `docs/claude-plans/subject-plan-sections-assessment.md` under *"Necessary to make Sections genuinely usable"* — output of two cold-context agents plus a cold UX pressure test, revised twice in response. **⚠️ The model decision is CLOSED: no migration, no `note_collection_sections` table, no new entity, no third collection level.** It was decided against an explicit table on production evidence — the curator's own 77-note plan proved the workflow is **notes-first**, so empty sections (the one thing labels cannot express) are not needed; once bulk generation assigns a section, generating the Algebra batch *creates* the Algebra section as a side effect. **The incident is reproduced:** a fresh plan is all `label = null` → one derived section named `Ungrouped`; the Builder renders every header as an editable input with no special case (`study-plan-builder-page-client.tsx:541`), and `handleRenameLeafSection` (`:1309-1319`) relabels every item whose derived name matches — with `oldName = "Ungrouped"` that is **all 77 items in one `PUT`**, after which the per-note Move dropdown ("all sections except mine") is empty and a second section can never be created. Confirmed empirically 2026-08-18: `Algebra, 77`, one row. **The learner surface guards exactly this** (`collection-detail-page-client.tsx:277`, `:3022`) — same bug class, guarded in the file the curator does not reach. **⚠️ Items 2 and 3 are each correct alone and WRONG TOGETHER:** the ported combobox carries local state plus a debounced auto-save and `LeafSortableNoteCard` is keyed by note id, which is stable across `refreshBuilder` — so after a rename mass-relabels a section, every card writes back its stale value, **silently reverting the rename one note at a time**. Key the card `${noteId}:${item.label ?? ""}`, as `LeafSectionBlock` already does. **⚠️ Snap typed case-variants in the COMBOBOX** — `algebra` beside `Algebra` renders identically under the `uppercase` header; do NOT case-fold `buildLeafSections`. **⚠️ Bulk generation's section is an EDITABLE field pre-filled from the batch subject**, never a hidden coupling — a silent one leaves no override and no way to prevent the degenerate "every batch is Engineering Mathematics" case. **⚠️ Desktop collapse is load-bearing, not cosmetic:** `LeafSectionBlock`'s collapse is `sm:hidden` / `hidden sm:block`, so on desktop a 77-note plan is an uncollapsible 77-card page — and because `addItems` appends at `max(position)+1`, **section order IS generation order**, so repairing a wrong order means dragging a header across exactly that page. **⚠️ Do NOT re-add "section destination in Add Notes"** — cut after the pressure test disproved its cost estimate (`handleAddLeafNotes` makes no `setOrder` call; `AddNotesModal` has two call sites with different section vocabularies; item 6 covers its cases for less). **⚠️ The unsectioned bucket is named "Not in a section" and must NOT become "Other notes"** — the display string doubles as the reserved sentinel, so a different one needs a two-string guard or a curator can type it and mint a real section that renders identically. **No new mastery signal** (`ConceptHealth` stays the readiness source; per-note concept counts already ship, so section readiness is client-side summation). **Adoption stays snapshot-based**; **"Remove" never means canonical note deletion**; **no runtime LLM for Sections and no automatic taxonomy inference**; **never surface the word "label" in UI copy**. **⚠️ Sequencing the release owes the curator:** `persistAdoptedPlan:1398-1402` returns early on an existing `sourcePlanId`, so **re-adopt is a hard no-op** — anyone adopting the CE Goal before it is sectioned keeps that copy permanently. Finish sectioning before publishing or promoting it. Full scope in `RELEASES.md`. Previous: **v0.87.0 — Failure Attribution** (Released, kicked off, rescoped and signed off 2026-08-18, base branch `releases/v0.87.0`). **⚠️ RESCOPED the same day it was kicked off, on its own pre-committed gate.** It opened as *Published Bounds*, targeting three unpublished word bounds (`title` 1–12, `overview` 8–90, `keyIdea` 4–40) that `v0.86.0`'s pressure test found surviving. Scope item 1 was *reproduce before fixing*, and **they never fired: 14 of 14 live topics across all eight domains generated cleanly, nothing out of bounds, nothing within 15% of a ceiling** — the `overview` floor of 8 was never approached against a shortest observed 28. The bounds are real and mismatched in unit but do not bite; fixing them would be principle-driven against zero evidence, at a prompt cost paid on every call. **Do not re-propose that fix without new evidence.** **When a bulk-generated topic fails, the curator is told only WHICH topic — never why.** `BulkGenerationResultEntity.failedTopics` is a `List<String>` of names; `NoteBulkGenerationService.processBatch` logs the exception server-side and discards it from the receipt. `buildGeneratedNoteContent` alone raises **six distinct** rejections (title, overview, key idea, core details, why-it-matters, quick recall), plus the whole-note bound and the filler guard. **That gap has now cost two investigations** — `v0.86.0` fixed `invalid core details` and the owner's other-subject failures persisted; `v0.87.0` opened against three unpublished word bounds and **a live 14-topic probe across all eight domains came back 14/14 clean, nothing within 15% of a ceiling**, killing that hypothesis too. **⚠️ This release makes failures LEGIBLE and changes no validation** — no bound is altered, published, raised or removed, because changing both at once would destroy the read it exists to produce. **⚠️ `failed_topics` keeps its shape** (the retry path reads it, and every existing receipt is a plain string list); the reason goes in a parallel nullable column. **⚠️ Never surface raw exception text** for non-`AppException` failures. Full scope in `RELEASES.md`. Previous: **v0.86.0 — Generation Recovery** (Released, kicked off and signed off 2026-08-18, base branch `releases/v0.86.0`). **⚠️ A cold-context pressure test found a blocking NPE that every existing test passed over** — the recovery interlock at `StudyPackService` returned `null` from its transaction callback and dereferenced it, and the outer `catch` swallowed that into a false `outcome=failed` with a stack trace, then re-wrote `FAILED` and bumped `updated_at`. The branch was near-unreachable before; this release makes it the steady state. **Four tests were found passing for the wrong reason** and were strengthened, each mutation-verified. **⚠️ `title` (12 words), `overview` (90) and `keyIdea` (40) remain UNPUBLISHED bounds** the prompt never states — item 4 fixed only the bullet arrays, and these three are the leading candidate for owner-reported failures in other subject areas, since they raise `invalid title` / `invalid overview` / `invalid key idea`. **Publish the bound in the prompt; never raise the number.** **⚠️ SCOPE WAS INVERTED BY THE PRODUCTION READ on kickoff day.** Opened against stuck *notes*; `docs/claude-plans/v0.86.0-stuck-generation-sizing.sql` returned **ZERO stuck notes** and put the damage on the two surfaces the kickoff had declared out of scope — ****37** stuck `exam_question_pool` rows (18 `GENERATING` + 19 `PENDING`, corrected 2026-08-18)** (oldest 2026-07-02; every exam start on those packs falls back to on-demand LLM generation) and **1 Long Exam session stuck since 2026-05-19** (a hard permanent block, since `LongExamService:169` returns the stuck session rather than creating a new one). **⚠️ The note half is PROSPECTIVE — do not describe it as a backlog**; it ships because the fix is nearly free once the sweeper exists and one occurrence is permanent for that learner. **The mechanism generalises the fix:** every surface self-heals a *running* task through a full `catch`, but `SimpleClientHttpRequestFactory` uses `HttpURLConnection`, whose socket reads ignore `Thread.interrupt()`, so `shutdownNow()` never unwinds the thread and JVM exit kills it before the catch runs — a note is one LLM call, a pool is a `generateLongExamParallel` fan-out, so the wider window takes the hits. **Pools are primary and cheapest — recovery already exists** at `sampleQuestions:94` (`refreshPool` on `FAILED`); only the transition *into* `FAILED` is missing. **⚠️ `PENDING` is as terminal as `GENERATING`** (a pool killed while queued never reaches `GENERATING` — this is what the first sizing query missed), and **⚠️ `created_at` cannot be the pool clock** because pool rows are reused, so a stamp goes at all four status-write sites (`:145`, `:215`, `:236`, `:327`). **`created_at` IS safe for sessions** — `buildGeneratingSession` always builds a new row. **⚠️ `LONG_EXAM` only:** Challenge sessions also need `challengeQuizQuestionBankService.releaseClaims`, so sweeping them would leak bank claims; Challenge, Adaptive and Interview are Known Limitations. **⚠️ Notes take the ENQUEUE stamp only** — the two-clock design was justified by ~97 stranded bulk rows and zero were observed. **⚠️ Do NOT copy the `analyticsTaskExecutor` drain onto the generation executors** (verified against spring-context 7.0.6: the flag makes `shutdown()` run the entire queue uninterrupted), **do NOT remove the `status != GENERATING` interlock at `StudyPackService:687-695`**, no auto-regeneration, no quota refund, no `REQUIRES_NEW`, no startup sweep. Previous: **v0.85.0 — Domain Signal Integrity** (Released, kicked off 2026-08-17, signed off 2026-08-18, base branch `releases/v0.85.0`). **⚠️ Its production impact is PROSPECTIVE — it changes the prompt for ZERO notes today**, verified against the mechanism: the declared property fires only on a non-null `domain_context`, every enum value in production use already matched the old keyword scan on its own label, and the four values that newly opt in have no notes. Classification sits at 12.7%, so the 463 close by curator classification (`docs/claude-plans/v0.85.0-domain-context-classification.sql`, per SUBJECT never per program), not by more code — tracked by `[CHECKPOINT — due 2026-09-17]`. **⚠️ `PROFESSIONAL_EDUCATION` and `PROFESSIONAL_PRACTICE_AND_REGULATION` are declared `false`** — both were delivered or proposed as `true` and corrected, on the rule that `false` is a no-op (it falls through to the untouched scan) while `true` is a new signal that is permanent per note, since Study Packs never auto-regenerate. **Do NOT fix a missing signal by lengthening `QUANTITATIVE_KEYWORDS`** — that restores the guess. **The domain signal that reaches the LLM is guessed from a name instead of declared, and it is wrong for roughly half the public catalog.** Sized against production: `docs/claude-plans/quantitative-context-coverage-read.sql`. **463 of 956 curator-owned public notes (48.4%) receive NO computation guidance in their generation prompt.** `OpenAiLlmStudyPackService.isQuantitativeContext` decides that by lowercasing a haystack and **substring-matching it against 49 English keywords**, across **7 call sites**. One keyword is `engineering`, and the haystack leads with the Domain Context's **display label** (or `course_program` when unset) — so **coverage tracks the program's NAME, not its content:** programs with "Engineering" in the name are **0% missing (214 notes)**; every other named program is **69% missing (463 of 670)**. `Civil Engineering` 197/0, `Education` 146/136, `Architecture` 90/75, `Nursing` 130/106, `Accountancy` 154/60. **⚠️ The failing subjects are the computational ones** — Nursing `Pharmacology` (dosage calculation), Accountancy `Income Tax` / `Taxation` / `Budgeting` / `Cash and Receivables` / `PPE`, Architecture `Structural Components`. **⚠️ `Accountancy` is the sharpest case: the keyword list contains `accounting`, which is not a substring of `accountancy`.** **⚠️ The "subjects rescue it" defence FAILS — 74 of ~117 rescues in Accountancy+Nursing (63%) come only from free-text tags**, so notes that pass do so accidentally and identically-subjected notes diverge. **The fix is a DECLARED per-value property, not a longer keyword list** — adding words would keep guessing. **⚠️ This release does NOT build the Domain Context Catalog**, which was proposed 2026-08-17 and **DECLINED on production evidence**: curator public notes are 12.7% classified (121 of 956) and only 4 of 8 enum values have ever been used, with `ACCOUNTANCY` and `NURSING` at zero despite 286 unclassified notes in those programs — the taxonomy is not too small, it is unused. Also declined: Domain Categories (one curator). Full reasoning: `docs/claude-plans/domain-context-catalog-assessment.md`. Previous: **v0.84.0 — Public Explore** (Released, merged and deployed 2026-08-17), which completed Discovery System Stage 0 end to end. **Slice C of Discovery System Stage 0 — the release that makes `/explore` anonymous.** Scope: `docs/claude-plans/discovery-system-stage-0-scoping.md`. **Both blocking owner decisions were taken 2026-08-17:** the anonymous Review Sets tab shows the **full published catalog with Adopt gated at click** (not a teaser, not hidden — anonymous and authenticated `/explore` must stay the same page, which is the whole argument for choosing the `/explore` URL over the cheaper `/public/library` retarget), and **`robots.ts` disallows `/explore` while the work is incomplete and allows it once metadata lands.** **⚠️ `/explore` is gated CLIENT-SIDE only** — `explore-page-client.tsx:49` calls `requireAuthenticatedOnboardedUser(router)` and there is **no `frontend/middleware.ts` at all**, so removing the gate is a component change, not an auth-architecture one. **⚠️ The `redirect` query param does NOT survive signup and must not be used to carry adopt intent** — `resolvePostLoginDestination` routes new signups through `/verify-email` and `/onboarding` first, dropping it; `docs/features/exam-hub.md` records this. The anonymous Explore audience is overwhelmingly new signups, exactly the population it fails for. **Mirror `frontend/lib/exam-intent.ts` instead** — a small client cookie module (set/get/clear, 30-minute max-age, `SameSite=Strict`) set at `/auth` and consumed after onboarding, the way `goal-prompt-banner.tsx` consumes the exam one. **⚠️ `/public/library` already emits `CollectionPage` JSON-LD for the same content**, so the canonical direction between it and `/explore` must be decided as part of the metadata work, not after it. **⚠️ Stage 3 stays doctrine-blocked** by `AGENTS.md`'s Explore Navigation Rule; nothing here redirects `/public/library`, and subject and note-detail pages are never-redirect regardless. Previous: **v0.83.2 — Anonymous Discovery Access** (Released, kicked off and signed off 2026-08-17), which shipped Slice A: anonymous `permitAll` for `GET /subjects` and `GET /course-programs`, fixing empty Subject and Course/Program filters for signed-out visitors, plus removal of the miscategorised `/exam` BackLink. **Slice A of Discovery System Stage 0** — full scope in `docs/claude-plans/discovery-system-stage-0-scoping.md`, which is the doc to read; it re-verified the parent pressure-test doc against current code and corrected one of its premises. **Two live bugs, both on anonymous public surfaces, both independent of whether the rest of the Discovery System ever proceeds.** **(1)** `GET /subjects` and `GET /course-programs` appear **zero times** in `SecurityConfig`, which ends `.anyRequest().authenticated()`, while `SubjectController` and `CourseProgramController` both explicitly serve anonymous `scope=public` and gate only `scope=mine`. The intent is in the controllers; the security config never granted it. `PublicLibraryPageClient` fetches all four facet sources in one `Promise.allSettled`, so the 401s become empty chip lists — **and `v0.83.0` sharpened the symptom**, because its Authored Depth chips come from a permitted path, so an anonymous visitor now sees Tags and Authored Depth populated beside **empty** Subject and Course/Program. **(2)** `/exam/page.tsx:31` carries a `BackLink` to `/public/library`, miscategorising `/exam` as its sub-page when `/exam` is top-level in both the marketing `Navbar` and `PublicFooter`; `docs/features/navigation.md`'s own no-back-link list omits it. **The fix is removal, not repointing.** **⚠️ The `permitAll` change is a security-config widening** — it needs tests asserting an anonymous `scope=public` **200** and an anonymous `scope=mine` **401**, and it is the reason this did not ride `v0.83.1`. **⚠️ Slice B was RESCOPED at this kickoff and is no longer instrumentation work:** `AnalyticsController` already passes `user == null ? null : user.userId()` on a `permitAll` endpoint with a nullable `user_id`, so anonymous-vs-authenticated is **already derivable from existing data** via `user_id IS NULL`. What was missing is an analysis convention, not a field — do not build a parallel viewer-type dimension. **⚠️ Slice C stays blocked on two owner decisions** (what a signed-out visitor sees on the Review Sets tab; the `robots.ts` position) and is NOT in this release. Previous: **v0.83.1 — Note Creation Integrity** (Released, kicked off and signed off 2026-08-17, merged to `main` the same day). **A patch for a live production 500 that predates the release which found it.** `StudyPackService.createGeneratedNote` (`:743`) and `ShareService.createRemixedNote` (`:111`) never set `target_profile_type`, which is **`NOT NULL` with no database default** — verified empirically against real PostgreSQL, the insert fails outright. Reachable via `/study` paste-text (`createStudyPackFromText` sends `{notesText}` with no `noteId`, so `resolveSourceNoteForGeneration` returns null), image upload, confirm-text, and share-remix; **`/study` is live, reached from the Dashboard Today Focus card's `STUDY_SUGGESTION` branch.** **⚠️ The LLM call succeeds and bills against quota before the insert 500s**, so the learner loses generation allowance and gets nothing. Dates to `e66491ed` (2026-03-21); found by `v0.83.0`'s cold-context pressure test and recorded there as a Known Limitation rather than fixed in-scope. **⚠️ The value must be DERIVED from the owner's profile, not a constant** — `SPEC.md:129` documents that contract, and a hardcoded `STUDENT` would falsify a line corrected during `v0.83.0` signoff. **⚠️ Every existing fixture passes the value explicitly, which is exactly why nothing caught this** — new tests must assert the PERSISTED value on a note created through the real paths, not that the call merely succeeds. Previous: **v0.83.0 — Target Audience Removal (Phase 2)** (Released, kicked off and signed off 2026-08-17, merged and deployed the same day, base branch `releases/v0.83.0`). Removed Target Audience from authoring and from Public Library discovery and replaced the retired facet with an Authored Depth `?level=` filter — **which had to be built, because the Public Library never had a depth filter at all**, making `ADR-001`'s claim that Depth inherits the cross-program student-level job false as written. **The replacement is partially populated: 80 of 120 curator public notes formerly tagged `STUDENT` carry NULL depth**, and equality filtering cannot reach NULL, so they stay unreachable until curators classify them — owed by `[CHECKPOINT — due 2026-09-16]`, which is a direct `notes.learner_level` read rather than a metric because **Public Library filtering emits zero analytics events**. **Step 2 of the revised Target Audience retirement**, executing phases 2 and 3 of the already-ratified `ADR-001` amendment (five axes → four): remove the authoring field, the Public Library filter chips, and the `?audience=` URL parameter. **⚠️ `notes.target_profile_type` is NOT dropped and no migration ships.** The column is the input `V117` derived depth from, and `[CHECKPOINT — due 2026-09-16]`'s kill criterion — *is `BOARD_TAKER` trustworthy outside Information Technology?* — cannot run without it; phase 4 (column drop, `bulk_generation_result.target_profile_type`, the `NoteTargetProfileType` enum) waits on that report. The column simply stops being read by product code. **⚠️ Phase 2 was deliberately UNGATED from `[CHECKPOINT — due 2026-09-13]` on 2026-08-16 (owner call)** — the audience chip has been a secondary filter since `v0.79.0` made course/program primary, so the Explore confound is recorded rather than waited on. **Do not re-gate it**, and do not re-derive a gate from the superseded four-phase plan in `target-audience-removal-proposal.md`. **⚠️ Do not re-propose a `V118` `STUDENT` depth backfill** — audited in `v0.82.0` before being written, zero eligible notes. **One open decision is carried into scope rather than assumed:** removing the audience filter leaves student-level material with an empty depth filter, because `V117` deliberately left `STUDENT` depth NULL (it spans four real depths). That is a product regression, not bookkeeping — and **the repo contradicts itself on its size**, the killed `V118` audit implying curator student notes already carry depth while the reversal doc records 6 curator `High School` NULL-depth `STUDENT` notes. Sized by a query in `docs/claude-plans/v0.82.0-post-deploy-narrowing.sql`; decided on that number, then either accepted with a dated follow-up or fixed by curator classification first. **Phase 1 removal touches a public, linkable contract** — a shared `?audience=` URL must degrade to an unfiltered view, never to an error. Previous: **v0.82.0 — Authored Depth Backfill** (Released 2026-08-16, merged and deployed 2026-08-17, base branch `releases/v0.82.0`). **⚠️ Owed immediately post-deploy and not yet run: the step-2 narrowing query, `docs/claude-plans/v0.82.0-post-deploy-narrowing.sql` — expected 819 rows.** The pre-merge capture ran and was exported to `docs/claude-plans/v0.82.0-curator-depth-backfill-population.csv` (828 ids; verified 2026-08-17 — all unique, all nine excluded Information Technology ids present, so 828 − 9 = 819 reconciles by identity and not by count alone), but **the superset is not the key**: the narrowed set is what the reversal and both `[CHECKPOINT — due 2026-09-16]` reads key on, because `NULL IS DISTINCT FROM 'BOARD_EXAM_REVIEW'` is TRUE and the superset would count every deliberately-excluded note as a correction on day one. Deploy timestamp for that checkpoint's read (b), recorded while cheap: **`2026-08-17 08:29:14 +08`** (merge commit `c7523652`, PR #1089). **Step 1 of the revised Target Audience retirement, and `v0.81.0` was Step 0 — now cleared:** a depth backfill is an authoring correction at scale, which used to strand Challenge bank rows into a session-failing collision; `V115`'s widened key means the regenerated question coexists with the preserved old-level row instead. **Ratifies the `ADR-001` amendment: five note metadata axes become FOUR.** *Target Audience has no long-term architectural responsibility and should be retired — but only after its information is migrated into Authored Depth and its live discovery contract is replaced.* **⚠️ The justification is that it is absent from every prompt and its access-control purpose was never implemented — NOT that Course / Program predicts it.** That ~99.3% correlation is an artifact of a board-heavy catalog and would break if general-student or professional content grew; a second opinion rejected it as grounds for an irreversible step. **Backfill is CURATOR-SCOPED, settled by production data:** of 5,550 affected notes **4,645 are learner-owned** (private, feed no filter, and writing depth there asserts an authoring decision their author never made — `ADR-001` constraint 2 and `v0.75.0`) and **905 are curator-owned**, essentially the 945 public notes the filter reads. Safe mappings only (`BOARD_TAKER → BOARD_EXAM_REVIEW`, `PROFESSIONAL → PROFESSIONAL`); **`STUDENT` stays NULL** because it spans four depths. **⚠️ Target Audience is NOT removed here** — no column drop, no field or authoring change. **⚠️ Phase 2 was UNGATED from `2026-09-13` at signoff (owner call)** — the audience chip is a secondary filter since `v0.79.0` made course/program primary, so the Explore confound is recorded rather than waited on. **Removing the authoring field, the filter chips and `?audience=` may ship now; DROPPING `notes.target_profile_type` may NOT** — it is the input `V117` derived depth from, and `[CHECKPOINT — due 2026-09-16]`'s kill criterion cannot run without it. **Second blocker, a product regression rather than a process one:** `STUDENT` depth is still NULL, so removing the audience filter leaves student material with an empty depth filter until curators classify it. **A `V118` for the `STUDENT` cohort was audited and killed — zero eligible notes; do not re-propose it.** That audit found a **second `BOARD_TAKER` mis-tag outside IT** (a `High School` note the curator had authored as `JUNIOR_HIGH`), which widened `V117`'s exclusion from one named program to a denylist. **Backlog checkpoints were consolidated from nine September dates to two** (`2026-09-11` onboarding funnel, `2026-09-16` everything else) by applying bootstrap-test clause 3 retroactively. **Target Audience must never become a runtime depth fallback** — migration evidence only. Previous: **v0.81.0 — Challenge Bank Integrity** (Released 2026-08-16, base branch `releases/v0.81.0`). **Two `v0.70.0` Known Limitations, open since 2026-08-04, fixed on their own merits and as Step 0 of the Target Audience retirement.** **(1)** `uq_challenge_quiz_question_bank_user_pack_key` is `unique (user_id, study_pack_id, question_key)` — **`learner_level` is EXCLUDED** (`V96:12`). A depth correction makes old rows unreachable via `sameLearnerLevel` gating while their keys stay absent from `disallowedQuestionKeys`, so **the LLM can regenerate a question the learner has already answered.** 5 of 6,235 today, *"unbounded once authoring corrections become routine."* **(2)** `persistGeneratedQuestions:118`'s `saveAll` flushes **at commit, outside its own `catch`**, so `docs/features/challenge-quiz.md`'s *"best-effort and never blocks a Challenge session"* is **already false** for constraint violations. **That claim about `copyTemplateQuestions` being a working precedent was DISPROVED during the release** — its assigned-UUID entity sends `save` down `em.merge()`, which queues a deferred insert, so its catch never fired either. **Why now:** the revised Target Audience direction needs Authored Depth backfilled onto **~905 curator-owned** notes — an authoring correction at scale, verbatim the condition defect (1) names. **⚠️ OUTCOME: only defect (1) was fully closed.** `V115` widened the key and `V116` stamped the unclaimable rows (verified post-deploy: **0 NULL-level rows of 7,581**). **Defect (2) was NOT fixed — the persistence guarantee is PARTIAL**: a concurrent same-level duplicate can still fail a session. The `REQUIRES_NEW` isolation meant to close it **broke every Challenge start** — `origin_session_id`/`claimed_session_id` are FKs to `quick_review_sessions`, and a second connection cannot see the session row the outer transaction has not committed, so every bank insert failed the FK check silently and Official template adoption returned empty on every start. Reverted after a cold-context pressure test that two earlier reviews had missed. **Do not propose `REQUIRES_NEW` as the fix.** **Migration shape settled by production data:** 4,645 of 5,550 affected notes are **learner-owned** and must never be backfilled (it would assert authoring decisions their authors never made, onto a curriculum floor); only the ~905 curator notes matter, because the Public Library filter reads public notes and `ADMIN` owns 905 of the 945. Previous: **v0.80.0 — Instrumentation Integrity** (Released 2026-08-15, base branch `releases/v0.80.0`). The events four September decisions rest on should actually arrive. **`trackAnalyticsEvent` (`frontend/lib/api.ts:2844`) posts with a raw `fetch`**, while every product call goes through `fetchWithAuth` (`:2164`), which refreshes and retries on 401. Analytics does neither and swallows the failure at `:2854`, and **access tokens live 15 minutes** (`application.yaml:428`) — so the learner's action succeeds while the event is discarded. **The loss is BIASED, not random, and that is the point:** impressions fire immediately after a data load with a fresh token; a click on an idle page does not — so **impression→click rates read systematically low**, which is precisely the metric behind both `[CHECKPOINT — due 2026-09-14]` rows (`v0.78.0` recommendation conversion, `v0.79.0` catalog-first). A pipeline that drops the click half of a ratio manufactures a wrong answer rather than adding noise. **⚠️ Ships INTO a live measurement window on purpose, and that inverts the usual caution:** the standing rule assumes a change alters *what* is counted; this alters only whether a counted thing arrives. `v0.78.0`'s window opened 2026-08-15, so shipping now leaves ~29 of 30 days clean and deferring leaves none — **waiting is the option that damages the read.** Also normalizes catalog names in `CourseProgramCatalogService.create` (`v0.79.0` made the catalog the sole public filter-chip source, taking that exposure from partial to total) and closes four weak tests recorded as `v0.79.0` limitations. **Anti-drift: no event changes meaning, firing condition, or metadata; no new or removed events; no existing catalog row is rewritten; no migration.** Previous: **v0.79.0 — Catalog-First Vocabulary** (Released 2026-08-15, base branch `releases/v0.79.0`). The course/program a learner picks should be one the product can act on. **Two sources of truth drifted:** `COURSE_PROGRAM_SUGGESTIONS` (`frontend/lib/learning-profile.ts`) offers **31** hardcoded values against the **21**-row canonical catalog seeded by `V106`, and **only 16 overlap** — so a learner picking a non-overlapping value gets a `course_program` matching no published plan, and `v0.78.0`'s named recommendation can never fire for them. **Production sizes it: 60 of the 189 planless learners are blocked by vocabulary, not content or features** — 15 on `Professional / Board Exam Review` (a study *context*, absent from the catalog by design), 3 Computer Science, 2 Software Engineering, plus free-typed values like `High School`. Fix is **catalog-first suggestions** from the already-`USER`-readable `GET /course-program-catalog` (**free text stays allowed**, no migration, no request queue) plus making `NoteService.listPublicCoursePrograms` return catalog names only, so a personal string stops minting a public filter chip. **⚠️ This is the COUNTER-PROPOSAL from `course-program-canonical-catalog-proposal.md`, NOT the `ADR-001` amendment** — locking the field and the *"Request Program"* queue remain PROPOSED and unratified; shipping this does not ratify them, it is the evidence-gathering step before that decision. **⚠️ Onboarding's copy of the field is deliberately EXCLUDED** to protect `[CHECKPOINT — due 2026-09-11]` (onboarding completion vs a 62.4% baseline); a follow-up applies it after that date. **No catalog rows are seeded, renamed, or retired, and no existing learner value is rewritten** — including the 15 already on the catch-all. Previous: **v0.78.0 — Post-Mastery Next Step** (Released 2026-08-15, base branch `releases/v0.78.0`). A learner who has just mastered a pack should be told **what to study next** — `PostSessionNextStepService.resolveQuickReviewNextStep` passes `null` as `secondaryAction` on the mastered branch (`PostSessionNextStepService.java:215`), so that slot has been empty by construction since `v0.74.0` removed Adaptive Practice from the screen. This fills it with the learner's **next Study Plan item**. **"Next" = lowest `NoteCollectionItemEntity.position` the learner has not practiced**, reusing `NoteCollectionService.toProgressResponse`'s existing `lastSessionCompletedAt != null` definition — a suggestion derived from a *different* notion of done could name a note the plan's own progress bar already counts complete. Plan selection when a note belongs to several: **Primary Review Set** (`users.primary_collection_id`) if it contains the note, else the most recently updated containing collection. **LEG (a) ONLY — leg (b) ("a similar note") is OUT and stays out:** similarity is the Discovery System, gated behind `[CHECKPOINT — due 2026-09-13]`, and a second recommender beside it would fragment both. **Do not change `TodayFocusType.STUDY_SUGGESTION`'s Dashboard behaviour** — `DashboardService.java:188` is its only existing call site; this release adds a second producer, it does not repurpose the first. **No 9th "what's next" resolver** — the branch goes inside `PostSessionNextStepService`, one of the 8 already counted. **⚠️ Standing constraint until 2026-09-12 still holds: do NOT remove the Challenge Quiz Adaptive Practice entry point**, and do not revert `v0.74.0`'s Quick Review removal; this release adds a secondary beside the existing `Take a Challenge` primary and moves no entry point. **`[CHECKPOINT — due 2026-09-12]`'s secondary metric acquires a deploy-split window, recorded before the read** — the confound is marginal (`post-session-next-step.tsx` has no Quiz-tab affordance; unlock fires from Note Detail) but it is written down rather than discovered in September. Also carries the **owner-executed** ~23-pack raw-LaTeX regeneration chore — curator time, not engineering; **re-run the sizing query first**, the display-time normaliser may have shrunk the list, and **do not re-propose the admin backfill**. Previous: **v0.77.0 — Evidence-Gated Weak Concept Recommendation** (Released 2026-08-14, base branch `releases/v0.77.0`). First slice of the ratified *Adaptive Practice as the recommendation engine* direction. `DashboardService.resolveTodayFocusWeakConcepts` resolves weak concepts from **the single latest completed Quick Review** today — the copy literally says *"Your latest Quick Review… showed N weak concepts"* — so one bad quiz triggers a recommendation. It now resolves from `ConceptHealth`'s existing **`incorrect_streak`** against the existing **`TWICE_MISSED_STREAK_THRESHOLD = 2`**, so recommendations follow persistent weakness. **Nothing new is recorded and no second evidence bar is invented.** **WITHIN-PACK ONLY** — cross-pack needs canonical concept identity (`concept` is free text keyed per Study Pack), which is ADR-sized. **⚠️ Standing constraint until 2026-09-12: do NOT remove the Challenge Quiz Adaptive Practice entry point.** This release builds the recommendation and removes nothing, so no gate is overridden. Previous: **v0.76.1 — Adaptive Practice Entry Attribution** (Released 2026-08-14, base branch `releases/v0.76.1`). `ADAPTIVE_PRACTICE_STARTED` now records **where** a session was launched from, so the open **`[CHECKPOINT — due 2026-09-12]`** can answer its secondary question — not just *whether* starts fell, but *where the surviving ones come from*. The recommendation-engine direction ratified 2026-08-14 rests on Dashboard discovery, which was untestable before this. **Seven values plus `direct`**, covering every route that can start a session; the server allowlists them and normalises unknown input, never persisting it verbatim. **It cannot contaminate the read it serves:** the primary metric is a total count, and this is additive metadata with no user-visible behaviour change. **Reuses the `entry` query-param convention** from `challenge-quiz-entry.ts`. **⚠️ STANDING CONSTRAINT, not release-scoped: do NOT remove the Challenge Quiz Adaptive Practice entry point before 2026-09-12.** It is ratified direction, but `v0.74.0` already removed the Quick Review route and a second removal inside the same window confounds that read instead of answering it. It also carries the two direction docs ratified 2026-08-14, retargeted out of a separate `main` PR **because merging to `main` auto-deploys to production** and two merges would mean two interruptions. Previous: **v0.76.0 — Messaging Architecture: The Money Surfaces** (Released 2026-08-14, base branch `releases/v0.76.0`). Second slice of the ratified Messaging Architecture initiative (2026-08-01): *"We sell the learning system. Features simply support that promise."* **Scope is the money surfaces only** — `/pricing` finishes the hierarchy `v0.68.0` started, plus the in-app upgrade prompts; the landing page and Exam Hub upsell are deferred to their own slices. **`FREE.title` is the item the roadmap names as explicitly owed, and the `v0.68.0` revert is its specification:** a candidate was written and reverted there because it was derived from `PLUS`/`PRO` symmetry, which contradicts the ratified `FREE = adopt` placement — if the new title only reads well beside its siblings, it is wrong again. **Positioning only: no pricing or quota changes, product names untouched everywhere, no feature becomes the hero, and `PROFESSIONAL` copy stays unwired** because that profile is enum-only with no feature set behind it. Previous: **v0.75.0 — Authoring by Inference** (Released 2026-08-14). Implements `ADR-001` → *"Authoring populates by inference, not manual classification"*: a curator should confirm inferred note metadata rather than compose three orthogonal axes by hand. **Both of that section's sequenced gates turned out to be already clear** — R4 resolved 2026-08-04, and the *"true first step"* it names (editable authoring metadata on a `STUDY_PACK_READY` note) shipped in `v0.70.0` per `AGENTS.md:1081`; the ADR was stale for four releases and is amended in this release. **Scope is DEPTH ONLY — Domain Context is deliberately excluded and this is binding:** `ADR-001` constraint 1 authorizes an inference chain for `learner_level` alone, and `domain_context IS NULL` is the promotion-backlog marker, so a pre-filled select a curator tabs past would silently convert an observable backlog row into a decided one. **The two legs differ in size:** leg 2 (author profile → depth) merely completes a pattern already shipped beside it — `bulk-generation-page-client.tsx:132` pre-fills `courseProgram` from the profile while `learnerLevel` has no fallback — whereas leg 1 (Review Set → depth) has **no source at all**, since `NoteCollectionEntity` carries no `learnerLevel` and no authoring surface knows its target Review Set. Inference is **always a UI pre-fill, never a server-side default write**. Previous: **v0.74.0 — Quiz Progression** (Released 2026-08-13). Closed a scored assessment whose answer key sat on an adjacent tab: `practice-quiz-card.tsx:25` renders the saved quiz with `revealAnswer`, and Quick Review administers **those same questions** (`note.quiz`) — so a learner can read every answer and then be "assessed" on them, which makes the score meaningless *and* corrupts `ConceptHealth`, locked since v0.37.0 to move only from genuine assessment. The fix is to lock (not hide) the Quiz tab until the learner scores a perfect Quick Review. **Two rationales run in parallel and must stay distinguishable:** *integrity* (don't hand someone the answer key to a test they haven't sat) is satisfied by any completed Quick Review, since the first attempt at each question is the genuine assessment `ConceptHealth` records; the **perfect-score gate is the *progression* layer on top**, and it is the falsifiable half. **Mastery = 5/5, and a perfect score reached through *Redo Mistakes* counts** (owner rulings 2026-08-12) — which is what keeps a perfect-score gate from being a dead end. Challenge Quiz stays open from the start, and that is coherent rather than arbitrary: Challenge generates its own questions, so the answer key cannot spoil it. Brief of record: `docs/claude-plans/next-release-candidates-consultation-prompt.md`. Previous: **v0.73.0 — Onboarding Redesign** (Released 2026-08-12). It went at the funnel's largest single leak — **132 learners, 35.2% of all signups, verify their email and never finish onboarding** — by making the first run explain the product rather than configure an account. **Justified on comprehension, not retention, and that distinction is binding:** activation is already 52.2%, which caps activation-volume work below the retention lever, and the volume hypothesis already failed against a pre-committed rule. Design direction: `docs/claude-plans/onboarding-redesign-ux-review.md`. Previous: **v0.72.1 — Constraint Check** (Released 2026-08-11). It opened on a production read — is the binding constraint retention *rate* or activation *volume*? **The volume hypothesis failed** (activation is already 52.2%, which structurally caps the volume lever below the rate lever), so the Onboarding Intent Router residuals were not taken and the release rescoped to validating the retention metric itself. **That read resized the project's oldest constraint: the "2.4% W1→W2 retention" figure quoted for months counts only days 7–14 after a first Study Pack, and sees 3 of the 11 learners who actually returned — the wider reading is ~7.2%.** Both halves hold: the number understated reality ~3.7×, *and* 141 of 152 activated learners never came back at all. The constraint is mis-sized, not imagined; **do not quote 2.4% as a current figure**. The admin funnel now reports four windows side by side with the strict figure preserved for comparability. Previous: **v0.72.0 — Return Loop** (Released 2026-08-11), which shipped H1+H5 (commitment device + pre-decided return action) under a decision rule pre-committed by an earlier release, and carries two dated checkpoints (proximal **2026-09-10**, distal **2026-11-09** — the latter re-specified to the wider window) recorded in `ROADMAP.md`'s Backlog Index. Before that, **v0.71.2 — Catalog Management** closed the Applicable Programs arc that ran from **v0.71.0** (Release B of `docs/architecture/ADR-001-canonical-knowledge-architecture.md`) through **v0.71.1**. Full shipped scope is in `RELEASES.md`; forward-looking scope and candidates are in `docs/product/ROADMAP.md`.

## Source-of-truth docs (read before implementing anything)

- `AGENTS.md` — implementation rules, anti-drift constraints, code quality rules. Always check this first.
- `docs/architecture/ADR-NNN-*.md` — **binding architecture decision records; read any that touch what you are changing.** `ADR-001-canonical-knowledge-architecture.md` (Accepted 2026-08-03) governs the Note metadata axes: Subject (*what*), **Domain Context** (*how it is authored* — the sole LLM domain constraint), **Note Learner Level** (*how deep*), **Applicable Programs** (*where it appears* — discovery only, never reaches a prompt), and Target Audience (*who* — discovery only, never depth). An ADR outranks a feature doc where they disagree.
- `docs/architecture/ARCHITECTURE.md` and `docs/architecture/DATA_MODEL.md` — system shape and schema
- `docs/product/ROADMAP.md` — what's in scope for the current release and future phases
- `docs/product/SPEC.md` — canonical product behavior
- `docs/product/EXAM_MODES.md` — quiz mode hierarchy (locked contract; exactly 5 modes)
- `docs/features/<feature>.md` — per-feature behavior rules (48 files; **read before changing any feature, update after shipping any behavioral change** — updating `RELEASES.md` alone is not enough)
- `RELEASES.md` — every completed and in-progress change; always update when shipping work
- `docs/gpt-contexts/GPT_CONTEXT.md` — version-stamped snapshot of the full product state; useful to understand where things stand
- `docs/curriculum/` — **read this BEFORE any work on a Review Set curriculum** (building a new board-exam review set, reshaping an existing one, or planning what to author into one). There is an established pipeline and it should not be re-invented. **Step 1 — run `docs/curriculum/review-set-reshape-read.sql` against production** (read-only, six queries): Q0 identifies the review sets, Q1/Q2 give the target set's current shape, Q3 gives a comprehensive set as the benchmark, **Q4 lists notes already tagged for the program but NOT yet in the set — the ready-to-add pool**, Q5 maps the overlap, Q6 gives the exact catalog program names. **Its results ARE the strategist's input** — that is the list of existing notes to hand over. **Step 2 — paste those results with `docs/gpt-contexts/REVIEW_SET_SHAPING_CONTEXT.md`**, the module that makes the strategist emit a machine-readable TSV. **Step 3 — save the TSV block verbatim and run `build_review_set_workbook.py`**, which turns it into the working `.xlsx` the curator builds from. **⚠️ Never hand-transcribe a strategist's proposal into rows** — that is the step the TSV exists to remove, and it is unreviewable once done. `review-set-workbook-spec.md` carries the sheet contract; edit the `.tsv` and regenerate, never the workbook.

## Task routing

| Task type | Agent | Rule |
|---|---|---|
| UX decisions, product questions, architecture tradeoffs, doc writing | **Claude Code** — handle directly | Core Claude Code responsibility |
| Frontend-only additions ≤ ~50 LOC, no new infrastructure (e.g. add a `GuidanceTip`, fix a prop, add a CSS class) | **Claude Code** — implement directly | Too small to justify a Codex prompt; cheaper to do inline |
| Isolated bug fixes in 1–3 files with a clear root cause | **Claude Code** — implement directly | Same — prompt overhead exceeds the fix cost |
| New features touching backend (new endpoint, migration, service logic) | **Codex** — write prompt first | Anti-drift rules in `AGENTS.md` must be enforced across files |
| Multi-system changes (frontend + backend together) | **Codex** — write prompt first | Scope requires full codebase context |
| Refactors or additions touching > 5 files or > ~100 LOC | **Codex** — write prompt first | Too large for reliable inline work |
| Tests for agreed behavior | **Codex** — write prompt first | Already the rule |

**Tiebreaker:** if the change requires reading `AGENTS.md` anti-drift rules and applying them across many files, use Codex. If it's dropping a known component in a known slot with no logic changes, Claude Code does it directly.

- Always check `docs/codex-prompts/` for an existing prompt before writing a new one — if one exists for the current release item, use it directly
- Always use `docs/skills/codex-prompt-generator.md` when writing a Codex prompt
- Never blur the line: if the user wants a Codex prompt, write one — don't start implementing instead
- Never implement automatically when the user asks for a prompt
- **After Codex delivers:** always ask Claude to audit the diff before committing — error states, transactions, idempotency, and load-on-refresh are the most common gaps

---

## Development commands

### Local database

```bash
# Start PostgreSQL only (preferred for local dev — backend runs in IDE)
docker compose up postgres -d

# Start everything including backend
docker compose up -d
```

DB: `study_snap` on port `5433` (mapped from container's 5432). User: `ss_user`.

### Backend (Spring Boot / Maven)

```bash
cd backend

# Run (requires .env file or env vars — see application.yaml for keys)
./mvnw spring-boot:run

# Build
./mvnw clean package -DskipTests

# Run all tests (requires Docker: NativeQueryPostgresIntegrationTest starts a
# PostgreSQL 16 container, applies the real Flyway migrations and PREPAREs every
# native query. Opt out only with -Dnativequery.pg.skip=true, which leaves
# PostgreSQL SQL and the migration set unverified.)
./mvnw test

# Run a single test class
./mvnw test -Dtest=QuizSessionStateUtilsTest

# Run a single test method
./mvnw test -Dtest=QuizSessionStateUtilsTest#methodName
```

Backend runs on `http://localhost:8080/api`.

Required env vars (put in `backend/.env`):
- `DB_USER`, `DB_PASSWORD`, `DB_HOST`, `DB_PORT`, `DB_NAME`
- `LLM_API_KEY` (OpenAI key)
- `JWT_SECRET`

LLM models are config-driven: `LLM_MODEL_FREE` (default: `gpt-4.1-mini`), `LLM_MODEL_PREMIUM` (default: `gpt-4.1`), and `LLM_MODEL_CRITIQUE` (default: `gpt-4.1-mini` for short Interview Practice feedback).

### Frontend (Next.js)

```bash
cd frontend

npm install
npm run dev        # http://localhost:3000
npm run build      # production build (uses --webpack flag)
npm run lint       # ESLint
npm test           # Jest (jsdom, runs in-band)

# Run a single test file
npm test -- path/to/file.test.ts

# Run tests matching a pattern
npm test -- --testNamePattern="guidance engine"
```

Path alias: `@/` maps to the frontend root.

---

## Architecture: what requires reading multiple files to understand

### The Note → Study Pack pipeline

The core async flow that touches the most files:

1. User creates a Note (`NoteEntity`, status `DRAFT`)
2. User triggers generation → `NoteService.startAsyncGenerationFromNote()` → marks note `GENERATING` and enqueues work
3. `LlmStudyPackService` (impl: `OpenAiLlmStudyPackService`) builds prompt context from note + user profile, calls OpenAI, validates JSON schema, persists `StudyPackEntity`
4. Note moves to `STUDY_PACK_READY` or `FAILED`
5. Frontend polls the note detail endpoint until status changes

Prompts live in `backend/src/main/resources/prompts/study-pack-v1/`. Each quiz mode has its own `{mode}-developer.txt` + `{mode}-system.txt` pair.

**Generation context** is resolved in `StudyPackGenerationContextResolver`; do not bypass it. Authoring domain resolves note `domainContext` -> exactly one joined catalog program (`note_course_program`) -> note `courseProgram` -> profile `courseProgram`, while curriculum level resolves note `learnerLevel` -> profile `learnerLevel` -> `COLLEGE`. Static note/Study Pack content uses the effective domain plus note-authored level. Quizzes and exams keep that level as the curriculum floor; a lower reader level may soften scaffolding and wording but never lower curriculum, terminology, or difficulty.

LLM fan-out batches run on a dedicated `llmParallelTaskExecutor`; the main `studyPackGenerationTaskExecutor` must not be passed to `generateLongExamParallel`.

Every account is guaranteed to have a non-null `learnerLevel` after onboarding; the teacher Generate Quiz modal's Target Level override pre-fills from the last generation on that note and falls back to the profile level.

### Quiz session model

All quiz modes (Quick Review, Challenge Quiz, Adaptive Practice, Board Exam) share a single `QuickReviewSessionEntity`. The mode is stored as `QuickReviewSessionMode` enum. Session state (question list, selected choices, timer, difficulty) is stored as a JSONB `sessionState` column. `QuizSessionStateUtils` owns all reads/writes to that JSON — do not manipulate the JSON directly in service code.

Challenge Quiz is the only mode with progressive generation: starts at 5 questions, adds 5 at a time up to 20 via `ChallengeQuizService.generateMoreQuestions()`. Board Exam Mode is exempt from progressive generation — always a fixed set.

Recent Sessions and library `lastSessionCompletedAt` aggregate completed activity across the shared session rows; use `getQuizSessionModeLabel` for mode-to-label mapping instead of inlining labels.

### Profile-type branching

Profile type (`ProfileType` enum: `STUDENT`, `BOARD_EXAM`, `TEACHER`, `PARENT`, `PROFESSIONAL`) drives dashboard emphasis, quiz mode availability, and some generation behavior. It does **not** fork entity tables — all profiles share the same Note/StudyPack/Session model.

Teacher detection in services uses: `user.getProfileType() == ProfileType.TEACHER || user.getRole() == UserRole.ADMIN`

**On note-authoring paths that gate on curator status, that check is preceded by an onboarding guard** (`v0.71.0`, completed in `v0.71.1`): `NoteService.isTeacherSelectableOwner`, `NoteGenerationService.isCurator` and `NoteBulkGenerationService.isCurator` all return `false` when `onboardingCompletedAt == null`, *then* apply the role check. Nobody curates during onboarding — the flow has no catalog picker, so a curator-role account reaching a note-authoring path mid-onboarding was asked for `courseProgramIds` no onboarding screen can supply, which made onboarding uncompletable for every ADMIN account. This removes no authority; once onboarding completes the account is a full curator. Do not restore the bare role check on these paths. **All three now carry the guard** — `NoteBulkGenerationService` was the last holdout, recorded as a `v0.71.0` Known Limitation and closed in `v0.71.1` group 1 item 3, so there is no longer an in-repo exception to point at.

`PARENT` and `PROFESSIONAL` exist as enum values with no feature implementation yet.

### Feature gating

`FeatureGateService` is the single source of truth for plan-based access control. Monthly quotas live in `UserUsageEntity` (reset by `BillingUsageResetJob`). The `Feature` enum values are `ADAPTIVE_QUIZ`, `LONG_EXAM_SESSION`, `INTERVIEW_PRACTICE`, `WEAK_CONCEPT_DETECTION` (`DIFFICULTY_SELECTION` removed in v0.60.1 — Challenge Quiz's manual difficulty selector was removed entirely).

Plan tiers: `FREE / PLUS / PRO`. Payments via Xendit hosted checkout (manual renewal, no auto-charge). Webhook-confirmed activation only — never trust frontend-reported payment state.

### Frontend data flow

The frontend uses Next.js App Router with a mix of server components (data fetching) and client components (interactivity). API calls go through `lib/api.ts`, a custom axios-based wrapper. Auth state (JWT + refresh token) is managed via cookies; there is no Redux or Zustand — state is component-local or passed as props.

Quiz mode visibility per profile is controlled by `lib/exam-mode-visibility.ts` — this is the single source of truth for which modes appear per profile type. Do not hardcode profile checks in UI components.

`lib/guidance-engine.ts` controls one-time contextual tip display. Tips are stored in localStorage via `lib/guidance.ts`. Do not add new one-time tips without going through `pickActiveGuidance()`.

### Versioning rule

The Study Pack is the generated version of a note. Never auto-regenerate. Regeneration always requires explicit user confirmation and updates the existing Study Pack in-place so quiz/session history stays linked. Public-note copies include the linked StudyPack when one exists (intentional, documented exception); owner self-copies still copy only user-authored fields and exclude generated content. Do not "fix" either of these rules back to the old copy-only model.

---

## Key conventions

**Backend:**
- Throw named exception subclasses (e.g. `NoteNotFoundException`) rather than `new AppException(...)` inline. Create a new subclass if none exists.
- Repeated string literals in the same class must be extracted to `private static final` constants.
- `assertThatThrownBy` lambdas must contain exactly one method call (Sonar S5778).
- Use `Math.clamp(value, min, max)` for range-clamping (Java 21 standard; Sonar S6877 flags the nested `Math.min/Math.max` form).

**Frontend:**
- Upgrade CTAs always go through `getUpgradeCtas(currentPlan)` from `src/config/plans.ts`. Never hardcode upgrade copy.
- Analytics events use the `AnalyticsEventType` Java enum (and matching frontend constant). Add to the enum before firing new events.
- Public note pages must not persist anonymous session state. No session is created until the user is authenticated.
- Use `globalThis` instead of `window`, `self`, or `global` for browser globals (`globalThis.localStorage`, `globalThis.setTimeout`, `globalThis.addEventListener`, etc.). ESLint enforces this rule; `window` references will fail the lint check.

**Commits:**
```
type: concise subject
- bullet of high-signal change
- bullet of high-signal change
```

**Always present a plan before implementing.** For any non-trivial change, describe what you intend to do and wait for explicit approval before writing code. Do not start implementing while explaining the plan.

**Never commit automatically.** Always present changes and wait for an explicit "commit" instruction from the user before running `git commit`. Do not stage, commit, or push as a side effect of implementing or updating docs.

**`main` is protected BY CONVENTION, and the enforcement claim was FALSE — corrected 2026-09-02.** This line previously read *"Direct pushes to `main` are blocked by a repository ruleset."* **They are not: a direct push to `main` succeeded during the `v0.103.0` cycle.** No ruleset currently enforces this. **⚠️ THE RULE REMAINS BINDING ANYWAY, and is now MORE important rather than less — nothing will stop a mistake.** All changes — including docs-only changes — must go on a feature or docs branch and be merged via pull request. **Never commit or push directly to `main`.** **⚠️ Do not "verify" this by testing a push.** If the ruleset is ever actually configured, correct this line back rather than deleting it; the two release-management exceptions below (kickoff and signoff commit directly on `releases/vX.Y.Z`) are unaffected.

**Release-management commits go directly on the release branch.** Release **kickoff** (opening a version) and release **sign-off** (closing / marking a version Released) are committed straight to `releases/vX.Y.Z` — do **not** create a separate sub-branch or PR for them. Feature and fix work still goes on its own branch and is merged into `releases/vX.Y.Z` via PR. (`main` stays protected; only the release branch receives these direct release-management commits.)

**Small documentation-only fixes may also go directly on the release branch, no branch/PR.** Scope: doc files only (`.md`, no `.java`/`.ts`/`.tsx`/migration/config/test changes), typically touching one file, correcting something rather than introducing new scope (a version-drift correction, a stale tracker line, a doc/copy fix, adding a process note or rule). Branch+PR overhead isn't worth it at this size. Anything touching actual code — even a genuine one-line bug fix — still goes on its own branch and PR regardless of size, since code changes should go through the normal review/CI path. Still wait for an explicit "commit it" before committing, per the rule above — this only changes *where* the commit lands, not whether to ask first.

Always update `RELEASES.md` with a bullet under the current version section when shipping any change.

When closing a release (marking it Released), commit the closure directly on the `releases/vX.Y.Z` branch (no separate branch/PR), and write a release notes file to `docs/releases/v{X.Y.Z}.md` using the Write tool. Follow the structure of existing files there: `# Release Notes: vX.Y.Z — Theme`, `## Release Theme` (one-sentence), `## Key Features` (bold emoji-prefixed titles with bullet points), `## Polish & Fixes` (flat bullet list). Do not output release notes as plain conversation text.

**Before closing a release, decide the right depth of pre-signoff pressure test — do not default to the heaviest option every time.** Per-PR `/audit-diff` is diff-scoped: it cannot see (a) pre-existing code a PR didn't touch that a new feature increases exposure to, or (b) an invariant interaction between two features shipped in *different* PRs that both touch the same shared method. Both classes of bug only surface via a whole-release view, but that view is expensive, so gate it on release shape:
- **Full pressure test** (three cold agents on non-overlapping halves, synthesized via `advisor`) ONLY for a permission substrate, a first-of-kind cross-user read, or a change to money/quota/production-data semantics. Realistically **one release in four or five.** `v0.93.0` qualified and cost **~490k tokens across three agents**; budget for that before choosing it.
- **One scoped cold agent** when any single trigger fires: the release moves an authorization or privacy boundary; two or more PRs touched the same shared method; **delivery introduced a defect the same session then fixed** (a measured blind-spot signal); or money, quota or production-data semantics changed. **Frame it as FALSIFICATION, not open-ended audit** — hand it a tight file list and the specific claims the implementing session made, and ask it to disprove each. That is far cheaper and targets the one thing cold agents are uniquely good at. `model: "sonnet"` is enough for claim-checking; reserve Opus for genuine unknown-unknowns.
- **Otherwise**, a single `advisor()` call summarizing what shipped is enough — cheaper, and still catches anti-drift violations.

**⚠️ EVERY TIER ABOVE READS CODE. NONE OF THEM EXERCISES TRANSPORT, AND A HIGHER TIER DOES NOT CLOSE THAT GAP — measured in `v0.119.0`.** That release ran the FULL three-agent test in isolated worktrees, which found six confirmed defects including two that would have hit the owner first — and it could not see that **both of the feature's JSON POSTs sent no `Content-Type`**, so Spring rejected every request with `HttpMediaTypeNotSupportedException` **before the controller was entered**. The feature could not make one successful request while **2,182 frontend tests passed**. Three things hid it, and each is general: the component tests **mock `lib/api` wholesale**, so nothing executed header construction; the controller tests called the handler **as a method**, which bypasses content negotiation entirely; and all three agents were scoped by feature area, so the frontend/backend seam fell in the gap between their partitions. **The owner found it on first use.**

**So a new endpoint owes ONE test that issues a REAL REQUEST, at every tier including the cheapest.** In this repo that is `MockMvc` with `.contentType(MediaType.APPLICATION_JSON)` and a body — the pattern already exists in `NoteControllerTest`, and a direct method call is **not** a substitute: it passes under the defect by construction. On the client side, `lib/api-*.test.ts` (nine existing files) pins the request shape; a component test that mocks the API layer proves nothing about it. **⚠️ When writing a pressure-test prompt, add the instruction that catches this class: *enumerate every file the release ADDED and name those with no test that executes them.*** `v0.119.0`'s blind-spot agent already had that heuristic and applied it correctly to the one file it was pointed at — the scoping, not the method, is what missed.

**⚠️ The cheapest checks are the earliest ones, and this is where the yield actually is.** Measured over `v0.93.0`: `advisor()` **before the Codex prompt was written** caught a Flyway/`application.yaml` collision that would have run 125 migrations against H2 and broken ~1,750 tests, a scope contradiction that would have shipped a *View progress* link that 404s, and an ambiguous row-count branch — each for a few thousand tokens, and each preventing work rather than finding it afterwards. **Call `advisor()` before writing any Codex prompt, and again on the diff before commit.** Skipping that checkpoint, not lacking a stronger option, is the failure mode.

**⚠️ BEFORE ANY DEEPER CHECK, ASK WHETHER A TEST EXERCISES THE PATH THE CHANGE TOUCHED — it is free, it is visible in `git diff --stat`, and it would have caught the last two silent no-ops on its own.** `v0.116.0` widened a curator predicate that could never fire, and `v0.117.0` added a callback to a component branch its only consumer never renders. **Both compiled, both passed the entire suite, both were written up as shipped — one with a named user-visible consequence that did not exist — and both were found only by a cold agent.** In each case the tell was the same and cost nothing: **the diff changed a behaviour while touching no test that runs it.** `v0.117.0`'s items 3 and 4 shipped with **zero** changes to a 1020-line builder suite that already covered the neighbouring control. **⚠️ "The suite is green" is evidence only about paths the suite executes**, so a green run on an unexercised change is not weak evidence, it is none. When a diff touches behaviour and no test file beside it moves, either write the guard or say plainly in the release that the item is unverified — the one thing that must not happen is recording it as shipped. **⚠️ AND THE GUARD MUST REACH ITS SUBJECT THE WAY PRODUCTION DOES:** both no-ops had a companion failure where a fixture hand-built a state no code path can produce, which is the same lesson from the other end.

**⚠️ Convert every recurring finding into an automated guard, because a guard is the only check that costs nothing to re-run.** `v0.93.0`'s PostgreSQL harness turned "a human must remember to check native SQL" into "the build fails," and found two live production defects on its first run. **But note what it did NOT do:** `PREPARE` validates syntax, types and `ON CONFLICT` arbiter resolution — never predicate correctness. A cold agent mutated `'ACCEPTED'` → `'PENDING'` in that same release's headline conditional insert, deleted its relationship-id predicate, and **all 1,760 tests passed** because every repository reference in the test tree was a Mockito mock. A guard is only worth what it actually executes; prefer a test that runs the statement against real data over one that merely parses it.

**⚠️ Release SIZE is the biggest lever on verification cost, and it compounds.** Verification scales worse than linearly with items shipped: `v0.92.0` carried eleven items, `v0.93.0` nine across three PRs, and it was the folding that forced the heaviest option. **At three or four items per release the gate above resolves to one `advisor()` call**, each `/audit-diff` stays sharp because the diff is small, and the automated guards carry the rest. When asked to fold more items, say what it does to the verification tier.
- Fix or explicitly document (in `RELEASES.md`, as a "Known limitations" note) every finding before signing off — never silently drop a finding.
- **When the full pressure test runs, its agents must start cold.** Spawn them with the Agent tool, no inherited context, and instruct them explicitly to read the real code rather than trust any summary — including summaries written by the session spawning them. In `v0.74.0` the two most severe defects were in code that session had written *and* reviewed, and one was actively protected by a test asserting the wrong behaviour; a reviewer carrying that session's context inherits its blind spots along with its knowledge.

**Also before closing: re-read every touched feature doc against the FINAL code state, not against the PR that last edited it.** This is a distinct step from "update `docs/features/<feature>.md` when shipping a behavioral change" — that rule is per-PR, and per-PR is exactly where it fails. A later PR in the same release changes the behaviour again and updates *some* docs but not others; each PR looks correct in isolation, and the drift is only visible once everything has landed. **`v0.74.0` accumulated seven such contradictions in one release** — `quick-review.md` claimed Quick Review does not write `ConceptHealth` (false for a month, and the claim the release's own justification rested on), four more lines in the same file, a `ui-standards.md` violation, and two statements in `quiz.md` that only a cold-context agent found. Cheapest reliable form: for each behavioural claim in a touched feature doc, locate the code that implements it and confirm the claim still describes it; a claim you cannot anchor to code is either stale or was never true.

**When a release changes what a user-facing claim MEANS, sweep by SURFACE, not by diff.** The re-read rule above says to check every *touched* feature doc. That is necessary and not sufficient, and the gap has now cost three releases in a row. `v0.93.0` made an `ACCEPTED` relationship stop implying progress access; `frontend/lib/linked-learner-status.ts` still told supporters progress "becomes available once the connection finishes activating" — it shipped false through two releases, because a grep for `"ACCEPTED"` across `app/` and `components/` never reached a file holding prose rather than status literals. `v0.94.0` then removed streaks from the progress payload, and `supported-learners-card.tsx` and `learning-connections-guide.tsx` both went on promising them — **the guide was not in that release's diff at all, and has never once been in a diff when the behaviour it describes changed.** Each time a cold agent found it by reading the surface. Cheapest reliable form: when a release changes a permission, a scope, or what a state means, list every place that *describes* it — help content, empty states, status/copy modules, dashboard cards, marketing — and check those, whether or not they appear in `git diff`. A file that explains a feature is exactly the file that never changes when the feature does.

**Also before closing: if anything in the release shipped ahead of its own evidence, it owes a `[CHECKPOINT — due YYYY-MM-DD]` row in `ROADMAP.md`'s Backlog Index, added in the signoff commit itself.** Full gate — the five required properties, the deploy-relative dating rule, the two-tier design for small denominators, and the verify-the-fallback-is-unbuilt rule — is in `.claude/commands/signoff.md`. **This is the only step that can catch a missing checkpoint:** kickoff step 9 scans for checkpoints that are *overdue*, which cannot detect one that was never written. `v0.72.0` shipped H1+H5 on an explicitly ambiguous read and signed off with none; the gap survived until the next kickoff's gate scan happened to surface it.

**Escalate to a fresh, independently-instructed review for hard-to-find bugs — gate it, don't default to it.** `advisor()` stays the default check before presenting any non-trivial root-cause finding as settled (call it before declaring something done, per its own instructions — the actual failure mode in practice is skipping that checkpoint, not lacking a stronger option). Escalate past `advisor()` to a fresh agent with no inherited context (Agent tool, `model: "opus"`, explicitly told to read the real code/data rather than trust a summary) only when a root-cause claim meets one of these:
- a data/metric relationship that shouldn't be mathematically or logically possible under the stated explanation (e.g. one `COUNT(DISTINCT ...)` exceeding another it should be bounded by);
- the bug class is inherently hard to reason about serially — concurrency, async ordering, effect/component lifecycle timing, migrations, anything where the mechanism has to be traced through indirect state rather than observed directly;
- the fix is about to ship and touches something that already feeds, or will feed, a real product/business decision (e.g. a metric behind a retention read).

A typical isolated bug fix needs neither — direct verification or a single `advisor()` call is enough. This escalation is expensive when used (tens of thousands of tokens, several minutes) — the trigger conditions above are the gate, not general caution.

**Always kick off a version before any implementation.** The kickoff checklist below is the **first commit** on a new `releases/vX.Y.Z` branch — committed directly to that branch — and must land **before** any feature/fix branch is cut or any code is written for the release. Do not start implementation on a version that has not been kicked off. If you find yourself implementing and the version is not yet opened (no `RELEASES.md` section, version refs not bumped), stop and run the kickoff first.

**Release kickoff checklist** (do this when opening a new version, before the first feature commit; commit these directly on the `releases/vX.Y.Z` branch — no separate branch/PR):
1. Add new version section to `RELEASES.md` and mark prior version Released.
2. Add new version section to `ROADMAP.md` and update "Current Release Baseline".
3. Bump `Current version` in `CLAUDE.md`.
4. Bump `version` in `frontend/package.json` and `backend/pom.xml` to match the new version number.
5. Update documentation baseline and version reference in `AGENTS.md`.
6. Update release baseline line in `README.md`.
7. Write release notes to `docs/releases/v{X.Y.Z}.md` for the version just closed.
8. Scan `ROADMAP.md`'s Backlog Index: bump `Last reviewed`, check whether any `Gate` condition became true, and verify every `docs/claude-prompt/*-out/` planning directory, **every `docs/claude-plans/` file AND every `docs/claude-findings/` file** still has a row — this is the only enforced checkpoint against a large planning effort (a multi-document Fable session, a paused exploration) silently going unindexed across release cycles. The `docs/claude-plans/` half was added 2026-08-10 after the `v0.71.1` kickoff found five files there unindexed; the Backlog Index intro carries the narrow *release artifact* exemption that covers finished one-off consultation prompts and sizing queries. Check all three directories — the invariant always said "or session-plan file," but the checklist only ever named some of them. **⚠️ `docs/claude-findings/` was added 2026-09-04 at the `v0.112.0` kickoff, after the scan found a production outage file dated that day AND a second incident file that had gone unindexed through FOUR consecutive kickoff scans — a scan performed exactly as written could not have caught either, because the step did not name the directory they live in.**
9. Scan the Backlog Index for `[CHECKPOINT — due YYYY-MM-DD]` rows past their due date (see "Gate types" in the Backlog Index intro) — a checkpoint that's overdue and unactioned is the same silent-drift risk step 8 guards against, just for vision-driven work shipped ahead of evidence instead of unindexed planning docs.
