spring:
  application:
    name: notelib-backend
  config:
    import: optional:file:backend/.env[.properties],optional:file:../backend/.env[.properties]
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:study_snap}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  servlet:
    multipart:
      max-file-size: ${MAX_IMPORT_FILE_SIZE:10MB}
      max-request-size: ${MAX_IMPORT_FILE_SIZE:10MB}

note:
  import:
    max-batch-files: ${NOTE_IMPORT_MAX_BATCH_FILES:20}

server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api
  tomcat:
    threads:
      # Render Starter is 0.5 CPU / 512 MB. The Spring Boot default of 200 worker
      # threads is pointless at 0.5 CPU and a native-memory hog (~1 MB stack each)
      # that starves the heap. Cap it so the JVM fits the container.
      max: ${SERVER_TOMCAT_THREADS_MAX:25}
      min-spare: ${SERVER_TOMCAT_THREADS_MIN_SPARE:5}

logging:
  level:
    root: ${LOG_LEVEL:INFO}

management:
  endpoint:
    health:
      show-details: never
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        # metrics exposes jvm.memory.used/max (tagged heap vs nonheap), jvm.gc.pause,
        # and thread counts via GET /actuator/metrics/{name} — not permitAll (see
        # SecurityConfig), so this stays authenticated-only. Added so a future memory
        # incident can be diagnosed from live metrics instead of inferring
        # heap-vs-native from the presence/absence of an OutOfMemoryError log line.
        include: health,metrics
      cors:
        allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}

studysnap:
  app-name: NoteLib
  settings:
    max-notes-chars: ${MAX_NOTES_CHARS_FREE:12000}
    max-image-bytes: ${MAX_IMAGE_BYTES:5000000}
    quiz-questions-free: ${QUIZ_QUESTIONS_FREE:5}
    model-free: ${LLM_MODEL_FREE:gpt-4.1-mini}
    model-premium: ${LLM_MODEL_PREMIUM:gpt-4.1}
    model-critique: ${LLM_MODEL_CRITIQUE:gpt-4.1-mini}
    prompt-dir: ${LLM_PROMPT_DIR:prompts/study-pack-v1}
  ocr:
    enabled: ${OCR_ENABLED:true}
    provider: ${OCR_PROVIDER:stub}
    confidence-threshold: ${OCR_CONFIDENCE_THRESHOLD:0.8}
    min-detected-chars: ${OCR_MIN_DETECTED_CHARS:30}
    min-detected-words: ${OCR_MIN_DETECTED_WORDS:6}
    hard-reject-confidence: ${OCR_HARD_REJECT_CONFIDENCE:0.45}
    free-monthly-upload-limit: ${OCR_FREE_MONTHLY_UPLOAD_LIMIT:20}
    premium-monthly-upload-limit: ${OCR_PREMIUM_MONTHLY_UPLOAD_LIMIT:100}
    free-max-image-bytes: ${OCR_FREE_MAX_IMAGE_BYTES:5000000}
    premium-max-image-bytes: ${OCR_PREMIUM_MAX_IMAGE_BYTES:10000000}
    max-pages-per-upload: ${OCR_MAX_PAGES_PER_UPLOAD:1}
    free-rate-limit-per-minute: ${OCR_FREE_RATE_LIMIT_PER_MINUTE:6}
    premium-rate-limit-per-minute: ${OCR_PREMIUM_RATE_LIMIT_PER_MINUTE:20}
    google-application-credentials: ${GOOGLE_APPLICATION_CREDENTIALS:}
    google-application-credentials-json: ${GOOGLE_APPLICATION_CREDENTIALS_JSON:}
  llm:
    api:
      provider: ${LLM_PROVIDER:openai}
      api-key: ${LLM_API_KEY:}
      base-url: ${LLM_BASE_URL:https://api.openai.com/v1}
  quiz-generation:
    mode: ${QUIZ_GENERATION_MODE:real}
    mock-delay-ms: ${QUIZ_GENERATION_MOCK_DELAY_MS:0}
  quick-review:
    study-tip:
      enabled: ${QUICK_REVIEW_STUDY_TIP_ENABLED:false}
      min-incorrect-count: ${QUICK_REVIEW_STUDY_TIP_MIN_INCORRECT_COUNT:1}
      max-questions: ${QUICK_REVIEW_STUDY_TIP_MAX_QUESTIONS:3}
  pricing:
    free-monthly-study-pack-limit: ${FREE_MONTHLY_STUDY_PACK_LIMIT:10}
    plus-monthly-study-pack-limit: ${PLUS_MONTHLY_STUDY_PACK_LIMIT:50}
    pro-monthly-study-pack-limit: ${PRO_MONTHLY_STUDY_PACK_LIMIT:100}
    free-monthly-challenge-quiz-limit: ${FREE_MONTHLY_CHALLENGE_QUIZ_LIMIT:20}
    plus-monthly-challenge-quiz-limit: ${PLUS_MONTHLY_CHALLENGE_QUIZ_LIMIT:100}
    pro-monthly-challenge-quiz-limit: ${PRO_MONTHLY_CHALLENGE_QUIZ_LIMIT:200}
    free-monthly-adaptive-practice-limit: ${FREE_MONTHLY_ADAPTIVE_PRACTICE_LIMIT:3}
    plus-monthly-adaptive-practice-limit: ${PLUS_MONTHLY_ADAPTIVE_PRACTICE_LIMIT:10}
    pro-monthly-adaptive-practice-limit: ${PRO_MONTHLY_ADAPTIVE_PRACTICE_LIMIT:30}
    pro-monthly-interview-practice-limit: ${PRO_MONTHLY_INTERVIEW_PRACTICE_LIMIT:10}
    free-monthly-ask-companion-limit: ${FREE_MONTHLY_ASK_COMPANION_LIMIT:0}
    plus-monthly-ask-companion-limit: ${PLUS_MONTHLY_ASK_COMPANION_LIMIT:20}
    pro-monthly-ask-companion-limit: ${PRO_MONTHLY_ASK_COMPANION_LIMIT:20}
    free-monthly-ocr-limit: ${FREE_MONTHLY_OCR_LIMIT:20}
    plus-monthly-ocr-limit: ${PLUS_MONTHLY_OCR_LIMIT:50}
    pro-monthly-ocr-limit: ${PRO_MONTHLY_OCR_LIMIT:100}
    free-monthly-note-generation-limit: ${NOTELIB_NOTE_GENERATION_LIMIT_FREE:10}
    plus-monthly-note-generation-limit: ${NOTELIB_NOTE_GENERATION_LIMIT_PLUS:25}
    pro-monthly-note-generation-limit: ${NOTELIB_NOTE_GENERATION_LIMIT_PRO:100}
    free-monthly-docx-export-limit: ${FREE_MONTHLY_DOCX_EXPORT_LIMIT:2}
    plus-monthly-docx-export-limit: ${PLUS_MONTHLY_DOCX_EXPORT_LIMIT:15}
    pro-monthly-docx-export-limit: ${PRO_MONTHLY_DOCX_EXPORT_LIMIT:-1}
    free-teacher-monthly-docx-export-limit: ${FREE_TEACHER_MONTHLY_DOCX_EXPORT_LIMIT:10}
    plus-teacher-monthly-docx-export-limit: ${PLUS_TEACHER_MONTHLY_DOCX_EXPORT_LIMIT:-1}
    pro-teacher-monthly-docx-export-limit: ${PRO_TEACHER_MONTHLY_DOCX_EXPORT_LIMIT:-1}
    free-monthly-quiz-share-link-limit: ${FREE_MONTHLY_QUIZ_SHARE_LINK_LIMIT:3}
    plus-monthly-quiz-share-link-limit: ${PLUS_MONTHLY_QUIZ_SHARE_LINK_LIMIT:10}
    pro-monthly-quiz-share-link-limit: ${PRO_MONTHLY_QUIZ_SHARE_LINK_LIMIT:0}
    free-monthly-pdf-export-limit: ${FREE_MONTHLY_PDF_EXPORT_LIMIT:2}
    plus-monthly-pdf-export-limit: ${PLUS_MONTHLY_PDF_EXPORT_LIMIT:15}
    pro-monthly-pdf-export-limit: ${PRO_MONTHLY_PDF_EXPORT_LIMIT:-1}
    adaptive-practice-pro-only: ${ADAPTIVE_PRACTICE_PRO_ONLY:false}
    interview-practice-pro-only: ${INTERVIEW_PRACTICE_PRO_ONLY:true}
    long-exam-pool-size: ${LONG_EXAM_POOL_SIZE:48}
    board-exam-pool-size: ${BOARD_EXAM_POOL_SIZE:24}
    # Kill-switch for eager exam-pool pre-warming (fires automatically on every Study
    # Pack generation, not on actual Long/Board Exam usage). Default off: with zero
    # Pro users today, this is pure LLM-fan-out cost/memory pressure for zero value.
    # A real Long/Board Exam start still works when this is off — its first use can
    # live-generate and then create a per-user pool for later sessions.
    exam-pool-prewarm-enabled: ${EXAM_POOL_PREWARM_ENABLED:false}
  limits:
    file-upload-max-size: ${MAX_IMPORT_FILE_SIZE:10}
    txt-upload-max-size: ${TXT_IMPORT_MAX_SIZE:1}
    pdf-upload-max-size: ${PDF_IMPORT_MAX_SIZE:10}
    docx-upload-max-size: ${DOCX_IMPORT_MAX_SIZE:10}
    pdf-max-pages: ${PDF_IMPORT_MAX_PAGES:30}
    extracted-text-max-length: ${EXTRACTED_TEXT_MAX_LENGTH:200000}
    free-ai-rate-limit-per-minute: ${AI_FREE_RATE_LIMIT_PER_MINUTE:5}
    premium-ai-rate-limit-per-minute: ${AI_PREMIUM_RATE_LIMIT_PER_MINUTE:20}
    free-ocr-rate-limit-per-minute: ${OCR_FREE_RATE_LIMIT_PER_MINUTE:6}
    premium-ocr-rate-limit-per-minute: ${OCR_PREMIUM_RATE_LIMIT_PER_MINUTE:20}
  billing:
    frontend-base-url: ${FRONTEND_BASE_URL:http://localhost:3000}
    backend-base-url: ${BACKEND_BASE_URL:http://localhost:8080}
    price-id-placeholder: ${PLAN_PREMIUM_PRICE_ID:not_used_for_now}
    pricing-regions:
      US:
        currency: USD
        active: ${PRICING_US_ACTIVE:true}
        plus:
          monthly:
            amount: ${PRICING_US_PLUS_MONTHLY_PRICE:3.99}
            duration-days: ${PRICING_US_PLUS_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_US_PLUS_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_US_PLUS_YEARLY_PRICE:0}
            duration-days: ${PRICING_US_PLUS_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_US_PLUS_YEARLY_ACTIVE:false}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
        pro:
          monthly:
            amount: ${PRICING_US_PRO_MONTHLY_PRICE:${PRICING_US_MONTHLY_PRICE:4.99}}
            duration-days: ${PRICING_US_PRO_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_US_PRO_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_US_PRO_YEARLY_PRICE:${PRICING_US_YEARLY_PRICE:39.99}}
            duration-days: ${PRICING_US_PRO_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_US_PRO_YEARLY_ACTIVE:true}
          exam-cycle:
            amount: ${PRICING_US_PRO_EXAM_CYCLE_PRICE:13.99}
            duration-days: 90
            active: ${PRICING_US_PRO_EXAM_CYCLE_ACTIVE:false}
      PH:
        currency: PHP
        active: ${PRICING_PH_ACTIVE:true}
        plus:
          monthly:
            amount: ${PRICING_PH_PLUS_MONTHLY_PRICE:179}
            duration-days: ${PRICING_PH_PLUS_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_PH_PLUS_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_PH_PLUS_YEARLY_PRICE:0}
            duration-days: ${PRICING_PH_PLUS_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_PH_PLUS_YEARLY_ACTIVE:false}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
        pro:
          monthly:
            amount: ${PRICING_PH_PRO_MONTHLY_PRICE:${PRICING_PH_MONTHLY_PRICE:249}}
            duration-days: ${PRICING_PH_PRO_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_PH_PRO_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_PH_PRO_YEARLY_PRICE:${PRICING_PH_YEARLY_PRICE:1999}}
            duration-days: ${PRICING_PH_PRO_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_PH_PRO_YEARLY_ACTIVE:true}
          exam-cycle:
            amount: ${PRICING_PH_PRO_EXAM_CYCLE_PRICE:599}
            duration-days: ${PRICING_PH_PRO_EXAM_CYCLE_DURATION_DAYS:90}
            active: ${PRICING_PH_PRO_EXAM_CYCLE_ACTIVE:true}
      GB:
        currency: GBP
        active: ${PRICING_GB_ACTIVE:true}
        plus:
          monthly:
            amount: ${PRICING_GB_PLUS_MONTHLY_PRICE:3.49}
            duration-days: ${PRICING_GB_PLUS_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_GB_PLUS_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_GB_PLUS_YEARLY_PRICE:0}
            duration-days: ${PRICING_GB_PLUS_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_GB_PLUS_YEARLY_ACTIVE:false}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
        pro:
          monthly:
            amount: ${PRICING_GB_PRO_MONTHLY_PRICE:${PRICING_GB_MONTHLY_PRICE:4.49}}
            duration-days: ${PRICING_GB_PRO_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_GB_PRO_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_GB_PRO_YEARLY_PRICE:${PRICING_GB_YEARLY_PRICE:35.99}}
            duration-days: ${PRICING_GB_PRO_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_GB_PRO_YEARLY_ACTIVE:true}
          exam-cycle:
            amount: ${PRICING_GB_PRO_EXAM_CYCLE_PRICE:11.99}
            duration-days: 90
            active: ${PRICING_GB_PRO_EXAM_CYCLE_ACTIVE:false}
      EU:
        currency: EUR
        active: ${PRICING_EU_ACTIVE:true}
        plus:
          monthly:
            amount: ${PRICING_EU_PLUS_MONTHLY_PRICE:3.99}
            duration-days: ${PRICING_EU_PLUS_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_EU_PLUS_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_EU_PLUS_YEARLY_PRICE:0}
            duration-days: ${PRICING_EU_PLUS_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_EU_PLUS_YEARLY_ACTIVE:false}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
        pro:
          monthly:
            amount: ${PRICING_EU_PRO_MONTHLY_PRICE:${PRICING_EU_MONTHLY_PRICE:4.99}}
            duration-days: ${PRICING_EU_PRO_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_EU_PRO_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_EU_PRO_YEARLY_PRICE:${PRICING_EU_YEARLY_PRICE:39.99}}
            duration-days: ${PRICING_EU_PRO_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_EU_PRO_YEARLY_ACTIVE:true}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
      AU:
        currency: AUD
        active: ${PRICING_AU_ACTIVE:true}
        plus:
          monthly:
            amount: ${PRICING_AU_PLUS_MONTHLY_PRICE:5.99}
            duration-days: ${PRICING_AU_PLUS_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_AU_PLUS_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_AU_PLUS_YEARLY_PRICE:0}
            duration-days: ${PRICING_AU_PLUS_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_AU_PLUS_YEARLY_ACTIVE:false}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
        pro:
          monthly:
            amount: ${PRICING_AU_PRO_MONTHLY_PRICE:${PRICING_AU_MONTHLY_PRICE:7.49}}
            duration-days: ${PRICING_AU_PRO_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_AU_PRO_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_AU_PRO_YEARLY_PRICE:${PRICING_AU_YEARLY_PRICE:59.99}}
            duration-days: ${PRICING_AU_PRO_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_AU_PRO_YEARLY_ACTIVE:true}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
      CA:
        currency: CAD
        active: ${PRICING_CA_ACTIVE:true}
        plus:
          monthly:
            amount: ${PRICING_CA_PLUS_MONTHLY_PRICE:5.49}
            duration-days: ${PRICING_CA_PLUS_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_CA_PLUS_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_CA_PLUS_YEARLY_PRICE:0}
            duration-days: ${PRICING_CA_PLUS_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_CA_PLUS_YEARLY_ACTIVE:false}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
        pro:
          monthly:
            amount: ${PRICING_CA_PRO_MONTHLY_PRICE:${PRICING_CA_MONTHLY_PRICE:6.49}}
            duration-days: ${PRICING_CA_PRO_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_CA_PRO_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_CA_PRO_YEARLY_PRICE:${PRICING_CA_YEARLY_PRICE:51.99}}
            duration-days: ${PRICING_CA_PRO_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_CA_PRO_YEARLY_ACTIVE:true}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
      SG:
        currency: SGD
        active: ${PRICING_SG_ACTIVE:true}
        plus:
          monthly:
            amount: ${PRICING_SG_PLUS_MONTHLY_PRICE:5.49}
            duration-days: ${PRICING_SG_PLUS_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_SG_PLUS_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_SG_PLUS_YEARLY_PRICE:0}
            duration-days: ${PRICING_SG_PLUS_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_SG_PLUS_YEARLY_ACTIVE:false}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
        pro:
          monthly:
            amount: ${PRICING_SG_PRO_MONTHLY_PRICE:${PRICING_SG_MONTHLY_PRICE:6.49}}
            duration-days: ${PRICING_SG_PRO_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_SG_PRO_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_SG_PRO_YEARLY_PRICE:${PRICING_SG_YEARLY_PRICE:51.99}}
            duration-days: ${PRICING_SG_PRO_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_SG_PRO_YEARLY_ACTIVE:true}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
      IN:
        currency: INR
        active: ${PRICING_IN_ACTIVE:true}
        plus:
          monthly:
            amount: ${PRICING_IN_PLUS_MONTHLY_PRICE:299}
            duration-days: ${PRICING_IN_PLUS_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_IN_PLUS_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_IN_PLUS_YEARLY_PRICE:0}
            duration-days: ${PRICING_IN_PLUS_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_IN_PLUS_YEARLY_ACTIVE:false}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
        pro:
          monthly:
            amount: ${PRICING_IN_PRO_MONTHLY_PRICE:${PRICING_IN_MONTHLY_PRICE:399}}
            duration-days: ${PRICING_IN_PRO_MONTHLY_DURATION_DAYS:30}
            active: ${PRICING_IN_PRO_MONTHLY_ACTIVE:true}
          yearly:
            amount: ${PRICING_IN_PRO_YEARLY_PRICE:${PRICING_IN_YEARLY_PRICE:3199}}
            duration-days: ${PRICING_IN_PRO_YEARLY_DURATION_DAYS:365}
            active: ${PRICING_IN_PRO_YEARLY_ACTIVE:true}
          exam-cycle:
            amount: 0
            duration-days: 90
            active: false
    xendit:
      base-url: ${XENDIT_BASE_URL:https://api.xendit.co}
      secret-key: ${XENDIT_SECRET_KEY:}
      webhook-token: ${XENDIT_WEBHOOK_TOKEN:}
  email:
    resend-api-key: ${RESEND_API_KEY:}
    resend-api-base-url: ${RESEND_API_BASE_URL:https://api.resend.com}
    resend-webhook-secret: ${RESEND_WEBHOOK_SECRET:}
    from: ${EMAIL_FROM:}
    support: ${EMAIL_SUPPORT:support@mail.notelib.app}
    app-base-url: ${APP_BASE_URL:http://localhost:3000}
    verification-token-hours: ${EMAIL_VERIFICATION_TOKEN_HOURS:24}
    resend-cooldown-seconds: ${EMAIL_VERIFICATION_RESEND_COOLDOWN_SECONDS:60}
    password-reset-token-minutes: ${PASSWORD_RESET_TOKEN_MINUTES:60}
    daily-limit: ${EMAIL_DAILY_LIMIT:100}
    transactional-reserve: ${EMAIL_TRANSACTIONAL_RESERVE:40}
    reengagement-enabled: ${EMAIL_REENGAGEMENT_ENABLED:true}
  retention:
    daily-cron: ${RETENTION_DAILY_CRON:0 45 2 * * *}
    weekly-cron: ${RETENTION_WEEKLY_CRON:0 0 18 * * SUN}
    knowledge-impact-digest-monthly-cron: ${RETENTION_KNOWLEDGE_IMPACT_DIGEST_MONTHLY_CRON:0 0 9 1 * *}
    inactivity-days: ${RETENTION_INACTIVITY_DAYS:3}
    inactivity-cooldown-days: ${RETENTION_INACTIVITY_COOLDOWN_DAYS:3}
    weak-concept-inactivity-days: ${RETENTION_WEAK_CONCEPT_INACTIVITY_DAYS:3}
    weak-concept-cooldown-days: ${RETENTION_WEAK_CONCEPT_COOLDOWN_DAYS:5}
    unfinished-note-days: ${RETENTION_UNFINISHED_NOTE_DAYS:2}
    unfinished-note-cooldown-days: ${RETENTION_UNFINISHED_NOTE_COOLDOWN_DAYS:3}
    weekly-cooldown-days: ${RETENTION_WEEKLY_COOLDOWN_DAYS:7}
    knowledge-impact-digest-cooldown-days: ${RETENTION_KNOWLEDGE_IMPACT_DIGEST_COOLDOWN_DAYS:30}
    weak-concept-threshold-percent: ${RETENTION_WEAK_CONCEPT_THRESHOLD_PERCENT:60}
  generation:
    recovery-cron: ${GENERATION_RECOVERY_CRON:0 */10 * * * *}
    enabled: ${GENERATION_RECOVERY_ENABLED:true}
    recovery-batch-size: ${GENERATION_RECOVERY_BATCH_SIZE:200}
    pool-pending-bound-minutes: ${GENERATION_POOL_PENDING_BOUND_MINUTES:60}
    pool-generating-bound-minutes: ${GENERATION_POOL_GENERATING_BOUND_MINUTES:60}
    long-exam-session-bound-minutes: ${GENERATION_LONG_EXAM_SESSION_BOUND_MINUTES:30}
    note-bound-minutes: ${GENERATION_NOTE_BOUND_MINUTES:120}
  security:
    google:
      client-id: ${GOOGLE_CLIENT_ID:}
      client-secret: ${GOOGLE_CLIENT_SECRET:}
      certificates-url: ${GOOGLE_CERTIFICATES_URL:https://www.googleapis.com/oauth2/v3/certs}
      token-endpoint: ${GOOGLE_TOKEN_ENDPOINT:https://oauth2.googleapis.com/token}
    jwt:
      secret: ${JWT_SECRET:replace-this-in-env-with-a-long-secret-at-least-32-chars}
      issuer: ${JWT_ISSUER:notelib}
      access-token-minutes: ${JWT_ACCESS_TOKEN_MINUTES:15}
      refresh-token-days: ${JWT_REFRESH_TOKEN_DAYS:7}
      refresh-token-days-keep-signed-in: ${JWT_REFRESH_TOKEN_DAYS_KEEP_SIGNED_IN:30}
    auth:
      max-failed-attempts: ${AUTH_MAX_FAILED_ATTEMPTS:5}
      lock-minutes: ${AUTH_LOCK_MINUTES:15}
      auth-rate-limit-per-minute: ${AUTH_RATE_LIMIT_PER_MINUTE:20}
