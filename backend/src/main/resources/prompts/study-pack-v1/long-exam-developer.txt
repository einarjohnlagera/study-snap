TASK:
Generate exactly {QUESTION_COUNT} NEW multiple-choice questions for a Long Exam mastery test.
{BATCH_HINT}

RULES:
- Base ONLY on the provided summary and key concepts.
- Difficulty target: {DIFFICULTY}.
- Curriculum level target: {LEARNER_LEVEL}.
- {LEARNER_LEVEL_GUIDANCE}
- {TRUE_FALSE_GUIDANCE}
- {COMPUTATION_GUIDANCE}
- {TIME_EXPECTATION}
- This is a comprehensive mastery test, not targeted weak-area practice.
- Cover the broadest reasonable range of note concepts.
- Do not cluster most questions in one domain when multiple concepts are available.
- Include a fair mix of recall, interpretation, and application questions.
- Questions must test understanding, not memorization.
- Questions must be unique.
- Do NOT repeat or rephrase existing questions.
- Avoid similar wording.
- Standard MCQ questions must have 4 choices.
- Standard MCQ questions must have a fully independent set of 4 answer choices. No two MCQ questions in this exam may share the same set of choices.
- The independent-choice rule does not apply inside a deliberate MATCHING block.
- Standard MCQ answers must be exactly one of: A, B, C, D.
- Exactly 1 choice must be correct.
- You may include 1 to 2 MULTI_SELECT questions when a concept has multiple defining attributes, clauses, properties, or category members.
- For MULTI_SELECT questions, set "questionFormat" to "MULTI_SELECT", keep exactly 4 choices, set "correctIndices" to 2 or 3 zero-based correct choice indexes, and set "answer" to the letter for the first correct index as a legacy fallback.
- Do not use MULTI_SELECT when all 4 choices are correct.
- You may include 1 MATCHING block when the notes contain named items that can be matched to distinct labels, laws, descriptions, steps, or categories.
- A MATCHING block must be exactly 2 to 4 consecutive questions. CRITICAL: every question in the block must use the IDENTICAL "choices" array — same 4 options, same order, copied exactly from the first item to every subsequent item. Each item maps to a distinct correct answer letter. If even one item requires different choices, the entire block is invalid — fall back to standard MCQ for all items instead.
- Only use MATCHING when you can identify at least 2 items that genuinely share the same 4-option set. If you cannot, omit MATCHING entirely and write standard MCQ questions. Do NOT create a MATCHING group with only 1 item.
- For MATCHING questions, set "questionFormat" to "MATCHING", set the same short "questionGroup" id such as "group-1" on each item in the block, set "correctIndices" to null, and set "answer" to that item's single correct letter.
- You may include 1 to 2 IDENTIFICATION questions when a concept has one precise, nameable term, definition target, formula name, law, method, component, person, place, or answer.
- Use IDENTIFICATION for focused active recall only. Do not use it for vague, subjective, multi-part, or explanation-heavy concepts.
- For IDENTIFICATION questions, set "questionFormat" to "IDENTIFICATION", set "choices" to an empty array, set "answer" to null, set "correctIndices" to null, set "questionGroup" to null, set "acceptableAnswerGroups" to null, and do not set "correctIndex".
- For IDENTIFICATION questions, set "acceptableAnswers" to 2 or 3 short synonyms or variants that should be accepted after trimming, lowercasing, and collapsing whitespace. Include the primary correct answer first.
- IDENTIFICATION answers are graded by EXACT text match after trimming, lowercasing, and collapsing whitespace. There is no mathematical, chemical, or code-aware comparison. So the answer must be a term, name, or label a learner can type unambiguously IN WORDS.
- NEVER use IDENTIFICATION when the correct answer is a symbolic or notational form: a mathematical expression or equation, a chemical formula, a code snippet, or a value with units. These have many equally-correct renderings — "x^2 + y^2", "x² + y²" and "y² + x²" are all correct and none of them match each other as text — so they cannot be graded fairly. Use an MCQ instead, with the notation forms as the four choices.
- This applies to every subject, not only mathematics: "Identify the chemical formula for water" and "Identify the expression that reverses a list" are just as invalid as the algebraic case.
- A formula's NAME is a valid IDENTIFICATION answer; the formula ITSELF is not. "Which law states that force equals mass times acceleration?" → "Newton's Second Law" is valid. "Identify the equation for Newton's Second Law" → "F = ma" is NOT.
- If the stem asks the learner to identify, give, write, or state an expression, formula, equation, or notation, the answer is notation by definition — do not emit that question as IDENTIFICATION at all.
- "acceptableAnswers"[0] must be the exact thing the stem asks for. Restating the stem is not an answer: for "Identify the algebraic expression for the sum of the squares of x and y", the answer "sum of squares" merely repeats the question and must not be used.
- For every non-IDENTIFICATION question, set "acceptableAnswers" to null.
- Vary question framing — do not start all questions with "Which of the following". Distribute across: positive framing (~40%), negative framing such as "Which is NOT correct?" or "All of the following are true EXCEPT..." (~25%), best-describes framing such as "Which best describes...?" (~20%), and assertion-style framing such as "Statement 1: ... Statement 2: ... Which is correct?" (~15%). Assertion-style "Which is correct?" items are MCQ with four options ("Both statements are correct", "Only Statement 1 is correct", "Only Statement 2 is correct", "Neither statement is correct") — never TRUE_FALSE and never ["True", "False"].
- Explanation is required for every question.
- Explanation must explain WHY the answer is correct — do not repeat, list, or reference answer choices by letter (A, B, C, D) or by choice text.
- Concept is required for every question.
- keyConcept is required for every question and MUST be copied verbatim from the provided key concepts list. Choose the single closest key concept for each question. Do not invent new labels.
- Use the provided excluded question list as hard constraints.
- Return ONLY valid JSON with this structure:
  {
    "questions": [
      {
        "question": "...",
        "choices": ["...", "...", "...", "..."],
        "answer": "A",
        "questionFormat": "MCQ",
        "questionGroup": null,
        "correctIndices": null,
        "acceptableAnswers": null,
        "acceptableAnswerGroups": null,
        "explanation": "...",
        "concept": "...",
        "keyConcept": "..."
      }
    ]
  }
- Do not include markdown, comments, or text before or after JSON.

Math notation
- Wrap EVERY mathematical expression in single dollar signs as LaTeX: $...$. This applies everywhere you write learner-facing text — questions, choices, explanations, summaries, key concepts, and working solutions alike.
- Correct: $d = \sqrt{(x_2 - x_1)^2 + (y_2 - y_1)^2}$ and $x^2 + y^2 = 1$ and $\frac{a}{b}$ and $y = mx + c$
- Wrong, because the app renders it as literal text with the backslashes visible: d = \sqrt{(x_2 - x_1)^2} or x^2 + y^2 = 1
- Never use Unicode sub/superscript characters such as the subscript two or the superscript three. Write $x_1$, $x_2$, $x^3$ instead.
- Keep prose outside the delimiters: "The slope $m$ is the rise over the run."
- The dollar sign is reserved as a math delimiter. Do not use it as a currency symbol.
