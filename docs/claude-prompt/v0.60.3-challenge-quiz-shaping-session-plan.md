# Session Plan — v0.60.3 Challenge Quiz Shaping & Onboarding Coverage Capture: Second Opinion

> **Purpose.** All four items in the `v0.60.3` candidate release are already fully scoped — root
> causes verified by direct file read, fix designs written, files/tests named — in
> `docs/product/ROADMAP.md`'s `## v0.60.3 — Challenge Quiz Shaping & Onboarding Coverage Capture`
> section. Before kickoff, get an independent second opinion from GPT on the plan as written. This
> is a review session, not a design session — do not ask GPT to re-derive the plan, ask it to find
> what's wrong with the one that already exists.

## Where this stands right now

Nothing has changed in code since the plan was written (2026-07-27). All four items are
Claude-Code-direct classified (no Codex prompt), independent of each other, not yet kicked off.
Two adjacent proposals from the same conversation — a daily Challenge Quiz quota model and a price
decrease — were deliberately **not** included in this candidate; they're logged separately in the
Backlog Index as held pending LLM cost instrumentation that doesn't exist today. Don't let GPT
re-open that question — it's out of scope for this review, already decided, see the hard
constraints below.

## What already exists — given to GPT as ground truth, don't rediscover it

- Full technical detail for all four items (mechanisms, fix designs, file:line anchors,
  classifications, test plans) is in the prompt file itself — GPT should treat that as verified,
  not re-derive it from first principles.
- `resolveGenerationProfile`'s dead `questionCount` field (Item 1), the `quotaExempt`-based session
  race fix (Item 2), and the `handleContinueFromStepTwo` practice-first gate (Item 4) were all
  confirmed by direct code read this session, not inferred.
- Item 1 reopens a proposal explicitly rejected on 2026-07-24 for a different reason (cost, not
  pacing) — the reopening argument and why the original objection doesn't apply are both in the
  prompt.

## Hard constraints (GPT starts cold, repeat these)

1. Do not propose the daily quota model (3/10/20) or a price decrease — both already discussed and
   explicitly held pending cost instrumentation, not part of this candidate, out of scope for this
   review.
2. Do not propose extending Board Exam Mode's practice-first *adoption* behavior (skip
   note-authoring, land directly on an adopted Review Set) to Students — Item 4 is scoped to
   coverage-gap *capture* only, and the plan explicitly calls out that the bigger adoption-behavior
   question is a separate, undecided one. Flag if this distinction seems wrong, but don't just
   quietly expand the scope back to it.
3. Do not propose a new backend endpoint, table, or migration for Item 4 — it must reuse the
   existing `POST /feedback` pipeline, matching this project's already-established preference for
   free-text over structured capture (a prior structured-feedback proposal was explicitly
   superseded in favor of this simpler approach).
4. NoteLib's own standing product philosophy (non-negotiable, don't relitigate): notes are the
   primary entity, quizzes exist to reinforce note-based studying not replace it, onboarding's job
   is reaching a first real Study Pack without collecting every preference up front, and "curation,
   never generation" governs Review Set/Study Plan assembly (unrelated to any of these four items,
   just don't let a recommendation drift into it).
5. Keep the review to the four items as scoped. If something looks systemically wrong (e.g., a
   pattern that should apply to all four, or a missing fifth item), say so explicitly rather than
   silently expanding any one item's scope.

## Prompt

Full paste-ready prompt: `v0.60.3-challenge-quiz-shaping-prompts/01-second-opinion.txt`

## Output

`docs/claude-prompt/v0.60.3-challenge-quiz-shaping-out/01-second-opinion.md` (once run — paste
GPT's response there, or summarize the verdict and link the raw transcript)

## Status

Run 2026-07-27, reconciled same day with an Opus follow-up. See
`v0.60.3-challenge-quiz-shaping-out/01-second-opinion.md` for the verdict and final resolution on
Items 2 and 4.
