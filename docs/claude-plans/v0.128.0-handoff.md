# `v0.128.0 — Onboarding Unfrozen` — session handoff

**Written 2026-09-07** at the end of a session that ran `v0.122.0` → `v0.127.0` and kicked off
`v0.128.0`. Kickoff commit `5c87f0e2` on `releases/v0.128.0`.

> **⚠️ CORRECTED 2026-09-07 BY THE IMPLEMENTING SESSION. THIS FILE SAID "Both scope items are
> UNSTARTED." THAT WAS WRONG: ITEM 1 SHIPPED IN `v0.96.0` ON 2026-08-29.** Section 2 below is
> superseded in full — kept, struck, and annotated rather than deleted, because the *reason* it was
> wrong is the finding. See the banner on section 2. Item 2 was correctly described and is the work.

Read `RELEASES.md`'s `## v0.128.0` section first — it is the contract. This file carries only what
that section does not, and what would otherwise be re-derived at cost.

---

## 1. Why the freeze was lifted — do not re-litigate it

`[CHECKPOINT — due 2026-09-11]` froze `frontend/app/onboarding` to protect a signup-funnel read
against a **62.4% completion baseline that cannot be re-run**. The owner lifted it 2026-09-07.

**⚠️ THE READ WAS TAKEN FIRST, FOUR DAYS EARLY, RATHER THAN ABANDONED — that is what makes the lift
sound rather than a trade-off.** Read-only production query, 2026-09-07:

| | |
|---|---:|
| signups all-time | **393** |
| completed | 249 — **63.4%** (baseline 62.4%) |
| signups since `v0.73.0` (2026-08-12) | **18** |
| completed since | 15 — 83.3% |

**⚠️ THE DECIDING FACT IS THE DENOMINATOR, NOT THE RATE. n=18.** At ~0.7 signups/day the
2026-09-11 date would have added about three more people. **83.3% is noise, so the checkpoint was
never going to be answerable on its own date — the freeze was protecting a read that cannot be
taken.** The checkpoint is discharged in the Backlog Index with that reasoning.

**⚠️ Do NOT re-freeze onboarding for this checkpoint. Do NOT quote 62.4% or 83.3% as a current
figure** — the first is a stale baseline, the second has n=18.

---

## 2. ~~Item 1 — Summary maths~~ — **SUPERSEDED: ALREADY SHIPPED IN `v0.96.0`**

> **⚠️ EVERYTHING BELOW THIS BANNER DESCRIBES CODE THAT HAD NOT EXISTED FOR NINE DAYS.** Verified
> 2026-09-07 against a clean tree at `74cc0df5`:
>
> - `git describe --contains 3652e3d6` → **`v0.96.0`**; commit dated **2026-08-29**.
> - `RELEASES_ARCHIVE.md:8280` records it shipped, "on all nine consumers including the SEO-indexed
>   public pages."
> - The file on disk wires `remark-math` as a **tokenizer** into `renderExtractedMath`, carrying the
>   "`rehype-katex` deliberately NOT used" design comment — i.e. **exactly the fix section 2
>   prescribes**, already applied.
> - Guards (a) and (b) already exist in `components/ui/summary-markdown.test.tsx`, **including the
>   underscore fixture at `:16`** — the one this file rightly says is the only fixture that proves
>   anything.
> - **Residual checked, not assumed:** `grep -rln 'from "react-markdown"'` over `app/`, `components/`,
>   `lib/` returns **one file** — `summary-markdown.tsx` itself. No surface renders Summary through a
>   second instance, so there is no leg (b) remainder. The "ten consumers" list below is doc drift
>   against the archive's nine, not a missed surface.
>
> **⚠️ CAUSE, and the reason this section is annotated rather than deleted: the kickoff copied
> `v0.96.0`'s PLANNED-SCOPE PROSE FORWARD instead of re-reading the file.**
> `RELEASES_ARCHIVE.md:8188–8226` carries the same sentences. Every warning below is *correct as
> engineering* — `_` really is markdown emphasis, `rehype-katex` really would be a second config —
> which is precisely why it survived a kickoff unchallenged and opened a release with an item that
> could not be done. **A kickoff that quotes a defect must anchor the quote to the current file.**

`frontend/components/ui/summary-markdown.tsx` runs `react-markdown` + `remark-gfm` with **no math
plugin**.

**⚠️ `_` IS MARKDOWN EMPHASIS.** `$x_1 + x_2$` becomes `<em>` *before* any post-processing could see
it, so a display-time regex cannot fix this. It needs **`remark-math` for TOKENIZATION ONLY**,
rendering through the **existing** KaTeX setup — `katex` is already a dependency and `renderMathText`
already handles `$…$`, `$$…$$`, `\[…\]`, plus `normalizeBareMath` for legacy undelimited content.
**⚠️ Keep ONE math configuration; do not introduce a second.**

**⚠️ `v0.95.1` shipped leg (a)** — the plain-text note surfaces, where `renderMathText` applies
directly. **This is leg (b).** **⚠️ Neither is `v0.86.0-note-item-limit-mismatch.md`'s LaTeX item**,
which is *corrupted escapes in stored content* — a different defect. **Do not close that one with
this.**

**⚠️ TEN NON-TEST CONSUMERS SHARE THIS RENDERER**, several public/SEO-indexed — which is why guard
(b) exists:

```
app/collections/[id]/collection-detail-page-client.tsx   app/demo/page.tsx
app/onboarding/page.tsx                                  app/p/[token]/page.tsx
app/public/library/[subject]/[slug]/page.tsx             app/study/study-pack-results.tsx
components/notes/note-detail-summary-card.tsx            components/notes/private-note-detail-page-client.tsx
components/study-pack/companion-result-bridge-card.tsx   components/study-pack/quiz-working-solution.tsx
```

`components/ui/summary-markdown.test.tsx` already exists — put guards there.

---

## 3. Item 2 — catalog-first onboarding. SMALLER THAN IT READS: the helper already exists

**⚠️ DO NOT BUILD A NEW MECHANISM.** `v0.79.0` already shipped
`buildCatalogFirstCourseProgramSuggestions` (`frontend/lib/learning-profile.ts:133`) and wired it
into **four** surfaces:

```
app/profile/page.tsx
components/dashboard/lightweight-profile-completion-prompt.tsx
components/notes/note-editor-page-client.tsx
components/notes/private-note-detail-page-client.tsx
```

**Onboarding is the ONE surface deliberately excluded — precisely to protect the read now
discharged.** It still passes the raw constant: `app/onboarding/page.tsx:1524` →
`suggestions={COURSE_PROGRAM_SUGGESTIONS}`, imported at `:52`.

**So the work is: fetch the catalog on that screen and route through the existing helper, mirroring
one of the four call sites.** Read one of them before writing anything.

> **⚠️ ADDED 2026-09-07 BY OWNER DECISION: instrumentation ships with this.**
> `trackCourseProgramValueSelected` fires from the **existing** commit point at `page.tsx:1119`
> (`updateLearningProfileContext`) — no new flow step. It adds **one** member (`"onboarding"`) to the
> frontend `CourseProgramSelectionSurface` union; the `COURSE_PROGRAM_VALUE_SELECTED` enum already
> exists and `surface` is **not** server-validated, so **no backend change and no migration**. Without
> it, `v0.79.0`'s off-catalog metric has a hole where pick volume is highest.

**⚠️ FREE TEXT STAYS ALLOWED.** `v0.79.0` shipped the **counter-proposal**, not the `ADR-001`
amendment: locking the field and the *"Request Program"* queue remain **PROPOSED AND UNRATIFIED**,
and shipping this does **not** ratify them. **Do not lock the field. Do not add a request queue.**

---

## 4. Verification — declared at kickoff

> **⚠️ RE-DECIDED 2026-09-07: half this rationale evaporated with item 1.** Guards (a) and (b) are
> already-passing `v0.96.0` tests, not work this release owes; **(c) is the only guard outstanding.**
> Tier is **KEPT but narrowed to item 2 on `model: "sonnet"`** — one file plus a test does not justify
> Opus, but the signup path plus the `v0.116.0`/`v0.117.0` silent-no-op risk still justifies a cold
> read. **⚠️ `app/onboarding/page.test.tsx` held a source-level pin asserting the OLD wiring** (kept
> "until the checkpoint"); it is removed because its premise is discharged, and replaced by
> behavioural guards in `onboarding-course-program.test.tsx`.
> Canonical wording is in `RELEASES.md`.

**ONE SCOPED COLD AGENT framed as falsification** (item 1 changes a renderer with ten consumers
including SEO pages; item 2 touches the signup path).

**Pre-declared guards, each naming the fixture that proves nothing:**

- **(a)** a Summary containing `$x_1 + x_2$` renders maths, **not** `<em>`. **⚠️ A fixture without an
  underscore passes under the defect and proves nothing** — the underscore IS the defect.
- **(b)** a Summary containing **no** maths renders **byte-identically** to today. Ten surfaces share
  this renderer.
- **(c)** onboarding completes end to end with a catalog-sourced program **AND** with free text.

**⚠️ MUTATION-VERIFY EVERY GUARD, AND CONFIRM THE MUTATION IS *PRESENT* BEFORE TRUSTING A GREEN
SUITE.** In `v0.127.0` a mutation silently failed to apply and the run passed clean; checking the
edit had landed is what caught it. This has now bitten twice in this repo.

---

## 5. Anti-drift that is easy to trip here

- **⚠️ NO Learning Connections work.** `[CHECKPOINT — due 2026-09-19]` is twelve days out, its kill
  criterion keys on `ACCEPTED`, and it decides whether that arc continues **at all**. Its denominator
  is also tiny (a production read found ~1 accepted relationship), so touching the surface would
  contaminate the one number that decides.
- **⚠️ The freeze is lifted, not the judgment behind it** — do not add, remove or reorder an
  onboarding FLOW step beyond what these two items require.
- No migration, no quota/entitlement/meter change, no new mode or sub-mode, no `ProfileType` gate.
- Do not change what `BOARD_EXAM_STARTED`, `ADAPTIVE_PRACTICE_STARTED`, `QUIZ_SHARE_LINK_*` or
  `GUIDANCE_TIP_SHOWN` record.

---

## 6. Process state worth knowing

- **The archive step now fires at signoff and works** — it ran for the first time at the `v0.127.0`
  signoff (7 live `RELEASES.md` sections against a rule of current + last 5, so `v0.121.0` moved).
  **Expect it to fire again at this signoff**, and verify byte-for-byte as that one did.
- **Kickoff REPLACES `CLAUDE.md`'s `Current version:` block; it does not prepend.** That line reached
  322,329 chars because every kickoff prepended. `docs/archive/CLAUDE_ARCHIVE.md` holds the old chain.
- **`docs/claude-plans/in-app-notifications-and-review-set-adoption-signals-stage1.md` is UNINDEXED**
  — a step-8 finding owed at the next kickoff regardless of whether it is built.
- Other sessions are working in `docs/curriculum/` and `docs/claude-plans/`. **Stage by explicit path,
  never `git add -A`** — that swept a peer's files once this session.
- Claude Max ends **2026-09-10**; what it buys is cold-agent verification, not the ability to edit
  files. `ROADMAP.md`'s ~140k-token Backlog Index is the largest remaining token cost and is **cheap
  to verify**, so it does not need Max.
