# Handoff — opening `v0.97.0`

Paste as the first message of a new Claude Code session, from the repo root.

---

I want to open `v0.97.0`. Read `CLAUDE.md`, `AGENTS.md`, `RELEASES.md` (the `v0.96.0` section) and
`docs/product/ROADMAP.md`'s Backlog Index first.

**⚠️ Trust those files over this message.** It is deliberately short because, unlike the previous
handoff, almost everything is already written down — every candidate below has a Backlog Index row
carrying its own reasoning, constraints and prior decisions. This document carries only what those
files do not, plus three things that are owed rather than merely recorded.

**⚠️ The previous handoff in this series contained two errors** — it cited an item as a `v0.94.0`
Known limitation when it was not in that list, and it named the wrong release for placement. Both
were caught only because the receiving session checked the files rather than trusting the prose.
Do the same here.

## Where things stand

`v0.96.0 — Authoring Integrity` is Released, merged and tagged (`56cdff50`). Five items shipped, one
folded mid-release. `main` is clean.

## The candidates, all already documented

Read these rows rather than re-deriving them. Cited by title, per the Backlog Index's own rule:

- **Unconfirmed connection requests never expire — the TTL applies to the carrier, not the request**
- **A provisional birth year is absent from the account data export** — owner already ruled to include it
- **Supporter onboarding — the Phase 4 remainder**
- **⚠️ HIGH PRIORITY — Canonical curated note title policy** — its second half only; see §1 below
- **⚠️ HIGH PRIORITY — Domain Context taxonomy direction** — recorded in `v0.96.0`; the row records what remains

The first three are **connection privacy and lifecycle**, deliberately held out of `v0.96.0` as a
different subject rather than deferred on merit. They make a coherent release together.

**⚠️ Supporter onboarding is NOT gate-blocked — it is UNDEFINED.** The phase plan never defines it;
the freeze was never the real obstacle. It needs a definition step before it can be scoped. Do not
treat the lifting of the onboarding freeze as having unblocked it.

## Three things that are OWED, not merely recorded

**1. The §5 production read is the owner's to run, and it is the only thing blocking the title
policy's second half.** Read-only SQL in `docs/claude-plans/canonical-curated-note-title-policy-audit.md`
§5. It decides whether `note-generation-developer.txt` also needs the title rule, or whether
`developer.txt` alone (already shipped) was sufficient. **Do not pre-empt it** — and note that an
assistant cannot run it, so it must be asked for explicitly rather than assumed done.

**2. Two dates are ACTIONS, not expiries.**
- **`[CHECKPOINT — due 2026-09-11]`** — the onboarding funnel read against a 62.4% completion
  baseline. The freeze existed to protect this read; someone has to actually take it.
- **`[CHECKPOINT — due 2026-09-17]`** — Domain Context classification. Read (a) cleared early
  (12.7% → 32.6%, recorded as an interim read) but **read (b), whether the 463 missing-guidance
  count shrank, has not been run.** Do not close the row on half a read.

**3. Three planning files exist ONLY in the working tree and two carry live findings.**
`git status` shows them untracked; an indexed Backlog row does not survive `git clean`.
- `hydraulics-fluid-mechanics-consolidation.sql` — carries a **data-loss finding**: the replacement
  notes have no course/program, so deleting the originals before backfilling would make them
  unreachable in the public library. Must not be run in the order written.
- `canonical-curated-note-title-policy-audit.md` — holds the §5 query above.
- `construction-subjects-domain-context-regeneration.sql` — arrived late in the `v0.96.0` cycle and
  has no row yet; kickoff step 8 should give it one.

## Two habits that repeatedly paid, and one that failed

- **`advisor()` before writing any Codex prompt, and again on the diff.** Across `v0.93.0`–`v0.96.0`
  this was the highest-yield check available. In `v0.96.0` it caught a checkpoint written with **no
  date** — which matches no scan pattern and would never have surfaced — and a doc sweep that had
  been done in one file and abandoned in its sibling.
- **Mutation-test every guarantee before claiming it.** In `v0.96.0` alone this caught three tests
  that passed for the wrong reason: two flush call sites pinned as an algorithm but not as *wiring*,
  a display-math assertion whose `A, B` selector survived the mutation it was meant to catch, and a
  repair wired into the wrong code path — the one nobody had reported, not the one that was.
- **What failed: assuming a rule is written correctly because it has been written correctly before.**
  The dateless checkpoint was a gate the same session had applied properly several times. Re-read the
  gate in `.claude/commands/signoff.md` rather than copying the shape of a neighbouring row.

## One process note

`v0.96.0` used **the release theme as the scope filter**, on the owner's instruction to include
everything un-gated. It worked: four items became five without losing coherence, and the three
connection items stayed out on subject rather than on merit. If the owner again asks to fold widely,
proposing a theme and letting it do the filtering is a good pattern — and `CLAUDE.md` still requires
saying what any fold does to the verification tier.
