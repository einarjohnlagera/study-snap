# GPT_CONTEXT.md - NoteLib Product Context Handoff

> **This is the core brief. Paste it as your first message in a new GPT chat session.**
> Then paste any module below that matches the conversation — see "Which modules to paste".
> Update this file whenever a new version ships or the roadmap shifts significantly.
> Last updated: v0.142.0 - 2026-09-11 (Released). **`v0.142.0` polished two already-shipped
surfaces the owner started actively announcing to users: the in-app notification inbox and the
adopted Official Review Set update panel — neither had been touched since it first went live.**
**⚠️ THE HEADLINE WAS A LIVE BUG ON 100% OF PRODUCTION'S NOTIFICATION POPULATION:** all 42
notifications at kickoff shared one type (`REVIEW_SET_UPDATE`), all carried a CTA, and activating
that CTA marked the notification read but never closed the panel — every notification was one tap
from this defect. Fixed by adding the missing `setIsOpen(false)`. **Notification cards were then
redesigned around a single tap target**: the card body is now either a link (safe CTA — marks
read, closes the panel, navigates) or a button (no CTA, or an unsafe one — marks read only), with
no separate CTA link or "Mark read" button; unread rows get a real background tint and dot, not
just a bolder title; every row shows a relative timestamp. **The Review Set update panel replaced
two uncapped raw-diff lists with meaning-based partitioning** — new topics, unavailable topics,
other curriculum changes — with repeated reorder/retire/move rows collapsing into a count behind a
**Review update** detail view, and the action renamed **Apply additions** → **Add N new topics**,
where N counts `ADDED_NOTE` rows only so a bundled `ADDED_SUBJECT_PLAN` summary row can no longer
inflate the promised count. **⚠️ A PRE-SIGNOFF FALSIFICATION PASS (one cold agent, 13 claims
tested against the actual code) found ONE real defect beyond a stale doc line: `applySourceUpdate`
discounted a concurrently-resolved addition from its own remaining-count but never marked that
same change `applied`, so the new panel's topic count could overstate what was actually left —
fixed the same day, mutation-verified, full backend suite green (2,361 tests).** **⚠️ THIS MADE
THE RELEASE TOUCH THE BACKEND, WHICH KICKOFF HAD DELIBERATELY AVOIDED** (Section grouping was
deferred to a frontend-only Option A for exactly this reason) — a narrow, deterministic
concurrency fix, not a design tradeoff, so it shipped rather than being deferred as a Known
Limitation. **No migration, no quota or entitlement change, no new notification type or
infrastructure.** **Previously — v0.141.0 - 2026-09-11 (Released).** **⚠️ `v0.141.0` ATTEMPTED a learner-facing rendering fix and REVERTED it at signoff, after a cold falsification pass measured it as a NET REGRESSION — 385 production strings worse against 3 better.** The defect is real and STILL OPEN: a quiz string containing BOTH a currency amount and a formula prints the formula as RAW LATEX. The `$` opener fired on `$50,000`, the closer correctly skipped every intervening amount, and the span closed on the FORMULA's final `$` — handing KaTeX a whole sentence, which failed, and the fallback re-emitted the source. **⚠️ THE ROW HAD CARRIED *"owner/curator work, hand-regenerate ~23 packs"* SINCE `v0.78.0`, AND THAT WAS NEVER GOING TO FIX ANYTHING — the defect reaches readers through the DISPLAY path, so regenerating a pack repaired nothing while the renderer kept mis-pairing.** **⚠️ THE ATTEMPT DECIDED MONEY-VS-MATHS FROM THE SINGLE CHARACTER AFTER THE DIGITS, WHICH CANNOT DISCRIMINATE** — `$1.5 \times 10^4$` puts a space exactly where the predicate looked for a currency boundary, so strings that rendered CORRECTLY began printing raw. Any future attempt must decide on the WHOLE SPAN and be measured against the 1,158-string production corpus first. **⚠️ A fix aimed at `normalizeBareMath` is a NO-OP and one session proposed exactly that: it returns early on ANY delimiter, and 339/339 backslash-bearing questions plus 498/498 explanations already carry one, so the repair is wired correctly everywhere and reaches almost nothing.** Also shipped: `SummaryMarkdown` normalises before `remark-math` tokenises (it handles DELIMITED maths only), and the shared Study Pack page renders maths at all where it previously emitted raw `{value}`. **⚠️ STILL OPEN: `study-pack-results.tsx:114` renders keyConcepts raw — a fourth surface, one line, excluded so the release did not outgrow the verification tier set at kickoff.** **`v0.140.0` put the Study Plan builder's commit control in reach: Save/Discard MOVED out of a card header that scrolls off a 79-note plan into a dirty-state sticky bar reading `Drag changes not saved`, and the two-choice `confirm()` on leaving became three — Save and leave / Discard and leave / Keep editing, with a FAILED save deliberately not navigating.** **⚠️ A COLD PRESSURE TEST FOUND A BLOCKING DEFECT 2,375 PASSING TESTS DID NOT: the bar (`sticky bottom-4 z-30`) covered the mobile tab bar (`fixed bottom-0 z-20`) by default; fixed with `useBottomViewportClaim`. It was determinable from class names alone, inside a gap the release had already declared unverifiable — *"our tests cannot check this"* is not *"unknown"*.** **⚠️ THE STRUCTURAL FINDING, WHICH OUTLASTS THE FEATURE: five Backlog rows were found stale IN THE SAME DIRECTION — overstating open work — and each shipped in a release that never re-read its row. `v0.138.0` added verification at KICKOFF; nothing updated a row when the thing it describes SHIPPED. `/signoff` now closes that.** The onboarding redesign came within one verification step of being rebuilt as an 8-screen rewrite that already existed. **⚠️ ALSO RECORDED, NOT FIXED: a real quiz-rendering defect where a currency `$` mis-pairs with a formula `$` and dumps raw LaTeX on screen — 22 questions and 49 explanations. A fix aimed at `normalizeBareMath` would be a NO-OP; the repair is wired correctly and reaches nothing, because 339/339 backslash-bearing questions already carry a delimiter.** **`v0.139.0` reopened the `v0.72.0` retention framing after its checkpoint FIRED: nine learners were shown the review-commitment prompt over a month and ZERO committed.** **⚠️ BUT THE PROMPT WAS NOT FAILING TO CONVERT — IT WAS FAILING TO BE SEEN, and that reframes the whole item:** it rendered only on `isFirstCompletedSessionEver`, i.e. ONCE per learner ever; **128 of 136 eligible learners had passed that moment before the feature shipped**, 8 were reachable, and 9 impressions were recorded. **`n = 9` was the design's CEILING, not a verdict.** **⚠️⚠️ AND THE ASK HAD NO BENEFIT TO OFFER — found by `advisor()` BEFORE the Codex prompt was written:** `RetentionService.isEligibleReviewDay` returns `true` for empty `review_days`, so choosing days **restricted** eligibility rather than granting reminders. Confirmed empirically: **619 digests sent to 115 recipients, ALL with `review_days = NULL`.** A one-tap *"remind me"* button would have shipped as a no-op. **SHIPPED:** the ask is re-showable (cap 3, 14-day cooldown, server-owned eligibility, `V144`); committing is now an **upgrade** (digest cooldown 7d → 1d **on chosen days only** — uncommitted learners are UNCHANGED and nobody loses the digest); learners with reminders OFF are no longer asked at all (the ask was inert for **252 of 396** accounts); abandonment is recorded, so *ignored* is finally distinguishable from *declined*. **⚠️ THE RELEASE DOES NOT RECOVER THE 128 STRANDED LEARNERS and says so — in-app reach is ~6/fortnight; email is the only surface that reaches them and R1 blocks it.** **⚠️ R1's cause is now ATTRIBUTED for the first time: the ~97 weekly `DUE_CONCEPTS_DIGEST` burst, caused by a flat 7-day cooldown synchronising every eligible learner onto one dispatch day (2026-08-31: 158 total against a 100/day cap).** **⚠️ A pre-signoff pressure test found a CROSS-PR defect neither PR's own review could see** — one PR supplied a state-machine guard, the other supplied a statement that could throw inside it, so a dismissal could be recorded for a prompt nobody saw. **PRIOR RELEASE — v0.138.0 - 2026-09-10 (Released).** **`v0.138.0` fixed a LIVE DETERMINISTIC production defect: four `BOARD_EXAM_REVIEW` notes were stuck `FAILED`, seven `LLM_INVALID_OUTPUT` failures between them, and a retry batch failed all three of its notes identically — so retrying was not a workaround.** **The cause was a bound the model was never told:** the generated title is validated at **12 WORDS** (`OpenAiLlmStudyPackService:87`) while the published contract stated no numeric bound at all — schema `maxLength: 160` CHARACTERS plus prose *"concise and specific"*. The same prompt already templated `{MAX_ITEM_CHARS}` and `{MAX_WORDS}`, and the codebase **documents this anti-pattern against itself** at `:2553-2561`, which removed an unpublished word ceiling from bullets and instructs *"do not reintroduce a bound the model cannot see."* The title had kept its. **Leg B1 published the bound** (`note-generation-developer.txt:31`); **the bound was NOT raised — 12 words stands.** **Leg A** now logs `generated_note_text_rejected field=… bound=blank|wordCount words=… min=… max=… chars=… value="…"` on both rejection branches, for `title`, `overview` and `keyIdea`. **⚠️⚠️ THE FIX IS UNVERIFIED AGAINST A REAL FAILURE AND THE RELEASE DOES NOT CLAIM OTHERWISE: the failing branch was INFERRED, never observed** — the old log recorded neither the rejected title, its word count, nor which bound failed. **The four notes stay `FAILED` until the OWNER re-runs them, and that re-run is the only end-to-end proof** — `[CHECKPOINT — due deploy + 2 days, backstop 2026-09-24]`, whose kill criterion is written before the read: `bound=blank` means B1 was the wrong branch. **⚠️ The source-title length correlation is NOT discriminating — FAILED titles average 8.5 words, inside the cap — and must not be cited as support.** **Also: the Backlog Index was verified against code for the first time.** Four rows asserted the absence of code that exists (one for 41 releases, and it had been offered as a `v0.137.0` release candidate); **two were catchable for free because the row's own cells contradicted each other**; and **19 rows carried four columns instead of five**, so the kickoff ritual's `Last reviewed` bump could never reach them. **Standing rule added: a claim about SHIPPED CODE decays exactly like a claim about production state — re-read it before repeating it.** **PRIOR RELEASE — v0.137.0 - 2026-09-09 (Released).** **`v0.137.0` is a PROCESS release with no product change: `v0.136.0` merged to `main` and auto-deployed on NEITHER Vercel nor Render.** Both are GitHub App integrations on the same push event and neither received it; GitHub declared no incident. **⚠️ THE RESULT WAS A LIVE SKEW — a `v0.136.0` backend serving a `v0.135.0` frontend, which called an endpoint form `v0.136.0` had just made a 400, so Your Impact was dead on arrival for six hours.** Fixed by redeploying; `scripts/check-deploys.sh` now detects it and `/signoff` runs it. **⚠️ IT TESTS FOR ABSENCE, NOT FAILURE — nothing failed, so notify-on-failure had nothing to fire on.** **⚠️⚠️ AND FOUR CLAIMS IN `ROADMAP.md` WERE FALSE: `V141`/`V142` HAD run (2026-09-08), and the profile-string write HAD been applied. A release was scoped on those lines before they were checked.** Migrations are current at `V143`. **Standing rules added: a production-state claim is a SNAPSHOT, not a fact — re-read before repeating; and removing/renaming/making-required an API form is breaking in BOTH directions, so both sides ship together.** **`v0.136.0` moved Your Impact to its own private `/impact` page and BOUNDED the query behind it.** The headline was a live defect, not the page move: Settings called the full creator-impact endpoint on every load and used the entire payload — 1,587 note records on the owner's account — to set ONE boolean. `GET /creator-impact/me` now REQUIRES `impacted=true|false` and pages; the parameterless full-payload form and its DTO are GONE, and a new `GET /creator-impact/me/summary` returns just `{distinctLearnersHelped, publicNoteCount}`. **⚠️ The brief's premise that this helps Public Profile is FALSE — impact was NEVER in the Public Profile payload, and a visitor never triggered an impact query.** Public Profile keeps only an owner-only `View impact →` card. **⚠️ `/impact` is NOT in the sidebar (anti-drift), so it has exactly ONE entry point, which is one click deeper than the section it replaced — that is an open `[CHECKPOINT — due deploy + 30 days]`, not a settled improvement.** **⚠️ `KNOWLEDGE_IMPACT_DASHBOARD_VIEWED` changed meaning TWICE (fire site moved, and it now requires a successful load), so its 7-view history is NOT comparable to what it now counts.** Views are labelled **page views** because `PUBLIC_NOTE_VIEWED` carries no `user_id` at all. **`v0.135.0` gave the notification substrate its FIRST REAL PRODUCER: publishing an Official Review Set update now notifies every learner who adopted it, deep-linked to their own copy.** **⚠️ EDITING STILL REACHES NOBODY — only the Publish update action fires, and a set going PUBLIC for the first time announces nothing.** A curator who adopted their own set is excluded. Notifications are keyed to the PUBLISHED REVISION, so a genuinely later update always gets through even after the previous one was read or dismissed, while a learner already holding an unopened signal does not accumulate more. **⚠️ The transitional `ACTION_REQUIRED` type is DELETED — the live types are `ANNOUNCEMENT` and `REVIEW_SET_UPDATE`.** **⚠️ NOTHING HAS BEEN DELIVERED IN PRODUCTION YET: `notifications` holds zero rows, so the badge has still never rendered — the next Review Set update publish will be the first notification NoteLib has ever sent.** **`v0.134.0` is a NOTIFICATION SUBSTRATE release that ships NO user-visible change, deliberately** — badge policy moved onto a derived `NotificationCategory`, the dedup key gained a string discriminator, responses now carry `actionable` so the client stops inferring policy from a type string, retention began expiring unread NON-actionable rows (unread ACTIONABLE rows are still kept forever), announcement fan-out moved off the admin request thread onto a bounded core-1/max-2 executor, and four delivery metrics were added. **⚠️ The numeric badge has still never rendered in production: `notifications` and `announcements` are both EMPTY, and nothing produces an actionable type yet.** **⚠️ NO new notification type, category or producer was added — that is Stage D, which is blocked on a dedup design correction.** **`v0.133.0` seeded the `Education` PROGRAM FAMILY, and it is a DATA change plus one admin surface — no authoring code changed.** The Applicable Programs combobox was ALREADY family-generic (it derives families dynamically from the catalog and holds no Engineering literal); it read *"Add all 18 Engineering programs"* only because Engineering was the only family with members. `V142` gives Education EIGHT members — `Education`, `Special Needs Education`, `Elementary Education`, `Secondary Education`, `Early Childhood Education`, `Technical-Vocational Teacher Education`, `Physical Education`, `Teacher Certification` — and the UI renders *"Add all 8 Education programs"* on its own. **⚠️ ALL EIGHT CARRY `exam_goal_slug = 'let'`, so within the Education family LET-tagged programs go from ONE to EIGHT** and all eight now appear under the LET exam goal on the public endpoint. **⚠️ `Special Needs Education – Generalist` was RENAMED to `Special Needs Education` keeping its `id`**, so `note_course_program` links survive; one stale `users.course_program` free-text string was handed to the owner as a `.sql` and **RUN by the owner on 2026-09-09 (verified read-only: 0 old / 1 new)**. **⚠️ CORRECTED — the earlier claim that *nothing breaks because that column is never resolved against the catalog by name* was FALSE and a cold agent refuted it: `bulk-generation-page-client.tsx:290` DOES match the catalog by name against this field, so the one affected account had lost Bulk Generate auto-selection until the write ran.** **⚠️ A Program Family can now be CREATED from the admin surface (`POST /course-program-catalog/families`, ADMIN-only) — a new family no longer costs a migration.** **⚠️ PROGRAM FAMILY IS DISCOVERY-ONLY AND NEVER REACHES A PROMPT.** `StudyPackGenerationContext` has no field for a family or an expanded program list and `courseProgram` is a single resolved String — structurally impossible today, keep it that way. Family membership does NOT select or override Domain Context. **⚠️ THE CATALOG IS NO LONGER "21 ROWS"** — `V106` seeded 21, production reached 44 through `POST /course-programs`, and `V142` adds 6. --- PRIOR (v0.132.0): - 2026-09-08 (Released). **`v0.132.0` gave Official Review Set curriculum a WORKING STATE.** A curator's additions to a published Review Set now carry no `published_at` stamp and are INVISIBLE to the anonymous public view, to a NEW adopter, and to drift detection until an explicit, atomic, row-locked, idempotent **Publish update** stamps the subtree. **⚠️ THIS IS A PER-ROW PUBLICATION STAMP, NOT A SNAPSHOT, VERSION OR HISTORY** — it works only because `applySourceUpdate` is additive-only, so the boundary gates what becomes visible as an ADDITION. **⚠️ `published_at` is STRICTLY SOURCE-SIDE even though the column lands on adopted rows too** (source and copies share both tables): a learner's own library, collection detail and `countAdoptionsByCollectionIds` must NEVER filter by it — nobody publishes their own copy, so filtering would empty every learner's library. **⚠️ BOTH anonymous read paths filter and must agree — `getPublic` AND `listPublic`** (Explore / Exam Hub / dashboard cards), including `childCount`, `itemCount` and `readyCount`; `listPublic` filters in JAVA on purpose because its count queries are shared with the authenticated `list()`. **⚠️ `childCount > 0` sets `isGoal`, which routes Adopt to `adoptGoal`, so an inflated count is not cosmetic.** **⚠️ ACCEPTED NARROWED CONTRACT: only ADDITIONS are hidden — removals, reorders and renames stay live**, because `position`/`label` are single columns and a delete retains no row. **⚠️ Initial publication is gated on `last_update_published_at`, NOT `published_at`** — V141 backfilled the latter for every pre-existing row with no visibility predicate. **⚠️ Publishing makes changes AVAILABLE and NEVER overwrites an adopter's copy**; one learner stays ONE adopter across every update. **⚠️ No public "Updated" date UI shipped** (capability preserved), and the P3 update NOTIFICATION is deferred, not built. **⚠️ Known gap: the Publish control and the Unpublished-changes indicator exist ONLY on the root detail page, not in the Builder where topics are actually added** — `[CHECKPOINT — due 2026-09-22, deploy-relative]`. **⚠️ THE OTHER HALF OF THE LOOP — THE LEARNER-SIDE UPDATE FLOW (`v0.116.0`), WHICH PUBLISHING IS WHAT FEEDS.** An adopter's own Review Set page shows **"Official Review Set updates available"** (or *"Official Review Set is up to date"*), and the learner chooses to apply it: `GET /collections/{id}/source-update` inspects, `POST` applies. **⚠️ APPLYING IS ADDITIVE-ONLY — only `ADDED_NOTE` and `ADDED_SUBJECT_PLAN` are applied, while `MOVED`, renames and removals are DETECTED AND REPORTED BUT NEVER APPLIED.** That is not an incidental limitation: it is the structural fact that lets the publication boundary be a per-row stamp rather than a snapshot. The learner keeps their own work (*"Your existing work was kept"*), re-adoption stays a hard no-op, one learner remains ONE adopter across every update, and a set whose source is gone reads **Detached from source** instead of erroring. **⚠️ SO PUBLISHING NEVER FORCES SYNCHRONIZATION — it makes an update AVAILABLE for the learner to accept, and an adopter's copy legitimately DRIFTS from the source over time.** Previously v0.131.0 - 2026-09-08 (Released). **`v0.131.0` polished the notification inbox shipped in `v0.130.0`.** The bell is now a **TOGGLE** — clicking it while open closes the panel **without refetching**, while opening still loads. The desktop panel closes on **outside click** and **Escape**, and has **no `Close` button**. **⚠️ The outside-click ref wraps the BELL AND THE PANEL together: if it wrapped only the panel the bell would count as "outside", and one click would close-then-reopen-and-refetch rather than close.** **⚠️ Mobile is unchanged — `AppModal` already handles its own backdrop and Escape; do not add a second handler.** The trigger keeps a **stable** `aria-label` with `aria-expanded` beside it, the convention already used by `theme-toggle.tsx` and `export-dropdown-menu.tsx`. **The bell is HIDDEN for the duration of any quiz** — `useExamFocusMode` now covers ordinary Challenge Quiz, Quick Review, Adaptive Practice and Interview Practice alongside Long Exam and Board Exam. **⚠️ EVERY FOCUSED SURFACE MUST KEEP AN IN-PAGE EXIT, AND THE HOOK'S GATE MUST MATCH THE EXIT'S GATE** — each renders a *Leave Quiz* / *Leave Practice* control on the same expression, asserted per surface. **⚠️ A `BackLink` elsewhere in the file does NOT satisfy that**, which is how a kickoff audit wrongly concluded Quick Review had no exit. **⚠️ Do NOT add focus mode to the shared quiz (`/quiz/[token]`) — `app-shell.tsx:593` returns early for that route and renders NO header, so it would be a silent no-op.** **⚠️ A sticky in-page bar on a focused surface belongs at `top-0`, not `top-16`.** **⚠️ The unread poll keeps running during focus mode**, deliberately, so the badge is accurate on exit. Previously: **`v0.130.0` shipped the notification inbox and its first producer.** A bell in the header carries a numeric badge for **actionable** notifications only — **announcements appear in the inbox but NEVER inflate the number, and at zero there is no badge element at all, not a `0`.** **⚠️ IDEMPOTENCY IS A UNIQUE INDEX ON `(recipient_user_id, dedup_key)`, NOT A SERVICE CHECK** — `deliver` attempts the insert and catches the violation; never add an `existsBy` pre-check. **⚠️ A notification is AWARENESS; the owning feature is TRUTH** — a row never stores request/grant state, dismissal never alters product state, and there is no authoritative `resolved` column. Admin "What's New" (`announcements`, `DRAFT → PUBLISHED → ENDED`) fans out into the same table; **content is IMMUTABLE after publish** (to correct copy: end it and publish a replacement) and title/body/CTA are **COPIED at fan-out and never re-read**. **⚠️ Audience targeting is EDITORIAL, NEVER authorization** — entitlement stays with `FeatureGateService`. **⚠️ CTA paths must be same-origin relative; no external or protocol-relative URLs.** It **polls, it does not stream** — no WebSocket/SSE exists. **⚠️ Stage 5 events (connection request, note share) are DEFERRED ON CONTAMINATION, NOT EFFORT: they would move `ACCEPTED`, the kill criterion of `[CHECKPOINT — due 2026-09-19]`, whose denominator is ONE.** **⚠️ Fan-out runs synchronously inside the admin publish request and holds one of twenty database connections throughout — a documented limitation with `[CHECKPOINT — due 2026-10-07]` keyed on audience size crossing 500.** Previously: **`v0.129.0` put an adoption count on Official Review Sets.** Explore cards show `40 adopted` and the preview shows `Adopted by N study libraries`. The number is the **current** count of collections whose `sourcePlanId` is that Official source, **excluding the source's own owner** — deleting an adopted copy decrements it. **⚠️ CORRECTED 2026-09-07: this used to say applying an update "mutates rows without creating any, so it cannot inflate" — THAT IS FALSE.** Applying an update never re-counts you against the set you already adopted, but when a curator adds a NEW subject plan to a Goal, `createSubjectAddition` saves a collection carrying that child's `sourcePlanId`, so the CHILD's count rises by one. That is correct by the count's own definition — you did become one of its adopters — but do not repeat the "creates none" claim. **⚠️ NO `DISTINCT` IS USED AND THAT IS DELIBERATE: `V76`'s partial UNIQUE index on `(owner_user_id, source_plan_id)` already makes one-learner-one-adoption a DATABASE INVARIANT**, so rows-per-source equals learners-per-source. `V138` adds the one index the count needs. **⚠️ PARENT AND CHILD COUNTS ARE INDEPENDENT AND MUST NEVER BE SUMMED** — a child Subject Plan's `sourcePlanId` points at the child source. **⚠️ Say "adopted", NEVER "N learners adopted this"** — the repo cannot prove every counted owner is semantically a learner, and `ProfileType` must not be used to filter adoptions to justify the word. **⚠️ Never rank Review Sets by the count, and never expose adopter identities or an adoption feed.** **⚠️ The backend returns the EXACT count; the small-count threshold of 5 is FRONTEND DISPLAY POLICY ONLY** (owner decision 2026-09-07) — below it nothing renders at all, not "fewer than 5" and not a range. **⚠️ It currently hides NOTHING: every top-level PUBLIC set is already ≥8, and only top-level sets reach Explore.** **⚠️ Legacy rows without provenance (8.8%) are EXCLUDED and under-count — never reconstruct provenance from titles or note overlap.** This is **Stage 2 only** of `docs/claude-plans/in-app-notifications-and-review-set-adoption-signals-stage1.md`; **no notification, inbox, bell, badge or announcement exists.** Previously: **`v0.128.0` made onboarding's Course / Program field catalog-first.** **`v0.128.0` made onboarding's Course / Program field catalog-first.** The signup screen now suggests programs from the live catalog first, with the hardcoded list kept beneath rather than replaced — it was the last of five surfaces still using only the hardcoded list, and **the one that matters most, because `users.course_program` is set overwhelmingly during onboarding and almost nowhere else.** **⚠️ FREE TEXT STAYS ALLOWED — the field is NOT locked to the catalog and there is no request queue; the `ADR-001` amendment proposing both remains PROPOSED AND UNRATIFIED and this does not ratify it.** A slow or failed catalog falls back to the hardcoded list instantly and never blocks signup. Selecting a program now emits `COURSE_PROGRAM_VALUE_SELECTED` with `surface: "onboarding"` — a **fifth** fire site, and the one `v0.79.0`'s 13.9% off-catalog profile baseline actually turns on. **⚠️ THE ONBOARDING FREEZE IS LIFTED AND `[CHECKPOINT — due 2026-09-11]` IS DISCHARGED (owner, 2026-09-07): the read was TAKEN first, four days early — 393 signups, 249 completed, 63.4% against a 62.4% baseline — and the deciding fact was the DENOMINATOR, not the rate. The post-`v0.73.0` cohort is n=18, so the checkpoint was never answerable on its own date. Do NOT re-freeze onboarding, and do NOT quote 62.4% or 83.3% as a current figure.** **⚠️ ITS SECOND PLANNED ITEM WAS WITHDRAWN, NOT SHIPPED: Study Pack summaries have rendered maths since `v0.96.0`** — the release was opened describing code that had not existed for nine days, because the kickoff copied forward planned-scope prose instead of re-reading the file. **⚠️ NO migration, NO backend change, NO quota or entitlement change, and NO Learning Connections work — `[CHECKPOINT — due 2026-09-19]` is untouched.** Previously: **`v0.127.0` fixed a learner-data-loss defect and made generation failures legible.** **(1)** A learner who set a `targetCompletionDate` on a top-level collection and later nested it under a Goal had it **silently NULLed and unrecoverably lost** — nothing else records it. It is now **promoted to the Goal**, earliest date winning, which is the rule the adoption path already applied; the two paths had disagreed and that was the defect. **⚠️ The clear on the child STAYS — it stops a nested collection resurfacing a stale top-level date on detach.** **(2)** `V136` persists `generation_failure_code`, `generation_failure_reason` and `generation_failed_at` on `notes`. **⚠️ The reason deliberately SURVIVES a retry that overwrites `status`, because the owner's manual retry is exactly what destroyed the evidence in production.** Raw exception text is never persisted for non-`AppException` failures. The catch is shared, so first generation and both bulk paths stamp the row too. **(3)** `V137` indexes `analytics_events (event_type, entity_id)` so the default public-library ranking reads index-only — sized on a real production read and proven useful by `EXPLAIN` before being written. **⚠️ NO quota, entitlement or meter change: the in-worker quota assert STAYS, because the charge lands between the two checks and removing it would bill past the limit. The defect was that its rejection was OPAQUE, not that it existed.** **⚠️ HONEST CAVEAT: the new failure columns have NO READER — their persistence is proven, their usefulness is not (`[CHECKPOINT — due 2026-10-07]`).** **`v0.126.0` is a DOCUMENTATION release with NO product change — do not describe any user-facing effect.** It resumed an archiving convention `docs/archive/README.md` documented in July, applied once, and that then lapsed for **85 releases**: `CLAUDE.md` carried one line of **322,329 characters** (63 chained release summaries, ~90% of the file, read every session) and `RELEASES.md` held **116 sections** against a design of *current + last few*. Both are now archived — **moves, not deletes, verified section by section** — with `CLAUDE.md` at ~11% of its former size. **⚠️ FOR ANY QUESTION ABOUT A RELEASE BEFORE `v0.121.0`, READ `docs/archive/RELEASES_ARCHIVE.md`; for pre-`v0.126.0` anti-drift commentary, `docs/archive/CLAUDE_ARCHIVE.md`.** **⚠️ HONEST CAVEAT: the first attempt DELETED rather than moved the `CLAUDE.md` chain and lost two corrections that existed nowhere else — caught by this release's own pressure test and restored before merge, but it is why the archive file exists.** **`v0.125.0` closed the last two unbounded reads.** **⚠️ A PERFORMANCE AND TOOLING RELEASE — a product answer must not describe new capability.** **(1)** The Add-notes picker used to download the user's ENTIRE note library and filter it in the browser; `GET /notes` gained an additive optional `search` parameter (title, subject, course program AND tags) so the picker loads a bounded 50 and search reaches everything behind it. **⚠️ SEARCH SHIPPED WITH THE BOUND, NEVER A BOUND ALONE — `v0.123.0` declined the bound precisely because client-side filtering makes notes past a limit UNADDABLE.** **(2)** The ADMIN Official-template backfill loaded every public note in full, bodies included, then ran two more queries per note — **~2,884 for one click**, now four, selecting the same notes in the same order. **⚠️ Public library search is UNCHANGED** — the new clause is separate, so nothing about Explore moved. **⚠️ HONEST CAVEAT FOR ANY PRODUCT ANSWER: the bound is applied server-side and the already-in-this-plan exclusion client-side after it, so a curator whose 50 most recently updated notes are all already in the plan sees an empty picker** — disclosed in-product by the truncation notice, which says to search. **⚠️ NO migration, no quota or meter change, no new capability.** **`v0.124.0` cut the request cost of the whole collection path.** **⚠️ IT IS A PERFORMANCE RELEASE — a product answer must not describe new capability; every page looks and behaves exactly as before.** **(1)** A Goal's builder loaded each child Subject plan in its own request — a 20-plan Review Set cost **22 requests**, fired concurrently against a connection pool of 20 — and now costs **3**, via one batch read (`GET /collections/{id}/goal/child-items`). **⚠️ `GoalCollectionDetailResponse` was deliberately NOT widened**: six of its seven consumers need the goal shape and not one child's items. **(2)** Opening ANY collection used to fetch the user's entire unbounded note library to derive just two values — an ADMIN-only visibility map and one Study Pack id — **both discarded for a learner**. The pack id now rides on the collection item at no extra query, and the visibility map loads only for an ADMIN, so **a learner now issues ZERO such calls on open**. **(3)** A TEACHER ran the entire builder load TWICE, because the plan's label resolves a render late and was a dependency of the load — the curator was paying double on exactly the pages this release makes cheaper. **⚠️ HONEST CAVEATS FOR ANY PRODUCT ANSWER: the release BRIEFLY put a new internal field on the anonymous public collection payload** (a shared mapper serves both the owner and `permitAll` paths) — caught by its own cold-agent verification and withheld before merge, never reaching production, and not exploitable in any case; **and it BRIEFLY let an ADMIN reach an enabled Publish button while the private-note check was still loading**, also caught and fixed pre-merge, now failing closed. **⚠️ NO migration, no quota or meter change, no new capability.** **⚠️ It also CORRECTED `v0.123.0`'s record: that release's lazy note list covered the BUILDER only, not the collection page, which had been claimed as both.** **`v0.123.0` fixed a Study Plan Builder page that would not stop reloading, and cut the request cost of editing a section.** **⚠️ IT IS A DEFECT AND PERFORMANCE RELEASE, NOT A FEATURE ONE — a product answer must not describe new capability.** **(1)** A section-label write the server REFUSED was retried without limit, and every retry re-fetched the curator's ENTIRE note library — a client-driven load amplifier against a backend with no headroom, propagated to every learner who adopted an affected published plan. A refused write is now attempted once; refocusing the field is the deliberate retry. **(2)** Section names are now canonical (trimmed AND internal whitespace collapsed) on every path, so two names differing only by spacing are ONE section and a doubled-space name is repaired on its next save rather than rewriting itself on every page load. **(3)** The ingress was **Bulk Generate**, whose Section field is pre-filled from the free-text Subject and sent with trim-only — now canonicalised at the authoring surface. **⚠️ The BACKEND deliberately still stores what it is given; collapsing there would rewrite stored learner data.** **(4)** Editing a section is now ONE request instead of up to four: the write consumes its own response, and the note library loads lazily on picker open. **⚠️ HONEST CAVEAT FOR ANY PRODUCT ANSWER: the release BRIEFLY INTRODUCED a second unbounded loop of the same class** — a failed lazy note-load re-armed itself through its own effect dependency, measured at 3,743 calls in five seconds — **caught by the release's own cold-agent verification and fixed before merge, so it never reached production.** **⚠️ NO migration, no backend change, no quota or meter change.** **`v0.122.0` PROMOTED the shared-quiz capability, which had shipped in `v0.110.0` and been announced NOWHERE for twelve releases.** **⚠️ IT IS A DISCOVERABILITY RELEASE, NOT A FEATURE ONE — a product answer must not describe it as new capability.** The entry point already worked; what was missing is that nothing told anyone. Counted at kickoff: eleven guidance tips mentioned sharing a quiz **zero** times, and `app/page.tsx` named Board Exam, Interview Practice and Adaptive Practice while naming shared quizzes **zero** times. **What shipped: (1)** a one-time note-detail tip firing on a quiz-ready note with no generated quiz, **with NO action button** — the entry point exists, and a second click target would confound the very read the release un-blocks; **(2)** one sibling line each on the landing and How-it-Works pages, **not** a sixth study mode (that section's heading counts its modes, and `EXAM_MODES.md` is a locked five-mode contract). **⚠️ NO behaviour change, no migration, no quota or meter change, and NOTHING claims the sharer can see the recipient's score — `getSharedQuizResults` grades in memory and never saves.** **⚠️ LEARNING CONNECTIONS WAS DELIBERATELY NOT PROMOTED, on a dated reason rather than taste:** `[CHECKPOINT — due 2026-09-19]`'s kill criterion keys on `ACCEPTED` relationships, so promoting that surface inside its window would have contaminated the one number deciding whether the arc continues. **That prohibition lifts after 2026-09-19.** **⚠️ THE RELEASE ALSO CORRECTED ITS OWN KICKOFF CLAIM: the Help Center was NOT entirely correct.** `learning-connections-guide.tsx` instructed teachers to use a note actions-menu item that is gated to non-teachers; teachers reach the same capability through a **Generate Quiz** button. Found by sweeping by surface at signoff, not by diff. **`v0.121.0` repaired the SHARED-QUIZ RECIPIENT path — the person who receives a quiz link, who may have no account at all.** **⚠️ THREE THINGS A PRODUCT ANSWER SHOULD CARRY. (1) Recipients were SCORED ON QUESTIONS THEY COULD NOT ANSWER:** `teacher-quiz-developer.txt` instructs the model to emit a MATCHING block, but `PublicQuizItem` drops `questionGroup` and the recipient page has no matching control, so a block arrived as independent MCQs with identical choices and was graded as such. Matching is now **excluded server-side**, filtered on the SHARED derivation so the projection and the grader walk the same list — filtering only the projection would have 400'd every submit. **In-app modes still receive MATCHING normally.** **(2) A wrong answer could not be corrected, and there were TWO locks, not one:** answers were append-only AND picking a single-choice answer disabled every other option on click. Answers are now index-addressed with a **Back** control; forward navigation past an unanswered question is still forbidden — answers became correctable, not skippable. **(3) The result screen now offers the SOURCE material, under an owner-ruled provenance capability rule: a source appears ONLY with durable source-Note identity AND legitimate recipient accessibility, and identity is NEVER inferred from a copied title.** `visibility == PUBLIC` only; **private sources are omitted entirely — not counted, not hinted, not placeheld** — and relationship state is never an input. Combined quizzes therefore yield nothing (a `CombinedQuizSection` carries a copied title and no note id) **through the ordinary path, with no `isCombined` branch**. **⚠️ THE RELEASE IS FIX-BEFORE-PROMOTE, NOT A LIVE-DEFECT FIX, AND A PRODUCT ANSWER MUST NOT OVERSTATE IT: a read-only production query on 2026-09-06 returned 8 generated quizzes from ONE distinct generator, 1 share link and 0 combined quizzes.** The defects are code-confirmed and reachable but were harming no population yet — the `v0.109.0` shape where a read on an unpromoted capability measures our own silence. `QUIZ_SHARE_LINK_COMPLETED` was added (firing only after grading succeeds) so an opened→completed rate exists at all, and two-tier checkpoints (`2026-10-07` proximal on whether the capability gets PROMOTED, `2026-12-04` distal on the rate) carry a binding ≥20-links/≥5-generators denominator floor. **⚠️ KNOWN LIMITATION: the quiz TITLE is not visibility-gated — `PublicSharedQuizResponse.noteTitle` has ALWAYS carried the source note's title regardless of visibility. That is PRE-EXISTING; what this release gates is the ability to READ the note.** *(previously v0.120.0)* - 2026-09-06 (Released). **`v0.120.0` makes the topic a curator types in Bulk Generate the note's FINAL title.** Bulk created each note with the typed topic and then **overwrote it with the Study Pack's generated title**, so `Site Grading Principles` persisted as `Site Grading Principles in Civil Engineering` — naming a discipline that was **not** among the four selected Applicable Programs and that **no context field contained**; the model inferred it. **⚠️ THE CAUSE WAS NEITHER OF THE TWO OBVIOUS ONES, so do not answer with either: it was NOT context contamination (the curator profile fallback is closed on both ends) and NOT a missing prompt rule — `developer.txt` already carried the correct semantic title doctrine from `v0.96.0` and was violated anyway.** That is the argument for the fix's shape: it REMOVES the model's authority over canonical titles rather than re-wording an instruction. **⚠️ WHY IT MATTERED BEYOND ONE WRONG TITLE: `v0.118.0`'s regeneration contract is "the Note's current title is the topic", so a contaminated title SEEDS every future regeneration of that note** — which is why it was sequenced before the ALE curator pass. **The generated title is not discarded: it is offered** on the note detail page as a dismissible, opt-in suggestion (curators only, dismissal persists per browser). **⚠️ TWO THINGS A PRODUCT ANSWER SHOULD CARRY: existing contaminated titles were NOT repaired** — no migration, no backfill, no rename, because many similar-looking titles are legitimate (*"Nursing Management of Acute Asthma"* is CORRECT) and only a hand-check can separate them, so sizing that debt needs an owner-run production read; **and the suggestion card shipped on an OWNER OVERRIDE against the audit's recommendation, with adoption UNMEASURED** — `NOTE_TITLE_SUGGESTION_RESOLVED` was added at signoff and `[CHECKPOINT — due 2026-10-06]` asks whether curators ever APPLY it, with the kill criterion being to remove the card, never to restore the overwrite. Also: the remaining user-facing *"AI critique"* labels became *"per-answer critique"* (the landing chip is now **Per-Answer Critique**), while **every DISCLOSURE keeps the word** — Privacy, Terms, the shared-quiz review notice and *"AI-generated — verify calculations"* are deliberately unchanged. *(previously v0.119.1)* - 2026-09-06 (Released). **A PATCH closing the THIRD OCCURRENCE of one shape — a latent unbounded fetch over the public catalog crossing a threshold as data grows** (2026-09-01 build failure, 2026-09-04 outage, 2026-09-05 outage). `GET /notes/public` is anonymous and was doing work proportional to the **whole catalog** (1,442 notes, up from ~950 in August) while holding a pooled connection, after which the health check starved on the same pool and the platform restarted a process whose only problem was that it was busy. **⚠️ THE HOLD WAS INSIDE THE TRANSACTION — this was NOT open-in-view, and `v0.112.0` Phase 3 does NOT address it; do not cite this outage as the case for Phase 3.** Fixed in four places: the two frontend callers that never needed a catalog, the ranked/legacy/discovery branches (the unbounded query is **deleted**, not bypassed), and the SEO note path, which was the heaviest of all. **⚠️ `/explore` RANKING IS UNCHANGED — the scoring moved into SQL faithfully, and a behaviour-preservation test written against the Java implementation passes unmodified.** **⚠️ ONE VISIBLE PRODUCT CHANGE: related notes on ~250 public SEO pages are now most-recent-first rather than recommendation-ranked** (owner decision; the ranked order was not cheaply available). **⚠️ Do NOT answer a future recurrence with a bigger pool — it was raised 10 → 20 and the same failure recurred at 20.** **⚠️ ONE OWNER ACTION IS STILL OUTSTANDING: Render's `healthCheckPath` must be repointed to `/api/actuator/health/liveness`, or the health-check fix is inert.** *(previously v0.119.0)* - 2026-09-06 (Released). **Adds CURATOR BULK REGENERATION — up to 50 notes rebuilt in one deliberate pass**, Study-Pack-only or Note + Study Pack, with a preflight that states both meters' cost and remaining allowance, the exact number of public notes affected and shared quizzes that will be turned off, and blocked notes with reasons. Each item stays Phase 1's atomic unit resolved from **its own** context — no batch-wide metadata is applied over authored titles. Continue-on-failure; a per-item receipt survives navigation; **retry re-runs only the FAILED items, as a new batch**. **⚠️ Available to ADMIN *and* TEACHER curators — an owner decision taken against the plan's recommendation, which is why a TEACHER is metered normally and the ADMIN bypass was NOT widened.** Also adds a **Review Set membership filter** to the Library. **⚠️ TWO THINGS A PRODUCT ANSWER SHOULD CARRY: adoption is UNMEASURED — instrumentation (`BULK_REGENERATION_STARTED`) was added at signoff because no durable signal existed at all, and `[CHECKPOINT — due 2026-10-06]` asks whether a TEACHER's allowance survives one real batch; and the quota race is NARROWED, NOT CLOSED — a burst of regenerations can still pass the pre-dispatch check and run out mid-flight, which now reports as *blocked* rather than an unexplained failure.** *(previously v0.118.0)* - 2026-09-05 (Released). **Adds Note + Study Pack regeneration as one operation** — content and Study Pack rewritten together, preserving `notes.id`, `notes.created_at`, `study_packs.id` and plan placement; a failed run leaves the original content, the original pack and both meters untouched, because both LLM calls complete before anything is written. **A scope selector (Study Pack / Note + Study Pack) gates it**, with an overwrite warning for learner-owned notes and a public-note notice. **`PUT /notes/{id}` now 409s while a note is `GENERATING`**, closing a pre-existing race. **Quiz mastery no longer survives a regeneration of the quiz it was earned on.** **⚠️ The AI metadata suggestion is now CREATION-ONLY — no regeneration offers it, in either scope; bulk generation still auto-applies generated TAGS while preserving the curator's Subject — but **`v0.120.0` REMOVED the generated-TITLE overwrite, so this line's original "title and tags" wording is no longer true.** *(previously v0.116.0)* - 2026-09-05 (Released). **⚠️ SLICES 1 AND 2 OF THE COMPREHENSIVE REVIEW SET OVERHAUL HAVE BOTH SHIPPED.** `v0.116.0` adds LEARNER-INITIATED ADDITIVE UPDATES to an adopted Review Set: new notes, placements and Subject Plans can be pulled in on demand, while **rename, reorder, retire and move are REPORTED and never applied** — until source-at-sync provenance has history, the system cannot tell learner customization from upstream change. **⚠️ Re-adoption is STILL a hard no-op; the update path is separate and explicit.** An adopted plan whose source is gone resolves as `Detached from source` and stays usable. Adoption now also stamps a Companion structure baseline, so a learner is told when their Companion no longer matches their plan (read-only — regeneration is ADMIN-only). **⚠️ Slices 0A/0B are owner-executed and unstarted; Slices 4-5 are DEFERRED and gated on `[CHECKPOINT — due 2026-10-05]`.** Previously: **⚠️ SLICE 1 OF THE COMPREHENSIVE REVIEW SET OVERHAUL HAS NOW SHIPPED — this line previously said "ARCHITECTURE AUDIT, nothing implemented" and that is no longer true.** What shipped: publishing a LEARNER-owned copied note clears its inherited `note_course_program` rows, which un-shadows the learner's own Course / Program field, so **classification authority is TRANSFERRED to the learner rather than merely revoked**. **Curators are excluded** — a curated note keeps its authored programs on publication. A null string stays null: the note is public but **UNSHELVED** until the learner answers, and is never backfilled from the joined catalog name. **⚠️ Slices 0A/0B (Domain Context classification) are OWNER-EXECUTED and unstarted; Slice 2 (additive Review Set updates) is NOT started.** **⚠️ Everything below about the audit's SIX FALSIFIED FACTS still stands unchanged.** **⚠️ A three-agent Stage 1 audit of the Comprehensive Review Set Overhaul FALSIFIED SIX WIDELY-REPEATED FACTS. Correct any answer that uses the old ones:** the Domain Context taxonomy is **ELEVEN values, not eight**; the course/program catalog holds **FORTY-FOUR programs, not 21 or 41**; the ALE unset residue is **88 rows, not 215**; and **ZERO notes are in the multi-program blocked state** — that exception has zero occupancy, so it is a PROSPECTIVE guard, not a live blocker. **⚠️ THE BIG MECHANISM, previously unwritten: classification is driven by the GENERATION GATE, not curator diligence.** Every note with 2+ programs is classified because the gate forces it; **699 of 809 single-program notes are NOT**, falling through to the program name as the authoring domain. **So the four zero-usage enum values measure which notes the gate FORCED, never which values are right** — do not cite zero usage as evidence a value is unwanted. **⚠️ The live consequence: ~239 curator notes (Nursing 106, Architecture 73, Accountancy 60) generate with NO computation guidance on the permanent-per-note path — fixable by CLASSIFICATION ALONE, no enum change, no release.** **⚠️ Note copy: options D and C are BOTH REFUTED** (C fires on zero notes; D is refuted by 608 null-string and 551 multi-program source notes). **⚠️ Adoption: an adopted Review Set is a PERMANENT SNAPSHOT in practice — re-adoption is a hard no-op — but the update mechanism ALREADY EXISTS and is idempotent**, so this is not a sync engine to design. Full reconciliation: `docs/claude-plans/v0.115.0-stage1-master-architecture-reconciliation.md`. Previously v0.114.0 - 2026-09-04 (Released). **`v0.114.0` is an EVIDENCE release and changes NO product behaviour — nothing a product question should be answered differently because of.** It adds one startup log line reporting the effective `hibernate.connection.handling_mode` and open-in-view state, and it finally ran the 2026-09-04 outage's falsification queries. **⚠️ THE ONE FACT WORTH CARRYING: the cause of that outage is still NOT established, and the leading hypothesis CHANGED.** The write side is empty on a widened window, so *connections held across slow LLM calls* — the hypothesis `v0.112.0` Phase 3 is built to fix — is now **positively unsupported**; meanwhile the *anonymous read burst* that a previous read had refuted is **back**, on evidence that read never consulted (one public note hit 16 times in 78 seconds against a 1–2/hour baseline, starting ~15 s before saturation). **⚠️ Not confirmed, and its direction is not settled.** **⚠️ So do NOT describe Phase 3 as the agreed fix.** Previously v0.113.1 - 2026-09-04 (Released). **`v0.113.1` is a PATCH closing the residuals `v0.113.0`'s three-agent pressure test found and left. Two of its five items DO change behaviour, and both RESTORE a path `v0.113.0` closed by accident rather than adding capability:** completing a plan-scoped session whose item source cannot be resolved now SKIPS and logs that item at ERROR instead of throwing — the throw rolled back a session already marked `COMPLETED`, and Adaptive Practice is not covered by the recovery sweeper, so one unresolvable item bricked the plan permanently while the active-session index refused every retry; and a LEGACY plan-scoped session whose collection was deleted now falls back to its pack anchor instead of 404ing on the session-addressed route. The anchor rule also collapsed to ONE authority (`QuickReviewSessionEntity.validateAnchor`), the nine H2 fixtures gained the anchor `CHECK`, and the real-row harness moved to PostgreSQL 18 to match production. **⚠️ No migration, no quota change, no new mode.** **`v0.113.0` made a quiz session anchor on what it is ACTUALLY about — a note/pack pair OR a Study Plan — ending a contention where three session types shared one anchor slot and plan-wide practice could silently hand a learner a single-note session.** A learner may now hold an active plan-scoped session AND an active note-scoped session on a pack inside that plan at the same time; that contention ending IS the release and is not a regression. **⚠️ THE THREE-AGENT PRESSURE TEST CHANGED THE RELEASE RATHER THAN RATIFYING IT — four blocking findings, two convergent.** Two were owner decisions scoped in rather than deferred: **deleting a Study Plan no longer destroys the completed sessions run against it** (the FK is `ON DELETE SET NULL`; the kickoff's cascade justification was falsified, since `user_id` has cascaded since `V4`), and **a plan-scoped completion now credits EVERY note it sampled**, so Study Plan progress advances where it previously never moved. **⚠️ ADOPTION IS UNPROVEN AND THE DENOMINATOR IS WHY: plan-scoped Adaptive Practice had been used ZERO times in production, and only 3 users currently carry the weak concepts it requires — so a short read would return zero regardless of merit.** Two-tier checkpoints (`2026-10-05` proximal on the eligible population, `2026-12-04` distal on starts) carry that as a binding denominator clause. Previously: v0.112.0. **`v0.112.0` configured the production connection pool for the first time and settled the question that decides the structural fix.** **⚠️ BEFORE THIS THERE WAS NO HikariCP CONFIGURATION ANYWHERE** — production ran on a framework-default pool of **10** and a 30 s acquisition timeout **nobody chose**; on 2026-09-04 the pool was exhausted, the health check starved on the same pool (it needs a pool connection to answer `/actuator/health`), and the instance was restarted. Now: `maximum-pool-size: 20` — **verified against the live database, `max_connections = 103` with 3 reserved, giving a deploy-overlap ceiling of 50 because Render runs two instances during a deploy** — plus `connection-timeout: 5000` (**a deliberate trade: waiters fail fast with a 500 instead of queueing 30 s, because that queueing is what got the instance restarted**) and `leak-detection-threshold: 60000`. **⚠️ PHASE 3, THE STRUCTURAL FIX, IS DEFERRED ON EVIDENCE AND THAT IS THE RELEASE'S OWN FINDING.** Phase 2 measured that `hibernate.connection.handling_mode` is `DELAYED_ACQUISITION_AND_HOLD`, set **unconditionally by Spring, not inherited from Hibernate**, so **the connection outlives the transaction** and relocating LLM calls alone would have shipped, looked correct, and not fixed the exhaustion. **⚠️ AND THE CAUSE IS NOT CONFIRMED:** the Render read found the pool fully checked out **while the database was IDLE** (CPU ~1–2% of a core) under **0–9 requests/min**, which **refutes** both a database-side stall and an anonymous read burst, and leaves an unconsidered **connection LEAK** fitting at least as well as the LLM-hold hypothesis Phase 3 addresses — **which Phase 3 does nothing for.** **⚠️ Do NOT build Phase 3 until leak detection names a holder** (`[CHECKPOINT — due 2026-10-04]`). Previously v0.111.0 - 2026-09-04 (Released). **`v0.111.0` took the Domain Context taxonomy from EIGHT values to ELEVEN** — `ARCHITECTURAL_DESIGN`, `ARCHITECTURAL_HISTORY_AND_THEORY` and `PLANNING_AND_SITE_DEVELOPMENT`, **all `quantitative = false`**. **⚠️ WHY, and it is not what the standing note said:** the ALE Review Set left **132 of 364 rows with no honest value**, and **8 notes could not be generated AT ALL** — a note with two Applicable Programs and a null Domain Context throws, because the resolver's program-name fallback is gated on `joinedPrograms.size() == 1`. **⚠️ The old *"`ARCHITECTURE` is a provable no-op"* verdict was TRUE FOR SINGLE-PROGRAM NOTES and irrelevant to multi-program ones — do not re-derive it.** **⚠️ `ARCHITECTURE` itself was rejected AGAIN**, on the naming rule: it equals a live catalog program name and would merge three different treatments. **⚠️ `ADR-001` was amended: the failure-condition RATIO is demoted to a watch figure** — its `8:21` denominator was stale, the live catalog holds **41** programs, and a denominator that grows by curator action is easier to satisfy by ADDING PROGRAMS. The guard is now the naming rule plus a composition matrix computed **against programs that exist**. Ratio now `11:41` = 0.268; 0.40 re-opens it. **⚠️ KNOWN LIMITATION: two of the three values serve exactly ONE live program today** and rest on clause (a)'s *firmly planned* reading. Previously v0.110.2 - 2026-09-04 (Released). **`v0.110.2` is a PATCH that stopped a regeneration from silently corrupting a quiz someone else was already taking.** **⚠️ THE DEFECT, found during `v0.110.0`'s item 4 audit and carried unfixed through TWO releases:** `GeneratedQuizService.generate` reuses the EXISTING quiz row and overwrites its questions, so **the id never changes**, while `quiz_share_links.generated_quiz_id` points at it and the service referenced share links **zero times**. Regenerating an already-shared quiz therefore mutated the quiz a recipient could be part-way through — **a changed question count 400'd them on submit, and an UNCHANGED count graded them against questions they never saw with no error at all.** The silent case is the worse one and is what every non-`TEACHER` hits, since they always receive 10 questions. **FIX:** regeneration deactivates **every live link** for that quiz — **not just the newest**, because only `token` is UNIQUE, `createShareLink` mints a new row over an inactive one, and `findActiveLink` accepts ANY active token, so one quiz can own several live links. **⚠️ It DEACTIVATES, never DELETES:** deleting would force a new link and spend share-link quota because of the fix; left inactive the owner turns it back on knowingly, at which point it serves the new questions as their informed choice. The owner is warned beforehand only when a live link exists, and the panel **re-reads** the link afterwards. **⚠️ A SECOND DEFECT WAS FOUND WHILE WIRING THAT UP:** the share panel keys its effect on `generatedQuiz.id`, which a regeneration does **not** change, so it never re-ran and the owner would have kept seeing *"Sharing on"* for a link the server had just turned off. **⚠️ VERIFIED AT SIGNOFF: the deactivation is transactional with the quiz write and both catch blocks rethrow, so a link is never turned off for a regeneration that did not land; and there is NO second writer of `GeneratedQuizEntity.questions`.** **⚠️ KNOWN LIMITATION:** turning the link back on serves the NEW questions under the SAME token — deliberate and visible to the owner, but a recipient holding the old URL sees different questions. **The permanent fix is snapshotting the single-note shared quiz at share time (the `combined_quizzes` model), which is a migration plus a table and was deliberately NOT taken in a patch.**

---

Here's the current context for our NoteLib product session. Treat this as a compact product snapshot, not a replacement for the repo docs. For implementation work, always defer to `AGENTS.md`, `RELEASES.md`, `docs/product/ROADMAP.md`, and the relevant `docs/features/*.md`.

---

## START HERE — orientation in 19 lines

Read this block first. Everything below is detail behind it.

1. **What it is:** NoteLib is a notes-first study workspace. Capture notes → generate Study Packs → practice with quizzes → track readiness → reuse the library.
2. **What we sell:** the *learning system*, not features. Hero: **"Always know what to learn next."** Features are evidence for that promise, never the promise itself. **Ratified 2026-08-05: NoteLib is a learning system built on top of a knowledge library — the library is the foundation, the learning journey is the product.** Three layers earned strictly in order: **Trust** (comprehensive Official Review Sets) → **Habit** (Study Packs, Companion, Progress, Review Sets) → **Community** (user-created knowledge). **Community content is no longer the primary acquisition strategy** — the community vision is not abandoned, it is being earned in the correct order. Do not propose UGC as top-of-funnel.
3. **Current state:** **⚠️ THIS LINE IS STALE BY NINE RELEASES AND IS KEPT AS HISTORY — current state is the `Last updated` stamp above (`v0.101.0`).** The Learning Connections arc ran through `v0.99.0`; `v0.100.0` resolved Domain Context authoring and `v0.101.0` shipped language and observability. Historical text follows. `v0.92.0 - Activity Sharing` is **Released** (kicked off and signed off 2026-08-27) — **Phase 2 of Learning Connections**: directional, opt-in activity sharing on a `linked_learner_grants` permission table, plus the carried quota-legibility work. **⚠️ Adoption of the whole Learning Connections arc is still ZERO: `linked_learner_relationships` was completely empty in production on 2026-08-26.** `v0.91.0` fixed the two reasons nobody could discover it (a landing page that advertised it as *Coming Soon* for three releases after it shipped, and a Help Center with no supporter section), but **adoption may still be zero, and PHASES ARE SEQUENCED, NOT EVIDENCE-GATED (owner, ruled twice)** — with zero connections a gating read returns nothing, so gating would be an indefinite stop. **Do not describe Phase 2's value as proven.** `[CHECKPOINT — due 2026-09-19]` and `[CHECKPOINT — due 2026-09-26]` are both observational and gate nothing. Before this, `v0.91.0 - Shared Learning Material` shipped Phase 1 (share a note with named connections without publishing it). `v0.90.0 - Invitation Integrity` is the head of a four-release arc that built **Learning Connections** end to end: `v0.89.0` shipped supporting another learner as a **capability, never a profile type** (quiz for someone → invite + accept → the product's first cross-user progress read, notes never included), `v0.89.1` made the write-once `users.birth_year` correctable and re-applied the guardian-consent gate to existing links, and `v0.90.0` keyed invitations to the typed **email address** rather than a resolved user id — closing an account-existence oracle and unlocking inviting someone who has not signed up. **⚠️ The whole arc has ZERO adoption: `linked_learner_relationships` was completely empty in production on 2026-08-26**, and `[CHECKPOINT — due 2026-09-19]` reads that table with a kill criterion attached. See the audit note in the stamp above before proposing anything here — the likeliest cause is that no surface tells anyone the feature exists, and the landing page actively says it does not. Before that arc: `v0.88.0` made Subject Plan sections authorable (still derived from the per-item label — no table, no new entity), `v0.87.0` made Bulk Generate failures legible by recording WHY each topic failed while changing no validation, and `v0.86.0` added an age-based recovery sweep for generation work stranded in a non-terminal status. Previous: `v0.85.0 - Domain Signal Integrity` (Released 2026-08-18) replaced the way the product decides whether a note's material is **quantitative** — the flag that puts computation guidance into a generation prompt. That was decided by lowercasing a haystack and **substring-matching 49 English keywords**, and the haystack leads with the Domain Context's **display label**. One keyword is `engineering`, so **coverage tracked a program's NAME, not its content:** programs named "Engineering" were 0% missing (214 notes); every other named program was 69% missing (463 of 670). **`Accountancy` is the sharpest case — the keyword list contains `accounting`, which is not a substring of `accountancy`**, so the CPA domain failed to match on its own name. Each of the 8 `DomainContext` values now **declares** whether its catalog is quantitative. **⚠️ Impact is PROSPECTIVE and this is the single most important thing to know about it: the release changes the prompt for ZERO notes today.** The declared property fires only on a note that has been classified, and classification sits at **12.7% (121 of 956 curator public notes)** — while every value in production use already matched the old scan on its own label, and the values that newly opt in (`ACCOUNTANCY`, `NURSING`) have **no notes at all**. **What changed is that classification became a lever:** before this, setting a note to `ACCOUNTANCY` did nothing for guidance, so there was no generation-quality reason to classify. The 463 close by **curator classification**, tracked by `[CHECKPOINT — due 2026-09-17]`. **⚠️ Do NOT propose fixing a missing signal by lengthening the keyword list** — that restores the guess. Previous: `v0.84.0 - Public Explore` (Released 2026-08-17) — **`/explore` is now anonymous** and the marketing nav names Explore, completing Discovery System Stage 0; an adoption started before signup survives sign-up, verification and onboarding via a one-shot cookie. `v0.83.2 - Anonymous Discovery Access` opened `GET /subjects` and `GET /course-programs` to anonymous readers, which had been silently 401ing and leaving two of four Public Library filters empty. `v0.83.1 - Note Creation Integrity` fixed a **live production 500** dating to 2026-03-21: `/study` paste-text, image upload and share-remix never set the `NOT NULL` `target_profile_type`, so **the LLM call billed against quota and then the insert failed.** `v0.83.0 - Target Audience Removal (Phase 2)` removed Target Audience from authoring and Public Library discovery, replacing it with an Authored Depth `?level=` filter; **the COLUMN is retained** as `V117`'s input, and dropping it waits on `[CHECKPOINT — due 2026-09-16]`.
4. **The single biggest constraint — RESIZED 2026-08-11, and the correction matters more than the number.** For months this line read *"W1→W2 retention is 2.4%."* That figure counted only activity in **days 7–14** after a learner's first Study Pack, and a production read found it sees **3 of the 11 learners who actually returned** — it misses everyone coming back in days 2–7 and everyone returning after day 14. Measured over a wider window, retention is **~7.2%**. **Both halves matter:** the number we steered by understated reality ~3.7×, *and* 7.2% is still bad — 141 of 152 activated learners never came back at all. The constraint is real; it was **mis-sized, not imagined**. Do not treat this correction as good news, and do not quote 2.4% as a current figure. Read "Retention Is the Proven Constraint" before proposing anything new.
5. **The funnel's largest single leak is onboarding, not retention or verification.** Of 375 all-time signups: 366 verify their email (97.6%), **234 complete onboarding (62.4%)**, 195 generate a first Study Pack (52.0%). So **132 people — 35.2% of everyone who ever signed up — verify and then never finish onboarding.** Verification loses 9. `v0.73.0` was built against this and is now being judged by a `[CHECKPOINT — due 2026-09-11]` measuring onboarding completion for **post-deploy signups only** against the **62.4%** baseline. **Kill criterion:** if it is not measurably above 62.4%, the comprehension hypothesis is unsupported — reopen the framing rather than proposing more onboarding polish. If completion *dropped*, the 5→8 screen split is the first suspect.
6. **Activation is already 52.2%, which structurally caps "get more users activated" as a lever.** Because returning learners = activated × retention rate, both levers are multiplicative, so the comparison is rate-independent: perfect activation would beat doubling retention only if activation were below 50%. It isn't. A read in `v0.72.1` tested exactly this and the volume hypothesis failed. Do not propose activation-volume work on the grounds that it will move returning-user counts — propose it, if at all, on comprehension or product-quality grounds.
7. **Decided 2026-08-12, and easy to re-derive wrongly: the onboarding exam-date field STAYS.** It is duplicated by the post-session commitment prompt, which prefills and requires the date — so removing it looks like obvious tidying. It is not: that prompt renders on session-*completion* screens, so it reaches only learners who finish a session, while onboarding reaches everyone who gets to Screen 3. Removing it would leave exam-bound learners who never complete a session with **no exam date at all**, disabling the board-exam countdown and degrading the exam-date segmentation the target-habit definition depends on. The field is also **optional**, so it was never a second required question. **Do not propose removing it without new evidence about the non-session-completing population.**
8. **Checkpoints were CONSOLIDATED at the `v0.82.0` signoff: nine September dates became TWO.** This applied bootstrap-test clause 3 retroactively — *a checkpoint metric must be operable at current scale* — after the table reached ~24 dated obligations across 18 rows at a traffic level where most of those rates have denominators in the tens. **`2026-09-11` — the onboarding funnel read, alone** (real denominator: 375 signups against a 62.4% completion baseline; it gates the onboarding catalog-first follow-up). **`2026-09-16` — everything else as ONE batch**, using the pre-written `docs/claude-plans/september-2026-checkpoint-reads.sql`; **Query 0 (delivery health) is read first and conditions all the rest**, which is itself the argument for batching. Unchanged: distal reads (10-15, 11-09), 09-30 Challenge Quiz, and 2027-02-01. **What earns its own date from here:** being wrong is expensive and hard to reverse, **or** the denominator is genuinely large. A conversion rate on a surface few learners reach does not — write it down, batch it, and accept that *"too small to read"* is itself a finding. **The sequencing consequence still holds** (a change that alters what a checkpoint measures before it is read destroys the read) — but **it is no longer an automatic veto**: `v0.82.0` ungated Target Audience Phase 2 from 09-13 by recording the confound instead, since audience is a secondary filter chip after `v0.79.0` made course/program primary.
9. **A framing died on real data 2026-08-13 — do not repeat it.** The Challenge Quiz adoption reads finally ran. Conversion after the `5/5 → 4/5` promotion change: **41.2% → 44.7% at 24h** and **58.8% → 47.0% at 7d**, neither distinguishable from zero — **while promotion exposure rose 68.6% → 92.0%.** A population *more* eligible for the lever converted no better, which makes it a harder negative, not a confounded one. **So *"low Challenge adoption is a motivation problem, not a placement problem"* is now UNCONFIRMED and should be reopened, not patched with further promotion tweaks.** The ineffective change is already reverted (`v0.74.0` moved the threshold back to verified mastery). One read remains — CTA impressions vs. clicks, newly measurable — at `[CHECKPOINT — due 2026-10-15]`.
10. **Two product rules shipped in the last two releases that proposals routinely violate.** (a) **The Study Pack's Quiz tab is LOCKED until the learner scores a perfect Quick Review** (`v0.74.0`). It stays visible and explains itself; it is not hidden. The reason is not gamification — that tab rendered the saved quiz **with answers revealed**, and Quick Review administers *those same questions*, so it was the answer key to its own test, which corrupted `ConceptHealth`. **Mastery = a perfect score, and reaching it via the retry round counts.** Challenge Quiz stays open from the start, because it generates its own questions and cannot be spoiled. **Adaptive Practice is no longer offered from Quick Review at all** — it remains reachable from the Dashboard and the mode-selection screen. (b) **Curator note authoring now pre-fills depth** (`v0.75.0`) from the selected Review Set, falling back to the author's own profile level. **Pre-fill only, never a server-side default write**, and **curators only** — the control is not shown to learners, so nothing is written on their behalf. The user-facing label is now **"Authored Depth"** (the column is still `notes.learner_level`). **Domain Context is never inferred** — there is no authorized source for it, and `domain_context IS NULL` is the promotion-backlog marker.
11. **Pricing is settled for now** — quota raised, price change deferred pending data. Don't reopen it.
12. **Positioning copy no longer needs a conversion test** (owner call, 2026-08-01). Pricing/checkout *mechanics* still do.
13. **Mastery comes only from graded assessment.** Self-review surfaces (Flashcards, Memorization) are firewalled from readiness by design.
14. **There are exactly 5 quiz modes.** The contract is locked. Do not propose a 6th.
15. **Biggest recurring failure mode in this repo:** repo-wide copy/terminology changes that under-scope themselves. `v0.68.0` under-counted its own sweep four times; `v0.67.1` scoped three items and shipped seven. Assume one grep is insufficient.
16. **A note is classified on FOUR durable axes, and one note can serve many programs.** Subject (*what*) · **Domain Context** (*how it is authored* — **the sole LLM domain constraint**, 11 ratified values since `v0.111.0` (8 at Release A plus `Architectural Design`, `History and Theory of Architecture` and `Planning and Site Development`), and since `v0.85.0` each one **declares whether its material is quantitative** rather than being guessed from its name; `GENERAL_EDUCATION`, `PROFESSIONAL_EDUCATION`, `PROFESSIONAL_PRACTICE_AND_REGULATION` and all three `v0.111.0` architecture values declare `false` and fall through to the unchanged keyword scan) · Note Learner Level, labelled **“Authored Depth”** in the UI since `v0.75.0` (*how deep* — a curriculum floor, and yes, a note owns its own depth; the rename was copy-only, the column is still `notes.learner_level`) · **Applicable Programs** (*where it appears* — one or many catalog programs, **discovery only, never reaches a prompt**) · ~~Target Audience~~ (*who* — **RETIRED from product surfaces in `v0.83.0`**). **⚠️ The field is gone from authoring, display and discovery, but the COLUMN is retained and still written** from the owner's profile — audit found it absent from every prompt, its access-control purpose never implemented, and **Course / Program predicts it ~99.3%** (licensure programs uniformly `BOARD_TAKER`, academic-level values uniformly `STUDENT`, `PROFESSIONAL` unused across all 945 public notes). **It WAS a live Public Library filter with a shareable `?audience=` URL param; `v0.83.0` removed both**, and old links degrade to an unfiltered view rather than erroring. **The model is FOUR durable axes.** What remains is **phase 4 — dropping the column**, and that is NOT authorized: it waits on `[CHECKPOINT — due 2026-09-16]`, whose kill criterion needs the column to run. **Never make Target Audience a runtime depth fallback** — it is one-time migration evidence. Full audit, evidence and counter-argument: `docs/claude-plans/target-audience-removal-proposal.md`. **Never propose the same note once per program** — that duplication is what Release B exists to remove. Multi-program notes **require** a Domain Context. Full detail, the 21 catalog programs and the 8 Domain Context values: `NOTES_AND_COLLECTIONS_CONTEXT.md` §0.
17. **Open questions awaiting your input:** (a) Explore's default tab — still unresolved three releases later; (b) ~~whether an admin curating a learner's own note should be constrained~~ **RESOLVED 2026-08-11 and shipped** — `ADR-001` → *Curation authority*: an Applicable Program row may only be authored onto a note its author owns. Ownership rather than visibility, because a learner can flip a curated public note back to private; no `source` provenance column was needed. **Settled, do not reopen:** the learner-depth question (2026-08-04 — notes own their depth; the *“authoring by inference” direction it left open **shipped as `v0.75.0`**, so that is no longer a pending direction either); **R4** (passed 2026-08-04 — it validated the Domain Context *value set*, **not** applicability, and must not be cited as settling anything about which programs a note applies to); and the four Program Family decisions (ratified 2026-08-05, shipped in Slice 3).
18. **Don't propose:** gating "Quiz for someone" on having a learning connection (it corrupts the 2026-09-19 read, and a shared-quiz recipient needs no account and no relationship by design); a learner-vs-learner leaderboard or competition built on the current progress aggregates (no shared denominator — see the audit note in the stamp); a 6th quiz mode, price changes, AI-generated per-concept definitions, feeding self-review into readiness, user-facing "Creator"/"Curated Learning" labels, a **12th** Domain Context value, or duplicate notes per program. **⚠️ The "no 9th value" rule was DISCHARGED by `v0.111.0` (2026-09-04), which added three on ALE evidence — it is not a rule that was broken, it is one whose bar was met and recorded in `ADR-001`'s revision log. `ARCHITECTURE` itself was rejected again on the naming rule.** **Also declined on production evidence 2026-08-17: a Domain Context Catalog** (admin-editable domain values, free text, or automatic creation) **and Domain Categories.** **⚠️ That 2026-08-17 reasoning is SUPERSEDED as of `v0.111.0` and is kept only as history** — it was true when the only authored curriculum was engineering. The taxonomy was then not too small but **unused**: only 4 of 8 values had ever been applied, `ACCOUNTANCY` and `NURSING` sit at zero against 286 unclassified notes in those very programs, and there is one curator. Reasoning: `docs/claude-plans/domain-context-catalog-assessment.md`.
19. **The repo is `studysnap` internally.** The product is NoteLib. Database and package names still say `studysnap` — that is intentional, not debt to fix.

---

## Which modules to paste

This file used to carry everything, which cost ~27k tokens before the conversation started. It is now the **core brief**, and the rest is split into modules you paste only when relevant. **A module never replaces this file** — paste core first, always.

| Conversation is about | Also paste |
|---|---|
| Onboarding, activation, retention, positioning, general product | *nothing — core is enough* |
| Quiz modes, exam simulation, practice mechanics | `QUIZ_AND_PRACTICE_CONTEXT.md` |
| A specific screen or feature surface, Note Collections, the Companion | `SURFACES_AND_FEATURES_CONTEXT.md` |
| What to build next, sequencing, why past releases went as they did | `STRATEGY_AND_ROADMAP_CONTEXT.md` |
| Pricing, plan tiers, paywalls, checkout | `MONETIZATION_CONTEXT.md` |
| A question that may already be settled — **read before reopening one** | `DECISION_HISTORY_CONTEXT.md` |
| Library/notes/collection structure, note fields, taxonomy | `NOTES_AND_COLLECTIONS_CONTEXT.md` |

---

## App: NoteLib

**What it is:** NoteLib is a notes-first study workspace. Users capture notes, generate Study Packs, practice with quizzes/exams, track readiness, and keep a reusable library of learning material.

**Core loop:** Capture -> Generate -> Review -> Improve -> Make a Copy -> Repeat

**Positioning:** **Ratified 2026-08-01 as a product-wide Messaging Architecture** — *"We sell the learning system. Features simply support that promise."* Ratified hero: **"Always know what to learn next."** / *"NoteLib turns your notes into a complete learning system — organized, prioritized, and ready whenever you sit down to study."* The hierarchy is **locked**: Hero (universal emotional outcome, profile-agnostic) → Supporting paragraph → Profile-specific bullets (via the existing `ProfileType`-keyed copy-resolution pattern, not a new mechanism) → Features as *evidence* for the promise. Board Exam Mode, Companion, Adaptive Practice, Review Sets, Progress, Knowledge Impact, and Explore all live at the Features layer — **none of them becomes the hero anywhere.**

Two things a new session usually gets wrong here:

- **The old "external copy hasn't caught up / needs a conversion test first" framing is dead.** `v0.68.0` shipped the ratified hero and supporting paragraph on `/pricing` word for word, plus the Plus/Pro taglines. And the "no positioning-copy change without a conversion test" bar was **explicitly lifted by the owner on 2026-08-01** — narrative consistency with the product vision is treated as a design decision, measured post-launch, not an optimization experiment. Pricing/checkout *mechanics* changes still need evidence; positioning copy does not.
- **Rollout is deliberately incremental, and the second slice is IN FLIGHT.** `v0.68.0` shipped `/pricing`'s hero, supporting paragraph and the Plus/Pro taglines. **`v0.76.0` (long since shipped — see the correction above; it is NOT open) was scoped to the *money surfaces*:** the `FREE` tagline — the one item the roadmap names as explicitly owed — the `PLAN_COMPARISON_ROWS` "Best for" row, the per-plan descriptions, and the in-app upgrade prompts. **The landing page and the Exam Hub upsell are deliberately NOT in it** — they answer different questions (*what is this?* / *why this mode?*) and each still needs its own scoping pass. **A trap worth knowing if you are asked about the `FREE` tagline:** an outcome-framed candidate was written in `v0.68.0` and **reverted**, because it had been derived from consistency with the Plus/Pro taglines, which contradicts the ratified `FREE = adopt` tier placement. If a proposed Free tagline only reads well beside its siblings, it is wrong again. `PROFESSIONAL`'s profile bullets are flagged **aspirational — do not wire live** until that profile has real capability behind it (it is enum-only today).

**Rebrand note:** The product is NoteLib. Code, package names, and database/schema names still use `studysnap` in many places unless explicitly changed.

**⚠️ The release notes below this line lag the orientation block and are kept as history, not as current state. Current state is orientation line 3: `v0.80.0`, Released 2026-08-15.** Four releases shipped on 2026-08-14/15 after the text below was written — `v0.77.0` (evidence-gated weak-concept recommendation), `v0.78.0` (post-mastery next step + named plan recommendation), `v0.79.0` (catalog-first vocabulary), `v0.80.0` (instrumentation integrity). Treat the entries below as the older history they are.

**Historical baseline: `v0.76.1 - Adaptive Practice Entry Attribution`, Released 2026-08-14.** Instrumentation only, no user-visible change: `ADAPTIVE_PRACTICE_STARTED` now records **where** a session was launched from (seven values plus `direct`), so the open `[CHECKPOINT — due 2026-09-12]` can test whether learners actually discover remediation from the Dashboard. **⚠️ Standing constraint: do NOT propose removing the Challenge Quiz Adaptive Practice entry point before 2026-09-12** — `v0.74.0` already removed the Quick Review route, and a second removal inside the same window confounds that read.

**Previous: `v0.76.0 - Messaging Architecture: The Money Surfaces`, Released 2026-08-14.** Copy only. `FREE.title` is now *"Start with ready-made study material"* — the Messaging Architecture item that had been explicitly owed since `v0.68.0` is closed. **Three copy rules were established and are binding:** upgrade *button* labels stay feature-named (a button says what the click does); paywall headlines are narrative for capability paywalls and factual for quota ones; `PLAN_CARD_SUBTEXT` describes the tier, never a feature, because it renders on every paywall.

**Previous: `v0.75.0 - Authoring by Inference`, Released and deployed 2026-08-14.** Curator authoring now infers a note's depth instead of asking for it: Review Sets carry an authored depth that child plans inherit, bulk-generate can author straight into a Review Set (pre-filling depth from it and adding the finished notes to it), and the depth control is renamed **Authored Depth**. **Two things worth carrying:** (a) the `ADR-001` section gating this work had been **stale for four releases** — it named two prerequisites that were both already satisfied, so unblocked work looked blocked; an ADR outranks a feature doc, so a stale gate inside one is expensive. (b) Its pre-signoff pressure test found **ten** issues, the blocker being that the depth pre-fill fired for *every* profile while the control renders only for curators — so learners silently persisted an invisible, unclearable value that acts as a curriculum floor. **Four findings were in code the authoring session had written, and two were protected by tests that session wrote or edited**, which is the second consecutive release where cold-context review was the deciding factor.

**Previous: `v0.74.0 - Quiz Progression`, Released and deployed 2026-08-13.** Closed a scored assessment whose answer key sat on an adjacent tab — see orientation line 10(a) for the rule and its reasoning. It also fixed quiz content rendering raw LaTeX (`\frac{...}` printed literally) by adding a math-notation rule to all ten content-generating prompts and normalising stored content at display time; **~23 affected Study Packs still await hand regeneration**, which is indexed in the Backlog Index rather than lost.

**Previous: `v0.73.0 - Onboarding Redesign`, Released and deployed 2026-08-12.** It went at the funnel's largest leak (line 5) and was justified on **comprehension, not retention** — the activation-volume argument was tested and failed. Eight of ten planned items shipped; one (removing the exam-date question) was never built and was then **decided against on review** — see the exam-date decision in the orientation block. It carries a `[CHECKPOINT — due 2026-09-11]`. **Its pre-signoff pressure test found the funnel it shipped to measure itself was miscounting the branch it introduced** — a screen that is a *branch* of Screen 5 had been ordered as a step after the final screen, producing a negative drop-off; fixed before signoff.

**Previous: `v0.71.0 - Applicable Programs`, Released 2026-08-10, merged and deployed.** Release B of `ADR-001` — making applicability a many-to-many fact so one canonical note surfaces under every program that needs it. Cut along the irreversibility boundary rather than by layer, because Release B is not reversible once reads move to the join. **Scoped as three slices and finished as five** — slice 4 (removing the "Primary Course / Program" second field) and slice 5 (the Onboarding Intent Router) were both added mid-release. All five shipped.

- **Slice 1 — merged (PR #990).** `note_course_program` join table, `V107` one-row-per-note backfill from the legacy string, and Teacher/Admin + Admin Dashboard write surfaces. Nothing read the join.
- **Slice 2 — merged (PR #991).** Discovery reads move to the join. **Its original safety premise turned out to be false and the semantics changed as a result:** the slice was scoped on "at one join row per note the join returns identical facet counts," but `V107` deliberately creates **no** join row for a catalog-*excluded* program value, so a pure-join rewrite would have changed *result sets* — an excluded value's facet vanishing, its filter returning 0 instead of N, its public shareable slug URL ceasing to resolve. Owner ruling 2026-08-05: reads are **join-first with a legacy-string fallback** (`EXISTS(join rows) OR (no join rows AND legacy string matches)`). Equivalence was then verified on real data: 0 differing facet rows, 0 differing note IDs across four filters. **Accepted cost: `notes.course_program` stays load-bearing on read paths.** Retiring the fallback is unscheduled.
- **Slice 3 — shipped.** Program Family expansion: selecting the `Engineering` family adds all three members as explicit selections, which the author can trim. Gate cleared 2026-08-05 by narrowing the question, not by answering it — expansion is unconditional, so no subject→program mapping was needed.
- **Slice 4 — shipped (added mid-release).** "Primary Course / Program" removed. One `Course / Program(s)` picker across Note Editor, Note Detail, Create-from-topic and Bulk Generate. **This is why there is no primary program any more.**
- **Slice 5 — shipped (added mid-release, after the pre-signoff pressure test).** The Onboarding Intent Router, plus repair of an activation-blocking regression the release itself introduced: onboarding creates its first note without sending a program, and a new `throw` on that path made onboarding uncompletable for every new user on the create-note branch.

**Deployed.** `V107` and `V108` have run in production. `V108` deletes join rows mechanically derived onto learner-owned non-copy notes — **a learner's personal free-text program must never be materialized into a catalog Applicable Program row**, which is now ratified doctrine in `ADR-001` under *Representation authority*.

**Superseded baseline:** `v0.70.0 - Canonical Knowledge Completion` released 2026-08-04 — the `course_programs` catalog (21 seeded programs, 11 excluded) + `program_families`, pool/bank learner-level re-keying, and authoring metadata made correctable after generation. **R4 resolved 2026-08-04, passed on all three steps**; bulk authoring is unblocked. Earlier baseline: `v0.69.0 - Canonical Knowledge Foundation`, deployed 2026-08-04 (PR #981). **What shipped:** Release A of `ADR-001` — the repo's first Architecture Decision Record — splitting the overloaded `notes.course_program` field into separate axes. `notes.domain_context` (a curated closed 8-value enum) is now the *sole* domain constraint sent to the LLM, and note-level `notes.learner_level` carries authored depth. Why it existed: authoring of the Civil Engineering Review Set's Engineering Mathematics plan had been deliberately halted, because one Algebra note under a one-program-per-note model would have to be duplicated for eleven engineering programs. **Two Planned Scope items were deliberately deferred to `v0.70.0`** — the `course_programs` catalog + `program_families`, and the question pool/bank re-keying — both blocked on production reads the release branch could not perform, not on engineering time. **The R4 verification did not run before signoff and is now a `[CHECKPOINT — due 2026-08-18]`**: it needs the new columns live in production, production runs `main`, and every release commit sat on the release branch — so signoff *is* what deployed it. **Bulk authoring must not begin until R4 step 2 passes.** A full pre-signoff pressure test (two agents plus an independent review) found five defects, two of them blockers that per-PR review was structurally unable to see — including silent permanent wiping of both new fields whenever a note's details were edited from the note detail page. Superseded baseline: `v0.68.0 - Topic Note Rename` shipped 2026-08-01 (PR #966). **What shipped:** the bare-topic drafting action renamed **"Generate Note" → "Create a Note"** (Company Redefinition Phase 4 §4 item 6), reserving "Generate" for operations that transform the learner's *own* material — "Generate Study Pack", "Generate Quiz", and "Regenerate" are deliberately unchanged, and that distinction is the whole point of the rename. Five further ungated items were folded in after kickoff: a "Retry Generate" → "Retry Generation" grammar fix (§4 item 8), the Companion Guidance Doctrine's docs-only text, the Messaging Architecture's first slice on `/pricing`, and — on explicit owner review after the first three shipped — two consistency batches the rename's own narrow scope had left behind (`plans.ts`'s pricing-table and upgrade-highlight strings, and the topic-note quota's entire user-facing vocabulary moving to **"topic note(s)"**).

**Two process facts from `v0.68.0` worth carrying forward, because they recurred:**

1. **Scoping under-counted itself four separate times in one release.** The rename's own "swept the whole repo" claim missed five surfaces inside the renamed action's *own* flow; the quota batch's sweep was truncated and missed the actual paywall copy (including a term the batch had explicitly rejected on evidence); a layout fix addressed the pill and title size but not the description-length drift that was the real cause; and `RELEASES.md` contained four wrong counts. All were caught by a **five-agent pre-signoff pressure test**, not by per-PR audits — because none of them appeared in any single PR's diff. If you are proposing a repo-wide copy or terminology change, assume one grep is not enough.
2. **A backend string can be user-facing copy.** The release was scoped "frontend-only" and still shipped a bug: `BulkNoteGenerationQuotaExceededException`'s 422 message rendered the retired vocabulary into the *same* `role="alert"` element as its renamed frontend twin. Fixed as an explicit scope amendment. Backend exception messages propagate verbatim to the UI through `api.ts` — treat them as copy, not internals.

Twelve pre-existing findings are recorded as **Known Limitations tagged `v0.68.1` candidates** in `RELEASES.md`. Note this project's own history here: `v0.67.0`'s candidate tagging was itself incomplete, and `v0.67.1` ended up shipping seven items after scoping three — so review that list directly rather than trusting the tags.

**Immediately prior releases:** `v0.67.1 - Explore Convergence Follow-ups` (2026-07-31) and `v0.67.0 - Explore Convergence` (2026-07-30, PR #949) — the latter notable for claiming its slot after **8 reclaims** by unrelated gate-cleared work, and for shipping on an **explicit owner gate override** with a dated `[CHECKPOINT — due 2026-09-13]` attached so the override gets checked against real engagement data. Full detail for both in the release list below.

---

## Retention Is the Proven Constraint (read this before proposing anything)

**⚠️ THE NUMBER WAS RESIZED 2026-08-11 — read this before quoting any retention figure.**

**What we said for months:** W1→W2 retention is **2.4%** (production read 2026-07-15; 3 of 127 eligible activated users returned in week 2), the core strategic constraint since v0.32.2 first flagged it (5.6% then), **not meaningfully improved** despite three feature releases aimed at it (v0.44.0, v0.46.0, v0.48.0).

**What `v0.72.1` found.** That metric asked one narrow question: did the learner do anything in **days 7–14** after their first Study Pack? A learner returning on day 3, or on day 20, scored as churned. Measured across four windows on the same population (152 learners, first pack ≥30 days ago):

| window | returned | rate |
|---|---|---|
| strict, days 7–14 *(the figure we quoted)* | 3 | **1.97%** |
| unbounded after day 7 | 7 | 4.61% |
| days 2–30 | 10 | 6.58% |
| unbounded after day 1 | 11 | **7.24%** |

Of the **11** learners who ever came back, the old metric saw **3 — 27%**. Four returned in days 2–7 before the window opened; four only after it closed.

**How to talk about this — both halves, always.** The number we steered by understated reality by roughly **3.7×**, *and* **141 of 152 activated learners never came back at all**. The constraint is **mis-sized, not imagined**. Treating the correction as good news is the failure mode to avoid; so is continuing to quote 2.4%. The small sample makes the exact multiplier uncertain (3.7× could be 2.5× or 5×) but **not** the finding — excluding days 2–7 and day 15+ is definitional arithmetic, not sampling noise. The admin dashboard now reports all four windows side by side, with the strict figure preserved unchanged so historical comparisons still work. Free-tier quota was essentially never hit at the old limits (5/25/50/month), which is one reason the owner ratified a large quota increase in v0.61.0 rather than treating quota as the retention lever on its own — see the Current Release note above.

**Diagnosis (two independent Fable sessions converged):** every content-rich retention trigger the product has shipped **default-OFF**, gated behind the exact engagement it's meant to create. The first study session also ends in a psychologically "complete" feeling (Zeigarnik effect) rather than an open loop that pulls the learner back. `v0.48.0` (merged 2026-07-15) shipped the cheap fixes for both (open-loop first-quiz ending, due-concepts digest default-ON) — **both remain UNPROVEN, mechanism shipped, lift not measured.** Do not describe either as a retention win in external-facing copy.

**H1+H5 SHIPPED in `v0.72.0` (2026-08-11) — this section previously said it was "still gated" and the query "has not yet been run."** Both are now false. What happened, recorded because the reasoning matters more than the outcome: the re-read ran on 2026-08-11, 13 days after its window closed (a kickoff gate scan caught that it had gone unrun). Result: **0 of 31** in the gated cohort vs. a **1.95%** pre-`v0.48.0` baseline. **That 0% was underpowered, not negative** — expected returns at baseline for n=31 is 0.60, and P(zero | no change) is **54.3%**. Ambiguous, so the pre-committed rule (*"positive **or ambiguous** → ship"*) fired and H1+H5 shipped. **Do not describe H1+H5 as validated by data** — it was a decision on a rule written before the answer was known, precisely so the outcome could not be rationalised afterwards.

**What shipped:** after any learner's first completed session, a prompt asks for their exam date and which weekdays they want to review; the due-concepts digest then respects those weekdays and links straight into a Quick Review session for the note with the most concepts due, rather than dropping them on the dashboard. Two dated checkpoints are open: **2026-09-10** (do learners commit when asked, and act on the digest — this one carries the kill criterion) and **2026-11-09** (the retention read itself, re-specified to the wider window after the finding above, since the original spec would have judged H1+H5 through the broken metric).

**The 2026-07-24 signup surge reversed the "go straight to Phase 2" plan and inserted a Diagnostic Read + a new Reusable Practice Assets initiative ahead of it — full detail in the Company Redefinition section of `STRATEGY_AND_ROADMAP_CONTEXT.md`, which supersedes the old post-v0.48.0 sequencing.** The retained/churned exam-dated user interview script is still written, ready, zero engineering cost, and still hasn't been run — the one open item on this whole track that can't happen from a keyboard.

**The target habit was redefined 2026-07-28 — read Round 2 through this, not the raw blended 2.4%.** The single W1→W2 calendar-week boolean is retired as the universal yardstick. Segment by whether `UserEntity.examDate` is set (not `profile_type`, a coarser proxy for the same thing):

- **Exam-bound learners** — the majority; `BOARD_EXAM` alone is **70.94%** of profile-typed accounts, confirmed product-wide rather than a surge artifact. Their arc is naturally episodic: signup → sustained practice → sit the exam → legitimately stop. Scored **only once their exam date has passed**, on whether they had activity in the final 7 pre-exam days. Still-in-flight users are excluded from the denominator entirely rather than penalized on a calendar clock that doesn't match their arc — going quiet *after* the exam is not churn.
- **Open-ended learners** — `STUDENT`, no exam date, ~27%+. Keep the existing W1→W2-style frame, which fits them better.

Two guardrails on reading this:

- **It does not excuse the 0/41 exam-dated-retention finding** (users going quiet *below* their own exam date). That is disengagement before the goal — a real problem under either frame.
- **Expect a small scored group at first** (recent signups, PRC-clustered exam dates). A near-single-digit denominator means "not yet measurable," not a verdict.

Full definition: `docs/product/ROADMAP.md`'s "Target-habit definition" Backlog Index row.

**Full backlog, current status, and exactly what un-parks each item lives in `docs/product/ROADMAP.md`'s Backlog Index table (~55 rows) — check it before proposing or resurfacing anything.** See "Roadmap Candidates: Gated & Ungated" below for a synthesized, status-grouped view of that same table, restricted to items still actually open (shipped/resolved rows are dropped from that view — check `RELEASES.md` for those). Do not propose roadmap items from partial memory of past sessions; the index is the current source of truth.

---

---

## The activation funnel — new as of 2026-08-11, and it relocates the biggest problem

All-time, production:

| stage | users | % of signups | lost here |
|---|---|---|---|
| signed up | 375 | — | — |
| verified email | 366 | 97.6% | 9 |
| **completed onboarding** | **234** | **62.4%** | **132** |
| generated a first Study Pack | 195 | 52.0% | 39 |

**132 learners — 35.2% of everyone who ever signed up — verify their email and then never finish onboarding.** That is the largest single drop anywhere in the funnel, and it was not visible before this read.

**Two things this settles, and one it does not.**

**Settled: "get more users activated" is not a lever on returning-user counts.** Returning learners = activated × retention rate, so both levers are multiplicative and the comparison is rate-independent — the retention rate cancels out. Perfect activation (every signup activating) would beat merely doubling retention only if activation were **below 50%**. It is **52.2%**. Scenarios, in returning learners per month: baseline **1.02**, retention doubles **2.03**, *every* signup activates **1.95**, activation recovered at half the retention rate **1.48**. A pre-committed rule required the sensitivity case to clear the retention case; it did not. **Do not propose onboarding or activation work on the grounds that it will move returning-user counts.** It is defensible on comprehension or product-quality grounds — 132 people meet the product and leave — but that is a different argument and should be made as one.

**Settled: verification is not the problem.** 97.6% verify. Any proposal built on "users bounce off the verification wall" is aiming at 9 people.

**NOT settled: which onboarding step they abandon on.** Profile type is only persisted at the *final* step, so the database cannot distinguish a step-1 abandon from a step-4 one, and clean step-level analytics only exist from 2026-07-28 (n≈5 so far). If a recommendation depends on knowing where the drop is, say so — that is an instrumentation question, not an unknowable one.

---

## Tech Stack

| Layer | Stack |
|---|---|
| Frontend | Next.js App Router, React 19, TypeScript, Tailwind, shadcn-style UI |
| Backend | Spring Boot, Java 21, PostgreSQL, Flyway |
| Auth | JWT, refresh tokens, Google OAuth, email verification |
| Payments | Xendit hosted checkout, webhook-confirmed access only |
| AI/OCR | OpenAI for notes/Study Packs/quizzes, Google Cloud Vision for OCR |
| Analytics | Shared analytics event model; backend persists after commit through async executor |

---

## Product Model

- **Note** is the primary entity. State: `DRAFT`, `GENERATING`, `FAILED`, `STUDY_PACK_READY`. Visibility: `PRIVATE`, `PUBLIC`.
- **Note metadata is FOUR durable axes as of the 2026-08-16 `ADR-001` amendment, plus Target Audience as a retiring transitional field** (was five; `v0.69.0` introduced the model) (the orientation block lists all five; this bullet details the four that `ADR-001` introduced or redefined, with Target Audience covered under Profile Types — **and note that a proposal to drop Target Audience entirely, reducing this to four axes, is open but unratified as of 2026-08-15**). *Subject* = what it is about. **Domain Context** = how it is authored, and the **only** thing that reaches the LLM as a domain constraint (curated closed 8-value set). **Note Learner Level** = how deep it is authored. *Applicable Programs* = where it appears — discovery only, **never reaches a prompt**. **Built as of `v0.71.0` Slices 1–2**: stored as explicit `note_course_program` rows, curated by Teacher/Admin, and read by Library/Public Library filters, facets, badges, and public search — join-first, falling back to the legacy string only for a note with no join rows. Facet counts can now correctly sum above the note total, which is expected under many-to-many and is explained in the filter panels rather than hidden. `courseProgram` survives as a legacy label and a fallback, and is no longer the classification apex. Resolution lives only in `StudyPackGenerationContextResolver`: domain = note domain context → note program → profile program; depth = note level → reader level → `College`. **Static content never falls back to the reader's level** (a Grade School reader cannot dilute a College note); **quizzes treat the note's level as a floor** — a lower reader level may soften wording and add scaffolding but never lowers curriculum, and a higher reader level never raises difficulty. Both new fields are **Teacher/Admin-only** and NULL on nearly every note; NULL is the designed norm, and `domain_context IS NULL` is itself the marker for "not yet promoted." **As of `v0.75.0`** the depth field is labelled **"Authored Depth"** in the UI (copy-only; the column is still `notes.learner_level`), and a curator creating a note gets it **pre-filled** — from the selected Review Set's own or inherited depth, else from the author's profile level. **That pre-fill is a UI default only, never a server-side write, and never applies to learners or to an existing note**, because a depth change on a generated note strands its Challenge-bank rows at the old level. **Note Collections also carry an optional `learnerLevel`** now, inherited down one level from a Goal to its child plans, used solely as that pre-fill source.
- **Study Pack** is generated content attached to a Note: summary, key concepts, quiz, metadata suggestions, and downstream quiz/exam entry points.
- **Note Collections (Study Plans / Review Sets)** organize owned notes into an ordered, curated unit, with an optional one-level Goal -> Subject hierarchy. This is the product's primary retention lever — see the dedicated vision section in `SURFACES_AND_FEATURES_CONTEXT.md`.
- **Learning Companion** is a persisted, curator-authored guidance layer (JSONB) on top-level Review Sets — the "premium guided learning experience" layer riding on top of the Study Plan journey. See the dedicated vision section in `SURFACES_AND_FEATURES_CONTEXT.md`.
- **ConceptHealth** is the recency spine for readiness and Progress: `lastCorrectAt`, `lastIncorrectAt`, due/not-due classification, and struggling state. This is the *only* mastery-integrity signal in the app, and it's locked (since v0.37.0) to move only from genuine assessment — see the Quiz / Practice Mode Contract in `QUIZ_AND_PRACTICE_CONTEXT.md`.
- **Quiz sessions** share `quick_review_sessions` with mode stored as enum and session state in JSONB. Question **format** (MCQ, True/False, Multi-Select, Matching, Identification, Enumeration) is a separate axis from mode.
- **Flashcards and Memorization** are non-scored review surfaces that sit entirely outside the quiz-session engine — no session row, no `ConceptHealth` write, ever.
- **Generated teacher quizzes** use `generatedQuiz`, not student quiz sessions.

Versioning rule:

- Never auto-regenerate generated content.
- Regeneration is explicit, user-confirmed, owner-only, and updates the existing Study Pack in place.
- Owner self-copy copies authored note fields only.
- Public-note copy is the exception: when the public source has a Study Pack, the copy includes that Study Pack and arrives `STUDY_PACK_READY`.

---

## Profile Types

| Profile | Current focus | Important rules |
|---|---|---|
| Student | Notes, Study Packs, Quick Review, Challenge Quiz, Adaptive Practice, Long Exam | Target Audience hidden; backend saves `STUDENT`. |
| Board Exam / Exam Reviewer | Exam-date context, Board Exam Mode, readiness for licensure-style prep | Target Audience hidden; backend saves `BOARD_TAKER`. |
| Teacher | Quiz generation, preview, DOCX export, Exam Builder | Teacher flow uses `generatedQuiz` only; never reuse student quiz sessions for preview. No adoption surface — Flashcards/Memorization and the Dashboard adoption nudge are hidden for Teacher. |
| Professional | Certification review, Long Exam as Full Practice Exam, Interview Practice | Target Audience hidden; backend saves `PROFESSIONAL`. |
| Parent | Enum exists, no real product implementation | Do not propose implementation without parent-child relationship design. |

**⚠️ The "Target Audience hidden; backend saves X" rows above describe a field whose retirement is RATIFIED (2026-08-16) but not yet executed.** Onboarding maps the learner's profile type onto the note's Target Audience, which is one reason the axis is ~99.3% predictable from other metadata. If you are asked about note taxonomy, read the proposal before treating Target Audience as permanent.

**Onboarding was REDESIGNED in `v0.73.0` (2026-08-12) — the "this flow is locked, do not redesign it" instruction that stood here for months is retired.** It is now **eight screens, one *required* question each**: profile type → course/program → learner level → first intent → input method → the note → generating → done. Screen 5 branches: the ready-made path shows an Official Study Plan, an availability check, or a "no plan yet" fallback where a learner can **ask for a plan that does not exist** (recorded as demand, no email is sent). Exam takers also see an **optional** exam-date field on Screen 3 — kept deliberately — see the exam-date decision in the orientation block. Tap-to-advance applies only to closed-set choices that stay inside onboarding (Screens 4 and 5's input method); typed input and the learner-level `<select>` keep an explicit Continue, because re-choosing an already-selected `<select>` value fires no change event and once stranded learners entirely. Backend content-creating mutations must still enforce profile setup for the legacy completed-but-null cohort through `ProfileSetupRequiredException`. **Onboarding still runs AFTER email verification, not straight after signup — this did not change, and the reason is a hard constraint rather than a preference.** Running it pre-verification was proposed and put out of scope: the generating screen calls `/notes/generate` and `/notes/{id}/generate`, both of which require a verified email, so it would **403 at the exact moment onboarding promises the first Study Pack** — and it would expose paid LLM generation to unverified throwaway accounts. The funnel also says it is the wrong target: **verification loses 9 learners, onboarding loses 132.** The live counter-proposal, unbuilt, is to run Screens 1–4 pre-verification and move the verification ask to the generating step — from the door to the payoff — which needs no backend change.

---

## Non-Negotiable Rules

- Do not reuse student quiz session logic for teacher preview.
- Do not auto-regenerate generated content.
- Do not grant paid access from frontend logic or redirect callbacks.
- Do not hardcode backend checkout pricing.
- Do not add new chart libraries for readiness.
- Do not add batch/progress infrastructure except the documented v0.29.1 terminal bulk-generation receipt.
- Do not expose Study Plan mastery/readiness on execution rows, list cards, published-plan cards, or public source plans.
- Do not expose per-concept review timing to Free users on Note Detail.
- Do not move Learning Style or reminder preferences into Profile.
- Do not label nav as "Account Settings"; use "Settings".
- Do not let Flashcards or Memorization write `ConceptHealth` — every quiz-session mode, including Quick Review, does.
- Do not propose inferring **Domain Context** from anything. No source is authorized, and `domain_context IS NULL` is the promotion-backlog marker a default would destroy. Depth may be inferred; the domain axis may not.
- Do not propose pre-filling authoring metadata into a control the author cannot see, or onto a note that already has a Study Pack.
- Do not propose unlocking the Study Pack Quiz tab on anything less than a perfect Quick Review, or hiding it instead of locking it — both were decided in `v0.74.0`.
- Do not plan-gate question *formats* — format variety is a learning-quality dimension, not a monetization lever. Gate modes/workflows/quotas instead.
- Do not nest Note Collections beyond two levels; no per-module mastery.
- Do not treat `/onboarding` as locked — it was **redesigned in `v0.73.0`** and the old "locked, do not redesign" rule is retired. What *is* fixed: it runs **after** email verification (a hard constraint, not a preference — see Profile Types), and it is being measured until 2026-09-11, so changing it before that read lands destroys the read.
- Do not let a learner receive an auto-generated Companion/Mentor Tip — curator-facing AI-assist only, mandatory human review before publish, never autonomous.
- Do not serve the Companion via a per-view/runtime LLM call — authored once, served static.
- Do not reorder the five authored Companion sections or infer a Mentor Tip's linked action at render time.
- Do not make Mentor Tip/Companion surfacing adaptive or LLM-driven — deterministic date/progress rules only; that tier is reserved for the gated PRO Personalization candidate.
- Do not let a surfacing condition permanently hide a Mentor Tip — "View Full Guide" always lists every authored tip.
- Do not backfill a reminder-email preference default change onto existing users — new-signup defaults apply only at account creation.
- Do not propose or re-surface roadmap candidates without checking `docs/product/ROADMAP.md`'s Backlog Index first.
- Do not treat quota/pricing numbers as living in one place — backend enforcement (`application.yaml`) and frontend marketing copy (`frontend/lib/pricing-config.ts`) are independent and must move together.
- Use `globalThis`, not `window`, in frontend code.
- Backend exceptions should be named `AppException` subclasses, not inline raw `new AppException(...)`.
- Repeated logic-bearing strings should be constants. Java range clamps should use `Math.clamp`.

---

## How We Work Together

- GPT: product thinking, roadmap decisions, UX philosophy, feature scoping, architecture tradeoffs, implementation prompt drafting.
- Claude Code: implementation prompt drafting, doc writing, code review, small direct changes.
- Codex: standard implementation, multi-system changes, refactors, tests.

For implementation tasks, GPT output should usually be a structured Codex prompt. Use the repo skills:

- `docs/skills/roadmap-feature-audit.md` before scoping roadmap/feature work.
- `docs/skills/codex-prompt-generator.md` before writing Codex prompts.
- `docs/skills/ux-product-review.md` for UX/product review.
- `docs/skills/release-doc-alignment.md` after shipping code/doc changes.

Always check `docs/codex-prompts/` for an existing prompt before writing a new one.

---

## Codex prompt format

This file used to inline the Long/Short Codex prompt templates. It no longer does — **use `docs/skills/codex-prompt-generator.md`**, which is the source of truth, or the `/codex-prompt` skill.

The inlined copy had drifted: it was missing the `## ERROR STATES` section the real template carries. That section is the first thing the post-delivery audit checks, so a prompt drafted from the stale copy would systematically omit it. Do not re-inline the template here — a second copy is exactly how that drift happened.

---

## Model / Effort Recommendations

| Task type | Tool | Effort |
|---|---|---|
| UX / product review | Claude Sonnet | Standard |
| Architecture discussion | Claude Sonnet or Opus | High |
| Roadmap / doc review | Claude Sonnet | Standard |
| Prompt drafting | Claude Sonnet | Standard |
| Standard feature implementation | Codex | Medium |
| Multi-system or ambiguous implementation | Codex | High |
| Refactor / cleanup / migration | Codex | Medium |
| Tests for agreed behavior | Codex | Low / Medium |

---

## Key Source-of-Truth Docs

- `AGENTS.md` - implementation rules and anti-drift constraints
- `CLAUDE.md` - Claude Code routing, commands, and commit rules
- `RELEASES.md` - current and historical release state
- `docs/product/ROADMAP.md` - current release sequencing and future scope; its Backlog Index table is the authoritative list of every open candidate, its status, and its gate condition
- `docs/product/SPEC.md` - canonical product behavior
- `docs/product/EXAM_MODES.md` - locked quiz mode hierarchy, question formats, and non-engine review surfaces
- `docs/product/PLANS.md` - plan tiers and quotas
- `docs/features/companion.md` - Learning Companion / Coach / Mentor Tips behavior rules
- `docs/features/` - per-feature behavior rules
- `docs/codex-prompts/` - ready prompts for active work
- `docs/releases/` - per-version release notes
- `docs/skills/` - reusable AI workflow guidance

---

Context loaded. What are we working on?
