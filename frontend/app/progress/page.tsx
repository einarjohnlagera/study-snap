import { ProgressReportClient } from "./progress-report-client";

export const metadata = {
  title: "My Progress | NoteLib",
};

export default function ProgressPage() {
  return <ProgressReportClient />;
}
