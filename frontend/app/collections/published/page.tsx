import { PublishedPlansPageClient } from "./published-plans-page-client";

export const metadata = {
  title: "Recommended Plans | NoteLib",
};

export default function PublishedPlansPage() {
  return <PublishedPlansPageClient />;
}
